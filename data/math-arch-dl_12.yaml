- en: 13 Fully Bayes model parameter estimation
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 13 全贝叶斯模型参数估计
- en: This chapter covers
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 本章涵盖
- en: Fully Bayes parameter estimation for unsupervised modeling
  id: totrans-2
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 无监督建模的全贝叶斯参数估计
- en: Injecting prior belief into parameter estimation
  id: totrans-3
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 将先验信念注入参数估计
- en: Estimating Gaussian likelihood parameters with known or unknown mean and precision
  id: totrans-4
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用已知或未知均值和精度的高斯似然参数估计
- en: Normal-gamma and Wishart distributions
  id: totrans-5
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 正态-伽马和Wishart分布
- en: 'Suppose we have a data set of interest: say, all images containing a cat. If
    we represent images as points in a high-dimensional feature space, our data set
    of interest forms a subspace of that feature space. Now we want to create an *unsupervised*
    model for our data set of interest. This means we want to identify a probability
    density function *p*(![](../../OEBPS/Images/AR_x.png)) whose sample cloud (the
    set of points obtained by repeatedly sampling the probability distribution many
    times) largely overlaps our subspace of interest. Of course, we do not know the
    exact subspace of interest, but we have collected a set of samples *X* from the
    data set of interest: that is, the training data. We can use the point cloud for
    *X* as a surrogate for the unknown subspace of interest. Thus, we are essentially
    trying to identify a probability density function *p*(![](../../OEBPS/Images/AR_x.png))
    whose sample cloud, by and large, overlaps *X*.'
  id: totrans-6
  prefs: []
  type: TYPE_NORMAL
  zh: 假设我们有一个感兴趣的数据集：比如说，所有包含猫的图像。如果我们把图像表示为高维特征空间中的点，那么我们感兴趣的数据集就构成了该特征空间的一个子空间。现在我们想要为我们的数据集创建一个*无监督*模型。这意味着我们想要识别一个概率密度函数
    *p*(![](../../OEBPS/Images/AR_x.png))，其样本云（通过多次重复采样概率分布而获得的点的集合）与我们的感兴趣子空间大致重叠。当然，我们不知道感兴趣的子空间的确切位置，但我们已经从感兴趣的数据集中收集了一组样本
    *X*：即训练数据。我们可以使用 *X* 的点云作为未知感兴趣子空间的一个替代。因此，我们本质上是在尝试识别一个概率密度函数 *p*(![](../../OEBPS/Images/AR_x.png))，其样本云大致与
    *X* 重叠。
- en: Once we have the model *p*(![](../../OEBPS/Images/AR_x.png)), we can use it
    to generate more data samples; these will be computer-generated cat images. This
    is generative modeling. Also, given a new image ![](../../OEBPS/Images/AR_a.png),
    we can estimate the probability of it being an image of a cat by evaluating *p*(![](../../OEBPS/Images/AR_a.png)).
  id: totrans-7
  prefs: []
  type: TYPE_NORMAL
  zh: 一旦我们有了模型 *p*(![](../../OEBPS/Images/AR_x.png))，我们就可以用它来生成更多的数据样本；这些将是计算机生成的猫图像。这是生成模型。此外，给定一个新图像
    ![](../../OEBPS/Images/AR_a.png)，我们可以通过评估 *p*(![](../../OEBPS/Images/AR_a.png))
    来估计它成为猫图像的概率。
- en: '13.1 Fully Bayes estimation: An informal introduction'
  id: totrans-8
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.1 全贝叶斯估计：非正式介绍
- en: 'Let’s recap Bayes’ theorem:'
  id: totrans-9
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们回顾贝叶斯定理：
- en: '![](../../OEBPS/Images/eq_13-01.png)'
  id: totrans-10
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-01](../../OEBPS/Images/eq_13-01.png)'
- en: Equation 13.1
  id: totrans-11
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 13.1
- en: 'Here, *X* = {![](../../OEBPS/Images/AR_x.png)[1], ![](../../OEBPS/Images/AR_x.png)[2],⋯}
    denotes the training data set. Our ultimate goal is to identify the likelihood
    function *p*(![](../../OEBPS/Images/AR_x.png)|*θ*). Estimating the likelihood
    function has two aspects: selecting the function family and estimating the parameters.
    We usually preselect the family from our knowledge of the problem and then estimate
    the model parameters. For instance, the family for our model likelihood function
    might be Gaussian: *p*(![](../../OEBPS/Images/AR_x.png)|*θ*) = 𝒩(![](../../OEBPS/Images/AR_x.png);
    ![](../../OEBPS/Images/AR_micro.png), **Σ**) as before, the semicolon separates
    the model variables from model parameters). Then *θ* = {![](../../OEBPS/Images/AR_micro.png), **Σ**}
    are the model parameters to estimate. We estimate *θ* such that the overall likelihood
    *p*(*X*|*θ*) = ∏*[i ]p*(![](../../OEBPS/Images/AR_x.png)*[i]*|*θ*) best fits the
    training data *X*.'
  id: totrans-12
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，*X* = {![](../../OEBPS/Images/AR_x.png)[1], ![](../../OEBPS/Images/AR_x.png)[2],⋯}
    表示训练数据集。我们的最终目标是识别似然函数 *p*(![](../../OEBPS/Images/AR_x.png)|*θ*). 估计似然函数有两个方面：选择函数族和估计参数。我们通常根据对问题的了解预先选择函数族，然后估计模型参数。例如，我们模型似然函数的函数族可能是高斯分布：*p*(![](../../OEBPS/Images/AR_x.png)|*θ*)
    = 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png), **Σ**)，正如之前所述，分号将模型变量与模型参数分开）。然后
    *θ* = {![](../../OEBPS/Images/AR_micro.png), **Σ**} 是需要估计的模型参数。我们估计 *θ* 以使整体似然
    *p*(*X*|*θ*) = ∏*[i ]p*(![](../../OEBPS/Images/AR_x.png)*[i]*|*θ*) 最好地拟合训练数据 *X*。
- en: We want to re-emphasize the mental picture that *best fit* implies that the
    sample cloud of the likelihood function (repeated samples from *p*(![](../../OEBPS/Images/AR_x.png)|*θ*))
    largely overlaps the training data set *X*. For the Gaussian case, this implies
    that the mean ![](../../OEBPS/Images/AR_micro.png) should fall at a place where
    there is a very high concentration of training data points, and the covariance
    matrix Σ should be such that the elliptical base of the likelihood function tightly
    contains as many training data points as possible.
  id: totrans-13
  prefs: []
  type: TYPE_NORMAL
  zh: 我们想再次强调*最佳拟合*意味着似然函数的样本云（从*p*(![](../../OEBPS/Images/AR_x.png)|*θ*)重复抽取的样本）与训练数据集*X*大体重叠。对于高斯情况，这意味着均值![](../../OEBPS/Images/AR_micro.png)应该落在训练数据点非常集中的地方，协方差矩阵Σ应该是这样的，即似然函数的椭圆底部尽可能紧密地包含尽可能多的训练数据点。
- en: 13.1.1 Parameter estimation and belief injection
  id: totrans-14
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.1.1 参数估计和信念注入
- en: There are various possible approaches to parameter estimation. The simplest
    approach is *maximum likelihood parameter estimation* MLE), introduced in section
    [6.6.2](../Text/06.xhtml#sec-max_likelihood_estimation). In MLE, we choose the
    parameter values that maximize *p*(*X*|*θ*), the likelihood of observing the training
    data set. This makes some sense. After all, the only thing we know to be true
    is that the input data set *X* *has been observed*—this being unsupervised data,
    we do not know anything else. It is reasonable to choose the parameters that maximize
    the probability of that known truth. If the training data set is large, MLE estimation
    works well.
  id: totrans-15
  prefs: []
  type: TYPE_NORMAL
  zh: 参数估计有各种可能的方法。最简单的方法是*最大似然参数估计*（MLE），在第[6.6.2](../Text/06.xhtml#sec-max_likelihood_estimation)节中介绍。在MLE中，我们选择最大化观察到的训练数据集似然*p*(*X*|*θ*)的参数值。这有些道理。毕竟，我们知道唯一真实的事情是输入数据集*X*已经被观察到了——由于这是无监督数据，我们不知道其他任何事情。选择最大化已知真相概率的参数是合理的。如果训练数据集很大，MLE估计效果良好。
- en: However, in the absence of a sufficiently large amount of training data, it
    often helps to inject our prior knowledge about the system into the estimation—prior
    knowledge can cover for the lack of data. This injection of guess/belief into
    the system is done via the prior probability density. To do this, we can no longer
    maximize the likelihood, as likelihood ignores the prior. We have to do maximum
    a posteriori (MAP) estimation, which maximizes the posterior probability. The
    posterior probability is the product of likelihood (which depends on the data)
    and the prior (which does not depend on data; we will make it reflect our prior
    belief).
  id: totrans-16
  prefs: []
  type: TYPE_NORMAL
  zh: 然而，在没有足够大量的训练数据的情况下，将我们对系统的先验知识注入到估计中通常是有帮助的——先验知识可以弥补数据不足。这种将猜测/信念注入系统的过程是通过先验概率密度完成的。为此，我们不能再最大化似然，因为似然忽略了先验。我们必须进行最大后验（MAP）估计，它最大化后验概率。后验概率是似然（依赖于数据）和先验（不依赖于数据；我们将使其反映我们的先验信念）的乘积。
- en: There are two possible MAP paradigms. We saw one of them in section [6.6.3](../Text/06.xhtml#sec-MAP_estimation),
    where we injected our belief that the unknown parameters must be *small* in magnitude
    and set *p*(*θ*) ∝ *e*^(−||*θ*||²) as a *regularizer*. The system was incentivized
    to select parameter values that are relatively smaller in magnitude. In this chapter,
    we study a different paradigm; let’s illustrate it with an example.
  id: totrans-17
  prefs: []
  type: TYPE_NORMAL
  zh: 有两种可能的MAP范式。我们在第[6.6.3](../Text/06.xhtml#sec-MAP_estimation)节中看到了其中之一，在那里我们注入了我们的信念，即未知参数的幅度必须*小*，并将*p*(*θ*)
    ∝ *e*^(−||*θ*||²)作为*正则化器*。系统被激励选择相对较小的参数值。在本章中，我们研究一个不同的范式；让我们用一个例子来说明它。
- en: 'Suppose we model the likelihood as a Gaussian: *p*(![](../../OEBPS/Images/AR_x.png)|*θ*)
    = 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png), **Σ**).
    We have to estimate the parameters *θ* = {![](../../OEBPS/Images/AR_micro.png), **Σ**}
    from the training data *X*, for which we must maximize the posterior probability.
    To compute the posterior probability, we need the prior probability. In addition,
    we must somehow inject constant values as our belief (lacking observed data) about
    the parameter values. How about modeling the prior probability as a Gaussian probability
    density function in the parameter space? Ignoring the covariance matrix parameter
    for the sake of simplicity, we can model the probability density of the mean parameter
    as *p*(![](../../OEBPS/Images/AR_micro.png)) = 𝒩(![](../../OEBPS/Images/AR_micro.png);
    ![](../../OEBPS/Images/AR_micro.png)[0], **Σ**[0]). We are essentially saying
    that we believe the parameter ![](../../OEBPS/Images/AR_micro.png) is likely to
    have a value near ![](../../OEBPS/Images/AR_micro.png)[0] with a confidence **Σ**[0].
    In other words, we are injecting a constant value as our belief in the parameter
    ![](../../OEBPS/Images/AR_micro.png). We can treat the covariance similarly. Later,
    we prove that in this paradigm, with a low volume of training data, the prior
    dominates. Once sufficient training data is digested, the effect of the prior
    fades, and the solution gets closer and closer to the MLE. This is the fully Bayes
    parameter estimation technique in a nutshell.'
  id: totrans-18
  prefs: []
  type: TYPE_NORMAL
  zh: 假设我们将似然函数建模为高斯分布：*p*(![](../../OEBPS/Images/AR_x.png)|*θ*) = 𝒩(![](../../OEBPS/Images/AR_x.png);
    ![](../../OEBPS/Images/AR_micro.png), **Σ**). 我们必须从训练数据 *X* 中估计参数 *θ* = {![](../../OEBPS/Images/AR_micro.png),
    **Σ**}，为此我们必须最大化后验概率。为了计算后验概率，我们需要先验概率。此外，我们还必须以某种方式注入常数值作为我们对参数值的信念（缺乏观察数据）。那么，将先验概率建模为参数空间中的高斯概率密度函数如何？为了简化起见，我们忽略协方差矩阵参数，可以将均值参数的概率密度建模为
    *p*(![](../../OEBPS/Images/AR_micro.png)) = 𝒩(![](../../OEBPS/Images/AR_micro.png);
    ![](../../OEBPS/Images/AR_micro.png)[0], **Σ**[0])。我们实际上是在说，我们相信参数 ![](../../OEBPS/Images/AR_micro.png)
    很可能具有接近 ![](../../OEBPS/Images/AR_micro.png)[0] 的值，并且具有 **Σ**[0] 的置信度。换句话说，我们正在将一个常数值作为对参数
    ![](../../OEBPS/Images/AR_micro.png) 的信念注入其中。我们可以类似地处理协方差。后来，我们证明在这个范式下，在训练数据量较少的情况下，先验概率占主导地位。一旦消化了足够多的训练数据，先验概率的影响就会减弱，解决方案就会越来越接近最大似然估计（MLE）。这就是简而言之的完全贝叶斯参数估计技术。
- en: In this chapter, we discuss Bayes estimation of parameters for a Gaussian likelihood
    function for a series of increasingly complex scenarios. In section [13.3](#sec-bayesinf-muonly),
    we deal with the case where the variance of the parameters to be estimated is
    known (constant) but the mean is unknown, so the mean is expressed as a (Gaussian)
    random variable. Then, in section [13.6](#sec-bayesinf-sigmaonly), we examine
    the case where the mean is known (constant) but the variance is unknown. Finally,
    in section [13.7](#sec-bayesinf-musigma), we examine the case where both are unknown.
    Both the univariate and multivariate cases are dealt with for each scenario.
  id: totrans-19
  prefs: []
  type: TYPE_NORMAL
  zh: 在本章中，我们讨论了一系列越来越复杂的场景中高斯似然函数参数的贝叶斯估计。在第 [13.3](#sec-bayesinf-muonly) 节中，我们处理了要估计的参数方差已知（常数）但均值未知的情况，因此均值被表示为一个（高斯）随机变量。然后，在第
    [13.6](#sec-bayesinf-sigmaonly) 节中，我们考察了均值已知（常数）但方差未知的情况。最后，在第 [13.7](#sec-bayesinf-musigma)
    节中，我们考察了两者都未知的情况。对于每个场景，都处理了单变量和多变量情况。
- en: NOTE Fully functional code for this chapter, executable via Jupyter Notebook,
    can be found at [http://mng.bz/woYW](http://mng.bz/woYW).
  id: totrans-20
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：本章的完整功能代码，可通过 Jupyter Notebook 执行，可以在 [http://mng.bz/woYW](http://mng.bz/woYW)
    找到。
- en: 13.2 MLE for Gaussian parameter values recap)
  id: totrans-21
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.2 高斯参数值的最大似然估计（MLE）回顾）
- en: We have discussed the details of this in section [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation).
    Here we recap the main results. Suppose we have a data set *X* = {*x*^((1)), *x*^((2)),⋯,
    *x*^((*n*))}. We have decided to model the data distribution as a Gaussian 𝒩(*x*;*μ*,
    *σ*)—we want to estimate the parameters *μ*, *σ* that best “explain" or “fit"
    the observed data set *X*. MLE is one of the simplest approaches to solving this
    problem. Here we estimate the parameters such that *the likelihood of the data
    observed during training is maximized*. This can be loosely visualized as estimating
    a probability density function whose peak coincides with the region in the input
    space with the densest population of training data. We looked at MLE in section
    [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation). Here we simply restate
    the expressions.
  id: totrans-22
  prefs: []
  type: TYPE_NORMAL
  zh: 我们已经在第 [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation) 节中讨论了这一细节。在这里，我们回顾一下主要结果。假设我们有一个数据集
    *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))}。我们决定将数据分布建模为高斯分布 𝒩(*x*;*μ*, *σ*)—我们想要估计参数
    *μ*, *σ*，以最好地“解释”或“拟合”观察到的数据集 *X*。最大似然估计是解决此问题的一种简单方法。在这里，我们估计参数，使得 *训练过程中观察到的数据的似然性最大化*。这可以粗略地理解为估计一个概率密度函数，其峰值与输入空间中训练数据密集区域相吻合。我们在第
    [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation) 节中讨论了最大似然估计。在这里，我们只是重新陈述了这些表达式。
- en: Let’s denote the (as yet unknown) mean and variance of the data distribution
    as *μ* and *σ*. Then from equation [5.22](../Text/05.xhtml#eq-univar-normal),
    we get
  id: totrans-23
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们用 *μ* 和 *σ* 表示数据分布的（尚未知的）均值和方差。然后从方程 [5.22](../Text/05.xhtml#eq-univar-normal)
    中，我们得到
- en: '![](../../OEBPS/Images/eq_13-01-a.png)'
  id: totrans-24
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-01-a.png)'
- en: 'Maximizing the log-likelihood *p*(*X*|*μ*, *σ*) has a closed-form solution:'
  id: totrans-25
  prefs: []
  type: TYPE_NORMAL
  zh: 最大化对数似然 *p*(*X*|*μ*, *σ*) 有一个闭式解：
- en: '![](../../OEBPS/Images/eq_13-02.png)'
  id: totrans-26
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-02.png)'
- en: Equation 13.2
  id: totrans-27
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 13.2
- en: Thus the MLE mean and variance are essentially the mean and variance of the
    training data samples (see section [6.6](../Text/06.xhtml#sec-model_param_estimation)
    for the derivation of these expressions).
  id: totrans-28
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，最大似然估计的均值和方差本质上就是训练数据样本的均值和方差（参见第 [6.6](../Text/06.xhtml#sec-model_param_estimation)
    节中这些表达式的推导）。
- en: The corresponding expressions for multivariate Gaussian MLE are
  id: totrans-29
  prefs: []
  type: TYPE_NORMAL
  zh: 多变量高斯最大似然估计的对应表达式是
- en: '![](../../OEBPS/Images/eq_13-03.png)'
  id: totrans-30
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-03.png)'
- en: Equation 13.3
  id: totrans-31
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 13.3
- en: These MLE parameter values are to be used to evaluate *p*(*x*) = 𝒩(*x*;*μ*,
    *σ*)—the probability of an unknown data point *x* coming from the distribution
    represented by the training data set *X*.
  id: totrans-32
  prefs: []
  type: TYPE_NORMAL
  zh: 这些最大似然估计参数值用于评估 *p*(*x*) = 𝒩(*x*;*μ*, *σ*)—即未知数据点 *x* 来自由训练数据集 *X* 表示的分布的概率。
- en: '13.3 Fully Bayes parameter estimation: Gaussian, unknown mean, known precision'
  id: totrans-33
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.3 完全贝叶斯参数估计：高斯分布，未知均值，已知精度
- en: 'MLE may not be that accurate when the available data set is small that is,
    *n*, size of the data set *X*, is small). In many problems, we have a prior idea
    of the mean and sigma of the data set. Unfortunately, MLE provides no way to bake
    such a prior belief into the estimation. Fully Bayes parameter estimation techniques
    try to fix this drawback: here we are not simply maximizing the likelihood of
    the observed data. Instead, we maximize the posterior probability of the estimated
    parameter(s). This posterior probability involves the product of the likelihood
    and a prior probability (see equation [13.1](#eq-posterior-prior-likelihood-evidence-ch12)).
    The likelihood term captures the effect of the training data—maximizing it alone
    is MLE—but does not capture the effect of a prior belief. On the other hand, the
    prior term does not depend on the data. This is where we bake in our belief or
    guess or prior knowledge about the data distribution. Thus, our estimate for the
    data distribution parameters will consider the data and the prior guess. We will
    soon see that the estimation is such that as the size of the data set (*n*, length
    of *X*) increases, the effect of the prior term decreases, and the effect of the
    likelihood term increases. In the limit, at infinite data availability, the Bayesian
    inference ields the MLE. At the other extreme, when no data is available (*n*
    = 0), the Fully Bayes estimates for the parameters are the same as the prior estimates.'
  id: totrans-34
  prefs: []
  type: TYPE_NORMAL
  zh: 当可用数据集较小（即，数据集 *X* 的大小 *n* 较小）时，最大似然估计（MLE）可能并不那么准确。在许多问题中，我们对数据集的均值和标准差有一个先验的想法。不幸的是，MLE
    提供了没有将这种先验信念融入估计的方法。完全贝叶斯参数估计技术试图解决这个问题：在这里，我们不仅仅是最大化观察数据的似然性。相反，我们最大化估计参数的后验概率。这个后验概率涉及到似然性和先验概率的乘积（见方程
    [13.1](#eq-posterior-prior-likelihood-evidence-ch12)）。似然项捕捉了训练数据的影响——仅最大化它是 MLE，但它不捕捉先验信念的影响。另一方面，先验项不依赖于数据。这就是我们将我们的信念、猜测或先验知识关于数据分布嵌入的地方。因此，我们对数据分布参数的估计将考虑数据和先验猜测。我们将很快看到，这种估计是随着数据集的大小（*n*，*X*
    的长度）增加，先验项的影响减小，似然项的影响增加。在极限情况下，在数据可用性无限的情况下，贝叶斯推理产生 MLE。在另一个极端情况下，当没有数据可用（*n*
    = 0）时，完全贝叶斯参数估计与先验估计相同。
- en: 'Let’s we examine Bayesian parameter estimation. For starters, we deal with
    a relatively simple case where we have a Gaussian data distribution with a known
    (constant) variance but unknown and modeled mean. The data distribution is Gaussian
    (as usual, the semicolon in 𝒩(*x*;*μ[n]*, *σ*) separates the variables from parameters):'
  id: totrans-35
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们考察贝叶斯参数估计。首先，我们处理一个相对简单的情况，即我们有一个具有已知（常数）方差但未知且已建模的均值的高斯数据分布。数据分布是高斯分布（通常，𝒩(*x*;*μ[n]*,
    *σ*) 中的分号将变量与参数分开）：
- en: '![](../../OEBPS/Images/eq_13-03-a.png)'
  id: totrans-36
  prefs: []
  type: TYPE_IMG
  zh: '![方程式13-03-a](../../OEBPS/Images/eq_13-03-a.png)'
- en: The training data set is denoted *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))},
    and its overall likelihood is
  id: totrans-37
  prefs: []
  type: TYPE_NORMAL
  zh: 训练数据集表示为 *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))}，其整体似然性为
- en: '![](../../OEBPS/Images/eq_13-03-b.png)'
  id: totrans-38
  prefs: []
  type: TYPE_IMG
  zh: '![方程式13-03-b](../../OEBPS/Images/eq_13-03-b.png)'
- en: The variance is known by assumption—hence it is treated as a constant instead
    of a random variable. The mean *μ* is unknown and is treated as a Gaussian random
    variable, with mean *μ*[0] and variance *σ*[0] (not to be confused with *μ* and
    *σ*, the mean and variance of the data itself ). So, the prior is
  id: totrans-39
  prefs: []
  type: TYPE_NORMAL
  zh: 假设方差是已知的——因此它被视为一个常数而不是随机变量。均值 *μ* 是未知的，被视为一个具有均值 *μ*[0] 和方差 *σ*[0]（不要与 *μ*
    和 *σ* 混淆，它们是数据本身的均值和方差）的高斯随机变量。因此，先验是
- en: '![](../../OEBPS/Images/eq_13-03-c.png)'
  id: totrans-40
  prefs: []
  type: TYPE_IMG
  zh: '![方程式13-03-c](../../OEBPS/Images/eq_13-03-c.png)'
- en: The posterior probability of the unknown *μ* parameter is a product of two Gaussians,
    which is a Gaussian itself. Let’s denote the as-yet-unknown) mean and variance
    of this product Gaussian as *μ[n]* and *σ[n]*. Here the subscript *n* is to remind
    us that the posterior has been obtained by digesting *n* data instances from *X*
    = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))}. Thus, the Gaussian posterior can be denoted
    as
  id: totrans-41
  prefs: []
  type: TYPE_NORMAL
  zh: 未知参数 *μ* 的后验概率是两个高斯分布的乘积，这本身也是一个高斯分布。让我们用 *μ[n]* 和 *σ[n]* 表示这个乘积高斯分布的尚未知的均值和方差。在这里，下标
    *n* 是为了提醒我们，后验是通过处理 *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))} 中的 *n* 个数据实例来获得的。因此，高斯后验可以表示为
- en: '![](../../OEBPS/Images/eq_13-03-d.png)'
  id: totrans-42
  prefs: []
  type: TYPE_IMG
  zh: '![方程式13-03-d](../../OEBPS/Images/eq_13-03-d.png)'
- en: Using Bayes’ theorem,
  id: totrans-43
  prefs: []
  type: TYPE_NORMAL
  zh: 使用贝叶斯定理，
- en: '![](../../OEBPS/Images/eq_13-03-e.png)'
  id: totrans-44
  prefs: []
  type: TYPE_IMG
  zh: '![方程式13-03-e](../../OEBPS/Images/eq_13-03-e.png)'
- en: 'By comparing the coefficients of *μ*² and *μ* on the exponents of the left
    and right sides, we determine the unknown parameters of the posterior distribution:'
  id: totrans-45
  prefs: []
  type: TYPE_NORMAL
  zh: 通过比较左右两侧指数上的 *μ*² 和 *μ* 的系数，我们确定后验分布的未知参数：
- en: '![](../../OEBPS/Images/eq_13-04.png)'
  id: totrans-46
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-04.png)'
- en: Equation 13.4
  id: totrans-47
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 13.4
- en: 'The significance of various closely named variables should be clearly understood:'
  id: totrans-48
  prefs: []
  type: TYPE_NORMAL
  zh: 应该清楚地理解各种名称相近的变量的意义：
- en: '*μ*, *σ* are the mean and variance of the *data* distribution *p*(*x*)—assumed
    to be Gaussian. The final goal is to estimate *μ*, *σ* that best fits the data
    set *X*. On the other hand, *μ*[0], *σ*[0] are the mean and variance of the *parameter*
    distribution *p*(*μ*), which captures our prior belief about the value of the
    data mean *μ* (remember, by assumption, the data mean is also a Gaussian random
    variable). *μ[n]*, *σ[n]* are the mean and variance of the posterior distribution
    *p*(*μ*|*X*) for the data mean *μ* as computed from *n* data point samples. This
    is a Gaussian random variable because it is a product of two Gaussians.'
  id: totrans-49
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*μ* 和 *σ* 是数据分布 *p*(*x*) 的均值和方差——假设为高斯分布。最终目标是估计 *μ* 和 *σ*，以最佳地拟合数据集 *X*。另一方面，*μ*[0]
    和 *σ*[0] 是参数分布 *p*(*μ*) 的均值和方差，它捕捉了我们关于数据均值 *μ* 的先验信念（记住，根据假设，数据均值也是一个高斯随机变量）。*μ[n]*
    和 *σ[n]* 是从 *n* 个数据点样本计算出的后验分布 *p*(*μ*|*X*) 的均值和方差。因为它是由两个高斯分布的乘积，所以它是一个高斯随机变量。'
- en: The posterior distribution of the unknown mean parameter, *p*(*μ*|*X*), is a
    Gaussian with mean *μ[n]*. So, it will attain a maximum when *μ* = *μ[n]*. In
    other words, the MAP estimate for the unknown mean *μ* is *μ*[MAP] = *μ[n]*.
  id: totrans-50
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 未知均值参数的后验分布 *p*(*μ*|*X*) 是一个以 *μ[n]* 为均值的正态分布。因此，当 *μ* = *μ[n]* 时，它将达到最大值。换句话说，未知均值
    *μ* 的 MAP 估计为 *μ*[MAP] = *μ[n]*。
- en: Even though *μ[n]* is the best estimate of *μ*, *σ[n]* is not approximating
    the *σ* of the data, *σ* is known in this case by assumption. Here, *σ[n]* is
    the variance of the posterior distribution, reflecting our *uncertainty* about
    the estimate of *μ*. That is why, as the number of data instances becomes very
    large, *σ[n]* approaches 0 (indicating we have zero uncertainty or full confidence
    in the estimate of the mean.)
  id: totrans-51
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 尽管 *μ[n]* 是 *μ* 的最佳估计，但 *σ[n]* 并没有近似数据的 *σ*，在这种情况下，*σ* 是通过假设而知的。在这里，*σ[n]* 是后验分布的方差，反映了我们对
    *μ* 估计的不确定性。这就是为什么，当数据实例的数量变得非常大时，*σ[n]* 趋近于 0（表示我们对均值估计没有不确定性或完全有信心）。
- en: The estimate for our data distribution is *p*(*x*) = 𝒩(*x*;*μ[n]*, *σ*), where
    *μ[n]* is given by equation [13.4](#eq-bayesinf-muonlyunivar). Note that it is
    a combination of the MLE *x̄* and prior guess *μ*[0]. Using this, given any arbitrary
    data instance *x*, we can infer the probability of *x* belonging to the class
    of the training data set *X*.
  id: totrans-52
  prefs: []
  type: TYPE_NORMAL
  zh: 我们数据分布的估计为 *p*(*x*) = 𝒩(*x*;*μ[n]*, *σ*)，其中 *μ[n]* 由方程 [13.4](#eq-bayesinf-muonlyunivar)
    给出。请注意，它是 MLE *x̄* 和先验猜测 *μ*[0] 的组合。使用这个，对于任何任意数据实例 *x*，我们可以推断 *x* 属于训练数据集 *X*
    的类的概率。
- en: NOTE Fully functional code for Bayesian estimation with unknown mean and known
    variance, executable via Jupyter Notebook, can be found at [http://mng.bz](http://mng.bz/ZA75)
    [/ZA75](http://mng.bz/ZA75).
  id: totrans-53
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：完全功能的贝叶斯估计代码，具有未知均值和已知方差，可通过 Jupyter Notebook 执行，可以在 [http://mng.bz](http://mng.bz/ZA75)
    [/ZA75](http://mng.bz/ZA75) 找到。
- en: Listing 13.1 PyTorch- Bayesian estimation with unknown mean, known variance
  id: totrans-54
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 13.1 PyTorch- 带有未知均值和已知方差的贝叶斯估计
- en: '[PRE0]'
  id: totrans-55
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: ① Parameters of the prior
  id: totrans-56
  prefs: []
  type: TYPE_NORMAL
  zh: ① 先验参数
- en: ② Mean of the posterior, following equation [13.4](#eq-bayesinf-muonlyunivar)
  id: totrans-57
  prefs: []
  type: TYPE_NORMAL
  zh: ② 后验均值，根据方程 [13.4](#eq-bayesinf-muonlyunivar)
- en: ③ Standard deviation of the posterior, following equation [13.4](#eq-bayesinf-muonlyunivar)
  id: totrans-58
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 后验标准差，根据方程 [13.4](#eq-bayesinf-muonlyunivar)
- en: 13.4 Small and large volumes of training data, and strong and weak priors
  id: totrans-59
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.4 小量和大量的训练数据，以及强和弱的先验
- en: 'Let’s examine the behavior of equation [13.4](#eq-bayesinf-muonlyunivar) when
    *n* = 0 (no data) and when *n* → ∞ (lots of data):'
  id: totrans-60
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们考察方程 [13.4](#eq-bayesinf-muonlyunivar) 在 *n* = 0（无数据）和 *n* → ∞（大量数据）时的行为：
- en: '![](../../OEBPS/Images/eq_13-04-a.png)'
  id: totrans-61
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-04-a.png)'
- en: This agrees with our notion that with little data, the posterior is dominated
    by the prior, while with lots of data, the posterior is dominated by the likelihood.
    With lots of data, the variance of the parameter is zero (we are saying with *full
    certainty* that the best value for the mean is the sample mean for the data, aka
    the MLE estimate for the mean). In general, with more training data (that is,
    larger values of *n*), the posterior shifts closer to the likelihood. This can
    be seen by analyzing equation [13.4](#eq-bayesinf-muonlyunivar). It agrees with
    our intuition that with little data, we try to compensate with our pre-existing
    (prior) belief as to the value of the parameters. As the number of training data
    instances increases, the effect of the prior is reduced, and the likelihood (which
    is a function of the data) begins to dominate.
  id: totrans-62
  prefs: []
  type: TYPE_NORMAL
  zh: 这与我们关于少量数据时后验主要由先验决定，而大量数据时后验主要由似然决定的观念相符。在大量数据的情况下，参数的方差为零（我们可以说我们有**完全的确定性**，即均值的最优值是数据的样本均值，也就是均值的MLE估计）。一般来说，随着训练数据的增加（即*n*的值增大），后验会越来越接近似然。这可以通过分析方程[13.4](#eq-bayesinf-muonlyunivar)来看到。这与我们的直觉相符，即少量数据时，我们试图用我们预先存在的（先验）关于参数值的信念来补偿。随着训练数据实例数量的增加，先验的影响减小，似然（它是数据的函数）开始占主导地位。
- en: 'A low variance for the prior (that is, small *σ*[0]) essentially means we have
    low uncertainty in our prior belief (remember, the entropy/uncertainty of a Gaussian
    is proportional to its variance). Such high-confidence priors resist being overwhelmed
    by the data and are called *strong priors*. On the other hand, a large *σ*[0]
    implies low /confidence in the prior mean value. This is a *weak prior* that is
    easily overwhelmed by the data. We can see this in the final expression for mean
    in equation [13.4](#eq-bayesinf-muonlyunivar): we have *nσ*[0]²/*σ*² in the denominator
    of the second term. In general, the second term vanishes with larger *n*, thereby
    removing the effect of the prior *μ*[0] and making the posterior mean coincide
    with the MLE mean. But the smaller the *σ*[0], the larger the *n* required to
    achieve this, and vice versa.'
  id: totrans-63
  prefs: []
  type: TYPE_NORMAL
  zh: 先验的方差低（即小的*σ*[0]）本质上意味着我们对先验信念的不确定性低（记住，高斯熵/不确定性与其方差成正比）。这样的高置信度先验抵抗被数据淹没，被称为**强先验**。另一方面，大的*σ*[0]意味着对先验均值值的信心低。这是一个**弱先验**，容易被数据淹没。我们可以在方程[13.4](#eq-bayesinf-muonlyunivar)中均值的最终权衡表达式中看到这一点：在第二项的分母中我们有*nσ*[0]²/*σ*²。一般来说，随着*n*的增大，第二项消失，从而消除了先验*μ*[0]的影响，使得后验均值与MLE均值相一致。但是，*σ*[0]越小，需要更大的*n*来实现这一点，反之亦然。
- en: 13.5 Conjugate priors
  id: totrans-64
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.5 共轭先验
- en: In section [13.3](#sec-bayesinf-muonly), given a Gaussian likelihood, choosing
    the Gaussian family for the prior made the posterior also belong to the Gaussian
    family. This simplified things considerably. If the prior was chosen from another
    family, the posterior—which is the product of the likelihood and prior—may not
    belong to a simple or even known distribution family.
  id: totrans-65
  prefs: []
  type: TYPE_NORMAL
  zh: 在[13.3](#sec-bayesinf-muonly)节中，给定高斯似然，选择高斯先验使得后验也属于高斯家族。这大大简化了问题。如果先验来自另一个家族，那么后验——似然和先验的乘积——可能不属于简单或甚至已知的分布家族。
- en: Thus, a Gaussian likelihood with a Gaussian prior results in a Gaussian posterior
    for the mean. Such priors are said to be *conjugate*. Formally, for a specific
    family of likelihood, the choice of the prior that results in the posterior belonging
    to the same family as the prior is called a conjugate prior. For instance, Gaussians
    for the mean (with known variance) are conjugate to a Gaussian likelihood. Soon
    we will see that for a Gaussian likelihood, a gamma distribution for the precision
    (inverse of the variance) results in a gamma posterior. In other words, a gamma
    prior to the precision is conjugate to a Gaussian likelihood. In the multivariate
    case, instead of gamma, we have the Wishart distribution as a conjugate prior.
  id: totrans-66
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，具有高斯先验的高斯似然导致均值后验也是高斯。这样的先验被称为**共轭**。形式上，对于特定的似然家族，选择使得后验属于与先验相同家族的先验被称为共轭先验。例如，均值的正态分布（已知方差）与正态似然是共轭的。很快我们就会看到，对于正态似然，精度的伽马分布（方差的倒数）导致伽马后验。换句话说，精度的伽马先验与正态似然是共轭的。在多元情况下，我们用Wishart分布作为共轭先验。
- en: '13.6 Fully Bayes parameter estimation: Gaussian, unknown precision, known mean'
  id: totrans-67
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.6 完全贝叶斯参数估计：高斯，未知精度，已知均值
- en: In section [13.3](#sec-bayesinf-muonly), we discussed fully Bayes parameter
    estimation with the assumption that we somehow know the variance *σ* and only
    want to estimate the mean *μ*. Now we examine the case where the mean is known
    but the variance is unknown and expressed as a random variable. The computations
    become simpler if we use precision *λ* instead of variance *σ*. They are related
    by the expression 1/*σ*². Thus we have a data set *X*, which is assumed to be
    sampled from a Gaussian distribution with a constant mean *μ*, while the precision
    *λ* is a random variable with a gamma distribution. The probability density function
    for the data is thus *p*(*x*|*μ*, *λ*) = 𝒩(*x*; *μ*, 1/√*λ*).
  id: totrans-68
  prefs: []
  type: TYPE_NORMAL
  zh: 在第 [13.3](#sec-bayesinf-muonly) 节中，我们讨论了在假设我们以某种方式知道方差 *σ* 并且只想估计均值 *μ* 的条件下，完全贝叶斯参数估计。现在我们考察均值已知但方差未知且表示为随机变量的情况。如果我们使用精度
    *λ* 而不是方差 *σ*，计算将变得简单。它们通过表达式 1/*σ*² 相关联。因此，我们有一个数据集 *X*，它被假定为从具有常数均值 *μ* 的高斯分布中抽取的，而精度
    *λ* 是具有伽马分布的随机变量。因此，数据的概率密度函数是 *p*(*x*|*μ*, *λ*) = 𝒩(*x*; *μ*, 1/√*λ*)。
- en: We model the prior random variable for precision with a gamma distribution.
    The likelihood is Gaussian, and since the product of a Gaussian and gamma is another
    gamma (due to the conjugate prior property of gamma), the resulting posterior
    is a gamma. The gamma function parameters for the posterior can be derived via
    coefficient comparison. The maximum of the posterior is our estimate for the parameter.
  id: totrans-69
  prefs: []
  type: TYPE_NORMAL
  zh: 我们用伽马分布来建模精度的先验随机变量。似然函数是高斯分布，由于高斯和伽马的乘积仍然是伽马（由于伽马分布的共轭先验性质），因此得到的结果后验是伽马分布。后验的伽马函数参数可以通过系数比较推导出来。后验的最大值是我们对参数的估计。
- en: Gamma distribution
  id: totrans-70
  prefs: []
  type: TYPE_NORMAL
  zh: 伽马分布
- en: 'The gamma distribution is introduced in the appendix; if necessary, please
    read that first. Here we state the relevant properties. The probability density
    function for a random variable *λ* having a gamma distribution is a function with
    two parameters *α*, *β*:'
  id: totrans-71
  prefs: []
  type: TYPE_NORMAL
  zh: 伽马分布在附录中介绍；如有必要，请先阅读。这里我们陈述相关的性质。具有伽马分布的随机变量 *λ* 的概率密度函数是具有两个参数 *α*，*β* 的函数：
- en: '![](../../OEBPS/Images/eq_13-05.png)'
  id: totrans-72
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-05.png)'
- en: Equation 13.5
  id: totrans-73
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 13.5
- en: Maximum of a gamma distribution
  id: totrans-74
  prefs: []
  type: TYPE_NORMAL
  zh: 伽马分布的最大值
- en: 'To maximize the gamma probability density function *p*(*λ*|*X*) = *λ*^((*α[n]*
    − 1))*e*^(−*β[n]λ*) for a random variable *λ*, we take the derivative and equate
    to zero:'
  id: totrans-75
  prefs: []
  type: TYPE_NORMAL
  zh: 为了最大化伽马概率密度函数 *p*(*λ*|*X*) = *λ*^((*α[n]* − 1))*e*^(−*β[n]λ*) 对于随机变量 *λ*，我们对它求导并令其等于零：
- en: '![](../../OEBPS/Images/eq_13-05-a.png)'
  id: totrans-76
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-05-a.png)'
- en: 13.6.1 Estimating the precision parameter
  id: totrans-77
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.6.1 估计精度参数
- en: 'Let’s return to the fully Bayes estimation of the precision parameter when
    the mean is known. We model the data distribution with a Gaussian: *p*(*x*|*μ*,
    *λ*) = 𝒩(*x*; *μ*, 1/√*λ*) (we have expressed this Gaussian in terms of the precision,
    *λ*, which is related to the variance *σ* as *λ* = 1/*σ* ²). The training data
    set is *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))}, and its overall likelihood
    is'
  id: totrans-78
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们回到当均值已知时对精度参数的完全贝叶斯估计。我们用高斯分布来建模数据分布：*p*(*x*|*μ*, *λ*) = 𝒩(*x*; *μ*, 1/√*λ*)（我们已经用精度
    *λ* 表达了这种高斯分布，而 *λ* 与方差 *σ* 相关，*λ* = 1/*σ* ²）。训练数据集是 *X* = {*x*^((1)), *x*^((2)),⋯,
    *x*^((*n*))}，其整体似然函数是
- en: '![](../../OEBPS/Images/eq_13-05-b.png)'
  id: totrans-79
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-05-b.png)'
- en: 'We model the prior for the precision with a gamma distribution with parameters
    *α*[0], *β*[0]:'
  id: totrans-80
  prefs: []
  type: TYPE_NORMAL
  zh: 我们用参数 *α*[0]，*β*[0] 的伽马分布来建模精度的先验：
- en: '*p*(*λ*) = *γ*(*λ*;*α*[0], *β*[0]) ∝ *λ*^((*α*[0]−1))*e*^(−*β*[0]*λ*)'
  id: totrans-81
  prefs: []
  type: TYPE_NORMAL
  zh: '*p*(*λ*) = *γ*(*λ*;*α*[0], *β*[0]) ∝ *λ*^((*α*[0]−1))*e*^(−*β*[0]*λ*)'
- en: We know the corresponding posterior—a product of a Gaussian and a gamma—is gamma
    distribution (due to the conjugate prior property of gamma distribution). Let’s
    denote the posterior as
  id: totrans-82
  prefs: []
  type: TYPE_NORMAL
  zh: 我们知道相应的后验——高斯和伽马分布的乘积——是伽马分布（由于伽马分布的共轭先验性质）。让我们将后验表示为
- en: '*p*(*λ*|*X*) = *γ*(*λ*;*α[n]*, *β[n]*)'
  id: totrans-83
  prefs: []
  type: TYPE_NORMAL
  zh: '*p*(*λ*|*X*) = *γ*(*λ*;*α[n]*, *β[n]*)'
- en: From Bayes’ theorem,
  id: totrans-84
  prefs: []
  type: TYPE_NORMAL
  zh: 从贝叶斯定理，
- en: '![](../../OEBPS/Images/eq_13-05-c.png)'
  id: totrans-85
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-05-c.png)'
- en: Substituting
  id: totrans-86
  prefs: []
  type: TYPE_NORMAL
  zh: 代入
- en: '![](../../OEBPS/Images/eq_13-05-d.png)'
  id: totrans-87
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-05-d.png)'
- en: and comparing the powers of *λ* and *e*, we get
  id: totrans-88
  prefs: []
  type: TYPE_NORMAL
  zh: 并比较 *λ* 和 *e* 的幂，我们得到
- en: '![](../../OEBPS/Images/eq_13-06.png)'
  id: totrans-89
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06.png)'
- en: Equation 13.6
  id: totrans-90
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 13.6
- en: Notice that as before, at low values of *n*, the posterior is dominated by the
    prior but gets closer and closer to the likelihood estimate as *n* increases.
    In other words, in the absence of sufficient data, we let our belief take over
    the estimation; but if and when data is available, the estimation is dominated
    by the data-based entity likelihood.
  id: totrans-91
  prefs: []
  type: TYPE_NORMAL
  zh: 注意，与之前一样，在 *n* 较低时，后验被先验主导，但随着 *n* 的增加，后验越来越接近似然估计。换句话说，在没有足够数据的情况下，我们让我们的信念主导估计；但如果数据可用，估计则由基于数据的实体似然主导。
- en: The MAP point estimate for the parameter *λ* given data set *X* is obtained
    by maximizing this posterior distribution *p*(*λ*|*X*) = *γ*(*λ*;*α[n]*, *β[n]*),
    which yields *λ[MAP]* = 1/*σ[MAP]*² = (*α[n]*–1/*β[n]*). (Section A.5 in the appendix
    shows how to obtain the maximum of a gamma distribution.) Thus our estimate for
    the training data distribution is *p*(*x*) = 𝒩(*x*; *μ*, *σ*[MAP]), where 1/*σ[MAP]*²
    = (*α[n]*–1/*β[n]*).
  id: totrans-92
  prefs: []
  type: TYPE_NORMAL
  zh: 给定数据集 *X*，参数 *λ* 的 MAP 点估计是通过最大化这个后验分布 *p*(*λ*|*X*) = *γ*(*λ*;*α[n]*, *β[n]*)，得到
    *λ[MAP]* = 1/*σ[MAP]*² = (*α[n]*–1/*β[n]*)。 (附录 A.5 展示了如何获得伽马分布的最大值。) 因此，我们对训练数据分布的估计是
    *p*(*x*) = 𝒩(*x*; *μ*, *σ*[MAP])，其中 1/*σ[MAP]*² = (*α[n]*–1/*β[n]*)。
- en: 'Given a large volume of data, the MAP estimate for the unknown precision/variance
    becomes identical to the MLE estimate (proof outline shown):'
  id: totrans-93
  prefs: []
  type: TYPE_NORMAL
  zh: 给予大量数据，未知精度/方差的 MAP 估计与 MLE 估计相同（证明概要如下）：
- en: '![](../../OEBPS/Images/eq_13-06-a.png)'
  id: totrans-94
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-a.png)'
- en: 'On the other hand, given no data, the MAP estimate for the unknown precision/variance
    is completely determined by the prior (proof outline shown):'
  id: totrans-95
  prefs: []
  type: TYPE_NORMAL
  zh: 另一方面，在没有数据的情况下，未知精度/方差的 MAP 估计完全由先验决定（证明概要如下）：
- en: '![](../../OEBPS/Images/eq_13-06-b.png)'
  id: totrans-96
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-b.png)'
- en: NOTE Fully functional code for Bayesian estimation with a known mean and unknown
    variance, executable via Jupyter Notebook, can be found at [http://mng.bz](http://mng.bz/2nZ9)
    [/2nZ9](http://mng.bz/2nZ9).
  id: totrans-97
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：具有已知均值和未知方差的贝叶斯估计的完整功能代码，可通过 Jupyter Notebook 执行，可在 [http://mng.bz](http://mng.bz/2nZ9)
    [/2nZ9](http://mng.bz/2nZ9) 找到。
- en: Listing 13.2 PyTorch- Bayesian estimation with unknown variance, known mean
  id: totrans-98
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 13.2 PyTorch- 具有未知方差和已知均值的贝叶斯估计
- en: '[PRE1]'
  id: totrans-99
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: ① Parameters of the prior
  id: totrans-100
  prefs: []
  type: TYPE_NORMAL
  zh: ① 先验参数
- en: ② Parameters of the posterior
  id: totrans-101
  prefs: []
  type: TYPE_NORMAL
  zh: ② 后验参数
- en: '13.7 Fully Bayes parameter estimation: Gaussian, unknown mean, unknown precision'
  id: totrans-102
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.7 完全贝叶斯参数估计：高斯，未知均值，未知精度
- en: In section [13.3](#sec-bayesinf-muonly), we saw that if the variance is known,
    the conjugate prior to the mean is a Gaussian (aka normal) distribution. Likewise,
    when the mean is known, the conjugate prior to the precision is a gamma distribution.
    If both are unknown, we end up with a normal-gamma distribution.
  id: totrans-103
  prefs: []
  type: TYPE_NORMAL
  zh: 在第 [13.3](#sec-bayesinf-muonly) 节中，我们看到了如果方差已知，均值的共轭先验是高斯分布（也称为正态分布）。同样，当均值已知时，精度的共轭先验是伽马分布。如果两者都未知，我们最终得到正态-伽马分布。
- en: 13.7.1 Normal-gamma distribution
  id: totrans-104
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.7.1 正态-伽马分布
- en: 'Normal-gamma is a probability distribution of two random variables, say, *μ*
    and *λ*, whose density is defined in terms of four parameters *μ*^′, *λ*^′, *α*^′,
    and *β*^′, as follows:'
  id: totrans-105
  prefs: []
  type: TYPE_NORMAL
  zh: 正态-伽马是两个随机变量，例如 *μ* 和 *λ* 的概率分布，其密度由四个参数 *μ*^′，*λ*^′，*α*^′，和 *β*^′ 定义，如下所示：
- en: '![](../../OEBPS/Images/eq_13-06-c.png)'
  id: totrans-106
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-c.png)'
- en: Although it looks complicated, a simple way to remember it is a product of a
    normal and a gamma distribution.
  id: totrans-107
  prefs: []
  type: TYPE_NORMAL
  zh: 尽管看起来很复杂，但记住它的简单方法是它是正态分布和伽马分布的乘积。
- en: The normal-gamma distribution attains a maximum at
  id: totrans-108
  prefs: []
  type: TYPE_NORMAL
  zh: 正态-伽马分布达到最大值在
- en: '![](../../OEBPS/Images/eq_13-06-d.png)'
  id: totrans-109
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-d.png)'
- en: 13.7.2 Estimating the mean and precision parameters
  id: totrans-110
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.7.2 估计均值和精度参数
- en: 'As before, we model the data distribution with a Gaussian: *p*(*x*|*μ* , *λ*)
    = 𝒩(*x*; *μ*, 1/√*λ*) we have expressed this Gaussian in terms of the precision,
    *λ*, which is related to the variance *σ* as *λ* = 1/*σ*²). The training data
    set is *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))}, and its overall likelihood
    is'
  id: totrans-111
  prefs: []
  type: TYPE_NORMAL
  zh: 与之前一样，我们用高斯分布建模数据分布：*p*(*x*|*μ* , *λ*) = 𝒩(*x*; *μ*, 1/√*λ*)，我们用精度 *λ* 表达了这个高斯分布，它与方差
    *σ* 相关，*λ* = 1/*σ*²）。训练数据集是 *X* = {*x*^((1)), *x*^((2)),⋯, *x*^((*n*))}，其整体似然为
- en: '![](../../OEBPS/Images/eq_13-06-e.png)'
  id: totrans-112
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-e.png)'
- en: 'We model the prior for the mean as a Gaussian with mean *μ*[0] and precision
    *λ*[0]*λ*:'
  id: totrans-113
  prefs: []
  type: TYPE_NORMAL
  zh: 我们将先验的均值建模为均值 *μ*[0] 和精度 *λ*[0]*λ* 的高斯分布：
- en: '![](../../OEBPS/Images/eq_13-06-f.png)'
  id: totrans-114
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-f.png)'
- en: 'We model the prior for the precision as a gamma distribution with parameters
    *α*[0], *β*[0]:'
  id: totrans-115
  prefs: []
  type: TYPE_NORMAL
  zh: 我们将先验的精度建模为参数 *α*[0]，*β*[0] 的伽马分布：
- en: '![](../../OEBPS/Images/eq_13-06-g.png)'
  id: totrans-116
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-g.png)'
- en: 'The overall prior probability for the mean and precision parameters is the
    product of the two, a normal-gamma distribution with parameters *μ*⁰, *λ*⁰, *α*⁰,
    *β*⁰:'
  id: totrans-117
  prefs: []
  type: TYPE_NORMAL
  zh: 均值和精度参数的整体先验概率是两者的乘积，即参数 *μ*⁰, *λ*⁰, *α*⁰, *β*⁰ 的正态-伽马分布：
- en: '![](../../OEBPS/Images/eq_13-06-h.png)'
  id: totrans-118
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-h.png)'
- en: 'The posterior probability for the mean and precision parameters is the joint
    (that is, product) of the likelihood and the prior. As such, we know it is another
    normal-gamma distribution (due to the conjugate prior property of normal-gamma):'
  id: totrans-119
  prefs: []
  type: TYPE_NORMAL
  zh: 均值和精度参数的后验概率是似然和先验的联合（即乘积）。因此，我们知道它又是另一个正态-伽马分布（由于正态-伽马分布的共轭先验性质）：
- en: '![](../../OEBPS/Images/eq_13-06-i.png)'
  id: totrans-120
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-i.png)'
- en: Using Bayes’ theorem,
  id: totrans-121
  prefs: []
  type: TYPE_NORMAL
  zh: 使用贝叶斯定理，
- en: '![](../../OEBPS/Images/eq_13-06-j.png)'
  id: totrans-122
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-j.png)'
- en: Substituting
  id: totrans-123
  prefs: []
  type: TYPE_NORMAL
  zh: 代入
- en: '![](../../OEBPS/Images/eq_13-06-k.png)'
  id: totrans-124
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-06-k.png)'
- en: 'and comparing coefficients, the unknown parameters of the posterior distribution
    can be determined:'
  id: totrans-125
  prefs: []
  type: TYPE_NORMAL
  zh: 通过比较系数，可以确定后验分布的未知参数：
- en: '![](../../OEBPS/Images/eq_13-07.png)'
  id: totrans-126
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07.png)'
- en: Equation 13.7
  id: totrans-127
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 13.7
- en: 'To obtain the fully Bayes parameter estimate, we take the maximum of the normal-gamma
    posterior probability density function:'
  id: totrans-128
  prefs: []
  type: TYPE_NORMAL
  zh: 为了获得完全的贝叶斯参数估计，我们取正态-伽马后验概率密度函数的最大值：
- en: '![](../../OEBPS/Images/eq_13-07-a.png)'
  id: totrans-129
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-a.png)'
- en: Thus the final probability density function for the data is
  id: totrans-130
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，数据的最终概率密度函数为
- en: '![](../../OEBPS/Images/eq_13-07-b.png)'
  id: totrans-131
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-b.png)'
- en: NOTE Fully functional code for Bayesian estimation with an unknown mean and
    unknown variance, executable via Jupyter Notebook, can be found at [http://mng.bz/1oQy](http://mng.bz/1oQy).
  id: totrans-132
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：完全功能的贝叶斯估计代码，用于未知均值和未知方差，可通过 Jupyter Notebook 执行，可在[http://mng.bz/1oQy](http://mng.bz/1oQy)找到。
- en: Listing 13.3 PyTorch code for a normal-gamma distribution
  id: totrans-133
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 13.3 PyTorch 代码：正态-伽马分布
- en: '[PRE2]'
  id: totrans-134
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: ① Since PyTorch doesn’t implement normal-gamma distribution, we implement a
    bare-bones version.
  id: totrans-135
  prefs: []
  type: TYPE_NORMAL
  zh: ① 由于 PyTorch 没有实现正态-伽马分布，我们实现了一个简化的版本。
- en: 'Listing 13.4 PyTorch: Bayesian estimation with unknown mean, unknown variance'
  id: totrans-136
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 13.4 PyTorch：具有未知均值和未知方差的贝叶斯估计
- en: '[PRE3]'
  id: totrans-137
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
- en: ① Parameters of the prior
  id: totrans-138
  prefs: []
  type: TYPE_NORMAL
  zh: ① 先验参数
- en: ② Parameters of the posterior
  id: totrans-139
  prefs: []
  type: TYPE_NORMAL
  zh: ② 后验参数
- en: '13.8 Example: Fully Bayesian inferencing'
  id: totrans-140
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.8 示例：完全贝叶斯推理
- en: Let’s revisit the problem discussed in section [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation)
    of predicting whether a resident of Statsville is female based on height. For
    this purpose, we have collected height samples from adult female residents of
    Statsville. Unfortunately, due to unforeseen circumstances, we collected a very
    small sample. Armed with our knowledge of Bayesian inference, we do not want to
    let this deter us from trying to build a model. Based on physical considerations,
    we can assume that the distribution of heights is Gaussian. Our goal is to estimate
    the parameters (*μ*, *σ*) of this Gaussian.
  id: totrans-141
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们回顾一下第 [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation) 节中讨论的问题，即根据身高预测
    Statsville 居民是否为女性。为此，我们已经收集了 Statsville 成年女性居民的身高样本。不幸的是，由于不可预见的情况，我们收集的样本非常小。凭借我们对贝叶斯推理的了解，我们不想因此放弃尝试构建模型。根据物理考虑，我们可以假设身高的分布是高斯分布。我们的目标是估计这个高斯分布的参数
    (*μ*, *σ*)。
- en: NOTE Fully functional code for this example, executable via Jupyter Notebook,
    can be found at [http://mng.bz/Pn4g](http://mng.bz/Pn4g).
  id: totrans-142
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：此例的完全功能代码，可通过 Jupyter Notebook 执行，可在[http://mng.bz/Pn4g](http://mng.bz/Pn4g)找到。
- en: 'Let’s first create the data set by sampling five points from a Gaussian distribution
    with *μ* = 152 and *σ* = 8. In real-life scenarios, we do not know the mean and
    standard deviation of the true distribution. But for the sake of this example,
    let’s assume that the mean height is 152 cm and the standard deviation is 8 cm.
    Our data matrix, *X*, is as follows:'
  id: totrans-143
  prefs: []
  type: TYPE_NORMAL
  zh: 首先，让我们通过从具有 *μ* = 152 和 *σ* = 8 的高斯分布中采样五个点来创建数据集。在现实场景中，我们不知道真实分布的均值和标准差。但为了这个例子，让我们假设平均身高为
    152 厘米，标准差为 8 厘米。我们的数据矩阵，*X*，如下所示：
- en: '![](../../OEBPS/Images/eq_13-07-c.png)'
  id: totrans-144
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-c.png)'
- en: 13.8.1 Maximum likelihood estimation
  id: totrans-145
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.8.1 最大似然估计
- en: 'If we relied on MLE, our approach would be to compute the mean and standard
    deviation of the data set and use this normal distribution as our model. We use
    the following equations to compute the mean and standard deviation of our normal
    distribution:'
  id: totrans-146
  prefs: []
  type: TYPE_NORMAL
  zh: 如果我们依赖MLE，我们的方法将是计算数据集的均值和标准差，并使用这个正态分布作为我们的模型。我们使用以下方程来计算正态分布的均值和标准差：
- en: '![](../../OEBPS/Images/eq_13-07-d.png)'
  id: totrans-147
  prefs: []
  type: TYPE_IMG
  zh: '![公式图](../../OEBPS/Images/eq_13-07-d.png)'
- en: The mean, *μ*, comes out to be 149.68, and the standard deviation, *σ*, is 11.52\.
    This differs significantly from the true mean (152) and standard deviation (8)
    because the number of data points is low. In such low-data scenarios, the maximum
    likelihood estimates are not very reliable.
  id: totrans-148
  prefs: []
  type: TYPE_NORMAL
  zh: 均值，*μ*，计算结果为149.68，标准差，*σ*，为11.52。这与真实的均值（152）和标准差（8）有显著差异，因为数据点的数量很少。在这种情况下，最大似然估计并不可靠。
- en: 13.8.2 Bayesian inference
  id: totrans-149
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.8.2 贝叶斯推理
- en: Can we do better than MLE? One potential method is to use Bayesian inference
    with a good prior. How do we select a good prior? Well, let’s say that we know
    from an old survey that the average and standard deviation of the height of adult
    female residents of Neighborville, the neighboring town, are 150 cm and 9 cm,
    respectively. Additionally, we have no reason to believe that the distribution
    of heights at Statsville is significantly different. So we can use this information
    to “initialize" our prior. The prior distribution encodes our beliefs about the
    parameter values.
  id: totrans-150
  prefs: []
  type: TYPE_NORMAL
  zh: 我们能否比MLE做得更好？一种潜在的方法是使用具有良好先验的贝叶斯推理。我们如何选择一个好的先验？好吧，让我们假设我们知道从一项旧调查中，邻镇Neighborville成年女性居民的身高平均值和标准差分别为150厘米和9厘米。此外，我们没有理由相信Statsville的身高分布与邻镇有显著差异。因此，我们可以使用这些信息来“初始化”我们的先验。先验分布编码了我们关于参数值的信念。
- en: 'Given that we are dealing with an unknown mean and unknown variance, we model
    the prior as a normal-gamma distribution:'
  id: totrans-151
  prefs: []
  type: TYPE_NORMAL
  zh: 由于我们处理的是未知均值和未知方差，我们将先验模型设为正态伽马分布：
- en: '*p*(*θ*) = 𝒩*γ*(*μ*[0], *λ*[0], *α*[0], *β*[0])'
  id: totrans-152
  prefs: []
  type: TYPE_NORMAL
  zh: '*p*(*θ*) = 𝒩*γ*(*μ*[0], *λ*[0], *α*[0], *β*[0])'
- en: We choose *p*(*θ*) such that *μ*[0] = 150, *λ*[0] = 100, *α*[0] = 10.5, and
    *β*[0] = 810\. This implies that
  id: totrans-153
  prefs: []
  type: TYPE_NORMAL
  zh: 我们选择*p*(*θ*)，使得*μ*[0] = 150，*λ*[0] = 100，*α*[0] = 10.5，和*β*[0] = 810。这意味着
- en: '*p*(*θ*) = 𝒩*γ*(150, 100, 10.5, 810)'
  id: totrans-154
  prefs: []
  type: TYPE_NORMAL
  zh: '*p*(*θ*) = 𝒩*γ*(150, 100, 10.5, 810)'
- en: '*p*(*θ*|*X*) is a normal-gamma distribution whose parameters can be computed
    using equations described in section [13.7](#eq-normal-gamma). The PyTorch code
    for computing the posterior is shown next.'
  id: totrans-155
  prefs: []
  type: TYPE_NORMAL
  zh: '*p*(*θ*|*X*)是一个正态伽马分布，其参数可以使用[13.7](#eq-normal-gamma)节中描述的方程计算。下面展示了计算后验的PyTorch代码。'
- en: Listing 13.5 PyTorch- Computing posterior probability using Bayesian inference
  id: totrans-156
  prefs: []
  type: TYPE_NORMAL
  zh: 列表13.5 PyTorch- 使用贝叶斯推理计算后验概率
- en: '[PRE4]'
  id: totrans-157
  prefs: []
  type: TYPE_PRE
  zh: '[PRE4]'
- en: ① Initializes the normal-gamma distribution
  id: totrans-158
  prefs: []
  type: TYPE_NORMAL
  zh: ① 初始化正态伽马分布
- en: ② Computes the posterior
  id: totrans-159
  prefs: []
  type: TYPE_NORMAL
  zh: ② 计算后验概率
- en: ③ The mode of the distribution refers to parameter values with the highest probability
    density.
  id: totrans-160
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 分布的模指的是具有最高概率密度的参数值。
- en: ④ Computes the standard deviation using precision
  id: totrans-161
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 使用精度计算标准差
- en: ⑤ map_mu and map_std refer to the parameter values that maximize the posterior
    distribution.
  id: totrans-162
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ map_mu和map_std指的是最大化后验分布的参数值。
- en: The MAP estimates for *μ* and *σ* obtained using Bayesian inference are 149.98
    and 9.56, respectively, which are better than the MLE estimates of 149.68 and
    11.52 (the true *μ* and *σ* are 152 and 9, respectively).
  id: totrans-163
  prefs: []
  type: TYPE_NORMAL
  zh: 使用贝叶斯推理获得的*μ*和*σ*的MAP估计值分别为149.98和9.56，分别优于MLE估计值149.68和11.52（真实的*μ*和*σ*分别为152和9）。
- en: Now that we’ve estimated the parameters, we can find out the probability that
    a sample lies in the range using the formula
  id: totrans-164
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们已经估计了参数，我们可以使用公式找出样本落在某个范围内的概率
- en: '![](../../OEBPS/Images/eq_13-07-e.png)'
  id: totrans-165
  prefs: []
  type: TYPE_IMG
  zh: '![公式图](../../OEBPS/Images/eq_13-07-e.png)'
- en: The details of this can be found in section [6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation).
  id: totrans-166
  prefs: []
  type: TYPE_NORMAL
  zh: 有关此内容的详细信息，请参阅[6.8](../Text/06.xhtml#sec-gauss_max_likelihood_estimation)节。
- en: '13.9 Fully Bayes parameter estimation: Multivariate Gaussian, unknown mean,
    known precision'
  id: totrans-167
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.9 完全贝叶斯参数估计：多元高斯分布，未知均值，已知精度
- en: This is the multivariate case; the univariate version is discussed in section
    [13.3](#sec-bayesinf-muonly). The computations follow along the same lines as
    the univariate ones.
  id: totrans-168
  prefs: []
  type: TYPE_NORMAL
  zh: 这是多元情况；一元版本在[13.3](#sec-bayesinf-muonly)节中讨论。计算方法与一元情况类似。
- en: We model the data distribution as a Gaussian *p*(![](../../OEBPS/Images/AR_x.png)|![](../../OEBPS/Images/AR_micro.png),
    **Λ**) = 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png),
    **Λ**^(−1)), where we have expressed the Gaussian in terms of the *precision matrix*
    Λ instead of the covariance matrix Σ, where **Λ** = **Σ**^(−1). The training data
    set is *X* ≡ {![](../../OEBPS/Images/AR_x.png)^((1)), ![](../../OEBPS/Images/AR_x.png)^((2)),
    ⋯, ![](../../OEBPS/Images/AR_x.png)^((*i*)), ⋯, ![](../../OEBPS/Images/AR_x.png)^((*n*))},
    and its overall likelihood is
  id: totrans-169
  prefs: []
  type: TYPE_NORMAL
  zh: 我们将数据分布建模为高斯分布 *p*(![](../../OEBPS/Images/AR_x.png)|![](../../OEBPS/Images/AR_micro.png),
    **Λ**) = 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png),
    **Λ**^(−1))，其中我们用精度矩阵 **Λ** 而不是协方差矩阵 **Σ** 来表示高斯分布，其中 **Λ** = **Σ**^(−1)。训练数据集是
    *X* ≡ {![](../../OEBPS/Images/AR_x.png)^((1)), ![](../../OEBPS/Images/AR_x.png)^((2)),
    ⋯, ![](../../OEBPS/Images/AR_x.png)^((*i*)), ⋯, ![](../../OEBPS/Images/AR_x.png)^((*n*))}，其整体似然为
- en: '![](../../OEBPS/Images/eq_13-07-f.png)'
  id: totrans-170
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-f](../../OEBPS/Images/eq_13-07-f.png)'
- en: 'We model the prior for the mean as a Gaussian:'
  id: totrans-171
  prefs: []
  type: TYPE_NORMAL
  zh: 我们将均值的前验建模为高斯分布：
- en: '![](../../OEBPS/Images/eq_13-07-g.png)'
  id: totrans-172
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-g](../../OEBPS/Images/eq_13-07-g.png)'
- en: The posterior probability density is a Gaussian (because it is the product of
    two Gaussians). Let’s denote it as
  id: totrans-173
  prefs: []
  type: TYPE_NORMAL
  zh: 后验概率密度是一个高斯分布（因为它是由两个高斯分布的乘积）。让我们用以下符号表示它
- en: '![](../../OEBPS/Images/eq_13-07-h.png)'
  id: totrans-174
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-h](../../OEBPS/Images/eq_13-07-h.png)'
- en: Using Bayes’ theorem,
  id: totrans-175
  prefs: []
  type: TYPE_NORMAL
  zh: 使用贝叶斯定理，
- en: '![](../../OEBPS/Images/eq_13-07-i.png)'
  id: totrans-176
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-i](../../OEBPS/Images/eq_13-07-i.png)'
- en: Let’s examine the exponent of the rightmost expression.
  id: totrans-177
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们检查最右边表达式的指数。
- en: '![](../../OEBPS/Images/eq_13-07-j.png)'
  id: totrans-178
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-j](../../OEBPS/Images/eq_13-07-j.png)'
- en: We ignored the last constant terms because they will be rolled into the overall
    constant of proportionality). Thus
  id: totrans-179
  prefs: []
  type: TYPE_NORMAL
  zh: 我们忽略了最后一个常数项，因为它们将被合并到整体比例常数中。因此
- en: '![](../../OEBPS/Images/eq_13-07-k.png)'
  id: totrans-180
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-k](../../OEBPS/Images/eq_13-07-k.png)'
- en: 'Comparing coefficients:'
  id: totrans-181
  prefs: []
  type: TYPE_NORMAL
  zh: 比较系数：
- en: '![](../../OEBPS/Images/eq_13-07-l.png)'
  id: totrans-182
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-l](../../OEBPS/Images/eq_13-07-l.png)'
- en: The posterior probability maximizes at *![](../../OEBPS/Images/AR_micro.png)[n]*.
    Thus *![](../../OEBPS/Images/AR_micro.png)[MAP]* = *![](../../OEBPS/Images/AR_micro.png)[n]*
  id: totrans-183
  prefs: []
  type: TYPE_NORMAL
  zh: 后验概率在 *![](../../OEBPS/Images/AR_micro.png)[n]* 处最大化。因此 *![](../../OEBPS/Images/AR_micro.png)[MAP]*
    = *![](../../OEBPS/Images/AR_micro.png)[n]*
- en: 'is the MAP estimate for the mean parameter of the multivariate Gaussian data
    distribution: *p*(![](../../OEBPS/Images/AR_x.png)) = 𝒩(![](../../OEBPS/Images/AR_x.png);
    *![](../../OEBPS/Images/AR_micro.png)[n]*, **Λ**^(-1)).'
  id: totrans-184
  prefs: []
  type: TYPE_NORMAL
  zh: 是多元高斯数据分布均值参数的 MAP 估计：*p*(![](../../OEBPS/Images/AR_x.png)) = 𝒩(![](../../OEBPS/Images/AR_x.png);
    *![](../../OEBPS/Images/AR_micro.png)[n]*, **Λ**^(-1))。
- en: 'Note the following:'
  id: totrans-185
  prefs: []
  type: TYPE_NORMAL
  zh: 注意以下内容：
- en: '![](../../OEBPS/Images/eq_13-07-m.png)'
  id: totrans-186
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 13-07-m](../../OEBPS/Images/eq_13-07-m.png)'
- en: With a large volume of data, the estimated mean parameter *![](../../OEBPS/Images/AR_micro.png)[MAP]*
    = *![](../../OEBPS/Images/AR_micro.png)[n]* approaches the MLE *![](../../OEBPS/Images/AR_micro.png)[MLE]*
    = ![](../../OEBPS/Images/AR_x2.png).
  id: totrans-187
  prefs: []
  type: TYPE_NORMAL
  zh: 在数据量较大的情况下，估计的均值参数 *![](../../OEBPS/Images/AR_micro.png)[MAP]* = *![](../../OEBPS/Images/AR_micro.png)[n]*
    接近 MLE *![](../../OEBPS/Images/AR_micro.png)[MLE]* = ![](../../OEBPS/Images/AR_x2.png)。
- en: With a low volume of data, the estimated posterior mean parameter *![](../../OEBPS/Images/AR_micro.png)[MAP]*
    = *![](../../OEBPS/Images/AR_micro.png)[n]* approaches the prior ![](../../OEBPS/Images/AR_micro.png)[0].
  id: totrans-188
  prefs: []
  type: TYPE_NORMAL
  zh: 在数据量较少的情况下，估计的后验均值参数 *![](../../OEBPS/Images/AR_micro.png)[MAP]* = *![](../../OEBPS/Images/AR_micro.png)[n]*
    接近先验 ![](../../OEBPS/Images/AR_micro.png)[0]。
- en: NOTE Fully functional code for multivariate Bayesian inferencing of the mean
    of a Gaussian likelihood with known precision, executable via Jupyter Notebook,
    can be found at [http://mng.bz/J2AP](http://mng.bz/J2AP).
  id: totrans-189
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：完全功能的多元贝叶斯推断高斯似然均值（已知精度）的代码，可通过 Jupyter Notebook 执行，可在 [http://mng.bz/J2AP](http://mng.bz/J2AP)
    找到。
- en: Listing 13.6 PyTorch- Multivariate Bayesian inferencing, unknown mean
  id: totrans-190
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 13.6 PyTorch- 多元贝叶斯推断，未知均值
- en: '[PRE5]'
  id: totrans-191
  prefs: []
  type: TYPE_PRE
  zh: '[PRE5]'
- en: ① Parameters of the prior
  id: totrans-192
  prefs: []
  type: TYPE_NORMAL
  zh: ① 先验参数
- en: ② Parameters of the posterior
  id: totrans-193
  prefs: []
  type: TYPE_NORMAL
  zh: ② 后验参数
- en: '13.10 Fully Bayes parameter estimation: Multivariate, unknown precision, known
    mean'
  id: totrans-194
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 13.10 完全贝叶斯参数估计：多元，未知精度，已知均值
- en: In section [13.6](#sec-bayesinf-sigmaonly), we discussed the univariate case,
    and now we examine the multivariate case. For the univariate case, we had to look
    at the gamma distribution. For the multivariate case, we have to look at the Wishart
    distribution.
  id: totrans-195
  prefs: []
  type: TYPE_NORMAL
  zh: 在第 [13.6](#sec-bayesinf-sigmaonly) 节中，我们讨论了单变量情况，现在我们来考察多元情况。对于单变量情况，我们必须查看伽马分布。对于多元情况，我们必须查看威沙特分布。
- en: 13.10.1 Wishart distribution
  id: totrans-196
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.10.1 威沙特分布
- en: 'Suppose we have a Gaussian random data vector ![](../../OEBPS/Images/AR_x.png)
    with probability density function 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png),
    **Σ**). Once again, we use *precision matrix* Λ instead of the covariance matrix
    Σ, where **Λ** = **Σ**^(−1). Consider the case where we know the mean ![](../../OEBPS/Images/AR_micro.png)
    but want to estimate the precision Λ. How do we express the prior? Note that *p*(**Λ**)
    is the probability density function of a *matrix*. So far, we have encountered
    probability distributions of scalars and vectors, not a matrix. Also, this is
    not an arbitrary matrix. We are talking about a *symmetric, non-negative definite*
    matrix (all covariance and precision matrices belong to this category). Consequently,
    the distribution we are looking for is not a joint distribution of all the *d*²
    matrix elements *d* denotes the dimensionality of the data: that is, all ![](../../OEBPS/Images/AR_x.png)
    and ![](../../OEBPS/Images/AR_micro.png) vectors are *d* × 1). Rather, it is a
    joint distribution of (*d*(*d* + 1))/2 elements in the matrix—the diagonal and
    those above or below (diagonal elements above and below are identical because
    the matrix is symmetric).'
  id: totrans-197
  prefs: []
  type: TYPE_NORMAL
  zh: 假设我们有一个高斯随机数据向量 ![](../../OEBPS/Images/AR_x.png) ，其概率密度函数为 𝒩(![](../../OEBPS/Images/AR_x.png);
    ![](../../OEBPS/Images/AR_micro.png), **Σ**). 再次，我们使用 *精度矩阵* Λ 而不是协方差矩阵 Σ，其中 **Λ**
    = **Σ**^(−1). 考虑我们已知均值 ![](../../OEBPS/Images/AR_micro.png) 但想估计精度 Λ 的情况。我们如何表达先验？请注意，*p*(**Λ**)
    是一个 *矩阵* 的概率密度函数。到目前为止，我们遇到了标量和向量的概率分布，而不是矩阵。此外，这也不是一个任意的矩阵。我们谈论的是一个 *对称、非负定*
    的矩阵（所有协方差和精度矩阵都属于这一类）。因此，我们寻找的分布不是所有 *d*² 矩阵元素的联合分布 *d* 表示数据的维度：即所有 ![](../../OEBPS/Images/AR_x.png)
    和 ![](../../OEBPS/Images/AR_micro.png) 向量都是 *d* × 1）。而是矩阵中 (*d*(*d* + 1))/2 个元素的联合分布——对角线及其上或下的元素（对角线上的元素上下相同，因为矩阵是对称的）。
- en: 'The space of such matrices is called a *Wishart ensemble*. The probability
    of a random-precision matrix Λ of size *d* × *d* can be expressed as a Wishart
    distribution. This distribution has two parameters:'
  id: totrans-198
  prefs: []
  type: TYPE_NORMAL
  zh: 这样的矩阵空间被称为 *Wishart 系统集*。一个大小为 *d* × *d* 的随机精度矩阵 Λ 的概率可以表示为Wishart分布。这个分布有两个参数：
- en: '*ν*, a scalar, satisfying *ν* > *d* − 1'
  id: totrans-199
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*ν*，一个满足 *ν* > *d* − 1 的标量'
- en: W, a *d* × *d* symmetric non-negative definite matrix
  id: totrans-200
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: W，一个 *d* × *d* 的对称非负定矩阵
- en: The probability density function is
  id: totrans-201
  prefs: []
  type: TYPE_NORMAL
  zh: 概率密度函数是
- en: '![](../../OEBPS/Images/eq_13-07-n1.png)'
  id: totrans-202
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_13-07-n1.png)'
- en: where
  id: totrans-203
  prefs: []
  type: TYPE_NORMAL
  zh: 其中
- en: 𝒲 denotes Wishart.
  id: totrans-204
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 𝒲 表示Wishart。
- en: '|**W**|, |**Λ**| denote the determinants of the matrices W and Λ, respectively.'
  id: totrans-205
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '|**W**|, |**Λ**| 分别表示矩阵 W 和 Λ 的行列式。'
- en: '*Tr*(*A*) denotes the trace of a matrix *A* (sum of the diagonal elements).'
  id: totrans-206
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*Tr*(*A*) 表示矩阵 *A* 的迹（对角线元素之和）。'
- en: '*Γ* denotes the multivariate gamma function'
  id: totrans-207
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*Γ* 表示多元伽马函数'
- en: '![](../../OEBPS/Images/eq_13-07-n2.png)'
  id: totrans-208
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_13-07-n2.png)'
- en: The Wishart is the multivariate version of the gamma distribution. Its expected
    value is
  id: totrans-209
  prefs: []
  type: TYPE_NORMAL
  zh: Wishart 是伽马分布的多变量版本。其期望值是
- en: 𝔼(**Λ**) = *ν***W**
  id: totrans-210
  prefs: []
  type: TYPE_NORMAL
  zh: 𝔼(**Λ**) = *ν***W**
- en: Its maxima occur at
  id: totrans-211
  prefs: []
  type: TYPE_NORMAL
  zh: 其最大值出现在
- en: '**Λ** = (*ν* − *d* − 1)**W** for *ν* ≥ *d* + 1'
  id: totrans-212
  prefs: []
  type: TYPE_NORMAL
  zh: '**Λ** = (*ν* − *d* − 1)**W** 对于 *ν* ≥ *d* + 1'
- en: 13.10.2 Estimating precision
  id: totrans-213
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 13.10.2 估计精度
- en: As before, we model the data distribution as a Gaussian *p*(![](../../OEBPS/Images/AR_x.png)|![](../../OEBPS/Images/AR_micro.png),
    **Λ**) = 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png),
    **Λ**^(−1)), where we have expressed the Gaussian in terms of the *precision matrix*
    Λ instead of the covariance matrix Σ, where **Λ** = **Σ**^(−1).
  id: totrans-214
  prefs: []
  type: TYPE_NORMAL
  zh: 如前所述，我们将数据分布建模为高斯分布 *p*(![](../../OEBPS/Images/AR_x.png)|![](../../OEBPS/Images/AR_micro.png),
    **Λ**) = 𝒩(![](../../OEBPS/Images/AR_x.png); ![](../../OEBPS/Images/AR_micro.png),
    **Λ**^(−1))，其中我们用 *精度矩阵* Λ 而不是协方差矩阵 Σ 来表示高斯分布，其中 **Λ** = **Σ**^(−1)。
- en: The training data set is *X* ≡ {![](../../OEBPS/Images/AR_x.png)^((1)), ![](../../OEBPS/Images/AR_x.png)^((2)),⋯,
    ![](../../OEBPS/Images/AR_x.png)^((*i*)),⋯, ![](../../OEBPS/Images/AR_x.png)^((*n*))},
    and its overall likelihood is
  id: totrans-215
  prefs: []
  type: TYPE_NORMAL
  zh: 训练数据集是 *X* ≡ {![](../../OEBPS/Images/AR_x.png)^((1)), ![](../../OEBPS/Images/AR_x.png)^((2)),⋯,
    ![](../../OEBPS/Images/AR_x.png)^((*i*)),⋯, ![](../../OEBPS/Images/AR_x.png)^((*n*))}，其整体似然函数是
- en: '![](../../OEBPS/Images/eq_13-07-n3.png)'
  id: totrans-216
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_13-07-n3.png)'
- en: We model the prior probability of the precision matrix as a Wishart distribution.
    Hence,
  id: totrans-217
  prefs: []
  type: TYPE_NORMAL
  zh: 我们将精度矩阵的先验概率建模为Wishart分布。因此，
- en: '![](../../OEBPS/Images/eq_13-07-n4.png)'
  id: totrans-218
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_13-07-n4.png)'
- en: 'The posterior is another Wishart (owing to the Wishart conjugate prior property):'
  id: totrans-219
  prefs: []
  type: TYPE_NORMAL
  zh: 后验分布是另一个Wishart分布（归功于Wishart共轭先验性质）：
- en: '![](../../OEBPS/Images/eq_13-07-n5.png)'
  id: totrans-220
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_13-07-n5.png)'
- en: Using Bayes’ theorem for the training data set *X*,
  id: totrans-221
  prefs: []
  type: TYPE_NORMAL
  zh: 使用贝叶斯定理对训练数据集*X*进行估计，
- en: '![](../../OEBPS/Images/eq_13-07-o1.png)'
  id: totrans-222
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o1.png)'
- en: Let’s study a pair of simple lemmas that will come in handy.
  id: totrans-223
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们研究一对有用的简单引理。
- en: '![](../../OEBPS/Images/eq_13-07-o2.png)'
  id: totrans-224
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o2.png)'
- en: where *Tr* refers to Trace of a matrix (sum of diagonal elements).
  id: totrans-225
  prefs: []
  type: TYPE_NORMAL
  zh: 其中*Tr*指的是矩阵的迹（对角元素之和）。
- en: The first lemma is almost trivial—the quadratic form ![](../../OEBPS/Images/AR_x.png)*^TA*![](../../OEBPS/Images/AR_x.png)
    is a scalar, so of course it is the same as its trace.
  id: totrans-226
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 第一个引理几乎是显而易见的——二次型![](../../OEBPS/Images/AR_x.png)*^TA*![](../../OEBPS/Images/AR_x.png)是一个标量，所以当然它与它的迹是相同的。
- en: 'The second lemma follows directly from the matrix property of a trace: *Tr*(*BC*)
    = *Tr*(*CB*).'
  id: totrans-227
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 第二个引理直接来自迹的矩阵属性：*Tr*(*BC*) = *Tr*(*CB*).
- en: Using the lemmas, the exponent of the likelihood term is
  id: totrans-228
  prefs: []
  type: TYPE_NORMAL
  zh: 使用引理，似然项的指数为
- en: '![](../../OEBPS/Images/eq_13-07-o3.png)'
  id: totrans-229
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o3.png)'
- en: where
  id: totrans-230
  prefs: []
  type: TYPE_NORMAL
  zh: 其中
- en: '![](../../OEBPS/Images/eq_13-07-o4.png)'
  id: totrans-231
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o4.png)'
- en: Thus, the posterior density is
  id: totrans-232
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，后验密度为
- en: '![](../../OEBPS/Images/eq_13-07-o5.png)'
  id: totrans-233
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o5.png)'
- en: Since *Tr*(*A*) + *Tr*(*B*) = *Tr*(*A* + *B*),
  id: totrans-234
  prefs: []
  type: TYPE_NORMAL
  zh: 由于*Tr*(*A*) + *Tr*(*B*) = *Tr*(*A* + *B*),
- en: '![](../../OEBPS/Images/eq_13-07-o6.png)'
  id: totrans-235
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o6.png)'
- en: 'Comparing coefficients, we determine the unknown parameters of the posterior
    distribution:'
  id: totrans-236
  prefs: []
  type: TYPE_NORMAL
  zh: 通过比较系数，我们确定后验分布的未知参数：
- en: '![](../../OEBPS/Images/eq_13-07-o7.png)'
  id: totrans-237
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o7.png)'
- en: where
  id: totrans-238
  prefs: []
  type: TYPE_NORMAL
  zh: 其中
- en: '![](../../OEBPS/Images/eq_13-07-o8.png)'
  id: totrans-239
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o8.png)'
- en: 'The maximum of the posterior density function, **𝒲**(**Λ**;*ν[n]*, **W**[n]),
    ields an estimate for the precision parameter of the data distribution: **Λ**
    = (*ν[n]* − *d* − 1)**W**[n] for *ν[n]* ≥ *d* + 1 i.e.,'
  id: totrans-240
  prefs: []
  type: TYPE_NORMAL
  zh: 后验密度函数的最大值，**𝒲**(**Λ**;*ν[n]*, **W**[n])，给出了数据分布精度参数的估计：**Λ** = (*ν[n]* − *d*
    − 1)**W**[n] 对于 *ν[n]* ≥ *d* + 1，即，
- en: '![](../../OEBPS/Images/eq_13-07-o9.png)'
  id: totrans-241
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_13-07-o9.png)'
- en: Summary
  id: totrans-242
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 摘要
- en: A generative model that models the underlying data distribution can be more
    powerful than a black box discriminative model. Once we choose a model family,
    we need to estimate the model parameters, *θ*. We can estimate the best values
    of *θ* from the training data *X* using Bayes’ theorem.
  id: totrans-243
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 一个能够模拟底层数据分布的生成模型可能比一个黑盒判别模型更强大。一旦我们选择了一个模型族，我们需要估计模型参数，*θ*。我们可以使用贝叶斯定理从训练数据*X*中估计*θ*的最佳值。
- en: The posterior distribution *p*(*θ*|*X*) is a function of the product of likelihood
    *p*(*X*|*θ*) and the prior *p*(*θ*). The prior expresses our belief in the value
    of the parameters. The posterior is dominated by the prior for small data sets
    and the likelihood for large data sets. Injecting belief via a good prior distribution
    can be helpful in settings with very little training data.
  id: totrans-244
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 后验分布*p*(*θ*|*X*)是似然率*p*(*X*|*θ*)和先验*p*(*θ*)的乘积的函数。先验表达了我们对参数值的信念。对于小数据集，后验主要由先验决定，对于大数据集，后验主要由似然率决定。通过一个好的先验分布注入信念，在训练数据非常少的情况下可能是有帮助的。
- en: Maximum likelihood estimation only relies on the data, in contrast to maximum
    a posteriori (MAP) estimation, which relies on the data as well as the prior information.
  id: totrans-245
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 最大似然估计仅依赖于数据，与最大后验估计（MAP）相对，后者既依赖于数据也依赖于先验信息。
- en: We can use Bayesian estimation for the mean of a Gaussian likelihood when the
    variance is known. When the likelihood is Gaussian *p*(*X*) ∼ *N*(*μ*, *σ*), we
    model the prior as a normal distribution *p*(*μ*) ∼ *N*(*μ*[0], *σ*[0]). The posterior
    distribution is also a normal distribution *p*(*μ*|*X*) ∼ *N*(*μ[n]*, *σ[n]*),
    where ![](../../OEBPS/Images/eq_13-07-p1a.png) and ![](../../OEBPS/Images/eq_13-07-p2a.png).
    We can also use the estimated parameter to make predictions about new instances
    of data.
  id: totrans-246
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当方差已知时，我们可以使用贝叶斯估计高斯似然率的均值。当似然率是高斯分布*p*(*X*) ∼ *N*(*μ*, *σ*), 我们将先验模型建模为正态分布*p*(*μ*)
    ∼ *N*(*μ*[0], *σ*[0])。后验分布也是一个正态分布*p*(*μ*|*X*) ∼ *N*(*μ[n]*, *σ[n]*)，其中![](../../OEBPS/Images/eq_13-07-p1a.png)和![](../../OEBPS/Images/eq_13-07-p2a.png)。我们还可以使用估计的参数对新数据实例进行预测。
- en: Weak priors imply a high degree of uncertainty/lower confidence in our prior
    belief and can easily be overwhelmed by the data. In contrast, strong priors imply
    a lower degree of uncertainty/higher confidence in our prior belief and will resist
    data overload.
  id: totrans-247
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 弱先验意味着我们对先验信念的不确定性程度高/信心低，并且很容易被数据所淹没。相比之下，强先验意味着我们对先验信念的不确定性程度低/信心高，并且能够抵抗数据过载。
- en: For a specific family of likelihood, the choice of the prior that results in
    the posterior belonging to the same family as the prior is called a conjugate
    prior.
  id: totrans-248
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 对于特定的似然族，导致后验属于与先验相同族的先验选择被称为共轭先验。
- en: The gamma function is ![](../../OEBPS/Images/eq_13-07-p3a.png), and the gamma
    distribution is ![](../../OEBPS/Images/eq_13-07-p4a.png). The gamma distribution
    varies with different values of *α* and *β*.
  id: totrans-249
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 伽马函数是 ![](../../OEBPS/Images/eq_13-07-p3a.png)，伽马分布是 ![](../../OEBPS/Images/eq_13-07-p4a.png)。伽马分布随
    *α* 和 *β* 的不同值而变化。
- en: In the case of Bayesian estimation of the precision of the Gaussian likelihood
    for a known mean, the precision *λ* is the inverse of the variance. We can model
    the prior as a gamma distribution ![](../../OEBPS/Images/eq_13-07-p5a.png). The
    posterior distribution is also a gamma distribution, ![](../../OEBPS/Images/eq_13-07-p6a.png),
    where ![](../../OEBPS/Images/eq_13-07-p7a.png) and ![](../../OEBPS/Images/eq_13-07-p8a.png).
  id: totrans-250
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在已知均值的情况下，对高斯似然精度的贝叶斯估计中，精度 *λ* 是方差的倒数。我们可以将先验模型化为一个伽马分布 ![](../../OEBPS/Images/eq_13-07-p5a.png)。后验分布也是一个伽马分布，![](../../OEBPS/Images/eq_13-07-p6a.png)，其中
    ![](../../OEBPS/Images/eq_13-07-p7a.png) 和 ![](../../OEBPS/Images/eq_13-07-p8a.png)。
- en: In Bayesian estimation of both the mean and precision of a Gaussian likelihood,
    we model the prior as a normal-gamma distribution. The posterior is another normal-gamma
    distribution. The posterior distribution can be used to predict new data instances.
  id: totrans-251
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在对高斯似然的均值和精度进行贝叶斯估计的情况下，我们将先验模型化为一个正态-伽马分布。后验也是一个正态-伽马分布。后验分布可以用来预测新的数据实例。
- en: The multivariate setting of Bayesian inferencing of the mean of a Gaussian likelihood
    is known as precision. We can model the prior as a multivariate normal distribution;
    the posterior is also a multivariate normal distribution.
  id: totrans-252
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 高斯似然均值的多变量贝叶斯推断被称为精度。我们可以将先验模型化为一个多元正态分布；后验也是一个多元正态分布。
- en: The Wishart distribution is the multivariate version of the gamma distribution.
    With multivariate Bayesian inferencing of the precision of a Gaussian likelihood
    with a known mean, we can model the prior as a Wishart distribution. The corresponding
    posterior is also a Wishart distribution.
  id: totrans-253
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Wishart 分布是伽马分布的多变量版本。在已知均值的情况下，使用多元贝叶斯推断高斯似然精度的精度，我们可以将先验模型化为一个 Wishart 分布。相应地，后验也是一个
    Wishart 分布。
