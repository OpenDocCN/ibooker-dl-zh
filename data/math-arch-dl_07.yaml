- en: '8 Training neural networks: Forward propagation and backpropagation'
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 8 训练神经网络：前向传播和后向传播
- en: This chapter covers
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 本章涵盖
- en: Sigmoid functions as differential surrogates for Heaviside step functions
  id: totrans-2
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Sigmoid 函数作为 Heaviside 步函数的微分替代
- en: 'Layering in neural networks: expressing linear layers as matrix-vector multiplication'
  id: totrans-3
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 神经网络中的分层：将线性层表示为矩阵-向量乘法
- en: Regression loss, forward and backward propagation, and their math
  id: totrans-4
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 回归损失、前向和后向传播及其数学
- en: So far, we have seen that neural networks make complicated real-life decisions
    by modeling the decision-making process with mathematical functions. These functions
    can become arbitrarily involved, but fortunately, we have a simple building block
    called a *perceptron* that can be repeated systematically to model any arbitrary
    function. We need not even explicitly know the function being modeled in closed
    form. All we need is a reasonably sized set of sample inputs and corresponding
    correct outputs. This collection of input and output pairs is known as *training
    data*. Armed with this training data, we can *train* a multilayer perceptron (MLP,
    aka neural network) to emit reasonably correct outputs on inputs it has never
    seen before.
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
  zh: 到目前为止，我们已经看到神经网络通过用数学函数建模决策过程来做出复杂的现实生活决策。这些函数可以变得非常复杂，但幸运的是，我们有一个简单的构建块，称为
    *感知器*，可以系统地重复以建模任何任意函数。我们甚至不需要显式地知道正在建模的函数的闭式形式。我们需要的只是一个合理大小的样本输入集和相应的正确输出。这个输入和输出对的集合被称为
    *训练数据*。有了这个训练数据，我们可以 *训练* 一个多层感知器（MLP，也称为神经网络），使其在从未见过的输入上产生合理的正确输出。
- en: Such neural networks, where we need to know the output for each input in the
    training data set, are known as *supervised* neural networks. The correct output
    for the training inputs is typically generated via a manual process called *labeling*.
    Labeling is expensive and time-consuming. Much research is going on toward unsupervised,
    semi-supervised, and self-supervised networks, eliminating or minimizing the labeling
    process. But as of now, the accuracy of unsupervised and self-supervised networks
    in general does not match that of supervised networks. In this chapter, we focus
    on supervised neural networks. In chapter [14](../Text/14.xhtml#ch-ae-vae), we
    will study unsupervised networks.
  id: totrans-6
  prefs: []
  type: TYPE_NORMAL
  zh: 这种需要我们知道训练数据集中每个输入的输出的神经网络被称为 *监督* 神经网络。训练输入的正确输出通常是通过一种称为 *标注* 的手动过程生成的。标注既昂贵又耗时。许多研究正在进行中，旨在开发无监督、半监督和自监督网络，以消除或最小化标注过程。但到目前为止，无监督和自监督网络在一般情况下的准确性并不匹配监督网络。在本章中，我们专注于监督神经网络。在第
    [14](../Text/14.xhtml#ch-ae-vae) 章中，我们将研究无监督网络。
- en: What is this process of “training” a neural network? It essentially estimates
    the parameter values that would make the network emit output values as close as
    possible to the known correct outputs on the training inputs. In this chapter,
    we discuss how this is done. But before that, we have to learn a few other things.
  id: totrans-7
  prefs: []
  type: TYPE_NORMAL
  zh: 这个“训练”神经网络的过程是什么？它本质上估计了使网络在训练输入上发出尽可能接近已知正确输出的参数值。在本章中，我们将讨论这是如何完成的。但在那之前，我们必须学习一些其他的东西。
- en: NOTE The complete PyTorch code for this chapter is available at [http://mng.bz/YAXa](http://mng.bz/YAXa)
    in the form of fully functional and executable Jupyter notebooks.
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：本章的完整 PyTorch 代码以完全功能性和可执行的 Jupyter 笔记本形式，可在 [http://mng.bz/YAXa](http://mng.bz/YAXa)
    获取。
- en: 8.1 Differentiable step-like functions
  id: totrans-9
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 8.1 可微的类似步骤函数
- en: In equation [7.3](../Text/07.xhtml#eq-perceptron), we expressed the perceptron
    as a combination of a Heaviside step function *ϕ* and an affine transformation
    ![](../../OEBPS/Images/AR_w.png)*^T*![](../../OEBPS/Images/AR_x.png) + *b*. This
    is the perceptron we used throughout chapter [7](../Text/07.xhtml#ch-func-approx)
    and with which we were able to express (model) pretty much all functions of interest.
  id: totrans-10
  prefs: []
  type: TYPE_NORMAL
  zh: 在方程 [7.3](../Text/07.xhtml#eq-perceptron) 中，我们将感知器表示为 Heaviside 步函数 *ϕ* 和仿射变换
    ![](../../OEBPS/Images/AR_w.png)*^T*![](../../OEBPS/Images/AR_x.png) + *b* 的组合。这是我们贯穿第
    [7](../Text/07.xhtml#ch-func-approx) 章所使用的感知器，并且我们能够表达（建模）几乎所有感兴趣的功能。
- en: 'Despite its expressive power, the Heaviside step function has a drawback: it
    has a discontinuity at *x* = 0 and is *not differentiable*. Why is differentiability
    important? As we shall see in chapter [8](../Text/08.xhtml#ch-training-neural-networks)
    (and got a glimpse of in section [3.3](../Text/03.xhtml#sec-grad)), optimal training
    of a neural network requires evaluation of the gradient vector of a loss function
    with respect to weights. Since the gradient is nothing but a vector of partial
    derivatives, differentiability is needed for training.'
  id: totrans-11
  prefs: []
  type: TYPE_NORMAL
  zh: 尽管Heaviside步进函数具有强大的表达能力，但它有一个缺点：它在 *x* = 0 处有间断，并且不可微分。为什么可微性很重要？正如我们将在第 [8](../Text/08.xhtml#ch-training-neural-networks)
    章（并在第 [3.3](../Text/03.xhtml#sec-grad) 节中略见一斑）中看到的那样，神经网络的优化训练需要评估损失函数相对于权重的梯度向量。由于梯度实际上是一个偏导数的向量，因此可微性对于训练是必需的。
- en: In this section, we discuss a few functions that are differentiable and yet
    can mimic the Heaviside step function. The most significant among them is the
    sigmoid function.
  id: totrans-12
  prefs: []
  type: TYPE_NORMAL
  zh: 在本节中，我们讨论一些可微分的函数，同时可以模拟 Heaviside 步进函数。其中最重要的是 S 形函数。
- en: 8.1.1 Sigmoid function
  id: totrans-13
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.1.1 S 形函数
- en: The sigmoid function is named after its characteristic S-shaped curve figure
    [8.1](../Text/08.xhtml#fig-sigmoid1d)). The corresponding equation is
  id: totrans-14
  prefs: []
  type: TYPE_NORMAL
  zh: S 形函数以其特征的 S 形曲线图而命名 [8.1](../Text/08.xhtml#fig-sigmoid1d))。相应的方程是
- en: '![](../../OEBPS/Images/eq_08-01.png)'
  id: totrans-15
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-01.png)'
- en: Equation 8.1
  id: totrans-16
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.1
- en: '![](../../OEBPS/Images/CH08_F01_Chaudhury.png)'
  id: totrans-17
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH08_F01_Chaudhury.png)'
- en: Figure 8.1 Graph of a 1D sigmoid function
  id: totrans-18
  prefs: []
  type: TYPE_NORMAL
  zh: 图 8.1 1D S 形函数的图像
- en: Parameterized sigmoid function
  id: totrans-19
  prefs: []
  type: TYPE_NORMAL
  zh: 参数化 S 形函数
- en: We can parametrize equation [1.5](../Text/01.xhtml#eq-sigmoid) as
  id: totrans-20
  prefs: []
  type: TYPE_NORMAL
  zh: 我们可以将方程 [1.5](../Text/01.xhtml#eq-sigmoid) 参数化为
- en: '![](../../OEBPS/Images/eq_08-02.png)'
  id: totrans-21
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-02.png)'
- en: Equation 8.2
  id: totrans-22
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.2
- en: This allows us to
  id: totrans-23
  prefs: []
  type: TYPE_NORMAL
  zh: 这使我们能够
- en: Adjust the steepness of the linear portion of the S curve by changing *w*
  id: totrans-24
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 通过改变 *w* 调整 S 曲线线性部分的陡峭程度
- en: Adjust the position of the curve by changing *b*
  id: totrans-25
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 通过改变 *b* 调整曲线的位置
- en: Figure [8.2](#fig-sigmoid-parameterized) shows how the parametrized sigmoid
    curve changes with different values for the parameters *w* and *b*. In particular,
    note that for large values of *w*, the parameterized sigmoid is virtually indistinguishable
    from the Heaviside step function (compare the dotted curve in figure [8.2a](#fig-sigmoid-parameterized-various-ws)
    with figure [7.7](../Text/07.xhtml#fig-step-1D)), even though it remains differentiable.
    This is exactly what we desire in neural networks.
  id: totrans-26
  prefs: []
  type: TYPE_NORMAL
  zh: 图 [8.2](#fig-sigmoid-parameterized) 展示了参数化的 S 形曲线如何随着参数 *w* 和 *b* 的不同值而变化。特别是，请注意，对于大的
    *w* 值，参数化的 S 形函数几乎与 Heaviside 步进函数无法区分（比较图 [8.2a](#fig-sigmoid-parameterized-various-ws)
    中的虚线曲线与图 [7.7](../Text/07.xhtml#fig-step-1D)），尽管它仍然是可微分的。这正是我们在神经网络中所期望的。
- en: '![](../../OEBPS/Images/CH08_F02_Chaudhury.png)'
  id: totrans-27
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH08_F02_Chaudhury.png)'
- en: Figure 8.2 Sigmoid curves corresponding to various parameter values in equation
    [8.2](../Text/08.xhtml#eq-sigmoid-parameterized)
  id: totrans-28
  prefs: []
  type: TYPE_NORMAL
  zh: 图 8.2 方程 [8.2](../Text/08.xhtml#eq-sigmoid-parameterized) 中对应于各种参数值的 S 形曲线
- en: Some properties of the sigmoid function
  id: totrans-29
  prefs: []
  type: TYPE_NORMAL
  zh: S 形函数的一些性质
- en: The sigmoid function has several interesting properties, some of which are listed
    here with proof outlines.
  id: totrans-30
  prefs: []
  type: TYPE_NORMAL
  zh: S 形函数有几个有趣的性质，其中一些性质在此列出，并附有证明概要。
- en: '*Expression with positive x*:'
  id: totrans-31
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*正 x 的表达式*：'
- en: '![](../../OEBPS/Images/eq_08-03.png)'
  id: totrans-32
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-03.png)'
- en: Equation 8.3
  id: totrans-33
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.3
- en: This expression can be easily proved by multiplying both the numerator and denominator
    of equation [1.5](../Text/01.xhtml#eq-sigmoid) by *e[x]*.
  id: totrans-34
  prefs: []
  type: TYPE_NORMAL
  zh: 通过将方程 [1.5](../Text/01.xhtml#eq-sigmoid) 的分子和分母都乘以 *e[x]*，可以轻松证明这个表达式。
- en: '*Sigmoid of negative x*:'
  id: totrans-35
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*负 x 的 S 形函数*：'
- en: '![](../../OEBPS/Images/eq_08-04.png)'
  id: totrans-36
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-04.png)'
- en: Equation 8.4
  id: totrans-37
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.4
- en: '*Derivative of sigmoid*:'
  id: totrans-38
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*S 形函数的导数*：'
- en: '![](../../OEBPS/Images/eq_08-05.png)'
  id: totrans-39
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-05.png)'
- en: Equation 8.5
  id: totrans-40
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.5
- en: Figure [8.3](#fig-sigmoid1d-with-derivative) shows the graph of the derivative
    of the sigmoid superimposed on the sigmoid graph itself. As expected, the derivative
    has its maximum value at the middle of the sigmoid curve (where the sigmoid is
    climbing more or less linearly) and is near zero at both ends (where the sigmoid
    is saturated and flat, hardly changing).
  id: totrans-41
  prefs: []
  type: TYPE_NORMAL
  zh: 图 [8.3](#fig-sigmoid1d-with-derivative) 显示了 S 形函数的导数图像叠加在 S 形函数图像本身上。正如预期的那样，导数在
    S 形曲线的中间（S 形函数大致线性上升的地方）达到最大值，在两端（S 形函数饱和和平坦，几乎不变化的地方）接近零。
- en: '![](../../OEBPS/Images/CH08_F03_Chaudhury.png)'
  id: totrans-42
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH08_F03_Chaudhury.png)'
- en: Figure 8.3 Graph of a 1D sigmoid function (solid curve) and its derivative (dashed
    curve)
  id: totrans-43
  prefs: []
  type: TYPE_NORMAL
  zh: 图 8.3 1D sigmoid 函数（实线）及其导数（虚线）的图
- en: 8.1.2 Tanh function
  id: totrans-44
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.1.2 双曲正切函数
- en: '![](../../OEBPS/Images/CH08_F04_Chaudhury.png)'
  id: totrans-45
  prefs: []
  type: TYPE_IMG
  zh: '![图像](../../OEBPS/Images/CH08_F04_Chaudhury.png)'
- en: Figure 8.4 Graph of a 1D tanh function.
  id: totrans-46
  prefs: []
  type: TYPE_NORMAL
  zh: 图 8.4 1D tanh 函数的图
- en: An alternative to the sigmoid function is the *hyperbolic tangent* tanh function,
    shown in figure [8.4](#fig-tanh1d). It is very similar to the sigmoid function,
    but the range of output values is from [−1,1] as opposed to [0,1]. In essence,
    it is the sigmoid function stretched and shifted so it is centered around 0\.
    The equation of the tanh function is given by
  id: totrans-47
  prefs: []
  type: TYPE_NORMAL
  zh: sigmoid 函数的另一种选择是*双曲正切*函数 tanh，如图[8.4](#fig-tanh1d)所示。它与 sigmoid 函数非常相似，但输出值的范围是从[−1,1]，而不是[0,1]。本质上，它是将
    sigmoid 函数拉伸并平移，使其以0为中心。tanh 函数的方程式如下
- en: '![](../../OEBPS/Images/eq_08-06.png)'
  id: totrans-48
  prefs: []
  type: TYPE_IMG
  zh: '![图像](../../OEBPS/Images/eq_08-06.png)'
- en: Equation 8.6
  id: totrans-49
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.6
- en: 'Why is tanh preferred over sigmoid? To understand this, consider figure [8.5](#fig-derivative-sigmoid-tanh-1d).
    It compares the derivatives of the sigmoid and tanh functions. As the plot shows,
    the derivative (gradient) of the function near *x* = 0 is much higher for tanh
    than for sigmoid. Stronger gradients mean faster convergence, as the weight updates
    happen in larger steps. Note that this holds mainly when the data is centered
    around 0: in most preprocessing steps, we standardize the data (make it 0 mean)
    before feeding it into the neural network.'
  id: totrans-50
  prefs: []
  type: TYPE_NORMAL
  zh: 为什么 tanh 比 sigmoid 更受欢迎？为了理解这一点，请考虑图[8.5](#fig-derivative-sigmoid-tanh-1d)。它比较了
    sigmoid 和 tanh 函数的导数。如图所示，函数在*x* = 0附近的导数（梯度）比 sigmoid 函数的导数要高得多。更强的梯度意味着收敛速度更快，因为权重更新是以更大的步骤发生的。请注意，这主要适用于数据集中在0附近的情况：在大多数预处理步骤中，我们在将其输入神经网络之前对数据进行标准化（使其均值为0）。
- en: '![](../../OEBPS/Images/CH08_F05_Chaudhury.png)'
  id: totrans-51
  prefs: []
  type: TYPE_IMG
  zh: '![图像](../../OEBPS/Images/CH08_F05_Chaudhury.png)'
- en: Figure 8.5 Graph of the derivatives of 1D sigmoid and tanh functions
  id: totrans-52
  prefs: []
  type: TYPE_NORMAL
  zh: 图 8.5 1D sigmoid 和 tanh 函数的导数图
- en: 8.2 Why layering?
  id: totrans-53
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 8.2 为什么分层？
- en: In section [7.5](../Text/07.xhtml#sec-layering), we encountered the idea of
    *layering* as the preferred way to organize multiple perceptrons. The main property
    of a layered network is that neurons in any layer take their input only from the
    outputs of the preceding layer. This means connections exist only between successive
    layers. No other connection exists in the MLP, which greatly simplifies the evaluation
    and training of the network, which will become apparent as we discuss forward
    propagation and backpropagation.
  id: totrans-54
  prefs: []
  type: TYPE_NORMAL
  zh: 在第[7.5](../Text/07.xhtml#sec-layering)节中，我们遇到了将*分层*作为组织多个感知器首选方法的想法。分层网络的主要特性是任何层的神经元只从前一层的输出中获取输入。这意味着只存在连续层之间的连接。在多层感知器（MLP）中不存在其他连接，这极大地简化了网络的评估和训练，这一点在我们讨论前向传播和反向传播时将变得明显。
- en: Why have layers at all? We have seen that multiple perceptrons allow us to model
    problems that cannot be solved by a single perceptron (such as the XOR problem
    discussed in section [7.4.1](../Text/07.xhtml#sec-mlp-xor)). In theory, it is
    possible to model all mathematical functions (and hence solve all quantifiable
    problems) with neurons organized in a single hidden layer (see Cybenko’s theorem
    and proof in section [7.5.3](../Text/07.xhtml#sec-cybenko-uat)). However, that
    does not mean a single hidden layer is the most *efficient* way of doing all modelings.
    We can often model complicated problems with *fewer* perceptrons if we organize
    them in more than one layer.
  id: totrans-55
  prefs: []
  type: TYPE_NORMAL
  zh: 为什么要有层？我们已经看到，多个感知器使我们能够模拟单一生成器无法解决的问题（例如第[7.4.1](../Text/07.xhtml#sec-mlp-xor)节中讨论的
    XOR 问题）。从理论上讲，使用组织在单个隐藏层中的神经元可以模拟所有数学函数（因此解决所有可量化的问题）（参见第[7.5.3](../Text/07.xhtml#sec-cybenko-uat)节中
    Cybenko 的定理和证明）。然而，这并不意味着单个隐藏层是进行所有建模的最*有效*方式。如果我们将它们组织在多个层中，我们通常可以用更少的感知器来模拟复杂问题。
- en: Why do extra layers help? The primary reason is the extra nonlinearity. Each
    layer brings in its own nonlinear (such as sigmoid) function. Nonlinear functions,
    with proper parametrization, can model more complicated functions. Hence, a larger
    count of nonlinear functions in the model typically implies greater expressive
    power.
  id: totrans-56
  prefs: []
  type: TYPE_NORMAL
  zh: 为什么额外的层有帮助？主要原因是额外的非线性。每一层都引入它自己的非线性函数（例如 sigmoid）。通过适当的参数化，非线性函数可以模拟更复杂的函数。因此，模型中非线性函数的数量越多，通常意味着表达力越强。
- en: 8.3 Linear layers
  id: totrans-57
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 8.3 线性层
- en: 'Various types of layers are used in popular neural network architectures. In
    subsequent chapters, we shall look at different kinds of layers, such as convolution
    layers. But in this section, we examine the simplest and most basic type of layer:
    the *linear* layer. Here every perceptron from the previous layer is connected
    to every perceptron in the next layer. Such a layer is also known as *fully connected*
    layer. Thus if the previous layer has *m* neurons and the next layer has *n* neurons,
    there are *mn* connections, each with its own weight.'
  id: totrans-58
  prefs: []
  type: TYPE_NORMAL
  zh: 在流行的神经网络架构中使用了各种类型的层。在随后的章节中，我们将探讨不同类型的层，例如卷积层。但在这个部分，我们检查最简单和最基本的层类型：*线性层*。在这里，前一层的每个感知器都与下一层的每个感知器相连。这样的层也被称为*全连接层*。因此，如果前一层有
    *m* 个神经元，下一层有 *n* 个神经元，那么就有 *mn* 个连接，每个连接都有自己的权重。
- en: NOTE We use the words *neuron* and *perceptron* interchangeably.
  id: totrans-59
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：我们交替使用“神经元”和“感知器”这两个词。
- en: Figure [8.6](#fig-linear-layer) shows a linear layer that is a slice of a bigger
    MLP. Figure [8.7](#fig-full-MLP-with-linear-layer) shows a bigger MLP with a linear
    layer. Consistent with previous chapters, we have used superscripts for layer
    IDs and subscripts for source and destination IDs.
  id: totrans-60
  prefs: []
  type: TYPE_NORMAL
  zh: 图[8.6](#fig-linear-layer)显示了一个线性层，它是更大的MLP的一部分。图[8.7](#fig-full-MLP-with-linear-layer)显示了一个带有线性层的更大的MLP。与前面的章节一致，我们使用上标表示层ID，下标表示源和目标ID。
- en: 'The weight of the connection from the *k*th neuron in layer (*l* − 1) to the
    *j*th neuron in layer *l* is denoted *w[jk]*^((*l*)). Here the subscript ordering
    is the destination (*j*) followed by the source (*k*). This is slightly counterintuitive
    but universally followed because it simplifies the matrix notation (described
    shortly). Note the following:'
  id: totrans-61
  prefs: []
  type: TYPE_NORMAL
  zh: 从层 (*l* − 1) 的第 *k* 个神经元到层 *l* 的第 *j* 个神经元的连接权重表示为 *w[jk]*^((*l*)）。这里下标顺序是目标
    (*j*) 后跟源 (*k*）。这稍微有些反直觉，但普遍遵循，因为它简化了矩阵表示（稍后描述）。注意以下几点：
- en: We have split a single perceptron (weighted sum followed by sigmoid) into two
    separate layers, weighted sum and sigmoid.
  id: totrans-62
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 我们将单个感知器（加权求和后跟sigmoid）拆分为两个独立的层，加权求和和sigmoid。
- en: We have used sigmoid instead of Heaviside as the nonlinear function.
  id: totrans-63
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 我们使用sigmoid函数而不是Heaviside函数作为非线性函数。
- en: 8.3.1 Linear layers expressed as matrix-vector multiplication
  id: totrans-64
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.3.1 线性层表示为矩阵-向量乘法
- en: 'Let’s revisit the perceptron in the context of the MLP. As we saw in equation
    [7.3](../Text/07.xhtml#eq-perceptron), a single perceptron takes a weighted sum
    of its inputs and then performs a step function on the result. In an MLP, the
    inputs to any perceptron in the *l*th layer come from the previous layer: the
    (*l* − 1)th layer.'
  id: totrans-65
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们在MLP的背景下回顾一下感知器。正如我们在方程[7.3](../Text/07.xhtml#eq-perceptron)中看到的，单个感知器对其输入进行加权求和，然后对结果执行阶跃函数。在MLP中，第
    *l* 层中任何感知器的输入来自前一层：(*l* − 1)层。
- en: '![](../../OEBPS/Images/CH08_F06_Chaudhury.png)'
  id: totrans-66
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH08_F06_Chaudhury.png)'
- en: Figure 8.6 Linear layer outputting layer *l* from layer (*l* − 1). The weights
    belonging to row 1 of the weight matrix (coming from all the input neurons, layer
    (*l* − 1), which sum together to form output neuron 1) are shown in bold.
  id: totrans-67
  prefs: []
  type: TYPE_NORMAL
  zh: 图8.6 线性层输出层 *l* 来自层 (*l* − 1)。权重矩阵的第1行（来自所有输入神经元，层 (*l* − 1)，它们相加形成输出神经元1）的权重以粗体显示。
- en: '![](../../OEBPS/Images/CH08_F07_Chaudhury.png)'
  id: totrans-68
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH08_F07_Chaudhury.png)'
- en: 'Figure 8.7 Multilayered neural networks: This is a complete deep neural network,
    a slice of which is shown in figure [8.6](#fig-linear-layer).'
  id: totrans-69
  prefs: []
  type: TYPE_NORMAL
  zh: 图8.7 多层神经网络：这是一个完整的深度神经网络，其中一部分如图[8.6](#fig-linear-layer)所示。
- en: 'Let *a*[0]^((*l* − 1)), *a*[1]^((*l* − 1)), ⋯, *a[m]*^((*l* − 1)) denote the
    outputs of the *m* neurons in layer (*l* − 1) (the left-most input column of nodes
    in figure [8.6](#fig-linear-layer)). And let *a*[0]^((*l*)), *a*[1]^((*l*)), ⋯,
    *a[n]*^((*l*)) denote the outputs of the *n* neurons in layer *l*. Note that we
    typically use the symbol *a*, standing for activation, to denote the output of
    individual neurons. Now consider the *j*th neuron in layer *l*. For instance,
    check *z*[1]^((*l*)) in figure [8.6](#fig-linear-layer): note the weights going
    into it and the activations at their source. Its output is *a[j]*^((*l*)), where'
  id: totrans-70
  prefs: []
  type: TYPE_NORMAL
  zh: 令 *a*[0]^((*l* − 1))，*a*[1]^((*l* − 1))，⋯，*a[m]*^((*l* − 1)) 表示层 (*l* − 1) 中
    *m* 个神经元的输出（图[8.6](#fig-linear-layer)中节点的最左侧输入列）。令 *a*[0]^((*l*))，*a*[1]^((*l*))，⋯，*a[n]*^((*l*)))
    表示层 *l* 中 *n* 个神经元的输出。请注意，我们通常使用符号 *a*，代表激活，来表示单个神经元的输出。现在考虑层 *l* 中的第 *j* 个神经元。例如，检查图[8.6](#fig-linear-layer)中的
    *z*[1]^((*l*))：注意进入它的权重和它们的源激活。其输出是 *a[j]*^((*l*))，其中
- en: '![](../../OEBPS/Images/eq_08-06-a.png)'
  id: totrans-71
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-06-a.png)'
- en: 'We can rewrite the summation in these equations as a dot product between the
    weight and activation vectors:'
  id: totrans-72
  prefs: []
  type: TYPE_NORMAL
  zh: 我们可以将这些方程中的求和重写为权重向量和激活向量之间的点积：
- en: '![](../../OEBPS/Images/eq_08-06-b.png)'
  id: totrans-73
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 8.6-b](../../OEBPS/Images/eq_08-06-b.png)'
- en: The complete set of equations for all *j*s together can be written in a super-compact
    way using matrix-vector multiplication,
  id: totrans-74
  prefs: []
  type: TYPE_NORMAL
  zh: 所有 *j* 的完整方程集可以用矩阵-向量乘法以超级紧凑的方式写出，
- en: '![](../../OEBPS/Images/eq_08-07.png)'
  id: totrans-75
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 8.7](../../OEBPS/Images/eq_08-07.png)'
- en: Equation 8.7
  id: totrans-76
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.7
- en: where
  id: totrans-77
  prefs: []
  type: TYPE_NORMAL
  zh: 其中
- en: '*W*^((*l*)) is an *n* × *m* matrix representing the weights of *all connections
    from layer *l* − 1 to layer l*:'
  id: totrans-78
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*W*^((*l*)) 是一个 *n* × *m* 矩阵，表示从层 *l* − 1 到层 l* 的 *所有连接* 的权重：'
- en: '![](../../OEBPS/Images/eq_08-08.png)'
  id: totrans-79
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 8.8](../../OEBPS/Images/eq_08-08.png)'
- en: Equation 8.8
  id: totrans-80
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.8
- en: '![](../../OEBPS/Images/AR_a.png)^((*l*)) represents the activations for the
    entire layer *l*. Applying the sigmoid function to a vector is equivalent to applying
    it individually to each element of the vector:'
  id: totrans-81
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '![](../../OEBPS/Images/AR_a.png)^((*l*)) 表示层 *l* 的激活。将 Sigmoid 函数应用于向量等同于将其逐个应用于向量的每个元素：'
- en: '![](../../OEBPS/Images/eq_08-08-a.png)'
  id: totrans-82
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 8.8-a](../../OEBPS/Images/eq_08-08-a.png)'
- en: The matrix-vector notation saves us from dealing with subscripts by working
    with all the weights, biases, activations, and so on in a *global fashion*.
  id: totrans-83
  prefs: []
  type: TYPE_NORMAL
  zh: 矩阵-向量表示法通过以全局方式处理所有权重、偏置、激活等，使我们免于处理下标。
- en: 8.3.2 Forward propagation and grand output functions for an MLP of linear layers
  id: totrans-84
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.3.2 线性层 MLP 的正向传播和全局输出函数
- en: 'Equation [8.7](../Text/08.xhtml#eq-linlayer-forwardprop) describes the forward
    propagation of a single linear layer. The final output of an MLP with fully connected
    (aka linear) layers 0⋯*L* on input ![](../../OEBPS/Images/AR_x.png) can be obtained
    by repeated application of this equation:'
  id: totrans-85
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 [8.7](../Text/08.xhtml#eq-linlayer-forwardprop) 描述了单个线性层的正向传播。具有全连接（即线性）层
    0⋯*L* 的 MLP 在输入 ![](../../OEBPS/Images/AR_x.png) 上的最终输出可以通过重复应用此方程获得：
- en: '*MLP*(![](../../OEBPS/Images/AR_x.png)) = ![](../../OEBPS/Images/AR_a.png)^((*L*))
    = *y* = *σ*(*W*^((*L*))…*σ*(*W*^((1))*σ*(*W*^((0))![](../../OEBPS/Images/AR_x.png)
    + ![](../../OEBPS/Images/AR_b.png)^((0)))+![](../../OEBPS/Images/AR_b.png)^((1)))⋯+![](../../OEBPS/Images/AR_b.png)^((*L*)))'
  id: totrans-86
  prefs: []
  type: TYPE_NORMAL
  zh: '*MLP*(![](../../OEBPS/Images/AR_x.png)) = ![](../../OEBPS/Images/AR_a.png)^((*L*))
    = *y* = *σ*(*W*^((*L*))…*σ*(*W*^((1))*σ*(*W*^((0))![](../../OEBPS/Images/AR_x.png)
    + ![](../../OEBPS/Images/AR_b.png)^((0)))+![](../../OEBPS/Images/AR_b.png)^((1)))⋯+![](../../OEBPS/Images/AR_b.png)^((*L*)))'
- en: Equation 8.9
  id: totrans-87
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.9
- en: 'In a computer implementation, this expression is evaluated step by step by
    repeated application of the linear layer:'
  id: totrans-88
  prefs: []
  type: TYPE_NORMAL
  zh: 在计算机实现中，这个表达式通过重复应用线性层逐步评估：
- en: '![](../../OEBPS/Images/eq_08-10.png)'
  id: totrans-89
  prefs: []
  type: TYPE_IMG
  zh: '![方程式 8.10](../../OEBPS/Images/eq_08-10.png)'
- en: Equation 8.10
  id: totrans-90
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.10
- en: It’s easy to see that equation [8.10](#eq-forwardprop-layered) is a restatement
    of equation [8.7](../Text/08.xhtml#eq-linlayer-forwardprop).
  id: totrans-91
  prefs: []
  type: TYPE_NORMAL
  zh: 很容易看出方程式 [8.10](#eq-forwardprop-layered) 是方程式 [8.7](../Text/08.xhtml#eq-linlayer-forwardprop)
    的重述。
- en: Close examination of these equations reveals a beautiful property. The complicated
    equation [8.9](#eq-MLP-out-nosubscript) is *never explicitly evaluated*. Instead,
    we evaluate the outputs of successive layers, one layer at a time, as per equation
    [8.10](#eq-forwardprop-layered). Every layer can be evaluated by taking the previous
    layer’s output as input. No other input is necessary. That is to say, we can evaluate
    ![](../../OEBPS/Images/AR_a.png)^((0)) directly from the input ![](../../OEBPS/Images/AR_x.png),
    then ![](../../OEBPS/Images/AR_a.png)^((1)) from ![](../../OEBPS/Images/AR_a.png)^((0)),
    ![](../../OEBPS/Images/AR_a.png)^((2)) from ![](../../OEBPS/Images/AR_a.png)^((1)),
    and so forth, all the way to ![](../../OEBPS/Images/AR_a.png)^((*L*)) (which is
    the grand output of the MLP). During the evaluation, we need to keep only the
    previous and current layers in memory at any given time. This process greatly
    simplifies the implementation as well as the conceptualization and is known as
    *forward propagation*.
  id: totrans-92
  prefs: []
  type: TYPE_NORMAL
  zh: 仔细检查这些方程揭示了一个美丽的特性。复杂的方程式 [8.9](#eq-MLP-out-nosubscript) 永远不会被明确评估。相反，我们按照方程式
    [8.10](#eq-forwardprop-layered) 逐层评估连续层的输出，一次评估一层。每一层都可以通过将前一层的输出作为输入来评估。不需要其他输入。也就是说，我们可以直接从输入
    ![](../../OEBPS/Images/AR_x.png) 评估 ![](../../OEBPS/Images/AR_a.png)^((0))，然后从
    ![](../../OEBPS/Images/AR_a.png)^((0)) 评估 ![](../../OEBPS/Images/AR_a.png)^((1))，从
    ![](../../OEBPS/Images/AR_a.png)^((1)) 评估 ![](../../OEBPS/Images/AR_a.png)^((2))，以此类推，直到
    ![](../../OEBPS/Images/AR_a.png)^((*L*))（这是 MLP 的全局输出）。在评估过程中，我们只需要在任何给定时间内存中保留前一层和当前层。这个过程极大地简化了实现以及概念化，被称为
    *正向传播*。
- en: Listing 8.1 PyTorch code for forward propagation
  id: totrans-93
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 8.1 PyTorch 代码用于正向传播
- en: '[PRE0]'
  id: totrans-94
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: '① x: activation of layer l-1 (1-d vector) *W[l]*: Weight matrix of layer l
    *b[l]*: Bias vector of layer l'
  id: totrans-95
  prefs: []
  type: TYPE_NORMAL
  zh: '① x: 层l-1的激活（1维向量） *W[l]*: 层l的权重矩阵 *b[l]*: 层l的偏差向量'
- en: ② Sigmoid activation function (nonlinear layer)
  id: totrans-96
  prefs: []
  type: TYPE_NORMAL
  zh: ② Sigmoid激活函数（非线性层）
- en: '③ x: 1-d input vector W: list of matrices for layers 0 to L. b: list of vectors
    for layers 0 to L'
  id: totrans-97
  prefs: []
  type: TYPE_NORMAL
  zh: '③ x: 1维输入向量 W: 从层0到L的矩阵列表 b: 从层0到L的向量列表'
- en: ④ Loops through layers 0 to L
  id: totrans-98
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 遍历层0到L
- en: ⑤ Computes Z
  id: totrans-99
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ 计算Z
- en: ⑥ Computes activation
  id: totrans-100
  prefs: []
  type: TYPE_NORMAL
  zh: ⑥ 计算激活
- en: 8.4 Training and backpropagation
  id: totrans-101
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 8.4 训练和反向传播
- en: Throughout the book, we have been discussing bits and pieces of this process.
    In sections [1.1](../Text/01.xhtml#alg-supervised_training) and [3.3](../Text/03.xhtml#sec-grad)
    (specifically, algorithm 3.1 ), we saw an overview of the process for training
    a supervised model (you are encouraged to reread those if necessary). Training
    is an iterative process by which the parameters of the neural network are estimated.
    The goal is to estimate the parameters (weights and biases) such that on the training
    inputs, the neural network outputs are close as possible to the known ground-truth
    outputs.
  id: totrans-102
  prefs: []
  type: TYPE_NORMAL
  zh: 在整本书中，我们一直在讨论这个过程的点点滴滴。在第[1.1](../Text/01.xhtml#alg-supervised_training)节和第[3.3](../Text/03.xhtml#sec-grad)节（特别是算法3.1），我们看到了训练监督模型的过程概述（如果需要，请重新阅读）。训练是一个迭代过程，通过这个过程估计神经网络的参数。目标是估计参数（权重和偏差），使得在训练输入上，神经网络的输出尽可能接近已知的真实输出。
- en: In general, iterative processes improve (get closer to the goal) gradually.
    In each iteration, we make small adjustments to the parameters. Here, *parameter*
    refers to the weights and biases of the MLP, the *w[jk]*^((*l*))s and *b[j]*^((*l*))s
    from section [8.2](#sec-linlayer-forwardprop). We keep adjusting the parameters
    so that in every iteration, the outputs on training data inputs come a little
    closer to the ground truth (GT). Eventually, after many iterations, we hopefully
    converge to optimal values. Note that there is no guarantee that the iterative
    process will converge to the best possible parameter values. The training might
    go completely astray or get stuck in a local minimum. (Local minima are explained
    in section [3.6](../Text/03.xhtml#sec-locglob-minima); you are encouraged to reread
    it if necessary.) There is no good way to know whether we have reached optimal
    values (global minima) for the weights and biases. We typically run the neural
    network on test data, and if the results are satisfactory, we stop training. Test
    data should be *held back* during training, meaning we should never use test data
    to train. In the unfortunate event that the network has not reached the desired
    level of accuracy, we typically throw in more training data and/or try a modified
    loss function and/or a different architecture. Simply retraining the network from
    a different random start may also work. This is an experimental science with a
    lot of trial and error.
  id: totrans-103
  prefs: []
  type: TYPE_NORMAL
  zh: 通常，迭代过程会逐渐改善（接近目标）。在每次迭代中，我们对参数进行小的调整。在这里，“参数”指的是MLP的权重和偏差，即第[8.2](#sec-linlayer-forwardprop)节中的*w[jk]*^((*l*))s和*b[j]*^((*l*))s。我们不断调整参数，使得在每次迭代中，训练数据输入上的输出越来越接近真实值（GT）。最终，经过多次迭代，我们希望收敛到最优值。请注意，没有保证迭代过程会收敛到最佳可能的参数值。训练可能会完全偏离轨道或陷入局部最小值。（局部最小值在第[3.6](../Text/03.xhtml#sec-locglob-minima)节中解释；如果需要，请重新阅读。）没有好的方法知道我们是否已经达到了权重和偏差的最优值（全局最小值）。我们通常在测试数据上运行神经网络，如果结果令人满意，我们就停止训练。测试数据应该在训练期间保留，这意味着我们永远不应该使用测试数据来训练。在不幸的情况下，网络没有达到所需的准确度水平，我们通常会添加更多的训练数据，或者尝试修改损失函数和/或不同的架构。简单地从不同的随机起点重新训练网络也可能有效。这是一个充满试验和错误的实验科学。
- en: How do we know how to adjust the parameter values in each iteration? We define
    a loss (aka error) function. There are many popular formulations of loss functions,
    and we review many of them later, but their common property is that when the neural
    network output agrees more with the known output (GT), the loss becomes lower,
    and vice versa. Thus if *y* denotes the output of the neural network and *ȳ* is
    the GT, a reasonable expression for the loss is the *mean squared error* (MSE)
    function (*y* − *ȳ*)². For now, we use the MSE loss as our representative loss
    function. Later we discuss others.
  id: totrans-104
  prefs: []
  type: TYPE_NORMAL
  zh: 我们如何知道如何在每次迭代中调整参数值？我们定义一个损失（也称为误差）函数。有许多流行的损失函数公式，我们稍后会回顾其中许多，但它们的共同特性是当神经网络输出与已知输出（GT）更一致时，损失会降低，反之亦然。因此，如果
    *y* 表示神经网络的输出，而 *ȳ* 是GT，则损失的一个合理表达式是*均方误差*（MSE）函数 (*y* − *ȳ*)²。现在，我们使用MSE损失作为我们的代表性损失函数。稍后我们将讨论其他函数。
- en: 'Once the loss function is defined, we have a crisp, quantitative definition
    of the goal of neural network training. The goal is to minimize the total loss
    over the entire training data set. Note the clause *entire training data set*:
    we do not want to do well on one or two input instances at the cost of doing badly
    over the rest. If we have to choose between a solution that gives 10% error on
    all of, say, 100 training input instances versus one that yields 0% error on 50
    training input instances but 40% on the remaining 50, we prefer the former.'
  id: totrans-105
  prefs: []
  type: TYPE_NORMAL
  zh: 一旦损失函数被定义，我们就有了神经网络训练目标的清晰、定量定义。目标是使整个训练数据集的总损失最小化。请注意条款“整个训练数据集”：我们不希望在损失其他数据的情况下，只在少数几个输入实例上表现良好。如果我们必须在所有100个训练输入实例上产生10%的错误与在50个训练输入实例上产生0%的错误但在剩余的50个实例上产生40%的错误之间做出选择，我们更倾向于前者。
- en: Each weight in the MLP, *w[jk]*^((*l*)), is adjusted by an amount proportional
    to *δw[jk]*^((*l*)). Similarly, each bias *b[j]*^((*l*)) is adjusted by an amount
    proportional to *δb[j]*^((*l*)). We can denote all this compactly by saying we
    have a weight vector ![](../../OEBPS/Images/AR_w.png) and bias vector ![](../../OEBPS/Images/AR_b.png).
    In each iteration, we change ![](../../OEBPS/Images/AR_w.png) by amount *δ*![](../../OEBPS/Images/AR_w.png)
    and ![](../../OEBPS/Images/AR_b.png) by *δ*![](../../OEBPS/Images/AR_b.png) so
    that their new values are ![](../../OEBPS/Images/AR_w.png) − *rδ*![](../../OEBPS/Images/AR_w.png)
    and ![](../../OEBPS/Images/AR_b.png) − *rδ*![](../../OEBPS/Images/AR_b.png) *r*
    is a constant known as the *learning rate* that needs to be set at the beginning
    of training). In this context, it is worthwhile to note that in section [8.3.1](#sec-linlayer-matmult),
    we expressed the collection of weights in an MLP with a matrix, while here we
    are referring to the same thing as a vector. These are not incompatible because
    we can always rasterize the elements of a matrix (that is, walk over the elements
    of the matrix from top to bottom and from left to right) into a vector.
  id: totrans-106
  prefs: []
  type: TYPE_NORMAL
  zh: 多层感知器（MLP）中的每个权重 *w[jk]*^((*l*）) 都通过一个与 *δw[jk]*^((*l*)）成比例的量进行调整。同样，每个偏置 *b[j]*^((*l*)）也通过一个与
    *δb[j]*^((*l*)）成比例的量进行调整。我们可以通过说我们有一个权重向量 ![](../../OEBPS/Images/AR_w.png) 和偏置向量
    ![](../../OEBPS/Images/AR_b.png) 来简洁地表示所有这些。在每次迭代中，我们通过改变 ![](../../OEBPS/Images/AR_w.png)
    的量 *δ*![](../../OEBPS/Images/AR_w.png) 和 ![](../../OEBPS/Images/AR_b.png) 的量 *δ*![](../../OEBPS/Images/AR_b.png)
    来改变它们，以便它们的新值是 ![](../../OEBPS/Images/AR_w.png) − *rδ*![](../../OEBPS/Images/AR_w.png)
    和 ![](../../OEBPS/Images/AR_b.png) − *rδ*![](../../OEBPS/Images/AR_b.png)，其中 *r*
    是一个称为*学习率*的常数，需要在训练开始时设置）。在这个上下文中，值得注意的是，在[8.3.1](#sec-linlayer-matmult)节中，我们用矩阵表示了MLP中的权重集合，而在这里我们用向量来指代同一事物。这些并不矛盾，因为我们总是可以将矩阵的元素（即从上到下和从左到右遍历矩阵的元素）转换成一个向量。
- en: How do we estimate the adjustment amounts *δ*![](../../OEBPS/Images/AR_w.png)
    and *δ*![](../../OEBPS/Images/AR_b.png)? This is where the notion of gradients
    comes in. These were discussed in detail in sections [3.3.1](../Text/03.xhtml#sec-gradient),
    [3.3.2](../Text/03.xhtml#subsec-level-surf), and [3.5](../Text/03.xhtml#sec-gradient-descent)
    (again, you are encouraged to reread if necessary). In general, if a loss, denoted
    𝕃, is expressed as a function of the parameters, such as 𝕃(![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png)), then the change in the parameters that optimally
    takes us toward lower loss is yielded by the gradient of the loss with respect
    to the parameters ∇[![](../../OEBPS/Images/AR_w.png), *b*]𝕃(![](../../OEBPS/Images/AR_w.png),
    *b*). The high-level process is described later in the chapter in algorithm [3.2](../Text/03.xhtml#alg-supervised_training_detailed).
    Here we look at the guts of it.
  id: totrans-107
  prefs: []
  type: TYPE_NORMAL
  zh: 我们如何估计调整量 *δ*![MLP](../../OEBPS/Images/AR_w.png) 和 *δ*![MLP](../../OEBPS/Images/AR_b.png)？这就是梯度概念发挥作用的地方。这些内容在
    [3.3.1](../Text/03.xhtml#sec-gradient)、[3.3.2](../Text/03.xhtml#subsec-level-surf)
    和 [3.5](../Text/03.xhtml#sec-gradient-descent) 节中已有详细讨论（再次提醒，如有必要，请重新阅读）。一般来说，如果损失
    𝕃 表示为参数的函数，例如 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))，那么使损失最优降低的参数变化由损失相对于参数的梯度
    ∇[![MLP](../../OEBPS/Images/AR_w.png), *b*]𝕃(![](../../OEBPS/Images/AR_w.png),
    *b*) 得出。本章稍后将在算法 [3.2](../Text/03.xhtml#alg-supervised_training_detailed) 中描述高级过程。这里我们来看看其核心内容。
- en: '8.4.1 Loss and its minimization: Goal of training'
  id: totrans-108
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.4.1 损失及其最小化：训练目标
- en: Given a training data set 𝕋 that is a set of <input, GT output> pairs 𝕋 = {⟨![](../../OEBPS/Images/AR_x.png),
    *ȳ*⟩}, the loss can be expressed as
  id: totrans-109
  prefs: []
  type: TYPE_NORMAL
  zh: 给定一个训练数据集 𝕃，它是一组 <输入，GT 输出> 对 𝕃 = {⟨![MLP](../../OEBPS/Images/AR_x.png), *ȳ*⟩}，损失可以表示为
- en: '![](../../OEBPS/Images/eq_08-11.png)'
  id: totrans-110
  prefs: []
  type: TYPE_IMG
  zh: '![eq_08-11.png](../../OEBPS/Images/eq_08-11.png)'
- en: Equation 8.11
  id: totrans-111
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.11
- en: where
  id: totrans-112
  prefs: []
  type: TYPE_NORMAL
  zh: 其中
- en: '![](../../OEBPS/Images/AR_y.png) = *MLP*(![](../../OEBPS/Images/AR_x.png))'
  id: totrans-113
  prefs: []
  type: TYPE_IMG
  zh: '![MLP](../../OEBPS/Images/AR_y.png) = *MLP*(![](../../OEBPS/Images/AR_x.png))'
- en: as per equation [8.9](#eq-MLP-out-nosubscript).
  id: totrans-114
  prefs: []
  type: TYPE_NORMAL
  zh: 如方程 [8.9](#eq-MLP-out-nosubscript) 所示。
- en: 'Now consider equation [8.7](../Text/08.xhtml#eq-linlayer-forwardprop) again.
    We can rasterize each layer’s weight matrix *W*^((*l*)) into a vector and then
    concatenate all these vectors from successive layers to form a giant weight vector
    ![](../../OEBPS/Images/AR_w.png), the vector of all weights in the MLP:'
  id: totrans-115
  prefs: []
  type: TYPE_NORMAL
  zh: 现在再次考虑方程 [8.7](../Text/08.xhtml#eq-linlayer-forwardprop)。我们可以将每一层的权重矩阵 *W*^((*l*))
    转换为一个向量，然后将这些从连续层中依次连接的向量组合成一个巨大的权重向量 ![MLP](../../OEBPS/Images/AR_w.png)，即 MLP
    中所有权重的向量：
- en: '![](../../OEBPS/Images/eq_08-11-a.png)'
  id: totrans-116
  prefs: []
  type: TYPE_IMG
  zh: '![eq_08-11-a.png](../../OEBPS/Images/eq_08-11-a.png)'
- en: 'Similarly, we can form a giant vector of all biases in the MLP:'
  id: totrans-117
  prefs: []
  type: TYPE_NORMAL
  zh: 同样，我们可以形成一个包含 MLP 中所有偏差的巨大向量：
- en: '![](../../OEBPS/Images/eq_08-11-b.png)'
  id: totrans-118
  prefs: []
  type: TYPE_IMG
  zh: '![eq_08-11-b.png](../../OEBPS/Images/eq_08-11-b.png)'
- en: 'The ultimate goal of training is to find ![](../../OEBPS/Images/AR_w.png) and
    ![](../../OEBPS/Images/AR_b.png) that will minimize the loss 𝕃. In chapter [3](../Text/03.xhtml#chapter-intro-vec-mat),
    we saw that we can solve for the minimum by setting the gradients ∇[![](../../OEBPS/Images/AR_w.png)]𝕃
    = 0 and ∇[![](../../OEBPS/Images/AR_b.png)]𝕃 = 0. Computing the loss gradient
    from a combination of equations [8.9](#eq-MLP-out-nosubscript) and [8.11](../Text/08.xhtml#eq-mse-loss)
    is intractable. Instead, we go for an iterative solution: *gradient descent* on
    the loss surface, as described in the next section.'
  id: totrans-119
  prefs: []
  type: TYPE_NORMAL
  zh: 训练的最终目标是找到 ![MLP](../../OEBPS/Images/AR_w.png) 和 ![MLP](../../OEBPS/Images/AR_b.png)，它们将最小化损失
    𝕃。在第 [3](../Text/03.xhtml#chapter-intro-vec-mat) 章中，我们了解到我们可以通过设置梯度 ∇[![MLP](../../OEBPS/Images/AR_w.png)]𝕃
    = 0 和 ∇[![MLP](../../OEBPS/Images/AR_b.png)]𝕃 = 0 来求解最小值。从方程 [8.9](#eq-MLP-out-nosubscript)
    和 [8.11](../Text/08.xhtml#eq-mse-loss) 的组合中计算损失梯度是不可行的。相反，我们采用迭代解法：在损失表面上进行 *梯度下降*，如下一节所述。
- en: Listing 8.2 PyTorch code for MSE loss
  id: totrans-120
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 8.2 用于 MSE 损失的 PyTorch 代码
- en: '[PRE1]'
  id: totrans-121
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: '① a: Activation of layer L (1D vector) : Ground truth (1D vector)'
  id: totrans-122
  prefs: []
  type: TYPE_NORMAL
  zh: '① a: 层 L 的激活（1D 向量）: 真实值（1D 向量）'
- en: ② See equation [8.11](../Text/08.xhtml#eq-mse-loss).
  id: totrans-123
  prefs: []
  type: TYPE_NORMAL
  zh: ② 见方程 [8.11](../Text/08.xhtml#eq-mse-loss)。
- en: 8.4.2 Loss surface and gradient descent
  id: totrans-124
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.4.2 损失表面和梯度下降
- en: Geometrically, the loss function 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    can be viewed as a surface in a high-dimensional space. The domain of this space
    corresponds to all the dimensions in ![](../../OEBPS/Images/AR_w.png) plus all
    the dimensions in ![](../../OEBPS/Images/AR_b.png). This is shown in figure [8.8](../Text/08.xhtml#fig-gradient-descent-3d)
    with a 2D domain. In chapter [3](../Text/03.xhtml#chapter-intro-vec-mat), we also
    saw that given a function 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png)),
    the best way to progress toward the minimum is to walk on the parameter space
    along the negative gradient. We adopt this approach to minimize the loss. We compute
    the gradients of the loss function with respect to weights and biases and update
    the weights and bias vectors by an amount proportional to the (negative) of these
    gradients. Doing this repeatedly takes us close to the minimum. In figure [8.8](../Text/08.xhtml#fig-gradient-descent-3d),
    the gradient descent path is shown with solid arrows, while an arbitrary non-optimal
    path to the minimum is shown with dashed arrows.
  id: totrans-125
  prefs: []
  type: TYPE_NORMAL
  zh: 几何上，损失函数 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    可以看作是在高维空间中的一个曲面。这个空间的域对应于 ![](../../OEBPS/Images/AR_w.png) 中的所有维度加上 ![](../../OEBPS/Images/AR_b.png)
    中的所有维度。这如图 [8.8](../Text/08.xhtml#fig-gradient-descent-3d) 中的二维域所示。在第 [3](../Text/03.xhtml#chapter-intro-vec-mat)
    章中，我们也看到了，给定一个函数 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))，向最小值前进的最佳方式是沿着负梯度在参数空间中行走。我们采用这种方法来最小化损失。我们计算损失函数相对于权重和偏差的梯度，并通过与这些梯度的（负）成比例的量来更新权重和偏差向量。重复这样做可以使我们接近最小值。在图
    [8.8](../Text/08.xhtml#fig-gradient-descent-3d) 中，梯度下降路径用实线箭头表示，而到最小值的任意非最优路径用虚线箭头表示。
- en: '![](../../OEBPS/Images/CH08_F08_Chaudhury.png)'
  id: totrans-126
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH08_F08_Chaudhury.png)'
- en: Figure 8.8 A representative loss 𝕃(*w*, *b*). Note that ![](../../OEBPS/Images/AR_w.png)
    and ![](../../OEBPS/Images/AR_b.png) have each been reduced to 1D for this .
  id: totrans-127
  prefs: []
  type: TYPE_NORMAL
  zh: 图 8.8 一个代表性的损失 𝕃(*w*, *b*)。注意，![](../../OEBPS/Images/AR_w.png) 和 ![](../../OEBPS/Images/AR_b.png)
    都已经在这个图中被简化为 1D。
- en: Thus the equations for updating weights and biases in gradient descent are
  id: totrans-128
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，梯度下降中更新权重和偏差的方程是
- en: '![](../../OEBPS/Images/eq_08-12.png)'
  id: totrans-129
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-12.png)'
- en: Equation 8.12
  id: totrans-130
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.12
- en: where *r* is a constant. Here
  id: totrans-131
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *r* 是一个常数。在这里
- en: '![](../../OEBPS/Images/eq_08-13.png)'
  id: totrans-132
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-13.png)'
- en: Equation 8.13
  id: totrans-133
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.13
- en: The vector update equation [8.12](../Text/08.xhtml#eq-wtsbiases-update-vector)
    can be expressed in terms of the scalar components as
  id: totrans-134
  prefs: []
  type: TYPE_NORMAL
  zh: 向量更新方程 [8.12](../Text/08.xhtml#eq-wtsbiases-update-vector) 可以用标量分量表示为
- en: '![](../../OEBPS/Images/eq_08-14.png)'
  id: totrans-135
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-14.png)'
- en: Equation 8.14
  id: totrans-136
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.14
- en: Note that we have to reevaluate these partial derivatives in each iteration
    since their values will change in every iteration.
  id: totrans-137
  prefs: []
  type: TYPE_NORMAL
  zh: 注意，我们必须在每次迭代中重新评估这些偏导数，因为它们的值会在每次迭代中改变。
- en: 8.4.3 Why a gradient provides the best direction for descent
  id: totrans-138
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.4.3 为什么梯度提供了下降的最佳方向
- en: Why does updating along the gradient reduce the function optimally? This is
    discussed in detail in chapter [3](../Text/03.xhtml#chapter-intro-vec-mat). Here
    we briefly recap the idea. Using multidimensional Taylor expansion, we can evaluate
    a function in the neighborhood of a known point. For instance, we can evaluate
    𝕃(![](../../OEBPS/Images/AR_w.png) + *δ*![](../../OEBPS/Images/AR_w.png)) for
    small offset *δ*![](../../OEBPS/Images/AR_w.png) from ![](../../OEBPS/Images/AR_w.png)
    as follows
  id: totrans-139
  prefs: []
  type: TYPE_NORMAL
  zh: 为什么沿着梯度更新可以最优地减少函数？这在第 [3](../Text/03.xhtml#chapter-intro-vec-mat) 章中有详细讨论。在这里，我们简要回顾一下这个想法。使用多维泰勒展开，我们可以在已知点的邻域内评估一个函数。例如，我们可以评估
    𝕃(![](../../OEBPS/Images/AR_w.png) + *δ*![](../../OEBPS/Images/AR_w.png))，其中 *δ*![](../../OEBPS/Images/AR_w.png)
    是从 ![](../../OEBPS/Images/AR_w.png) 出发的小偏移量，如下所示
- en: '![](../../OEBPS/Images/eq_08-15.png)'
  id: totrans-140
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-15.png)'
- en: Equation 8.15
  id: totrans-141
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.15
- en: 'where *H*, called the *Hessian matrix*, is defined as in equation [3.9](../Text/03.xhtml#eq-hessian).
    Since we are not going too far from ![](../../OEBPS/Images/AR_w.png), ||*δ*![](../../OEBPS/Images/AR_w.png)||
    is small. This means the quadratic and higher-order terms are negligibly small,
    and we can drop them (the approximation is perfect in the limit when ||*δ*![](../../OEBPS/Images/AR_w.png)||
    → 0):'
  id: totrans-142
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *H*，称为 *海森矩阵*，其定义如方程 [3.9](../Text/03.xhtml#eq-hessian)。由于我们没有离开 ![](../../OEBPS/Images/AR_w.png)
    太远，||*δ*![](../../OEBPS/Images/AR_w.png)|| 很小。这意味着二次和更高阶的项可以忽略不计，我们可以将它们省略（当 ||*δ*![](../../OEBPS/Images/AR_w.png)||
    → 0 时，近似是完美的）：
- en: '![](../../OEBPS/Images/eq_08-15-a.png)'
  id: totrans-143
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-15-a.png)'
- en: 'But we know the dot product (![](../../OEBPS/Images/AR_delta.png))*^T* ▽![](../../OEBPS/Images/AR_w.png)𝕃
    will attain its maximum value when both the vectors point in the same direction:
    that is,'
  id: totrans-144
  prefs: []
  type: TYPE_NORMAL
  zh: 但我们知道点积 (![](../../OEBPS/Images/AR_delta.png))*^T* ▽![](../../OEBPS/Images/AR_w.png)𝕃将在两个向量指向同一方向时达到其最大值：也就是说，
- en: '![](../../OEBPS/Images/eq_08-15-b.png)'
  id: totrans-145
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-15-b.png)'
- en: for some constant of proportionality *r*.
  id: totrans-146
  prefs: []
  type: TYPE_NORMAL
  zh: 对于某个比例常数 *r*。
- en: In implementation, *r* is called the *learning rate*. A higher learning rate
    causes the optimization to progress more rapidly but also runs the risk of overshooting
    the minimum. We learn about these in more detail later. For now, simply note that
    *r* is a *tunable hyperparameter* of the system.
  id: totrans-147
  prefs: []
  type: TYPE_NORMAL
  zh: 在实现中，*r* 被称为 *学习率*。较高的学习率会导致优化过程更快地进展，但也存在超过最小值的危险。我们将在后面更详细地了解这些内容。现在，只需注意*r*是系统的*可调超参数*。
- en: 'Thus, the largest decrease in value from 𝕃(![](../../OEBPS/Images/AR_w.png))
    to 𝕃(![](../../OEBPS/Images/AR_w.png) – ![](../../OEBPS/Images/AR_delta.png))
    happens when *δ*![](../../OEBPS/Images/AR_w.png) is along the negative gradient.
    This is why we move toward the negative gradient in gradient descent: it is the
    fastest way to reach the minimum. The straight arrows in figure [8.8](../Text/08.xhtml#fig-gradient-descent-3d)
    illustrate the direction of the gradient. The dashed arrows show an arbitrary
    nongradient path for comparison.'
  id: totrans-148
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，从𝕃(![](../../OEBPS/Images/AR_w.png))到𝕃(![](../../OEBPS/Images/AR_w.png) –
    ![](../../OEBPS/Images/AR_delta.png))的最大值减少发生在*δ*![](../../OEBPS/Images/AR_w.png)沿着负梯度方向时。这就是为什么我们在梯度下降中朝着负梯度移动：这是达到最小值最快的方式。图[8.8](../Text/08.xhtml#fig-gradient-descent-3d)中的直线箭头说明了梯度的方向。虚线箭头显示了一个任意的非梯度路径以供比较。
- en: We can deal with the bias vector ![](../../OEBPS/Images/AR_b.png) similarly.
  id: totrans-149
  prefs: []
  type: TYPE_NORMAL
  zh: 我们可以类似地处理偏置向量 ![](../../OEBPS/Images/AR_b.png)。
- en: 8.4.4 Gradient descent and local minima
  id: totrans-150
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.4.4 梯度下降和局部最小值
- en: We should note that gradient descent can get stuck in a *local minimum*. Figure
    [8.9](../Text/08.xhtml#fig-non-convex-local-minima) shows this.
  id: totrans-151
  prefs: []
  type: TYPE_NORMAL
  zh: 我们应该注意，梯度下降可能会陷入一个 *局部最小值*。图[8.9](../Text/08.xhtml#fig-non-convex-local-minima)展示了这一点。
- en: '![](../../OEBPS/Images/CH08_F09_Chaudhury.png)'
  id: totrans-152
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH08_F09_Chaudhury.png)'
- en: Figure 8.9 A nonconvex function with local and global minima. Depending on the
    point, gradient descent will take us to one or the other.
  id: totrans-153
  prefs: []
  type: TYPE_NORMAL
  zh: 图8.9 具有局部和全局最小值的非凸函数。根据点，梯度下降可能会带我们到其中一个。
- en: 'In earlier eras, optimization techniques tried hard to avoid local minima and
    converge to the global minimum. Techniques like simulated annealing and tunneling
    were carefully designed to avoid local minima. Modern-day neural networks have
    adopted a different attitude: they do not try very hard to avoid local minima.
    Sometimes a local minimum is an acceptable (accurate enough) solution. Otherwise,
    we can retrain the neural network: it will start from a random position, so this
    time it may go to a better minimum.'
  id: totrans-154
  prefs: []
  type: TYPE_NORMAL
  zh: 在早期时代，优化技术努力避免局部最小值并收敛到全局最小值。像模拟退火和隧道这样的技术被精心设计来避免局部最小值。现代神经网络采取了不同的态度：它们并不非常努力地避免局部最小值。有时局部最小值是一个可接受的（足够准确）解。否则，我们可以重新训练神经网络：它将从随机位置开始，所以这次它可能达到更好的最小值。
- en: '![](../../OEBPS/Images/CH08_F10_Chaudhury.png)'
  id: totrans-155
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH08_F10_Chaudhury.png)'
- en: Figure 8.10 MLP with layers 0, …, *L*, one neuron per layer. Again, we have
    split every layer into a weighted sum and a sigmoid.
  id: totrans-156
  prefs: []
  type: TYPE_NORMAL
  zh: 图8.10 层0, …, *L*的MLP，每层一个神经元。再次强调，我们将每一层都分解为一个加权求和和一个sigmoid函数。
- en: 8.4.5 The backpropagation algorithm
  id: totrans-157
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.4.5 反向传播算法
- en: We have seen that gradient descent progresses by repeatedly updating the weights
    and biases via equation [8.12](../Text/08.xhtml#eq-wtsbiases-update-vector). This
    is equivalent to repeatedly updating individual weights and biases using individual
    partial derivatives via equation [8.14](#eq-wtsbiases-update-scalar).
  id: totrans-158
  prefs: []
  type: TYPE_NORMAL
  zh: 我们已经看到，梯度下降通过反复更新权重和偏置（方程[8.12](../Text/08.xhtml#eq-wtsbiases-update-vector)）来进展。这相当于通过方程[8.14](#eq-wtsbiases-update-scalar)反复使用单个偏导数更新单个权重和偏置。
- en: Obtaining a closed-form solution for the gradients ∇[![](../../OEBPS/Images/AR_w.png)]𝕃(![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png)), ∇[![](../../OEBPS/Images/AR_b.png)]𝕃(![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png)) from equations [8.9](#eq-MLP-out-nosubscript)
    and [8.11](../Text/08.xhtml#eq-mse-loss)—or, equivalently, obtaining a closed-form
    solution for the partial derivatives *∂*𝕃/*∂w[jk]*^((*l*)), *∂*𝕃/*∂b[j]*^((*l*)),
    —is very difficult. Backpropagation is an algorithm that allows us to evaluate
    the gradients and update the weights and biases one layer at a time, like forward
    propagation (equation [8.10](#eq-forwardprop-layered)).
  id: totrans-159
  prefs: []
  type: TYPE_NORMAL
  zh: 从方程 [8.9](#eq-MLP-out-nosubscript) 和 [8.11](../Text/08.xhtml#eq-mse-loss) 获得梯度
    ∇[![](../../OEBPS/Images/AR_w.png)]𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png)),
    ∇[![](../../OEBPS/Images/AR_b.png)]𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    的闭式解，或者，等价地，获得偏导数 *∂*𝕃/*∂w[jk]*^((*l*)), *∂*𝕃/*∂b[j]*^((*l*)) 的闭式解，是非常困难的。反向传播是一种算法，允许我们像前向传播（方程
    [8.10](#eq-forwardprop-layered)）一样逐层评估梯度并更新权重和偏置。
- en: Backpropagation algorithm on a simple network
  id: totrans-160
  prefs: []
  type: TYPE_NORMAL
  zh: 简单网络的反向传播算法
- en: We first discuss backpropagation on a simple MLP with only a single neuron per
    layer. The main simplification resulting from this is that individual weights
    and biases no longer need subscripts, with only one weight and one bias between
    two successive layers. They still need superscripts to indicate layer IDs, however.
    Figure [8.10](#fig-MLP-one-neuron-per-layer) shows this MLP. We use MSE loss (equation
    [8.11](../Text/08.xhtml#eq-mse-loss)), but we work on a single input-output pair
    *x[i]*, *y[i]*. The total loss (summation over all the training data instances)
    can easily be derived by repeating the same steps.
  id: totrans-161
  prefs: []
  type: TYPE_NORMAL
  zh: 我们首先讨论一个简单的 MLP 的反向传播，该 MLP 每层只有一个神经元。由此产生的主要简化是，单个权重和偏置不再需要下标，在连续两层之间只有一个权重和一个偏置。然而，它们仍然需要上标来表示层
    ID。图 [8.10](#fig-MLP-one-neuron-per-layer) 显示了这个 MLP。我们使用 MSE 损失（方程 [8.11](../Text/08.xhtml#eq-mse-loss)），但我们只在一个输入输出对
    *x[i]*, *y[i]* 上工作。通过重复相同的步骤，可以很容易地推导出总损失（对所有训练数据实例求和）。
- en: 'We first define an auxiliary variable:'
  id: totrans-162
  prefs: []
  type: TYPE_NORMAL
  zh: 我们首先定义一个辅助变量：
- en: '![](../../OEBPS/Images/eq_08-15-c.png)'
  id: totrans-163
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.15-c](../../OEBPS/Images/eq_08-15-c.png)'
- en: The physical significance of *δ*^((*l*)) is that it is the rate of change of
    the loss with the (pre-activation) output of layer *l* (remember, in this network,
    layer *l* has a single neuron).
  id: totrans-164
  prefs: []
  type: TYPE_NORMAL
  zh: δ^((*l*)) 的物理意义是它表示损失相对于层 l 的（预激活）输出的变化率（记住，在这个网络中，层 l 有一个神经元）。
- en: 'Let’s establish a few important equations for the MLP in figure [8.10](#fig-MLP-one-neuron-per-layer):'
  id: totrans-165
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们为图 [8.10](#fig-MLP-one-neuron-per-layer) 中的 MLP 建立几个重要方程：
- en: '*Forward propagation for an arbitrary layer* *l* ∈ {0, *L*}'
  id: totrans-166
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*任意层 l 的前向传播* *l* ∈ {0, *L*}'
- en: '![](../../OEBPS/Images/eq_08-16.png)'
  id: totrans-167
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.16](../../OEBPS/Images/eq_08-16.png)'
- en: Equation 8.16
  id: totrans-168
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.16
- en: '![](../../OEBPS/Images/eq_08-17.png)'
  id: totrans-169
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17](../../OEBPS/Images/eq_08-17.png)'
- en: Equation 8.17
  id: totrans-170
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.17
- en: '*Loss*—Here we are working with a single training data instance, *x[i]*, whose
    GT output is *ȳ[i]*:'
  id: totrans-171
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*损失*—在这里我们处理单个训练数据实例，*x[i]*，其 GT 输出为 *ȳ[i]*：'
- en: '![](../../OEBPS/Images/eq_08-17-a.png)'
  id: totrans-172
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17-a](../../OEBPS/Images/eq_08-17-a.png)'
- en: '*Partial derivative of loss with respect to the weight and bias in terms of
    an auxiliary variable for the last layer, L*—Using the chain rule for partial
    derivatives,'
  id: totrans-173
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*损失相对于权重和偏置的偏导数，以最后一层辅助变量 L 为条件*—使用偏导数的链式法则，'
- en: '![](../../OEBPS/Images/eq_08-17-b.png)'
  id: totrans-174
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17-b](../../OEBPS/Images/eq_08-17-b.png)'
- en: Examining the terms on the right, we see
  id: totrans-175
  prefs: []
  type: TYPE_NORMAL
  zh: 检查右侧的项，我们看到
- en: '![](../../OEBPS/Images/eq_08-17-c.png)'
  id: totrans-176
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17-c](../../OEBPS/Images/eq_08-17-c.png)'
- en: (auxiliary variable for layer *L*). And using the forward propagation equations,
  id: totrans-177
  prefs: []
  type: TYPE_NORMAL
  zh: （层 *L* 的辅助变量）。并且使用前向传播方程，
- en: '![](../../OEBPS/Images/eq_08-17-d.png)'
  id: totrans-178
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17-d](../../OEBPS/Images/eq_08-17-d.png)'
- en: Together, they lead to
  id: totrans-179
  prefs: []
  type: TYPE_NORMAL
  zh: 一起，它们导致
- en: '![](../../OEBPS/Images/eq_08-17-e.png)'
  id: totrans-180
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17-e](../../OEBPS/Images/eq_08-17-e.png)'
- en: Similarly,
  id: totrans-181
  prefs: []
  type: TYPE_NORMAL
  zh: 同样，
- en: '![](../../OEBPS/Images/eq_08-17-f.png)'
  id: totrans-182
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.17-f](../../OEBPS/Images/eq_08-17-f.png)'
- en: 'Consequently, we have the following pair of equations expressing the partial
    derivative of loss with respect to weight and bias, respectively, in terms of
    the auxiliary variable for the last layer:'
  id: totrans-183
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，我们得到以下一对方程，分别表示损失相对于权重和偏置的偏导数，以最后一层的辅助变量为条件：
- en: '![](../../OEBPS/Images/eq_08-18.png)'
  id: totrans-184
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.18](../../OEBPS/Images/eq_08-18.png)'
- en: Equation 8.18
  id: totrans-185
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.18
- en: '![](../../OEBPS/Images/eq_08-19.png)'
  id: totrans-186
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.19](../../OEBPS/Images/eq_08-19.png)'
- en: Equation 8.19
  id: totrans-187
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.19
- en: '*Auxiliary variable for the last layer, L*—Using the chain rule for partial
    derivatives,'
  id: totrans-188
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*最后一层的辅助变量 L*—使用偏导数的链式法则，'
- en: '![](../../OEBPS/Images/eq_08-19-a.png)'
  id: totrans-189
  prefs: []
  type: TYPE_IMG
  zh: '![方程 8.19-a](../../OEBPS/Images/eq_08-19-a.png)'
- en: Using equation [8.5](../Text/08.xhtml#eq-sigmoid-derivative) for the derivative
    of a sigmoid, we get
  id: totrans-190
  prefs: []
  type: TYPE_NORMAL
  zh: 使用方程 [8.5](../Text/08.xhtml#eq-sigmoid-derivative) 对 Sigmoid 函数的导数进行计算，我们得到
- en: '*δ*^((*L*)) = (*a*^((*L*))−*ȳ[i]*) *σ*(*z*^((*L*)))(1−*σ*(*z*^((*L*))))'
  id: totrans-191
  prefs: []
  type: TYPE_NORMAL
  zh: '*δ*^((*L*)) = (*a*^((*L*))−*ȳ[i]*) *σ*(*z*^((*L*)))(1−*σ*(*z*^((*L*))))'
- en: which, using equation [8.17](#eq-forwardprop-a-simple), leads to
  id: totrans-192
  prefs: []
  type: TYPE_NORMAL
  zh: 使用方程 [8.17](#eq-forwardprop-a-simple)，这导致
- en: '*δ*^((*L*)) = (*a*^((*L*))−*ȳ[i]*) *a*^((*L*))(1−*a*^((*L*)))'
  id: totrans-193
  prefs: []
  type: TYPE_NORMAL
  zh: '*δ*^((*L*)) = (*a*^((*L*))−*ȳ[i]*) *a*^((*L*))(1−*a*^((*L*)))'
- en: Equation 8.20
  id: totrans-194
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.20
- en: '*Partial derivative of the loss with respect to the weight and bias in terms
    of an auxiliary variable for an arbitrary layer l*—Using the chain rule for partial
    derivatives,'
  id: totrans-195
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*关于任意层 l* 的辅助变量，损失相对于权重和偏置的偏导数——使用偏导数的链式法则，'
- en: '![](../../OEBPS/Images/eq_08-20-a.png)'
  id: totrans-196
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-20-a.png)'
- en: Using the definition of the auxiliary variable and the forward propagation equation
    [8.16](#eq-forwardprop-z-simple), this leads to
  id: totrans-197
  prefs: []
  type: TYPE_NORMAL
  zh: 使用辅助变量的定义和前向传播方程 [8.16](#eq-forwardprop-z-simple)，这导致
- en: '![](../../OEBPS/Images/eq_08-21.png)'
  id: totrans-198
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-21.png)'
- en: Equation 8.21
  id: totrans-199
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.21
- en: Similarly,
  id: totrans-200
  prefs: []
  type: TYPE_NORMAL
  zh: 类似地，
- en: '![](../../OEBPS/Images/eq_08-21-a.png)'
  id: totrans-201
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-21-a.png)'
- en: Using the definition of the auxiliary variable and the forward propagation equation
    [8.16](#eq-forwardprop-z-simple), this leads to
  id: totrans-202
  prefs: []
  type: TYPE_NORMAL
  zh: 使用辅助变量的定义和前向传播方程 [8.16](#eq-forwardprop-z-simple)，这导致
- en: '![](../../OEBPS/Images/eq_08-22.png)'
  id: totrans-203
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-22.png)'
- en: Equation 8.22
  id: totrans-204
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.22
- en: '*Auxiliary variable for an arbitrary layer, l*—Using the chain rule for partial
    derivatives,'
  id: totrans-205
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*任意层 l* 的辅助变量——使用偏导数的链式法则，'
- en: '![](../../OEBPS/Images/eq_08-22-a.png)'
  id: totrans-206
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-22-a.png)'
- en: Using the definition of the auxiliary variable and the forward propagation equation
    [8.16](#eq-forwardprop-z-simple), this leads to
  id: totrans-207
  prefs: []
  type: TYPE_NORMAL
  zh: 使用辅助变量的定义和前向传播方程 [8.16](#eq-forwardprop-z-simple)，这导致
- en: '![](../../OEBPS/Images/eq_08-22-b.png)'
  id: totrans-208
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-22-b.png)'
- en: which yields
  id: totrans-209
  prefs: []
  type: TYPE_NORMAL
  zh: 这将得到
- en: '*δ*^((*l*)) = *δ*^((*l*+1)) *w*^((*l*+1)) *a*^((*l*))(1−*a*^((*l*)))'
  id: totrans-210
  prefs: []
  type: TYPE_NORMAL
  zh: '*δ*^((*l*)) = *δ*^((*l*+1)) *w*^((*l*+1)) *a*^((*l*))(1−*a*^((*l*)))'
- en: Equation 8.23
  id: totrans-211
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.23
- en: 'We first encountered the one-layer-at-a-time property in section [8.3.2](#sec-layered-forwardprop)
    in connection with the forward propagation equations. Let’s recap that in the
    context of training our simple network. Consider equations [8.16](#eq-forwardprop-z-simple)
    and [8.17](#eq-forwardprop-a-simple). We initialize the system with some values
    of weights *w*^((*l*)) and biases *b*^((*l*)). Using those, we can evaluate the
    layer 0 outputs. For starters, we can evaluate *z*^((0)) and *a*^((0)) easily
    (since all the inputs are known):'
  id: totrans-212
  prefs: []
  type: TYPE_NORMAL
  zh: 我们首次遇到逐层处理属性是在第 [8.3.2](#sec-layered-forwardprop) 节中，与前向传播方程相关。让我们在训练简单网络的情况下回顾一下这一点。考虑方程
    [8.16](#eq-forwardprop-z-simple) 和 [8.17](#eq-forwardprop-a-simple)。我们使用一些权重 *w*^((*l*))
    和偏置 *b*^((*l*)) 的值初始化系统。使用这些值，我们可以评估第 0 层的输出。首先，我们可以轻松地评估 *z*^((0)) 和 *a*^((0))（因为所有输入都是已知的）：
- en: '![](../../OEBPS/Images/eq_08-23-a.png)'
  id: totrans-213
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-23-a.png)'
- en: 'Once we have *z*^((0)) and *a*^((0)), we can use them to evaluate *z*^((1))
    and *a*^((1)) via equations [8.16](#eq-forwardprop-z-simple) and [8.17](#eq-forwardprop-a-simple).
    But if we have *z*^((1)) and *a*^((1)), we can use them to evaluate *z*^((2))
    and *a*^((2)) via equations [8.16](#eq-forwardprop-z-simple) and [8.17](#eq-forwardprop-a-simple)
    again. And we can proceed in this fashion up to layer *L* to obtain *a*^((*L*)),
    which is the grand output of the MLP. In other words, we can iteratively evaluate
    the outputs of successive layers using *only* the outputs from the previous layer.
    No other layers need to be known. At any given iteration, we only have to keep
    the previous layer in memory: we can build the current layer from that. A single
    sequence of applications of equations [8.16](#eq-forwardprop-z-simple) and [8.17](#eq-forwardprop-a-simple)
    for layers 0 to *L* is known as a *forward pass*.'
  id: totrans-214
  prefs: []
  type: TYPE_NORMAL
  zh: 一旦我们有了 *z*^((0)) 和 *a*^((0))，我们可以使用它们通过方程 [8.16](#eq-forwardprop-z-simple) 和
    [8.17](#eq-forwardprop-a-simple) 来评估 *z*^((1)) 和 *a*^((1))。但如果我们有 *z*^((1)) 和
    *a*^((1))，我们再次可以通过方程 [8.16](#eq-forwardprop-z-simple) 和 [8.17](#eq-forwardprop-a-simple)
    使用它们来评估 *z*^((2)) 和 *a*^((2))。以此类推，我们可以一直进行到层 *L*，以获得 *a*^((*L*))，这是多层感知器 (MLP)
    的最终输出。换句话说，我们可以迭代地评估连续层的输出，只使用前一层的输出。不需要知道其他层。在任何给定迭代中，我们只需要记住前一层的输出：我们可以从那里构建当前层。将方程
    [8.16](#eq-forwardprop-z-simple) 和 [8.17](#eq-forwardprop-a-simple) 应用于层 0 到 *L*
    的单个序列被称为 *前向传播*。
- en: A similar trick can be applied to evaluate the auxiliary variables, except we
    go *backward*. We can evaluate the auxiliary variable for the last layer, *δ*^((*L*)),
    via equation [8.20](#eq-aux-lastlayer-simple). But once we have *δ*^((*L*)), we
    can evaluate *δ*^((*L* − 1)) via equation [8.23](#eq-aux-simple). From that, we
    can evaluate *δ*^((*L* − 2)). We can proceed in this fashion all the way to layer
    0, evaluating successively *δ*^((*L*)), *δ*^((*L* − 1)), ⋯, *δ*^((0)). Every time
    we evaluate a *δ*^((*l*)), we can also evaluate the *∂*𝕃**/***∂w*^((*l*)) and
    *∂*𝕃**/***∂b*^((*l*)) for the same layer via equations [8.21](#eq-dw-aux-simple)
    and [8.22](#eq-db-aux-simple), respectively. We can also update the weight and
    bias of that layer right there using the just estimated partial derivatives, since
    the current values will never be needed again during training. Thus, starting
    from the last layer, we can update the weights and biases of all layers until
    layer 0 in this fashion. This is *backpropagation*.
  id: totrans-215
  prefs: []
  type: TYPE_NORMAL
  zh: 可以应用类似的技巧来评估辅助变量，但我们需要进行*反向计算*。我们可以通过方程 [8.20](#eq-aux-lastlayer-simple) 评估最后一层的辅助变量，*δ*^((*L*))。但是一旦我们有了
    *δ*^((*L*))，我们可以通过方程 [8.23](#eq-aux-simple) 评估 *δ*^((*L* − 1))。从那里，我们可以评估 *δ*^((*L*
    − 2))。我们可以以此类推，一直计算到层 0，依次评估 *δ*^((*L*))，*δ*^((*L* − 1))，⋯，*δ*^((0))。每次评估一个 *δ*^((*l*))，我们也可以通过方程
    [8.21](#eq-dw-aux-simple) 和 [8.22](#eq-db-aux-simple) 分别评估同一层的 *∂*𝕃**/***∂w*^((*l*))
    和 *∂*𝕃**/***∂b*^((*l*))。我们还可以使用刚刚估计的偏导数立即更新该层的权重和偏置，因为当前值在训练过程中将不再需要。因此，从最后一层开始，我们可以以这种方式更新所有层的权重和偏置，直到层
    0。这就是*反向传播*。
- en: 'Of course, we have to proceed in tandem: one forward propagation which sets
    the values of *z*s and *a*s) for layers 0 to *L*, followed by a backpropagation
    layer for *L* to 0. Repeat these steps until convergence.'
  id: totrans-216
  prefs: []
  type: TYPE_NORMAL
  zh: 当然，我们必须同时进行：一次前向传播，为层 0 到 *L* 设置 *z* 和 *a* 的值，然后是层 *L* 到 0 的反向传播层。重复这些步骤，直到收敛。
- en: NOTE Fully functional code for forward propagation, MSE loss, and backpropagation,
    executable via Jupyter Notebook, can be found at [http://mng.bz/pJrw](http://mng.bz/pJrw).
  id: totrans-217
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：前向传播、均方误差损失和反向传播的完整代码，可通过 Jupyter Notebook 执行，可以在 [http://mng.bz/pJrw](http://mng.bz/pJrw)
    找到。
- en: Listing 8.3 PyTorch code for forward and backward propagation
  id: totrans-218
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 8.3 PyTorch 代码用于前向和反向传播
- en: '[PRE2]'
  id: totrans-219
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: ① Forward propagation
  id: totrans-220
  prefs: []
  type: TYPE_NORMAL
  zh: ① 前向传播
- en: ② Computes MSE loss
  id: totrans-221
  prefs: []
  type: TYPE_NORMAL
  zh: ② 计算均方误差损失
- en: ③ Arrays to store *δ*^((*l*)), *∂*𝕃**/***∂w*^((*l*)), *∂*𝕃**/***∂b*^((*l*))
    for layers 0 to *L*
  id: totrans-222
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 存储层 0 到 *L* 的 *δ*^((*l*))，*∂*𝕃**/***∂w*^((*l*))，*∂*𝕃**/***∂b*^((*l*)) 的数组
- en: ④ Activation of the last layer - *a*^((*L*))
  id: totrans-223
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 激活最后一层 - *a*^((*L*))
- en: ⑤ Computes the *δ* and gradients for layer *L*
  id: totrans-224
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ 计算层 *L* 的 *δ* 和梯度
- en: ⑥ Computes the *δ* and gradients for layers 0 to *L* − 1
  id: totrans-225
  prefs: []
  type: TYPE_NORMAL
  zh: ⑥ 计算层 0 到 *L* − 1 的 *δ* 和梯度
- en: Backpropagation algorithm on an arbitrary network of linear layers
  id: totrans-226
  prefs: []
  type: TYPE_NORMAL
  zh: 在任意线性层网络上的反向传播算法
- en: In section [8.4.5.1](#sec-backprop-simplenet), we saw a simple network with
    only one neuron per layer. There was only one connection and hence one weight,
    one activation, and one auxiliary variable per layer. Consequently, we could drop
    the subscripts (although we had to keep the superscript indicating the layer)
    of all these variables. Now we examine a more generic network consisting of linear
    layers 0, ⋯, *L*. An arbitrary slice of this network is shown in figure [8.6](#fig-linear-layer).
  id: totrans-227
  prefs: []
  type: TYPE_NORMAL
  zh: 在 [8.4.5.1](#sec-backprop-simplenet) 节中，我们看到了一个只有每层一个神经元的简单网络。只有一个连接，因此每层只有一个权重、一个激活和一个辅助变量。因此，我们可以省略所有这些变量的下标（尽管我们必须保留表示层的上标）。现在我们考察一个更通用的网络，由线性层
    0，⋯，*L* 组成。这个网络的任意切片如图 [8.6](#fig-linear-layer) 所示。
- en: The ultimate goal is to evaluate the partial derivatives of the loss with respect
    to the weights and biases. Using them, we can update the current weights and biases
    to optimally reduce the loss.
  id: totrans-228
  prefs: []
  type: TYPE_NORMAL
  zh: 最终目标是评估损失相对于权重和偏置的偏导数。使用它们，我们可以更新当前的权重和偏置，以最优地减少损失。
- en: Our overall strategy is as follows. We use the auxiliary variables again. We
    first derive expressions that allow us to compute the auxiliary variable for the
    last layer. Then we derive an expression that allows us to compute auxiliary variables
    for an arbitrary layer, *l*, given the auxiliary variables for layer *l* + 1.
    Since we can directly compute auxiliary variables for the last layer, *L*, we
    can use this expression to compute auxiliary variables for the second-to-last
    layer *L* − 1. But once we have them, we can compute auxiliary variables for layer
    *L* − 2. We proceed like this until we reach layer 0. Thus we can compute all
    the auxiliary variables. We also derive expressions that allow us to compute,
    from the auxiliary variables, the partial derivatives of loss with respect to
    weights and biases. This gives us everything we need. Since we start by computing
    things pertaining to the last layer and proceed iteratively toward the initial
    layer, the process is called *backpropagation*.
  id: totrans-229
  prefs: []
  type: TYPE_NORMAL
  zh: 我们的整体策略如下。我们再次使用辅助变量。我们首先推导出允许我们计算最后一层辅助变量的表达式。然后我们推导出一个表达式，允许我们在给定层 *l* + 1
    的辅助变量的情况下，计算任意层 *l* 的辅助变量。由于我们可以直接计算最后一层 *L* 的辅助变量，我们可以使用这个表达式来计算倒数第二层 *L* − 1
    的辅助变量。但是一旦我们有了它们，我们就可以计算层 *L* − 2 的辅助变量。我们就这样一直进行下去，直到达到层 0。因此我们可以计算所有的辅助变量。我们还推导出允许我们从辅助变量中计算损失相对于权重和偏差的偏导数的表达式。这给了我们所需的一切。由于我们首先计算与最后一层相关的事物，然后迭代地朝向初始层进行，这个过程被称为
    *反向传播*。
- en: 'You will notice the similarity between the expressions derived next and those
    derived for the one-neuron-per-layer network. The differences are explained:'
  id: totrans-230
  prefs: []
  type: TYPE_NORMAL
  zh: 你将注意到接下来推导的表达式与为单神经元每层网络推导的表达式之间的相似性。差异如下解释：
- en: '*Forward propagation (arbitrary layer l)*—Forward propagation through this
    network has already been described in section [8.3.1](#sec-linlayer-matmult) and
    can be succinctly represented by equation [8.7](../Text/08.xhtml#eq-linlayer-forwardprop)
    repeated here for handy reference). On the left are the scalar equations, for
    one neuron at a time; and on the right are the vector equations, for the entire
    layer. They are equivalent:'
  id: totrans-231
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*前向传播（任意层 l）*—通过这个网络的前向传播已在第 [8.3.1](#sec-linlayer-matmult) 节中描述，可以用方程 [8.7](../Text/08.xhtml#eq-linlayer-forwardprop)
    简要表示，此处重复以方便参考）。左侧是针对单个神经元的标量方程；右侧是针对整个层的向量方程。它们是等价的：'
- en: '![](../../OEBPS/Images/eq_08-24.png)'
  id: totrans-232
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-24.png)'
- en: Equation 8.24
  id: totrans-233
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.24
- en: Indices *j* and *k* iterate over all the neurons in the relevant layer. By convention,
    we always use these variables for arbitrary neurons in a layer. The variable *l*
    is used to index the layers. When indexing weights, we typically use *j* to indicate
    the destination and *k* to indicate the source—remember that weights are indexed
    (destination, source) somewhat unexpectedly to simplify the math. Typically, vectors
    correspond to entire layers. Individual vector elements correspond to specific
    neurons and are indexed by *j* or *k*.
  id: totrans-234
  prefs: []
  type: TYPE_NORMAL
  zh: 索引 *j* 和 *k* 遍历相关层中的所有神经元。按照惯例，我们总是用这些变量表示层中的任意神经元。变量 *l* 用于索引层。在索引权重时，我们通常用
    *j* 表示目标，用 *k* 表示源——记住权重是按（目标，源）意外地索引的，以简化数学。通常，向量对应整个层。单个向量元素对应特定的神经元，并由 *j*
    或 *k* 索引。
- en: '*Loss*—Unlike the simple network, here, the final *L*th layer can have multiple
    neurons. Hence the loss function becomes'
  id: totrans-235
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*损失*—与简单的网络不同，在这里，最终的 *L* 层可以包含多个神经元。因此，损失函数变为'
- en: '![](../../OEBPS/Images/eq_08-25.png)'
  id: totrans-236
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-25.png)'
- en: Equation 8.25
  id: totrans-237
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.25
- en: 'where the summation happens over all neurons in the last layer. Note that ![](../../OEBPS/Images/AR_a.png)^((*L*))
    is the output of the MLP: that is, ![](../../OEBPS/Images/AR_a.png)^((*L*)) =
    ![](../../OEBPS/Images/AR_y.png) = *MLP*(![](../../OEBPS/Images/AR_x.png)) for
    the training input ![](../../OEBPS/Images/AR_x.png) (see equation [8.10](#eq-forwardprop-layered)).
    The GT output corresponding to ![](../../OEBPS/Images/AR_x.png) is the constant
    vector *ȳ*. The closer ![](../../OEBPS/Images/AR_y.png) is to *ȳ*, the smaller
    the loss. Note that we need to average the loss over the entire training data
    set. Here we are showing the loss computation for a single training data instance.
    The computation simply needs to be replicated for each training data instance,
    and the results averaged.'
  id: totrans-238
  prefs: []
  type: TYPE_NORMAL
  zh: 其中求和发生在最后一层的所有神经元上。注意，![](../../OEBPS/Images/AR_a.png)^((*L*))是MLP的输出，即![](../../OEBPS/Images/AR_a.png)^((*L*))
    = ![](../../OEBPS/Images/AR_y.png) = *MLP*(![](../../OEBPS/Images/AR_x.png))对于训练输入![](../../OEBPS/Images/AR_x.png)（见方程式[8.10](#eq-forwardprop-layered)）。对应于![](../../OEBPS/Images/AR_x.png)的GT输出是常数向量*ȳ*。![](../../OEBPS/Images/AR_y.png)越接近*ȳ*，损失越小。注意，我们需要在整个训练数据集上平均损失。这里我们展示了单个训练数据实例的损失计算。计算只需为每个训练数据实例复制，然后取平均值。
- en: '*Auxiliary variables*—Now that a layer has multiple neurons, we have one auxiliary
    variable per neuron. Thus the auxiliary variable has a subscript identifying the
    specific neuron in that layer. It continues to have a superscript indicating its
    layer. We define'
  id: totrans-239
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*辅助变量*——由于一个层有多个神经元，我们为每个神经元有一个辅助变量。因此，辅助变量有一个下标来标识该层中的特定神经元。它继续有一个上标来表示其层。我们定义'
- en: '![](../../OEBPS/Images/eq_08-25-a.png)'
  id: totrans-240
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-25-a](../../OEBPS/Images/eq_08-25-a.png)'
- en: '*Auxiliary variable* *for the last layer*'
  id: totrans-241
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*最后一层的辅助变量*'
- en: '![](../../OEBPS/Images/eq_08-25-b.png)'
  id: totrans-242
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-25-b](../../OEBPS/Images/eq_08-25-b.png)'
- en: Using equation [8.25](#eq-loss-mlp) and observing that only one of the terms
    in the summation—the *j*th term—will survive the differentiation with respect
    to *a[j]*^((*L*)) since the *a[j]*s are independent of each other), we get
  id: totrans-243
  prefs: []
  type: TYPE_NORMAL
  zh: 使用方程式[8.25](#eq-loss-mlp)并观察，由于*a[j]*s彼此独立，因此在相对于*a[j]*^((*L*))的微分过程中，求和中只有一个项——第*j*项会幸存，我们得到
- en: '![](../../OEBPS/Images/eq_08-25-c.png)'
  id: totrans-244
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-25-c](../../OEBPS/Images/eq_08-25-c.png)'
- en: Also, using the lower-left equation from [8.24](../Text/08.xhtml#eq-MLP-out-nosubscript-1)
    and equation [8.5](../Text/08.xhtml#eq-sigmoid-derivative), we get
  id: totrans-245
  prefs: []
  type: TYPE_NORMAL
  zh: 此外，使用[8.24](../Text/08.xhtml#eq-MLP-out-nosubscript-1)中的左下方程和方程式[8.5](../Text/08.xhtml#eq-sigmoid-derivative)，我们得到
- en: '![](../../OEBPS/Images/eq_08-25-d.png)'
  id: totrans-246
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-25-d](../../OEBPS/Images/eq_08-25-d.png)'
- en: Combining these, we get
  id: totrans-247
  prefs: []
  type: TYPE_NORMAL
  zh: 结合这些，我们得到
- en: '![](../../OEBPS/Images/eq_08-26.png)'
  id: totrans-248
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-26](../../OEBPS/Images/eq_08-26.png)'
- en: Equation 8.26
  id: totrans-249
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式8.26
- en: '![](../../OEBPS/Images/eq_08-27.png)'
  id: totrans-250
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-27](../../OEBPS/Images/eq_08-27.png)'
- en: Equation 8.27
  id: totrans-251
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式8.27
- en: Here, ∘ denotes the Hadamard product between two vectors. It is basically a
    vector of elementwise products of corresponding vector elements. Thus,
  id: totrans-252
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，∘ 表示两个向量之间的Hadamard积。它基本上是一个元素级对应向量元素乘积的向量。因此，
- en: '![](../../OEBPS/Images/eq_08-28.png)'
  id: totrans-253
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-28](../../OEBPS/Images/eq_08-28.png)'
- en: Equation 8.28
  id: totrans-254
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式8.28
- en: '![](../../OEBPS/Images/eq_08-29.png)'
  id: totrans-255
  prefs: []
  type: TYPE_IMG
  zh: '![方程式8-29](../../OEBPS/Images/eq_08-29.png)'
- en: Equation 8.29
  id: totrans-256
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式8.29
- en: Equations [8.26](#eq-auxvar-last-layer-scalar) and [8.27](#eq-auxvar-last-layer-vector)
    are identical. The former is a scalar equation expressing individual auxiliary
    variables of the last layer. The latter is a vector equation expressing all the
    auxiliary variables of the last layer together. We can compute these directly
    if we have performed a forward pass and have its results, the *a[j]*^((*L*))s
    available along with the training data GT.
  id: totrans-257
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式[8.26](#eq-auxvar-last-layer-scalar)和[8.27](#eq-auxvar-last-layer-vector)是相同的。前者是一个表示最后一层单个辅助变量的标量方程。后者是一个表示最后一层所有辅助变量的向量方程。如果我们已经执行了前向传递并有了其结果，包括*a[j]*^((*L*))s以及训练数据的GT，我们可以直接计算这些方程。
- en: '*Auxiliary variable for an arbitrary layer, l*—This is significantly different
    and harder to understand than the one-neuron-per-layer case. We are trying to
    evaluate *δ[j]*^((*l*)) = *∂*𝕃**/***∂z[j]*^((*l*)) in the general case: that is,
    for an arbitrary layer *l*. The loss does not *directly* depend on the inner layer
    variable *z[j]*^((*l*)). The loss directly depends only on the last layer activations,
    which depend on the previous layer, and so forth. The *z*s in any one layer form
    a *complete* dependency set for the loss 𝕃, meaning the loss can be expressed
    in terms of only these and no other variables. In particular, we can express the
    loss as 𝕃(*z*[0]^((*l*+1)), *z*[1]^((*l*+1)), *z*[2]^((*l*+1)),⋯). You can form
    a mental picture that *z[j]*^((*l*)) fans out to 𝕃 *through* all the *z*s in the
    next layer, *z*[0]^((*l*+1)), *z*[1]^((*l*+1)), *z*[2]^((*l*+1)), and so on. Then,
    using the chain rule of partial differentiation,'
  id: totrans-258
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*任意层的辅助变量*—这比每层只有一个神经元的情形显著不同且更难以理解。我们试图评估在一般情况下的 *δ[j]*^((*l*)) = *∂*𝕃**/***∂z[j]*^((*l*))：即对于任意层
    *l*。损失并不*直接*依赖于内部层变量 *z[j]*^((*l*)）。损失直接依赖于仅最后一层的激活，这些激活依赖于前一层，依此类推。任何一层中的 *z*
    形成一个*完整*的依赖集，对于损失 𝕃，这意味着损失可以用这些变量来表示，而无需其他变量。特别是，我们可以将损失表示为 𝕃(*z*[0]^((*l*+1)),
    *z*[1]^((*l*+1)), *z*[2]^((*l*+1)),⋯)。你可以形成一个心理图像，*z[j]*^((*l*)) 通过下一层的所有 *z*
    扩散到 𝕃，即 *z*[0]^((*l*+1)), *z*[1]^((*l*+1)), *z*[2]^((*l*+1))，等等。然后，使用偏导数的链式法则，'
- en: '![](../../OEBPS/Images/eq_08-29-a.png)'
  id: totrans-259
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-29-a.png)'
- en: Now, by definition,
  id: totrans-260
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，根据定义，
- en: '![](../../OEBPS/Images/eq_08-29-b.png)'
  id: totrans-261
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-29-b.png)'
- en: And using equation [8.24](../Text/08.xhtml#eq-MLP-out-nosubscript-1),
  id: totrans-262
  prefs: []
  type: TYPE_NORMAL
  zh: 并且使用方程 [8.24](../Text/08.xhtml#eq-MLP-out-nosubscript-1)，
- en: '![](../../OEBPS/Images/eq_08-29-c.png)'
  id: totrans-263
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-29-c.png)'
- en: while
  id: totrans-264
  prefs: []
  type: TYPE_NORMAL
  zh: 而
- en: '![](../../OEBPS/Images/eq_08-29-d.png)'
  id: totrans-265
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-29-d.png)'
- en: 'Combining all these, we get the scalar expression for a single auxiliary variable.
    It is presented here along with its equivalent vector equation for the entire
    layer:'
  id: totrans-266
  prefs: []
  type: TYPE_NORMAL
  zh: 将所有这些结合起来，我们得到单个辅助变量的标量表达式。它及其整个层的等价向量方程在此处展示：
- en: '![](../../OEBPS/Images/eq_08-30.png)'
  id: totrans-267
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-30.png)'
- en: Equation 8.30
  id: totrans-268
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.30
- en: '![](../../OEBPS/Images/eq_08-31.png)'
  id: totrans-269
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-31.png)'
- en: Equation 8.31
  id: totrans-270
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.31
- en: Here, ∘ denotes the Hadamard multiplication explained earlier and *W*^((+1*l*))
    is the matrix representing the weights of *all connections from layer l to layer
    (*l*+1)* (see equation [8.8](../Text/08.xhtml#eq-MLP-weight-matrix)). Equations
    [8.30](#eq-auxvar-scalar) and [8.31](../Text/08.xhtml#eq-auxvar-vector) allow
    us to evaluate *δ*^((*l*))s from the *δ*^((*l*+1))s if the results of forward
    propagation (*a*s) are available. We have already shown that the auxiliary variables
    for the last layer are directly computable from the activations of that layer.
    Hence, we can evaluate all the layers’ auxiliary variables.
  id: totrans-271
  prefs: []
  type: TYPE_NORMAL
  zh: 这里，∘ 表示前面解释过的Hadamard乘法，*W*^((+1*l*)) 是表示从层 *l* 到层 (*l*+1)* 的所有连接的矩阵（参见方程 [8.8](../Text/08.xhtml#eq-MLP-weight-matrix)）。方程
    [8.30](#eq-auxvar-scalar) 和 [8.31](../Text/08.xhtml#eq-auxvar-vector) 允许我们在前向传播的结果
    (*a*s) 可用的情况下，从 *δ*^((*l*+1))s 评估 *δ*^((*l*))s。我们已经表明，最后一层的辅助变量可以直接从该层的激活中计算出来。因此，我们可以评估所有层的辅助变量。
- en: '*Derivatives of loss with respect to weights and biases in terms of auxiliary
    variables*—We have already seen how to compute auxiliary variables. Now we will
    express the partial derivatives of loss with respect to weights and biases in
    terms of those. This will provide us with the gradients we need to update the
    weights and biases along the negative gradient, which is the optimal move to minimize
    loss:'
  id: totrans-272
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*关于辅助变量的损失相对于权重和偏差的导数*—我们已经看到了如何计算辅助变量。现在我们将关于权重和偏差的损失的偏导数用这些变量来表示。这将为我们提供沿着负梯度更新权重和偏差所需的梯度，这是最小化损失的最优移动：'
- en: '![](../../OEBPS/Images/eq_08-32.png)'
  id: totrans-273
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-32.png)'
- en: Equation 8.32
  id: totrans-274
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.32
- en: '![](../../OEBPS/Images/eq_08-33.png)'
  id: totrans-275
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-33.png)'
- en: Equation 8.33
  id: totrans-276
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 8.33
- en: 'Equations [8.32](#eq-partialderiv-loss-wt-scalar) and [8.33](../Text/08.xhtml#eq-partialderiv-loss-wt-vector)
    are equivalent. The first is scalar and pertains to individual weights in layer
    *l*, and the second describes the entire layer. Similarly, equations [8.34](#eq-partialderiv-loss-bias-scalar)
    and [8.35](#eq-partialderiv-loss-bias-vector) are equivalent:'
  id: totrans-277
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 [8.32](#eq-partialderiv-loss-wt-scalar) 和 [8.33](../Text/08.xhtml#eq-partialderiv-loss-wt-vector)
    是等价的。第一个是标量，与层 *l* 中的单个权重相关，第二个描述了整个层。同样，方程 [8.34](#eq-partialderiv-loss-bias-scalar)
    和 [8.35](#eq-partialderiv-loss-bias-vector) 也是等价的：
- en: '![](../../OEBPS/Images/eq_08-34.png)'
  id: totrans-278
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_08-34.png)'
- en: Equation 8.34
  id: totrans-279
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.34
- en: '![](../../OEBPS/Images/eq_08-35.png)'
  id: totrans-280
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-35.png)'
- en: Equation 8.35
  id: totrans-281
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 8.35
- en: The first is scalar and pertains to individual biases in layer *l*, and the
    second describes the entire layer.
  id: totrans-282
  prefs: []
  type: TYPE_NORMAL
  zh: 第一个是标量，与层 *l* 中的单个偏差有关，第二个描述了整个层。
- en: '8.4.6 Putting it all together: Overall training algorithm'
  id: totrans-283
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 8.4.6 将所有内容组合起来：整体训练算法
- en: 'Previously, we discussed forward propagation: passing an input vector ![](../../OEBPS/Images/AR_x.png)
    through a sequence of linear layers and generating an output prediction. We learned
    about MSE loss, 𝕃, which calculates the deviation of the output prediction from
    the GT, *y*. We also learned to compute the gradients of 𝕃 with respect to parameters
    *W* and *b* using backpropagation. In the following algorithm, we describe how
    these components come together in the training process:'
  id: totrans-284
  prefs: []
  type: TYPE_NORMAL
  zh: 之前，我们讨论了前向传播：将输入向量 ![](../../OEBPS/Images/AR_x.png) 通过一系列线性层传递并生成输出预测。我们学习了
    MSE 损失，𝕃，它计算输出预测与 GT，*y* 的偏差。我们还学习了如何使用反向传播计算 𝕃 对参数 *W* 和 *b* 的梯度。在以下算法中，我们描述了这些组件如何在训练过程中结合：
- en: Algorithm 8.5 Training a neural network
  id: totrans-285
  prefs: []
  type: TYPE_NORMAL
  zh: 算法 8.5 训练神经网络
- en: Initialize ![](../../OEBPS/Images/AR_w.png), *b* with random values
  id: totrans-286
  prefs: []
  type: TYPE_NORMAL
  zh: 使用随机值初始化 ![](../../OEBPS/Images/AR_w.png), *b*
- en: '**while** 𝕃 > *threshold* **do**'
  id: totrans-287
  prefs: []
  type: TYPE_NORMAL
  zh: '**while** 𝕃 > *threshold* **do**'
- en: ⊳ Forward pass
  id: totrans-288
  prefs: []
  type: TYPE_NORMAL
  zh: ⊳ 前向传播
- en: '**for** *l* ← 0 to *L* **do**'
  id: totrans-289
  prefs: []
  type: TYPE_NORMAL
  zh: '**for** *l* ← 0 to *L* **do**'
- en: '![](../../OEBPS/Images/AR_z.png)^((*l*)) = *W*^((*l*)) ![](../../OEBPS/Images/AR_a.png)^((*l*–1))
    + ![](../../OEBPS/Images/AR_b.png)^((*l*))'
  id: totrans-290
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/AR_z.png)^((*l*)) = *W*^((*l*)) ![](../../OEBPS/Images/AR_a.png)^((*l*–1))
    + ![](../../OEBPS/Images/AR_b.png)^((*l*))'
- en: '![](../../OEBPS/Images/AR_a.png)^((*l*)) = *σ*(![](../../OEBPS/Images/AR_z.png)^((*l*)))'
  id: totrans-291
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/AR_a.png)^((*l*)) = *σ*(![](../../OEBPS/Images/AR_z.png)^((*l*)))'
- en: '**end** **for**'
  id: totrans-292
  prefs: []
  type: TYPE_NORMAL
  zh: '**end** **for**'
- en: ⊳ Loss
  id: totrans-293
  prefs: []
  type: TYPE_NORMAL
  zh: ⊳ 损失
- en: 𝕃 = 1/2 ||![](../../OEBPS/Images/AR_a.png)^((*L*)) – *ȳ*||²
  id: totrans-294
  prefs: []
  type: TYPE_NORMAL
  zh: 𝕃 = 1/2 ||![](../../OEBPS/Images/AR_a.png)^((*L*)) – *ȳ*||²
- en: ⊳ Gradients for the last layer
  id: totrans-295
  prefs: []
  type: TYPE_NORMAL
  zh: ⊳ 最后一层的梯度
- en: '![](../../OEBPS/Images/AR_delta.png) ^((*L*)) = (![](../../OEBPS/Images/AR_a.png)^((*L*))
    – *ȳ*) ∘ ![](../../OEBPS/Images/AR_a.png)^((*L*)) ∘ (![](../../OEBPS/Images/AR_1.png)
    – ![](../../OEBPS/Images/AR_a.png)^((*L*)))'
  id: totrans-296
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/AR_delta.png) ^((*L*)) = (![](../../OEBPS/Images/AR_a.png)^((*L*))
    – *ȳ*) ∘ ![](../../OEBPS/Images/AR_a.png)^((*L*)) ∘ (![](../../OEBPS/Images/AR_1.png)
    – ![](../../OEBPS/Images/AR_a.png)^((*L*)))'
- en: ▽*W*^((*L*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*L*))(![](../../OEBPS/Images/AR_a.png)^((*L*–1)))^T
  id: totrans-297
  prefs: []
  type: TYPE_NORMAL
  zh: ▽*W*^((*L*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*L*))(![](../../OEBPS/Images/AR_a.png)^((*L*–1)))^T
- en: ▽*b*^((*L*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*L*))
  id: totrans-298
  prefs: []
  type: TYPE_NORMAL
  zh: ▽*b*^((*L*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*L*))
- en: ⊳ Gradients for the remaining layers
  id: totrans-299
  prefs: []
  type: TYPE_NORMAL
  zh: ⊳ 剩余层的梯度
- en: '**for** *l* ← *L* – 1 to 0 **do**'
  id: totrans-300
  prefs: []
  type: TYPE_NORMAL
  zh: '**for** *l* ← *L* – 1 to 0 **do**'
- en: '![](../../OEBPS/Images/AR_delta.png) ^((*l*)) = ((*W*^((*l*+1)))*^T* ![](../../OEBPS/Images/AR_delta.png)^((*l*+1)))
    ![](../../OEBPS/Images/AR_a.png)^((*l*)) ∘ (![](../../OEBPS/Images/AR_1.png) –
    ![](../../OEBPS/Images/AR_a.png)^((*l*)))'
  id: totrans-301
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/AR_delta.png) ^((*l*)) = ((*W*^((*l*+1)))*^T* ![](../../OEBPS/Images/AR_delta.png)^((*l*+1)))
    ![](../../OEBPS/Images/AR_a.png)^((*l*)) ∘ (![](../../OEBPS/Images/AR_1.png) –
    ![](../../OEBPS/Images/AR_a.png)^((*l*)))'
- en: ▽*W*^((*l*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*l*)) (![](../../OEBPS/Images/AR_a.png)^((*l*–1)))^T
  id: totrans-302
  prefs: []
  type: TYPE_NORMAL
  zh: ▽*W*^((*l*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*l*)) (![](../../OEBPS/Images/AR_a.png)^((*l*–1)))^T
- en: ▽*b*^((*l*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*l*))
  id: totrans-303
  prefs: []
  type: TYPE_NORMAL
  zh: ▽*b*^((*l*))𝕃 = ![](../../OEBPS/Images/AR_delta.png) ^((*l*))
- en: '**end** **for**'
  id: totrans-304
  prefs: []
  type: TYPE_NORMAL
  zh: '**end** **for**'
- en: ⊳ Parameter update
  id: totrans-305
  prefs: []
  type: TYPE_NORMAL
  zh: ⊳ 参数更新
- en: '*W* = *W* – *r*▽*[W]*𝕃'
  id: totrans-306
  prefs: []
  type: TYPE_NORMAL
  zh: '*W* = *W* – *r*▽*[W]*𝕃'
- en: '*b* = *b* – *r*▽*[b]*𝕃'
  id: totrans-307
  prefs: []
  type: TYPE_NORMAL
  zh: '*b* = *b* – *r*▽*[b]*𝕃'
- en: '**end** **while**'
  id: totrans-308
  prefs: []
  type: TYPE_NORMAL
  zh: '**end** **while**'
- en: 8.5 Training a neural network in PyTorch
  id: totrans-309
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 8.5 使用 PyTorch 训练神经网络
- en: 'Now that we’ve seen how the training process works, let’s look at how this
    can be implemented in PyTorch. For this purpose, let’s take the following example.
    Consider an e-commerce company that’s trying to solve the problem of demand prediction:
    the company would like to estimate the number of mobile phones that will be sold
    in the upcoming week so that it can manage its inventory accordingly. Our goal
    is to develop a model that can make such a prediction. Let’s assume that the demand
    for a given week is a function of three variables: (a) the number of mobile phones
    sold in the previous week, (b) discounts offered, and (c) the number of weeks
    to the next holiday. Let’s call these variables `prev_week_sales`, `discount_fraction`,
    and `weeks_to_next_holidays`, respectively. This example can be modeled as a regression
    problem wherein we predict the number of mobile phones sold in the upcoming week
    from an input vector of the form [`prev_week_sales`, `discount_fraction`, `weeks_to_next_holidays`].'
  id: totrans-310
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们已经了解了训练过程是如何工作的，让我们看看如何在PyTorch中实现它。为此，让我们看以下示例。考虑一家试图解决需求预测问题的电子商务公司：该公司希望估计即将到来的周内将售出的手机数量，以便相应地管理其库存。我们的目标是开发一个可以进行此类预测的模型。让我们假设特定周的销量是三个变量的函数：(a)上周售出的手机数量，(b)提供的折扣，以及(c)到下一个假期的周数。我们将这些变量分别称为`prev_week_sales`、`discount_fraction`和`weeks_to_next_holidays`。这个例子可以建模为一个回归问题，其中我们从一个形式为`[prev_week_sales,
    discount_fraction, weeks_to_next_holidays]`的输入向量中预测即将到来的周内售出的手机数量。
- en: NOTE Fully functional code for this section, executable via Jupyter Notebook,
    can be found at [http://mng.bz/O1Ra](http://mng.bz/O1Ra).
  id: totrans-311
  prefs: []
  type: TYPE_NORMAL
  zh: 备注：本节完全功能的代码，可通过Jupyter Notebook执行，可在[http://mng.bz/O1Ra](http://mng.bz/O1Ra)找到。
- en: From historical data, we generate a large data set, `X`, that contains the values
    of the three variables for the last *N* weeks. `X` is represented as an *N* x
    3 matrix, with each row representing an individual training data instance and
    *N* being the total number of data points available. We also have a GT vector
    *ȳ* of length *N*, containing the actual sales of mobile phones for each of the
    weeks in the training data set. Table [8.1](#tab-demand-prediction-data-set) shows
    sample data points from our training set.
  id: totrans-312
  prefs: []
  type: TYPE_NORMAL
  zh: 从历史数据中，我们生成一个包含过去*N*周三个变量值的大数据集`X`。`X`表示为一个*N* x 3的矩阵，其中每一行代表一个单独的训练数据实例，而*N*是可用的数据点的总数。我们还有一个长度为*N*的GT向量*ȳ*，其中包含训练数据集中每周的实际手机销量。表[8.1](#tab-demand-prediction-data-set)显示了我们的训练集的样本数据点。
- en: Table 8.1 Sample training data for demand prediction
  id: totrans-313
  prefs: []
  type: TYPE_NORMAL
  zh: 表8.1 需求预测的样本训练数据
- en: '| Previous week sales | Discount fraction (%) | Weeks to next holidays | Number
    of units sold |'
  id: totrans-314
  prefs: []
  type: TYPE_TB
  zh: '| 上周销量 | 折扣比例 (%) | 到下一个假期的周数 | 销售单位数 |'
- en: '| --- | --- | --- | --- |'
  id: totrans-315
  prefs: []
  type: TYPE_TB
  zh: '| --- | --- | --- | --- |'
- en: '| 76,440 | 63 | 2 | 94,182 |'
  id: totrans-316
  prefs: []
  type: TYPE_TB
  zh: '| 76,440 | 63 | 2 | 94,182 |'
- en: '| 41,512 | 50 | 3 | 51,531 |'
  id: totrans-317
  prefs: []
  type: TYPE_TB
  zh: '| 41,512 | 50 | 3 | 51,531 |'
- en: '| 77,395 | 77 | 9 | 95,938 |'
  id: totrans-318
  prefs: []
  type: TYPE_TB
  zh: '| 77,395 | 77 | 9 | 95,938 |'
- en: '| … | … | … | … |'
  id: totrans-319
  prefs: []
  type: TYPE_TB
  zh: '| … | … | … | … |'
- en: '| 21,532 | 70 | 4 | 28,559 |'
  id: totrans-320
  prefs: []
  type: TYPE_TB
  zh: '| 21,532 | 70 | 4 | 28,559 |'
- en: NOTE In this section, `X` and *ȳ* refer to the entire batch of training data
    instances. This may be infeasible in practical settings because of large data
    sets. To address this, we typically use mini-batches of `X` and *ȳ*. We introduce
    the concept of mini-batches formally in the next chapter.
  id: totrans-321
  prefs: []
  type: TYPE_NORMAL
  zh: 备注：在本节中，`X`和*ȳ*指的是整个批次的训练数据实例。由于数据集很大，这在实际设置中可能不可行。为了解决这个问题，我们通常使用`X`和*ȳ*的迷你批次。我们将在下一章正式介绍迷你批次的概念。
- en: 'One important point about the data set is that the range of values for each
    feature is completely different. For example, the previous week’s sales are expressed
    as a number on the order of tens of thousands of units, whereas the discount fraction
    is a percentage number between 0 and 100\. In machine learning, it is a good practice
    to bring all the values to a common scale, because doing so can help improve the
    speed of training and reduce the chance of getting stuck at a local minimum. For
    our example, let’s use min-max normalization to scale all the features to 0–1\.
    The following code snippet shows how to perform min-max normalization in PyTorch.
    For the rest of the discussion, we assume that we are operating on the normalized
    data:'
  id: totrans-322
  prefs: []
  type: TYPE_NORMAL
  zh: 关于数据集的一个重要观点是，每个特征的值域完全不同。例如，上周的销售量以数万个单位的数字表示，而折扣分数是一个介于0和100之间的百分比数字。在机器学习中，将所有值归一化到共同尺度是一个好的实践，因为这样做可以帮助提高训练速度并减少陷入局部最小值的机会。对于我们的例子，让我们使用最小-最大归一化将所有特征缩放到0-1范围。以下代码片段展示了如何在PyTorch中执行最小-最大归一化。在接下来的讨论中，我们假设我们正在处理归一化后的数据：
- en: '[PRE3]'
  id: totrans-323
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
- en: ① Clones the data so as not to mutilate the original data
  id: totrans-324
  prefs: []
  type: TYPE_NORMAL
  zh: ① 复制数据，以免损坏原始数据
- en: ② Calculates the min and max values of each column of *X*
  id: totrans-325
  prefs: []
  type: TYPE_NORMAL
  zh: ② 计算*X*每一列的最小值和最大值
- en: ③ Normalizes *X*
  id: totrans-326
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 归一化*X*
- en: ④ Calculates the min and max values of *y*
  id: totrans-327
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 计算最小值和最大值*y*
- en: ⑤ Normalizes *y*
  id: totrans-328
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ 归一化*y*
- en: 'To solve the regression problem, let’s first define a two-layer neural network
    model that can take in 3D input vectors of the form [`prev_week_sales`, `discount_fraction`,
    `is_holidays_ongoing`] and generate output predictions. The following code snippet
    gives the PyTorch implementation:'
  id: totrans-329
  prefs: []
  type: TYPE_NORMAL
  zh: 为了解决回归问题，我们首先定义一个两层神经网络模型，它可以接受形式为[`prev_week_sales`，`discount_fraction`，`is_holidays_ongoing`]的3D输入向量，并生成输出预测。以下代码片段给出了PyTorch实现：
- en: '[PRE4]'
  id: totrans-330
  prefs: []
  type: TYPE_PRE
  zh: '[PRE4]'
- en: ① Defines the network as a sequence of linear and sigmoid layers
  id: totrans-331
  prefs: []
  type: TYPE_NORMAL
  zh: ① 将网络定义为一系列线性层和sigmoid层
- en: ② First hidden layer with a weight matrix of size input_size × hidden1_size)
  id: totrans-332
  prefs: []
  type: TYPE_NORMAL
  zh: ② 第一个隐藏层，权重矩阵大小为input_size × hidden1_size）
- en: ③ Second hidden layer with a weight matrix of size hidden1_size × hidden2_size)
  id: totrans-333
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 第二个隐藏层，权重矩阵大小为hidden1_size × hidden2_size）
- en: ④ Output layer with a weight matrix of size hidden2_size × output_size)
  id: totrans-334
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 输出层，权重矩阵大小为hidden2_size × output_size）
- en: ⑤ *X* is an *N* × 3 matrix. Each row is a (3D vector) representing a single
    data point.
  id: totrans-335
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ *X*是一个*N* × 3的矩阵。每一行是一个（3D向量），代表一个单独的数据点。
- en: Neural network models in PyTorch should subclass `torch.nn.Module` and implement
    the `forward` method. Our two-layer neural network contains two linear layers,
    each followed by a sigmoid (nonlinear) activation layer. Finally, we have a linear
    layer that converts the final activation into the output prediction. These layers
    are chained together using the `torch.nn.Sequential` class to form the two-layer
    neural network. Whenever our model is called using `nn(X)`, the `forward` method
    is invoked, and the input `X` is passed through the individual layers to obtain
    the final output.
  id: totrans-336
  prefs: []
  type: TYPE_NORMAL
  zh: PyTorch中的神经网络模型应该继承自`torch.nn.Module`并实现`forward`方法。我们的两层神经网络包含两个线性层，每个线性层后面跟着一个sigmoid（非线性）激活层。最后，我们有一个将最终激活转换为输出预测的线性层。这些层通过`torch.nn.Sequential`类链接在一起，形成两层神经网络。每次我们使用`nn(X)`调用模型时，都会调用`forward`方法，并将输入`X`传递到各个层以获得最终输出。
- en: 'Now that we have defined the neural network and its forward pass, we need to
    define the loss function. We can use the MSE loss defined in equation [8.11](../Text/08.xhtml#eq-mse-loss).
    The loss function essentially compares the demand predicted by the neural network
    model with the actual demand from the GT and returns larger values when the difference
    is higher and smaller values when the difference is lower. MSE loss is readily
    available in PyTorch through the `torch.nn.MSELoss` class. The following code
    snippet shows a sample invocation:'
  id: totrans-337
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们已经定义了神经网络及其前向传递，我们需要定义损失函数。我们可以使用方程[8.11](../Text/08.xhtml#eq-mse-loss)中定义的均方误差损失。损失函数本质上比较神经网络模型预测的需求与GT的实际需求，当差异较大时返回较大的值，当差异较小时返回较小的值。均方误差损失在PyTorch中通过`torch.nn.MSELoss`类提供。以下代码片段展示了示例调用：
- en: '[PRE5]'
  id: totrans-338
  prefs: []
  type: TYPE_PRE
  zh: '[PRE5]'
- en: ① Instantiates the loss function
  id: totrans-339
  prefs: []
  type: TYPE_NORMAL
  zh: ① 实例化损失函数
- en: '② compute loss _pred: Output of the neural network _gt: ground truth'
  id: totrans-340
  prefs: []
  type: TYPE_NORMAL
  zh: ② 计算损失_pred：神经网络的输出_gt：真实值
- en: Finally, we need a way to compute the gradients of the loss with respect to
    the parameters of our model so we can start the training process. Luckily, we
    don’t have to explicitly compute the gradients ourselves because PyTorch automatically
    does this for us using automatic differentiation, aka autograd. (Refer to section
    [3.1](../Text/03.xhtml#sec3_1) for more details about autograd.) For our current
    example, we can instruct PyTorch to run backpropagation and compute gradients
    by calling `loss.backward()`. With this, we’re ready to start training. PyTorch
    code for training the neural network is shown next.
  id: totrans-341
  prefs: []
  type: TYPE_NORMAL
  zh: 最后，我们需要一种方法来计算损失相对于模型参数的梯度，这样我们就可以开始训练过程。幸运的是，我们不需要显式地计算梯度，因为 PyTorch 会自动使用自动微分（autograd）为我们完成这项工作。（有关
    autograd 的更多详细信息，请参阅第 [3.1](../Text/03.xhtml#sec3_1) 节。）对于我们的当前示例，我们可以通过调用 `loss.backward()`
    来指示 PyTorch 运行反向传播并计算梯度。有了这个，我们就准备好开始训练了。训练神经网络的 PyTorch 代码如下所示。
- en: Listing 8.4 Training a neural network
  id: totrans-342
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 8.4 训练神经网络
- en: '[PRE6]'
  id: totrans-343
  prefs: []
  type: TYPE_PRE
  zh: '[PRE6]'
- en: ① Instantiates the neural network
  id: totrans-344
  prefs: []
  type: TYPE_NORMAL
  zh: ① 实例化神经网络
- en: ② Instantiates the loss function
  id: totrans-345
  prefs: []
  type: TYPE_NORMAL
  zh: ② 实例化损失函数
- en: ③ Instantiates the optimizer
  id: totrans-346
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 实例化优化器
- en: ④ Training loop
  id: totrans-347
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 训练循环
- en: ⑤ Forward pass
  id: totrans-348
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ 前向传播
- en: ⑥ Computes the loss
  id: totrans-349
  prefs: []
  type: TYPE_NORMAL
  zh: ⑥ 计算损失
- en: ⑦ Clears the gradients and prevents accumulation of gradients from the previous
    step
  id: totrans-350
  prefs: []
  type: TYPE_NORMAL
  zh: ⑦ 清除梯度并防止上一步的梯度累积
- en: ⑧ Runs backpropagation (computes gradients)
  id: totrans-351
  prefs: []
  type: TYPE_NORMAL
  zh: ⑧ 运行反向传播（计算梯度）
- en: ⑨ Updates the weights
  id: totrans-352
  prefs: []
  type: TYPE_NORMAL
  zh: ⑨ 更新权重
- en: In the training loop, we iteratively run the forward pass, compute the loss,
    calculate the gradients, and update the weights. The neural network is initialized
    with random weights and hence makes arbitrary predictions for the demand in the
    early iterations of the training loop. This translates to a high initial loss
    value. However, as training proceeds, the weights are updated to minimize the
    loss value, and the predicted demand comes closer to the actual GT. To update
    the weights, we use what is known as an *optimizer*. During training, the gradients
    are computed by calling the `backward()` function on the `loss` object. Following
    that, the `optimizer.step` call updates the weights and biases. In this example,
    we used the stochastic gradient descent–based optimizer, which can be invoked
    using `torch.optim.SGD`. PyTorch offers various optimizers, such as Adam, AdaGrad,
    and so on, which will be discussed in detail in the next chapter. We typically
    run the training loop until the loss reaches a value low enough to be acceptable.
    Once the training loop completes, we have a model that can readily take in new
    data points and generate output predictions.
  id: totrans-353
  prefs: []
  type: TYPE_NORMAL
  zh: 在训练循环中，我们迭代地运行前向传播，计算损失，计算梯度，并更新权重。神经网络以随机权重初始化，因此在训练循环的早期迭代中做出任意预测。这导致初始损失值很高。然而，随着训练的进行，权重被更新以最小化损失值，预测的需求更接近实际
    GT。为了更新权重，我们使用所谓的 *优化器*。在训练过程中，通过在 `loss` 对象上调用 `backward()` 函数来计算梯度。随后，`optimizer.step`
    调用更新权重和偏差。在这个例子中，我们使用了基于随机梯度下降的优化器，可以使用 `torch.optim.SGD` 来调用。PyTorch 提供了各种优化器，如
    Adam、AdaGrad 等，这些将在下一章中详细讨论。我们通常运行训练循环，直到损失达到足够低的值，以被接受。一旦训练循环完成，我们就拥有了一个可以轻松接受新数据点并生成输出预测的模型。
- en: Summary
  id: totrans-354
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 摘要
- en: The sigmoid function *σ*(*x*) = 1/1+*e^(–x)* has an S-shaped graph, is a differential
    version of the Heaviside step function, and, as such, is used in perceptrons.
    Thus the overall perceptron function becomes *P*(![](../../OEBPS/Images/AR_x.png))
    ≡ *σ*(![](../../OEBPS/Images/AR_w.png)*^T*![](../../OEBPS/Images/AR_x.png) + *b*).
    It is parametrized by ![](../../OEBPS/Images/AR_w.png) and *b*, which control
    the slope and position, respectively, of the S-shaped curve.
  id: totrans-355
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Sigmoid 函数 *σ*(*x*) = 1/(1+*e^(–x*)* 有一个 S 形的图像，是 Heaviside 步函数的微分形式，因此被用于感知器中。因此，感知器函数整体变为
    *P*(![](../../OEBPS/Images/AR_x.png)) ≡ *σ*(![](../../OEBPS/Images/AR_w.png)*^T*![](../../OEBPS/Images/AR_x.png)
    + *b*)。它由 ![](../../OEBPS/Images/AR_w.png) 和 *b* 参数化，分别控制 S 形曲线的斜率和位置。
- en: Neural networks solve real-life problems that require intelligence by approximating
    the function that solves the problem in question. They are built of multiple perceptrons
    interconnected by weighted edges. Instead of connecting perceptrons haphazardly,
    we connect them as layers. In a layered network, a perceptron is only connected
    to perceptrons from the immediately preceding layer. Intra-layer and other connections
    are not allowed.
  id: totrans-356
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 神经网络通过近似解决特定问题的函数来解决需要智能的现实生活中问题。它们由多个通过加权边相互连接的感知器组成。我们不是随意连接感知器，而是将它们连接成层。在分层网络中，一个感知器仅与前一层的感知器连接。不允许存在层内和其他连接。
- en: Supervised neural networks have manually generated outputs for a sample set
    of input values (ground truth). This entire data set consisting of inputs and
    known outputs is known as the training data set.
  id: totrans-357
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 监督神经网络为输入值样本集手动生成输出（真实值）。这个由输入和已知输出组成的数据集称为训练数据集。
- en: Loss is defined as the mismatch between the ground truth and actual output generated
    by the neural network on training data inputs. The simplest way to compute loss
    is to take the Euclidean distance between the neural network-generated output
    and ground-truth vectors. This is called the MSE (mean squared error) loss. Mathematically,
    given a training data set 𝕋 that is a set of <input, GT output> pairs 𝕋 = {⟨![](../../OEBPS/Images/AR_x.png),
    *ȳ*⟩}, the loss can be expressed as
  id: totrans-358
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 损失被定义为神经网络在训练数据输入上生成的真实值与实际输出之间的不匹配。计算损失的最简单方法是计算神经网络生成的输出和真实向量之间的欧几里得距离。这被称为均方误差（MSE）损失。从数学上讲，给定一个训练数据集
    𝕜，它是一组<input, GT 输出>对 𝕜 = {⟨![](../../OEBPS/Images/AR_x.png), *ȳ*⟩}，损失可以表示为
- en: '![](../../OEBPS/Images/eq_08-35-a.png)'
  id: totrans-359
  prefs: []
  type: TYPE_IMG
  zh: '![公式](../../OEBPS/Images/eq_08-35-a.png)'
- en: where the output is ![](../../OEBPS/Images/AR_y.png)*[i]* = *MLP*(![](../../OEBPS/Images/AR_x.png)*[i]*).
  id: totrans-360
  prefs: []
  type: TYPE_NORMAL
  zh: 其中输出为![公式](../../OEBPS/Images/AR_y.png)*[i]* = *MLP*(![](../../OEBPS/Images/AR_x.png)*[i]*).
- en: Training is the process of optimizing the connection weights and biases of a
    specific neural network so that the loss is minimal. Note that during inferencing,
    the neural network typically sees data it has never seen during training. Inferencing
    outputs are good only if the distribution of training inputs roughly matches the
    overall input distribution.
  id: totrans-361
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 训练是优化特定神经网络连接权重和偏置的过程，以使损失最小。注意，在推理过程中，神经网络通常看到它在训练期间从未见过的数据。推理输出只有在训练输入的分布大致匹配整体输入分布时才是好的。
- en: We minimize the loss by iteratively adjusting the weights and biases. The quickest
    way to reach the closest minimum of a multivariate function is to follow the gradient.
    Hence, we adjust the weights and biases following the gradient of the loss function.
    Mathematically,
  id: totrans-362
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 我们通过迭代调整权重和偏置来最小化损失。到达多元函数最近最小值的最快方式是遵循梯度。因此，我们根据损失函数的梯度调整权重和偏置。从数学上讲，
- en: '![](../../OEBPS/Images/eq_08-35-b.png)'
  id: totrans-363
  prefs: []
  type: TYPE_IMG
  zh: '![公式](../../OEBPS/Images/eq_08-35-b.png)'
- en: 'A forward pass is the process of generating outputs from inputs with a neural
    network: more specifically, a multilayer perceptron MLP). Thus an MLP does inferencing
    via a forward pass. A beautiful property of a layered network is that we can do
    a forward pass dealing with one layer at a time, proceeding iteratively from layer
    0 (closest to the input) to the output layer. Mathematically,'
  id: totrans-364
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 前向传播是使用神经网络从输入生成输出的过程：更具体地说，是一个多层感知器（MLP）。因此，MLP通过前向传播进行推理。分层网络的一个美丽特性是我们可以一次处理一层，从层0（最接近输入层）迭代到输出层。从数学上讲，
- en: '![](../../OEBPS/Images/eq_08-35-c.png)'
  id: totrans-365
  prefs: []
  type: TYPE_IMG
  zh: '![公式](../../OEBPS/Images/eq_08-35-c.png)'
- en: where *W*^((*l*)), ![](../../OEBPS/Images/AR_b.png)^((*l*)) represent the weights
    and biases for layer *l*, and ![](../../OEBPS/Images/AR_a.png)^((*l*)) represent
    the output for layer *l* activation), which is also the input for layer *l* +
    1.
  id: totrans-366
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *W*^((*l*)), ![](../../OEBPS/Images/AR_b.png)^((*l*)) 代表层 *l* 的权重和偏置，而 ![](../../OEBPS/Images/AR_a.png)^((*l*))
    代表层 *l* 的激活输出，这同时也是层 *l* + 1 的输入。
- en: A backward pass is the process by which the gradients of the loss with respect
    to all the weights and biases are generated. It relies on the result of the preceding
    forward pass and proceeds from the output layer toward the input layer. It uses
    auxiliary variables ![](../../OEBPS/Images/AR_bw.png)^((*l*)), which can be computed
    by iterating backward from the last (closest to output) layer to the first (closest
    to input) layer—hence the name *backward propagation*—and all the required gradients
    can be computed from those auxiliary variables. Mathematically,
  id: totrans-367
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 反向传递是一个生成所有权重和偏置相对于损失函数梯度的过程。它依赖于前一个正向传递的结果，并从输出层向输入层进行。它使用辅助变量 ![](../../OEBPS/Images/AR_bw.png)^((*l*))，这些变量可以通过从最后一个（最接近输出层）层反向迭代到第一个（最接近输入层）层来计算——因此得名*反向传播*——并且所有所需的梯度都可以从这些辅助变量中计算出来。数学上，
- en: '![](../../OEBPS/Images/eq_08-35-d.png)'
  id: totrans-368
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_08-35-d.png)'
- en: Training progresses by alternating forward and backward passes on the training
    data set.
  id: totrans-369
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 训练过程通过在训练数据集上交替进行正向和反向传递来推进。
