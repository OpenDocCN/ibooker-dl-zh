- en: 9 Loss, optimization, and regularization
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 9 损失、优化和正则化
- en: This chapter covers
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 本章涵盖了
- en: Geometrical and algebraic introductions to loss functions
  id: totrans-2
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 损失函数的几何和代数介绍
- en: Geometrical intuitions for softmax
  id: totrans-3
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: softmax的几何直觉
- en: Optimization techniques including momentum, Nesterov, AdaGrad, Adam, and SGD
  id: totrans-4
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 包括动量、Nesterov、AdaGrad、Adam和SGD在内的优化技术
- en: Regularization and its relationship to Bayesian approaches
  id: totrans-5
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 正则化及其与贝叶斯方法的关系
- en: Overfitting while training, and dropout
  id: totrans-6
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 训练过程中的过拟合和dropout
- en: By now, it should be etched in your mind that neural networks are essentially
    function approximators. In particular, neural network classifiers model the decision
    boundaries between the classes in the feature space (a space where every input
    feature combination is a specific point). Supervised classifiers mark sample training
    data inputs in this space with a—perhaps manually generated—class label (ground
    truth). The training process iteratively learns a function that essentially creates
    decision boundaries separating the sampled training data points into individual
    classes. If the training data set is a reasonable representative of the true distribution
    of possible inputs, the network (the learned function that models the class boundaries)
    will classify never-before-seen inputs with good accuracy.
  id: totrans-7
  prefs: []
  type: TYPE_NORMAL
  zh: 到现在为止，你应该已经深刻地认识到神经网络本质上是一种函数逼近器。特别是，神经网络分类器在特征空间（每个输入特征组合都是一个特定点的空间）中模拟了类之间的决策边界。监督分类器在这个空间中标记样本训练数据输入，并带有——可能是手动生成的——类标签（真实标签）。训练过程迭代地学习一个函数，这个函数本质上创建了决策边界，将样本训练数据点分离成单个类别。如果训练数据集是可能输入的真实分布的合理代表，那么网络（模型类边界的学到的函数）将以良好的准确性对从未见过的输入进行分类。
- en: When we select a specific neural network architecture (with a fixed set of layers,
    each with a fixed set of perceptrons with specific connections), we have essentially
    frozen the *family* of functions we use as a function approximator. We still have
    to “learn” the exact weights of the connectors between various *perceptrons* aka
    *neurons*). The training process iteratively sets these weights so as to best
    classify the training data points. To do this, we design a loss function that
    measures the departure of the network output from the desired result. The network
    continually tries to minimize this loss. There are a variety of loss functions
    to choose from.
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
  zh: 当我们选择一个特定的神经网络架构（具有固定的一组层，每个层具有固定的一组具有特定连接的感知器）时，我们本质上冻结了我们用作函数逼近器的*函数族*。我们仍然需要“学习”各种*感知器*（也称为*神经元*）之间的连接器的确切权重。训练过程迭代地设置这些权重，以便最好地分类训练数据点。为此，我们设计一个损失函数来衡量网络输出与期望结果之间的偏差。网络持续尝试最小化这个损失。有各种各样的损失函数可供选择。
- en: The iterative process through which loss is minimized is called *optimization*.
    We also have a multitude of optimization algorithms to choose from. In this chapter,
    we study loss functions, optimization algorithms, and associated topics like L1
    and L2 regularization and dropout. We also learn about overfitting, a potential
    pitfall to avoid while training a neural network.
  id: totrans-9
  prefs: []
  type: TYPE_NORMAL
  zh: 通过迭代最小化损失的过程称为*优化*。我们还有许多优化算法可供选择。在本章中，我们研究损失函数、优化算法以及相关的主题，如L1和L2正则化和dropout。我们还了解过拟合，这是在训练神经网络时需要避免的一个潜在陷阱。
- en: NOTE The complete PyTorch code for this chapter is available at [http://mng.bz/aZv9](http://mng.bz/aZv9)
    in the form of fully functional and executable Jupyter notebooks.
  id: totrans-10
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：本章的完整PyTorch代码以完全功能性和可执行的Jupyter笔记本形式，可在[http://mng.bz/aZv9](http://mng.bz/aZv9)找到。
- en: 9.1 Loss functions
  id: totrans-11
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 9.1 损失函数
- en: A loss function essentially measures the badness of the neural network output.
    In the case of a supervised network, the loss for an individual training data
    instance is the distance of the actual output of the neural network (aka prediction)
    from the desired ideal outputs known or manually labeled ground truth [GT]) on
    that particular training input instance. Total training loss is obtained by summing
    the losses from all training data instances. Training is essentially an iterative
    optimization process that minimizes the total training loss.
  id: totrans-12
  prefs: []
  type: TYPE_NORMAL
  zh: 损失函数本质上衡量了神经网络输出的不良程度。在监督网络的情况下，单个训练数据实例的损失是神经网络的实际输出（也称为预测）与特定训练输入实例上已知的或手动标记的理想输出（真实标签[GT]）之间的距离。总训练损失是通过将所有训练数据实例的损失相加得到的。训练本质上是一个迭代优化过程，该过程最小化总训练损失。
- en: 9.1.1 Quantification and geometrical view of loss
  id: totrans-13
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.1 损失的量化与几何视角
- en: Loss surfaces and their minimization are described in detail in section [8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc).
    Here we only do a quick review.
  id: totrans-14
  prefs: []
  type: TYPE_NORMAL
  zh: 损失表面及其最小化在[8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc)节中详细描述。这里我们只做简要回顾。
- en: A full neural network can be described by the equation
  id: totrans-15
  prefs: []
  type: TYPE_NORMAL
  zh: 一个完整的神经网络可以用以下方程描述
- en: '![](../../OEBPS/Images/eq_09-01.png)'
  id: totrans-16
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-01.png)'
- en: Equation 9.1
  id: totrans-17
  prefs: []
  type: TYPE_NORMAL
  zh: 方程9.1
- en: Equation [9.1](#eq-nn-forward-pass-overall) says that given an input ![](../../OEBPS/Images/AR_x.png),
    the neural network with weights ![](../../OEBPS/Images/AR_w.png) and biases ![](../../OEBPS/Images/AR_b.png)
    emits the *output vector* or *prediction vector* ![](../../OEBPS/Images/AR_y.png).
    The weights and biases may be organized into layers; this equation does not care.
    The vectors ![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png),
    respectively, denote the sets of *all* weights and biases from all layers aggregated.
    Evaluating the function *f*(⋅) is equivalent to performing one forward pass on
    the network. In particular, given a training input instance ![](../../OEBPS/Images/AR_x.png)^((*i*)),
    the neural network emits ![](../../OEBPS/Images/AR_y.png)^((*i*)) = *f*(![](../../OEBPS/Images/AR_x.png)^((*i*))|![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png)). We refer to ![](../../OEBPS/Images/AR_y.png)^((*i*))
    as the output of the *i*th training data instance.
  id: totrans-18
  prefs: []
  type: TYPE_NORMAL
  zh: 方程[9.1](#eq-nn-forward-pass-overall)表明，给定一个输入 ![](../../OEBPS/Images/AR_x.png)，权重为
    ![](../../OEBPS/Images/AR_w.png) 和偏置为 ![](../../OEBPS/Images/AR_b.png) 的神经网络会发出*输出向量*或*预测向量*
    ![](../../OEBPS/Images/AR_y.png)。权重和偏置可以组织成层；这个方程并不关心这一点。向量 ![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png) 分别表示从所有层聚合的所有权重和偏置的集合。评估函数 *f*(⋅) 等同于在网络中进行一次前向传递。特别是，给定一个训练输入实例
    ![](../../OEBPS/Images/AR_x.png)^((*i*))，神经网络发出 ![](../../OEBPS/Images/AR_y.png)^((*i*))
    = *f*(![](../../OEBPS/Images/AR_x.png)^((*i*))|![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png))。我们将 ![](../../OEBPS/Images/AR_y.png)^((*i*))
    称为第 *i* 个训练数据实例的输出。
- en: During supervised training, for each training input instance ![](../../OEBPS/Images/AR_x.png)^((*i*)),
    we have the GT (the known output), *ȳ*^((*i*)). We refer to *ȳ*^((*i*)) as the
    *GT vector* (as usual, we use superscript indices for training data instances).
  id: totrans-19
  prefs: []
  type: TYPE_NORMAL
  zh: 在监督训练期间，对于每个训练输入实例 ![](../../OEBPS/Images/AR_x.png)^((*i*))，我们都有GT（已知的输出），*ȳ*^((*i*)）。我们将
    *ȳ*^((*i*)）称为*GT向量*（如通常所做，我们使用上标索引表示训练数据实例）。
- en: 'Ideally, the output vector ![](../../OEBPS/Images/AR_y.png)^((*i*)) should
    match the GT vector *ȳ*^((*i*)). The mismatch between them is the loss for that
    training data instance 𝕃^((*i*))(![](../../OEBPS/Images/AR_y.png)^((*i*)), *ȳ*^((*i*))|![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png)), which we sometimes denote as 𝕃^((*i*))(![](../../OEBPS/Images/AR_y.png)^((*i*)),
    *ȳ*^((*i*))). The overall training loss (to be minimized by the optimization process)
    is the sum of losses over all training data instances:'
  id: totrans-20
  prefs: []
  type: TYPE_NORMAL
  zh: 理想情况下，输出向量 ![](../../OEBPS/Images/AR_y.png)^((*i*)) 应该与GT向量 *ȳ*^((*i*)）相匹配。它们之间的不匹配是针对该训练数据实例
    𝕃^((*i*))(![](../../OEBPS/Images/AR_y.png)^((*i*)), *ȳ*^((*i*))) 的损失，我们有时将其表示为
    𝕃^((*i*))(![](../../OEBPS/Images/AR_y.png)^((*i*)), *ȳ*^((*i*))). 所有训练数据实例的总体训练损失（由优化过程最小化）是所有训练数据实例损失的加和：
- en: '![](../../OEBPS/Images/eq_09-02.png)'
  id: totrans-21
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-02.png)'
- en: Equation 9.2
  id: totrans-22
  prefs: []
  type: TYPE_NORMAL
  zh: 方程9.2
- en: where the summation is over all training data instances, and *n* is the size
    of the training data set. Note that this summation over all training data points
    is needed to compute the loss for each training data instance. Thus an *epoch*,
    a single training loop over all training data instances, costs *O*(*n*²), where
    *n* is the number of training data points. Training usually requires many epochs.
    This makes the training process very expensive. In section [9.2.2](../Text/09.xhtml#sec-SGD),
    we study ways of mitigating this.
  id: totrans-23
  prefs: []
  type: TYPE_NORMAL
  zh: 其中求和是针对所有训练数据实例的，*n* 是训练数据集的大小。请注意，对所有训练数据点的这种求和是计算每个训练数据实例损失所必需的。因此，一个*epoch*，即对所有训练数据实例的单次训练循环，需要
    *O*(*n*²) 的时间复杂度，其中 *n* 是训练数据点的数量。训练通常需要许多个epoch。这使得训练过程非常昂贵。在[9.2.2](../Text/09.xhtml#sec-SGD)节中，我们研究减轻这种影响的方法。
- en: '![](../../OEBPS/Images/CH09_F01_Chaudhury.png)'
  id: totrans-24
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH09_F01_Chaudhury.png)'
- en: Figure 9.1 The loss surface can be viewed as a canyon.
  id: totrans-25
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.1 损失表面可以看作是一个峡谷。
- en: NOTE In this chapter, we use *n* to indicate the number of training data points
    and *N* to indicate the dimensionality of the output vector. For classifiers,
    the dimensionality of the output vector, *N*, matches the number of classes. We
    also use superscript (*i*) to index training data points and subscript *j* to
    index output vector dimensions. For classifiers, *j* indicates the class.
  id: totrans-26
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：在本章中，我们用*n*表示训练数据点的数量，用*N*表示输出向量的维度。对于分类器，输出向量的维度*N*与类别的数量相匹配。我们还使用上标(*i*)来索引训练数据点，下标*j*来索引输出向量维度。对于分类器，*j*表示类别。
- en: We can visualize 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    as a hypersurface in high-dimensional space. Figures [8.8](../Text/08.xhtml#fig-gradient-descent-3d)
    and [8.9](../Text/08.xhtml#fig-non-convex-local-minima) show some low-dimensional
    examples of loss surfaces. These are illustrative examples. In reality, the loss
    surface is typically high dimensional and very complex. One good mental picture
    is that of a canyon (see figure [9.1](#fig-grand-canyon)). Traveling “downhill”
    at any point effectively follows the negative direction of the local gradient
    the gradient of a loss surface was introduced in section [8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc)).
    Traveling downhill along the gradient does not always lead to the global minimum.
    For instance, going downhill following the dashed arrow will take us to a local
    minimum, whereas the global minimum is where the water is going, indicated by
    the solid arrow. (See also section [8.4.4](../Text/08.xhtml#sec-graddesc_local_minima)
    and figure [8.9](../Text/08.xhtml#fig-non-convex-local-minima).)
  id: totrans-27
  prefs: []
  type: TYPE_NORMAL
  zh: 我们可以将 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    视为高维空间中的超曲面。图[8.8](../Text/08.xhtml#fig-gradient-descent-3d)和[8.9](../Text/08.xhtml#fig-non-convex-local-minima)展示了损失表面的低维示例。这些都是说明性的例子。实际上，损失表面通常是高维且非常复杂的。一个很好的心理图像是峡谷（见图[9.1](#fig-grand-canyon)）。在任何一点“向下走”实际上遵循局部梯度的负方向，即损失表面的梯度在[8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc)节中引入。沿着梯度向下走并不总是导致全局最小值。例如，沿着虚线箭头向下走将带我们到一个局部最小值，而全局最小值是水流的方向，由实线箭头指示。（也参见[8.4.4](../Text/08.xhtml#sec-graddesc_local_minima)节和图[8.9](../Text/08.xhtml#fig-non-convex-local-minima)。）
- en: Many loss formulations are possible, quantifying the mismatch 𝕃^((*i*))(![](../../OEBPS/Images/AR_y.png)^((*i*)),
    *ȳ*^((*i*))); some of them are described in the following subsections.
  id: totrans-28
  prefs: []
  type: TYPE_NORMAL
  zh: 许多损失公式都是可能的，量化了不匹配 𝕃^((*i*))(![](../../OEBPS/Images/AR_y.png)^((*i*)), *ȳ*^((*i*)));
    其中一些将在以下小节中描述。
- en: 9.1.2 Regression loss
  id: totrans-29
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.2 回归损失
- en: 'Regression loss is the simplest loss formulation. It is the L2 norm of the
    difference between the output and GT vectors. This loss was introduced in equation
    [8.11](../Text/08.xhtml#eq-mse-loss). We restate it here: the loss on the *i*th
    training data instance is'
  id: totrans-30
  prefs: []
  type: TYPE_NORMAL
  zh: 回归损失是最简单的损失公式。它是输出向量和GT向量之间差异的L2范数。这个损失在方程[8.11](../Text/08.xhtml#eq-mse-loss)中引入。我们在此重申：第*i*个训练数据实例的损失是
- en: '![](../../OEBPS/Images/eq_09-02-a.png)'
  id: totrans-31
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-02-a.png)'
- en: where the summation is over the components of the output vector. *N* is the
    number of classes. The GT vector and output vector are both *N*-dimensional.
  id: totrans-32
  prefs: []
  type: TYPE_NORMAL
  zh: 其中求和是对输出向量的分量进行的。*N*是类别的数量。GT向量和输出向量都是*N*-维的。
- en: NOTE Fully functional code for regression loss, executable via Jupyter Notebook,
    can be found at [http://mng.bz/g1a8](http://mng.bz/g1a8).
  id: totrans-33
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：完整的回归损失功能代码，可通过Jupyter Notebook执行，可在[http://mng.bz/g1a8](http://mng.bz/g1a8)找到。
- en: Listing 9.1 PyTorch code for regression loss
  id: totrans-34
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.1 PyTorch代码用于回归损失
- en: '[PRE0]'
  id: totrans-35
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: ① Imports the regression loss (mean squared error loss)
  id: totrans-36
  prefs: []
  type: TYPE_NORMAL
  zh: ① 导入回归损失（均方误差损失）
- en: ② N-d prediction vector
  id: totrans-37
  prefs: []
  type: TYPE_NORMAL
  zh: ② N维预测向量
- en: ③ N-d ground truth vector
  id: totrans-38
  prefs: []
  type: TYPE_NORMAL
  zh: ③ N维真实向量
- en: ④ Computes the regression loss
  id: totrans-39
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 计算回归损失
- en: 9.1.3 Cross-entropy loss
  id: totrans-40
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.3 交叉熵损失
- en: Cross-entropy loss was discussed in the context of entropy in section [6.3](../Text/06.xhtml#sec-cross-entropy).
    If necessary, this would be a great time to reread that. Here we review the idea
    quickly.
  id: totrans-41
  prefs: []
  type: TYPE_NORMAL
  zh: 交叉熵损失在[6.3](../Text/06.xhtml#sec-cross-entropy)节中讨论了熵的上下文。如果需要，现在重新阅读那部分是个好时机。在这里，我们快速回顾一下这个想法。
- en: Cross-entropy loss is typically used to measure the mismatch between a classifier
    neural network output and the corresponding GT in a classification problem. Here,
    the GT is a one-hot vector whose length equals the number of classes. All but
    one of its elements are 0\. The single nonzero element is 1, and it occurs at
    the index corresponding to the correct class for that training data instance.
  id: totrans-42
  prefs: []
  type: TYPE_NORMAL
  zh: 交叉熵损失通常用于衡量分类问题中分类器神经网络输出与相应 GT 之间的不匹配。在这里，GT 是一个长度等于类数的 one-hot 向量。除了一个元素外，其余元素都是
    0。唯一的非零元素是 1，它出现在对应于该训练数据实例正确类别的索引处。
- en: Thus the GT vector looks like *ȳ*^((*i*)) = [0,…,0,1,0,…,0]. The prediction
    vector should have elements with values between 0 and 1. Each element of the prediction
    vector ![](../../OEBPS/Images/AR_y.png)^((*i*)) indicates the probability of a
    specific class. In other words, ![](../../OEBPS/Images/AR_y.png)^((*i*)) = [*p*[0],
    *p*[1],…, *p*[*N*−1]], where *p[j]* is the probability of the input *i* belonging
    to the *j*th class. In section [6.3](../Text/06.xhtml#sec-cross-entropy), we illustrated
    with an example image classifier that predicts whether an image contains a cat
    class 0), a dog (class 1), an airplane (class 2), or an automobile (class 3).
    One of the four is always assumed to be present in the image. If, for the *i*th
    training data instance, the GT vector is an image of cat, we have *ȳ*^((*i*))
    = [1,0,0,0]. A prediction vector ![](../../OEBPS/Images/AR_y.png)^((*i*)) = [0.8,0.15,0.04,0.01]
    is good, while ![](../../OEBPS/Images/AR_y.png)^((*i*)) = [0.25,0.25,0.25,0.25]
    is bad. Note that sum of the elements of the GT as well as the prediction vector
    is always 1 since they are probabilities. Mathematically, given a training dataset
    *X*,
  id: totrans-43
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，GT 向量看起来像 *ȳ*^((*i*)) = [0,…,0,1,0,…,0]。预测向量应该具有介于 0 和 1 之间的值。预测向量 ![](../../OEBPS/Images/AR_y.png)^((*i*))
    的每个元素表示特定类的概率。换句话说，![](../../OEBPS/Images/AR_y.png)^((*i*)) = [*p*[0], *p*[1],…,
    *p*[*N*−1]]，其中 *p[j]* 是输入 *i* 属于第 *j* 类的概率。在第 [6.3](../Text/06.xhtml#sec-cross-entropy)
    节中，我们用一个示例图像分类器来说明，该分类器预测图像是否包含猫（类别 0）、狗（类别 1）、飞机（类别 2）或汽车（类别 3）。四个类别中总有一个假设存在于图像中。如果对于第
    *i* 个训练数据实例，GT 向量是一张猫的图片，那么我们就有 *ȳ*^((*i*)) = [1,0,0,0]。预测向量 ![](../../OEBPS/Images/AR_y.png)^((*i*))
    = [0.8,0.15,0.04,0.01] 是好的，而 ![](../../OEBPS/Images/AR_y.png)^((*i*)) = [0.25,0.25,0.25,0.25]
    是不好的。请注意，GT 以及预测向量的元素之和总是 1，因为它们是概率。从数学上讲，给定一个训练数据集 *X*，
- en: '![](../../OEBPS/Images/eq_09-02-b.png)'
  id: totrans-44
  prefs: []
  type: TYPE_IMG
  zh: '![交叉熵损失公式](../../OEBPS/Images/eq_09-02-b.png)'
- en: Given such GT and prediction vectors, the cross-entropy loss (CE loss) is
  id: totrans-45
  prefs: []
  type: TYPE_NORMAL
  zh: 给定这样的真实标签 GT 和预测向量，交叉熵损失（CE 损失）是
- en: '![](../../OEBPS/Images/eq_09-03.png)'
  id: totrans-46
  prefs: []
  type: TYPE_IMG
  zh: '![交叉熵损失公式](../../OEBPS/Images/eq_09-03.png)'
- en: Equation 9.3
  id: totrans-47
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.3
- en: where the summation is over the elements of the prediction vector and *N* is
    the number of classes.
  id: totrans-48
  prefs: []
  type: TYPE_NORMAL
  zh: 其中求和是对预测向量的元素进行的，*N* 是类数。
- en: Intuitions behind cross-entropy loss
  id: totrans-49
  prefs: []
  type: TYPE_NORMAL
  zh: 交叉熵损失背后的直觉
- en: Note that only one element—the one corresponding to the GT class—survives in
    the summation of equation [9.3](#eq-ce-loss). The other elements vanish because
    they are multiplied by the 0 GT value. The (logarithm of) the predicted probability
    of the correct GT class is multiplied by 1. Hence, the CE loss always boils down
    to −*log*(*y*[*j*^*]^((*i*))), where *j*^* is the GT class. If this probability
    is 1, the CE loss becomes 0, rightly so, as the correct class is being predicted
    with a probability of 1. If the predicted probability of the correct class is
    0, the CE loss is −*log*(0) = ∞, again rightly so, since this is the worst possible
    prediction. The closer the prediction for the correct class is to 1, the smaller
    the loss.
  id: totrans-50
  prefs: []
  type: TYPE_NORMAL
  zh: 注意，方程 [9.3](#eq-ce-loss) 的求和中只保留了一个元素——对应于 GT 类别的元素。其他元素消失，因为它们被乘以 0 的 GT 值。正确的
    GT 类别的预测概率（对数）乘以 1。因此，CE 损失始终简化为 −*log*(*y*[*j*^*]^((*i*)))，其中 *j*^* 是 GT 类别。如果这个概率是
    1，那么 CE 损失变为 0，这是正确的，因为以 1 的概率预测了正确的类别。如果正确类别的预测概率是 0，那么 CE 损失是 −*log*(0) = ∞，这也是正确的，因为这是最糟糕的预测。预测对正确类别的预测越接近
    1，损失就越小。
- en: NOTE Fully functional code for cross-entropy loss, executable via Jupyter Notebook,
    can be found at [http://mng.bz/g1a8](http://mng.bz/g1a8).
  id: totrans-51
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：完整的交叉熵损失代码，可通过 Jupyter Notebook 执行，可以在 [http://mng.bz/g1a8](http://mng.bz/g1a8)
    找到。
- en: Listing 9.2 PyTorch code for cross-entropy loss
  id: totrans-52
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 9.2 PyTorch 交叉熵损失代码
- en: '[PRE1]'
  id: totrans-53
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: ① N-d prediction vector
  id: totrans-54
  prefs: []
  type: TYPE_NORMAL
  zh: ① N 维预测向量
- en: ② N-d one-hot ground truth vector
  id: totrans-55
  prefs: []
  type: TYPE_NORMAL
  zh: ② N 维 one-hot 真实标签向量
- en: ③ Computes the cross-entropy loss
  id: totrans-56
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 计算交叉熵损失
- en: Special case of two classes
  id: totrans-57
  prefs: []
  type: TYPE_NORMAL
  zh: 两个类别的特殊情况
- en: 'What happens if *N* = 2 (that is, we have only two classes)? Let’s denote the
    predicted probability of class 0, for the *i*th training input, as *y*^((*i*)):
    that is, ![](../../OEBPS/Images/AR_y.png)[0]^((*i*)) = *y*^((*i*)). Then, since
    these are probabilities, the prediction on the other class ![](../../OEBPS/Images/AR_y.png)[1]^((*i*))
    = 1 − *y*^((*i*)). Also, let *ȳ*^((*i*)) denote the GT probability for class 0
    on this *i*th training input. Then 1 − *ȳ*^((*i*)) is the GT probability for class
    1. (We have slightly abused notations—up to this point, *ȳ* has denoted a vector,
    but here it denotes a scalar.)'
  id: totrans-58
  prefs: []
  type: TYPE_NORMAL
  zh: 如果*N* = 2（即我们只有两个类别）会发生什么？让我们用*y*^((*i*))表示第*i*个训练输入的类别0的预测概率：即![](../../OEBPS/Images/AR_y.png)[0]^((*i*))
    = *y*^((*i*)）。由于这些是概率，对另一个类别的预测![](../../OEBPS/Images/AR_y.png)[1]^((*i*)) = 1
    − *y*^((*i*)）。此外，用*ȳ*^((*i*))表示第*i*个训练输入的类别0的GT概率。那么1 − *ȳ*^((*i*))是类别1的GT概率。（我们稍微滥用了符号——到目前为止，*ȳ*表示一个向量，但在这里它表示一个标量。）
- en: Then, following equation [9.3](#eq-ce-loss), the CE loss on the *i*th training
    data instance becomes
  id: totrans-59
  prefs: []
  type: TYPE_NORMAL
  zh: 然后，根据方程式[9.3](#eq-ce-loss)，第*i*个训练数据实例的CE损失变为
- en: 𝕃^((*i*))(*y*^((*i*)), *ȳ*^((*i*))) = −*ȳ*^((*i*)) *log*(*y*^((*i*))) − (1−*ȳ*^((*i*)))
    *log*(1−*y*^((*i*)))
  id: totrans-60
  prefs: []
  type: TYPE_NORMAL
  zh: 𝕃^((*i*))(*y*^((*i*)), *ȳ*^((*i*))) = −*ȳ*^((*i*)) *log*(*y*^((*i*))) − (1−*ȳ*^((*i*)))
    *log*(1−*y*^((*i*)))
- en: Equation 9.4
  id: totrans-61
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式9.4
- en: NOTE Fully functional code for binary cross-entropy loss, executable via Jupyter
    Notebook, can be found at [http://mng.bz/g1a8](http://mng.bz/g1a8).
  id: totrans-62
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：二进制交叉熵损失的完整代码，可通过 Jupyter Notebook 执行，可在[http://mng.bz/g1a8](http://mng.bz/g1a8)找到。
- en: Listing 9.3 PyTorch code for binary cross-entropy loss
  id: totrans-63
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.3 PyTorch代码用于二进制交叉熵损失
- en: '[PRE2]'
  id: totrans-64
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: ① Imports the binary cross-entropy loss
  id: totrans-65
  prefs: []
  type: TYPE_NORMAL
  zh: ① 导入二进制交叉熵损失
- en: ② Outputs the probability of class 0 - *y*[0]. A single value is sufficient
    because *y*[1] = 1 − *y*[0].
  id: totrans-66
  prefs: []
  type: TYPE_NORMAL
  zh: ② 输出类别0的概率 - *y*[0]。单个值就足够了，因为 *y*[1] = 1 − *y*[0]。
- en: ③ The ground truth is either 0 or 1.
  id: totrans-67
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 真实值是0或1。
- en: ④ Computes the cross-entropy loss
  id: totrans-68
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 计算交叉熵损失
- en: 9.1.4 Binary cross-entropy loss for image and vector mismatches
  id: totrans-69
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.4 图像和向量不匹配的二进制交叉熵损失
- en: Given a pair of normalized tensors (such as images or vectors) whose elements
    all have values between 0 and 1, a variant of the two-class CE loss can be used
    to estimate the mismatch between the tensors. Note that an image with pixel-intensity
    values between 0 and 255 can always be normalized by dividing each pixel-intensity
    value by 255, thereby converting it to the 0 to 1 range. Such a comparison of
    two images is used in image autoencoders, for example. We study autoencoders later;
    here, we provide a brief overview in the following sidebar.
  id: totrans-70
  prefs: []
  type: TYPE_NORMAL
  zh: 给定一对归一化的张量（例如图像或向量），其元素值均在0到1之间，可以使用两分类CE损失的一个变体来估计张量之间的不匹配。请注意，像素强度值在0到255之间的图像可以通过将每个像素强度值除以255来归一化，从而将其转换为0到1的范围。这种两种图像的比较在图像自编码器中得到了应用，例如。我们稍后研究自编码器；在这里，我们将在以下侧边栏中提供简要概述。
- en: Autoencoders
  id: totrans-71
  prefs: []
  type: TYPE_NORMAL
  zh: 自编码器
- en: 'Autoencoders take an image as input, create a low-dimensional descriptor from
    the image—this descriptor is often referred to as an *embedding* of the image—and
    try to reconstruct the input image from the embedding. The image embedding is
    a compressed representation of the image. Reconstruction is a lossy process: the
    small, subtle variations in the signal are lost, and only the essential part is
    retained. The loss is the mismatch between the input image and the reconstructed
    image. By minimizing this loss, we incentivize the system to retain the essence
    of the input as much as possible within the embedding-size budget.'
  id: totrans-72
  prefs: []
  type: TYPE_NORMAL
  zh: 自编码器以图像为输入，从图像中创建一个低维描述符——这个描述符通常被称为图像的*嵌入*——并尝试从嵌入中重建输入图像。图像嵌入是图像的压缩表示。重建是一个有损过程：信号中的微小、细微的变化会丢失，只保留基本部分。损失是输入图像和重建图像之间的不匹配。通过最小化这个损失，我们激励系统在嵌入大小预算内尽可能多地保留输入的本质。
- en: Let *ȳ* denote the input image. Let ![](../../OEBPS/Images/AR_y.png)^((*i*))
    denote the reconstructed image outputted by the autoencoder. The binary cross-entropy
    loss is defined as
  id: totrans-73
  prefs: []
  type: TYPE_NORMAL
  zh: 用*ȳ*表示输入图像。用![](../../OEBPS/Images/AR_y.png)^((*i*))表示由自编码器输出的重建图像。二进制交叉熵损失定义为
- en: '![](../../OEBPS/Images/eq_09-05.png)'
  id: totrans-74
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-05.png)'
- en: Equation 9.5
  id: totrans-75
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式9.5
- en: Note that here *N* is the number of pixels in the image, not the number of classes,
    as before. The summation is over the pixels in the image. Also, the GT vector
    *ȳ*^((*i*)) is not a one-hot vector; rather, it is the input image. These differences
    aside, equation [9.5](#eq-pixel-ce-loss) is based on the same idea as equation
    [9.4](#eq-binary-ce-loss).
  id: totrans-76
  prefs: []
  type: TYPE_NORMAL
  zh: 注意，在这里 *N* 是图像中的像素数，而不是之前的类别数。求和是针对图像中的像素。此外，GT 向量 *ȳ*^((*i*)) 不是一个 one-hot
    向量；而是输入图像。尽管有这些差异，方程 [9.5](#eq-pixel-ce-loss) 的思想与方程 [9.4](#eq-binary-ce-loss)
    相同。
- en: Why does it work?
  id: totrans-77
  prefs: []
  type: TYPE_NORMAL
  zh: 为什么它有效？
- en: Binary cross-entropy loss attains its minimum when the input matches the GT.
    We outline the proof next. (Note that we drop the superscripts and subscripts
    for simplicity.) We have
  id: totrans-78
  prefs: []
  type: TYPE_NORMAL
  zh: 当输入与 GT 匹配时，二元交叉熵损失达到最小值。我们将在下面概述证明。（注意，为了简化，我们省略了上标和下标。）我们有
- en: −𝕃 = *ȳlog*(*y*) + (1 − *ȳ*)*log*(1 − *y*)
  id: totrans-79
  prefs: []
  type: TYPE_NORMAL
  zh: −𝕃 = *ȳlog*(*y*) + (1 − *ȳ*)*log*(1 − *y*)
- en: At the minimum, *∂*𝕃**/***∂y* = 0.
  id: totrans-80
  prefs: []
  type: TYPE_NORMAL
  zh: 在最小值处，*∂*𝕃**/***∂y* = 0。
- en: '![](../../OEBPS/Images/eq_09-05-a.png)'
  id: totrans-81
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-05-a.png)'
- en: Thus, the minimum of the binary cross-entropy loss occurs when the network output
    matches the GT. However, this does not mean this loss becomes zero when the output
    matches the GT.
  id: totrans-82
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，二元交叉熵损失的最小值发生在网络输出与 GT 匹配时。但这并不意味着当输出与 GT 匹配时，这种损失变为零。
- en: NOTE Binary cross-entropy loss is not necessarily zero even in the ideal case
    of the output matching the input (although the loss is indeed *minimal* in the
    ideal case, meaning the loss is higher for non-ideal cases with mismatched input
    and output).
  id: totrans-83
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：即使在输出与输入匹配的理想情况下，二元交叉熵损失也不一定是零（尽管在理想情况下，损失确实是 *最小* 的，这意味着对于输入和输出不匹配的非理想情况，损失更高）。
- en: Examining equation [9.5](#eq-pixel-ce-loss), when the two inputs match, we have
    −𝕃(*ȳ*, ![](../../OEBPS/Images/AR_y.png))|[![](../../OEBPS/Images/AR_y.png) =
    *ȳ*]. We intuitively expect this loss to be zero since the output is ideal. But
    it isn’t. For example, if, for *ȳ[j]*^((*i*)) = *y[j]*^((*i*)) = 0.25
  id: totrans-84
  prefs: []
  type: TYPE_NORMAL
  zh: 检查方程 [9.5](#eq-pixel-ce-loss)，当两个输入匹配时，我们有 −𝕃(*ȳ*, ![](../../OEBPS/Images/AR_y.png))|[![](../../OEBPS/Images/AR_y.png)
    = *ȳ*]。我们直观地期望这种损失为零，因为输出是理想的。但事实并非如此。例如，如果，对于 *ȳ[j]*^((*i*)) = *y[j]*^((*i*))
    = 0.25
- en: '![](../../OEBPS/Images/eq_09-05-b.png)'
  id: totrans-85
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-05-b.png)'
- en: In fact, the binary cross-entropy loss is zero only in special cases, like *y[j]*^((*i*))
    = *ȳ[j]*^((*i*)) = 1.
  id: totrans-86
  prefs: []
  type: TYPE_NORMAL
  zh: 事实上，二元交叉熵损失仅在特殊情况下为零，例如 *y[j]*^((*i*)) = *ȳ[j]*^((*i*)) = 1。
- en: 9.1.5 Softmax
  id: totrans-87
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.5 Softmax
- en: 'Suppose we are building a classifier: for instance, the image classifier we
    illustrated in section [6.3](../Text/06.xhtml#sec-cross-entropy), which predicts
    whether an image contains a cat (class 0), a dog (class 1), and airplane (class
    2), or an automobile (class 3). Our classifier can emit a score vector ![](../../OEBPS/Images/AR_s.png)
    corresponding to an input image. Element *j* of the score vector corresponds to
    the *j[th]* class. We take the max of the score vector and call that the neural
    network-predicted label for the image. For instance, in the example image classifier,
    a score vector may be [9.99   10   0.01   -10]. Since the highest score occurs
    at index 1, we conclude that the image contains a dog class 1).'
  id: totrans-88
  prefs: []
  type: TYPE_NORMAL
  zh: 假设我们正在构建一个分类器：例如，我们在第 [6.3](../Text/06.xhtml#sec-cross-entropy) 节中展示的图像分类器，它预测图像是否包含猫（类别
    0）、狗（类别 1）、飞机（类别 2）或汽车（类别 3）。我们的分类器可以发出一个与输入图像对应的分数向量 ![](../../OEBPS/Images/AR_s.png)。分数向量中的元素
    *j* 对应于 *j[th]* 类。我们取分数向量的最大值，并将其称为神经网络预测的图像标签。例如，在示例图像分类器中，一个分数向量可能是 [9.99   10
      0.01   -10]。由于最高分数出现在索引 1 处，我们得出结论，该图像包含狗类别 1）。
- en: The scores are unbounded; they can be any real number in the range [−∞,∞]. In
    general, however, neural networks behave better when the loss function involves
    a bounded set of numbers in the same range. The training converges faster and
    to better minima, and the inferencing is more accurate. Consequently, it is desirable
    to convert the example scores to probabilities. These will be numbers in the range
    [0,1] (and the elements of the vector will sum to 1).
  id: totrans-89
  prefs: []
  type: TYPE_NORMAL
  zh: 分数是无界的；它们可以是范围 [−∞,∞] 内的任何实数。然而，通常情况下，当损失函数涉及同一范围内的有界数字集时，神经网络表现更好。训练会更快地收敛到更好的最小值，推理也更准确。因此，将示例分数转换为概率是可取的。这些数字将在范围
    [0,1] 内（并且向量的元素之和为 1）。
- en: The softmax function converts unbounded scores to probabilities. Given a score
    vector ![](../../OEBPS/Images/AR_s.png) = [*s*[0]   *s*[1]   *s*[2]  …  *s*[*N*-1]],
    the corresponding softmax vector is
  id: totrans-90
  prefs: []
  type: TYPE_NORMAL
  zh: Softmax 函数将无界分数转换为概率。给定一个分数向量 ![](../../OEBPS/Images/AR_s.png) = [*s*[0]   *s*[1]
      *s*[2]  …  *s*[*N*-1]]，相应的 softmax 向量是
- en: '![](../../OEBPS/Images/eq_09-06.png)'
  id: totrans-91
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-06.png)'
- en: Equation 9.6
  id: totrans-92
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 9.6
- en: 'A few noteworthy points:'
  id: totrans-93
  prefs: []
  type: TYPE_NORMAL
  zh: 几个值得注意的点：
- en: The vector has as many elements as possible classes.
  id: totrans-94
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 向量具有尽可能多的类别元素。
- en: The sum of the elements in the previous vector is 1.
  id: totrans-95
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 上一个向量中元素的总和为 1。
- en: The *j*th element of the vector represents the predicted probability of class
    *j*.
  id: totrans-96
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 向量的第 *j* 个元素代表类别 *j* 的预测概率。
- en: The formulation can handle arbitrary scores, including negative ones.
  id: totrans-97
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 该公式可以处理任意得分，包括负得分。
- en: So in our example classification problem with the four classes (cat, dog, airplane,
    automobile), the score vector
  id: totrans-98
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，在我们的这个包含四个类别（猫、狗、飞机、汽车）的示例分类问题中，得分向量
- en: '![](../../OEBPS/Images/AR_s.png) = [9.99   10   0.01   –10]'
  id: totrans-99
  prefs: []
  type: TYPE_NORMAL
  zh: '![](../../OEBPS/Images/AR_s.png) = [9.99   10   0.01   –10]'
- en: will yield the softmax vector
  id: totrans-100
  prefs: []
  type: TYPE_NORMAL
  zh: 将产生 softmax 向量
- en: '*softmax* (![](../../OEBPS/Images/AR_s.png)) = [0.497   0.502   2.30e–5   1.04e–9].'
  id: totrans-101
  prefs: []
  type: TYPE_NORMAL
  zh: '*softmax* (![](../../OEBPS/Images/AR_s.png)) = [0.497   0.502   2.30e–5   1.04e–9].'
- en: 'The probability of cat is 0.497, and the probability of dog is slightly higher,
    0.502. The probabilities of airplane and automobile are much lower: the neural
    network predicts that the image is that of a dog, but it is not very confident;
    it could also be a cat.'
  id: totrans-102
  prefs: []
  type: TYPE_NORMAL
  zh: 猫的概率是 0.497，狗的概率略高，为 0.502。飞机和汽车的概率要低得多：神经网络预测图像是狗的图像，但它并不十分自信；它也可能是猫的。
- en: Why the name softmax?
  id: totrans-103
  prefs: []
  type: TYPE_NORMAL
  zh: 为什么叫 softmax？
- en: 'The softmax function is a smooth (differentiable) approximation to the argmaxonehot
    function, which emits a one-hot vector corresponding to the index of the max score.
    The argmaxonehot function is *discontinuous*. To see this, consider a pair of
    two class score vectors:'
  id: totrans-104
  prefs: []
  type: TYPE_NORMAL
  zh: Softmax 函数是 argmaxonehot 函数的平滑（可微）近似，它输出一个对应于最大得分索引的 one-hot 向量。argmaxonehot
    函数是 *不连续的*。为了看到这一点，考虑一对二类得分向量：
- en: '![](../../OEBPS/Images/eq_09-06-a.png)'
  id: totrans-105
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-06-a.png)'
- en: 'Performing an argmaxonehot operation on them will yield the following one-hot
    vectors, respectively:'
  id: totrans-106
  prefs: []
  type: TYPE_NORMAL
  zh: 对其执行 argmaxonehot 操作将分别产生以下 one-hot 向量：
- en: '![](../../OEBPS/Images/eq_09-06-b.png)'
  id: totrans-107
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-06-b.png)'
- en: Thus we see that the vectors *argmaxonehot*(![](../../OEBPS/Images/AR_p.png))
    and *argmaxonehot*(![](../../OEBPS/Images/AR_q.png)) are significantly far from
    each other, even though the points ![](../../OEBPS/Images/AR_p.png) and ![](../../OEBPS/Images/AR_q.png)
    are very close to each other. On the other hand, the corresponding softmax vectors
    are
  id: totrans-108
  prefs: []
  type: TYPE_NORMAL
  zh: 因此我们看到，向量 *argmaxonehot*(![](../../OEBPS/Images/AR_p.png)) 和 *argmaxonehot*(![](../../OEBPS/Images/AR_q.png))
    相对于彼此非常遥远，尽管点 ![](../../OEBPS/Images/AR_p.png) 和 ![](../../OEBPS/Images/AR_q.png)
    非常接近。另一方面，相应的 softmax 向量是
- en: '![](../../OEBPS/Images/eq_09-06-c.png)'
  id: totrans-109
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-06-c.png)'
- en: Although the predicted classes still match those from the argmaxonehot vector,
    the softmax vectors are very close to each other. The closer the scores, the closer
    the softmax probabilities. In other words, the softmax is continuous.
  id: totrans-110
  prefs: []
  type: TYPE_NORMAL
  zh: 虽然预测的类别仍然与 argmaxonehot 向量中的那些匹配，但 softmax 向量彼此非常接近。得分越接近，softmax 概率也越接近。换句话说，softmax
    是连续的。
- en: Figure [9.2](#fig-twoclass-softmax-argmaxonehot) depicts this geometrically.
    The argmaxonehot functions as a function of the score vector [*s*0, *s*1] (for
    selecting classes 0 and 1, respectively) are shown in figures [9.2a](#fig-argmaxonehot-class0)
    and [9.2c](#fig-argmaxonehot-class1). These are step functions on the (*s*0, *s*1)
    plane, with *s*0 = *s*1 being the decision boundary. Their softmax approximations
    are shown in figures [9.2b](#fig-softmax-argmaxonehot-class0) and [9.2d](#fig-softmax-argmaxonehot-class1).
    In section [8.1](../Text/08.xhtml#sec-sigmoid-etc), we introduced the 1D sigmoid
    function (see figure [8.1](../Text/08.xhtml#fig-sigmoid1d)), which approximates
    the 1D step function. Here we see the higher-dimensional analog of that.
  id: totrans-111
  prefs: []
  type: TYPE_NORMAL
  zh: 图 [9.2](#fig-twoclass-softmax-argmaxonehot) 几何地描述了这一点。argmaxonehot 函数作为得分向量
    [*s*0, *s*1]（分别用于选择类别 0 和 1）的函数，如图 [9.2a](#fig-argmaxonehot-class0) 和 [9.2c](#fig-argmaxonehot-class1)
    所示。这些是在 (*s*0, *s*1) 平面上的步函数，*s*0 = *s*1 是决策边界。它们的 softmax 近似如图 [9.2b](#fig-softmax-argmaxonehot-class0)
    和 [9.2d](#fig-softmax-argmaxonehot-class1) 所示。在 [8.1](../Text/08.xhtml#sec-sigmoid-etc)
    节中，我们介绍了 1D sigmoid 函数（见图 [8.1](../Text/08.xhtml#fig-sigmoid1d)），它近似 1D 步函数。这里我们看到其高维类似物。
- en: '![](../../OEBPS/Images/CH09_F02a_Chaudhury.png)'
  id: totrans-112
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH09_F02a_Chaudhury.png)'
- en: '(a) Step function: *z* = 1 if *s*0 > = *s*1, else *z* = 0'
  id: totrans-113
  prefs: []
  type: TYPE_NORMAL
  zh: (a) 步函数：*z* = 1 如果 *s*0 >= *s*1，否则 *z* = 0
- en: '![](../../OEBPS/Images/CH09_F02b_Chaudhury.png)'
  id: totrans-114
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH09_F02b_Chaudhury.png)'
- en: '(b) Softmax: differential approximation of the step function'
  id: totrans-115
  prefs: []
  type: TYPE_NORMAL
  zh: (b) Softmax：步函数的微分近似
- en: '![](../../OEBPS/Images/CH09_F02c_Chaudhury.png)'
  id: totrans-116
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH09_F02c_Chaudhury.png)'
- en: '(c) Step function: *z* = 1 if *s*1 > = *s*0, else *z* = 0'
  id: totrans-117
  prefs: []
  type: TYPE_NORMAL
  zh: (c) 步函数：*z* = 1如果*s*1 >= *s*0，否则*z* = 0
- en: '![](../../OEBPS/Images/CH09_F02d_Chaudhury.png)'
  id: totrans-118
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F02d_Chaudhury.png)'
- en: '(d) Softmax: differential approximation of the step function'
  id: totrans-119
  prefs: []
  type: TYPE_NORMAL
  zh: (d) Softmax：步函数的微分近似
- en: Figure 9.2 Two-class argmaxonehot and softmax (function of score vector [*s*0,
    *s*1]) on the (*s*0, *s*1) plane. The decision boundary is the 45*[o]* line *s*0
    = *s*1.
  id: totrans-120
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.2在(*s*0, *s*1)平面上，两类的argmaxonehot和softmax（分数向量的函数[*s*0, *s*1]）。决策边界是45*[o]*线
    *s*0 = *s*1。
- en: Listing 9.4 PyTorch code for softmax
  id: totrans-121
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.4 PyTorch代码实现softmax
- en: '[PRE3]'
  id: totrans-122
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
- en: ① Imports the softmax function
  id: totrans-123
  prefs: []
  type: TYPE_NORMAL
  zh: ① 导入softmax函数
- en: ② Scores are typically raw, un-normalized outputs of a neural network.
  id: totrans-124
  prefs: []
  type: TYPE_NORMAL
  zh: ② 分数通常是神经网络的原始、未经归一化的输出。
- en: ③ Computes the softmax
  id: totrans-125
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 计算softmax
- en: 9.1.6 Softmax cross-entropy loss
  id: totrans-126
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.6 Softmax交叉熵损失
- en: From the preceding discussion, it should be clear that it is desirable to make
    the last layer of a classifier neural network a softmax layer. Then, given an
    input, the network will emit probabilities for each class. During training, we
    can evaluate the loss on these probabilities with regard to the known GT probabilities.
    This can be done via the CE loss (see section [9.1.3](#sec-ce-loss-again)). Thus
    the softmax is often followed by the CE loss during classifier training. Consequently,
    the combination (softmax CE loss) is available as a single operation in many deep
    learning packages, such as PyTorch. This is convenient because we do not need
    to call softmax and then CE loss. But the deeper reason for combining them is
    that the combination tends to be numerically better.
  id: totrans-127
  prefs: []
  type: TYPE_NORMAL
  zh: 从前面的讨论中，应该清楚，将分类器神经网络的最后一层设置为softmax层是可取的。然后，给定一个输入，网络将输出每个类的概率。在训练过程中，我们可以通过CE损失（见第9.1.3节）来评估这些概率与已知GT概率之间的损失。这可以通过CE损失（见第9.1.3节）来实现。因此，softmax通常在分类器训练过程中跟随CE损失。因此，组合（softmax
    CE损失）在许多深度学习包中作为单个操作提供，例如PyTorch。这很方便，因为我们不需要调用softmax然后CE损失。但将它们结合的更深层次的原因是，这种组合在数值上往往更好。
- en: 'Let’s look at an example to see how the softmax CE loss changes as the output
    prediction changes. Consider the image classification problem again, where we’d
    like to classify whether an image contains one of four categories: cat (class
    0), dog (class 1), airplane (class 2), or automobile (class 3). Figure [9.3](#fig-softmax-good-bad-visual)
    represents this visually. Suppose the image we’re classifying actually contains
    a dog class 1). The GT is represented as a one-hot vector [0 1 0 0]. If our classifier
    predicts the vector [0.498 0.502 0 0], it’s predicting both cat and dog with almost
    equal probability. This is a bad prediction because we would ideally expect it
    to confidently predict a dog (class 1). Consequently, the CE loss is high (0.688).
    On the other hand, if our classifier predicts [0.003 0.997 0 0], it is highly
    certain (with a probability of 0.997) that the image contains a dog. This is a
    good prediction, and hence the CE loss is low (0.0032). Softmax CE loss is probably
    the most popular loss method used for training in classifiers at the moment.'
  id: totrans-128
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们通过一个例子来看看softmax CE损失是如何随着输出预测的变化而变化的。再次考虑图像分类问题，我们希望将图像分类为四个类别之一：猫（类别0）、狗（类别1）、飞机（类别2）或汽车（类别3）。图[9.3](#fig-softmax-good-bad-visual)展示了这一点。假设我们正在分类的图像实际上包含狗类别1）。GT表示为一个one-hot向量[0
    1 0 0]。如果我们的分类器预测的向量是[0.498 0.502 0 0]，它几乎以相同的概率预测了猫和狗。这是一个不良的预测，因为我们理想上希望它自信地预测狗（类别1）。因此，CE损失较高（0.688）。另一方面，如果我们的分类器预测[0.003
    0.997 0 0]，它高度确定（概率为0.997）图像包含狗。这是一个好的预测，因此CE损失较低（0.0032）。softmax CE损失可能是目前用于分类器训练的最流行的损失方法。
- en: '![](../../OEBPS/Images/CH09_F03_Chaudhury.png)'
  id: totrans-129
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F03_Chaudhury.png)'
- en: Figure 9.3 Softmax output and cross-entropy loss for good and bad output predictions
  id: totrans-130
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.3良好和不良输出预测的softmax输出和交叉熵损失
- en: NOTE Fully functional code for softmax CE loss, executable via Jupyter Notebook,
    can be found at [http://mng.bz/g1a8](http://mng.bz/g1a8).
  id: totrans-131
  prefs: []
  type: TYPE_NORMAL
  zh: 备注：完整的softmax CE损失功能代码，可通过Jupyter Notebook执行，可在[http://mng.bz/g1a8](http://mng.bz/g1a8)找到。
- en: Listing 9.5 PyTorch code for softmax cross-entropy loss
  id: totrans-132
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.5 PyTorch代码实现softmax交叉熵损失
- en: '[PRE4]'
  id: totrans-133
  prefs: []
  type: TYPE_PRE
  zh: '[PRE4]'
- en: ① Ground truth class index Ranges from 0 to _ –1
  id: totrans-134
  prefs: []
  type: TYPE_NORMAL
  zh: ① 真实类别索引范围从0到_ –1
- en: ② Computes the softmax cross-entropy loss
  id: totrans-135
  prefs: []
  type: TYPE_NORMAL
  zh: ② 计算softmax交叉熵损失
- en: 9.1.7 Focal loss
  id: totrans-136
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.7 Focal损失
- en: As training progresses, where should we focus our attention? This question becomes
    especially significant when there is *data imbalance*, meaning the number of training
    data instances available for some classes is significantly smaller than others.
    In such cases, not all training data is equally important. We have to use our
    training data instances wisely.
  id: totrans-137
  prefs: []
  type: TYPE_NORMAL
  zh: 随着训练的进行，我们应该关注哪里？当存在 *数据不平衡* 时，这个问题变得尤为重要，这意味着某些类别的训练数据实例数量显著少于其他类别。在这种情况下，并非所有训练数据都同等重要。我们必须明智地使用我们的训练数据实例。
- en: Intuitively, the greater bang for the buck is trying to improve the training
    data instances that are not doing well. In other words, instead of trying to squeeze
    out every bit of juice from the examples in which the network is doing well (the
    so-called “easy” examples), it is better to focus on the examples where the network
    is not doing as well (“hard” examples).
  id: totrans-138
  prefs: []
  type: TYPE_NORMAL
  zh: 直观地说，提高表现不佳的训练数据实例的性价比更高。换句话说，与其试图从网络表现良好的示例（所谓的“简单”示例）中榨取每一滴汁液，不如关注网络表现不佳的示例（“困难”示例）。
- en: 'To stop focusing on easy examples and focus instead on hard examples, we can
    put more weight on the loss from training data instances that are far from the
    GT, and vice versa: that is, put less weight on the loss from training data instances
    that are close to GT. Consider the binary CE loss of equation [9.4](#eq-binary-ce-loss)
    one more time. The loss for the *i*th training instance can be rewritten as follows:'
  id: totrans-139
  prefs: []
  type: TYPE_NORMAL
  zh: 为了停止关注简单示例，转而关注困难示例，我们可以对远离 GT 的训练数据实例的损失赋予更多权重，反之亦然：即，对接近 GT 的训练数据实例的损失赋予较少权重。再次考虑方程式
    [9.4](#eq-binary-ce-loss) 的二进制 CE 损失。第 *i* 个训练实例的损失可以重新写为以下形式：
- en: '![](../../OEBPS/Images/eq_09-06-d.png)'
  id: totrans-140
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-06-d.png)'
- en: NOTE Going forward in this subsection, we drop the superscript (*i*) for the
    sake of notational simplification, although it remains implied.
  id: totrans-141
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：在接下来的小节中，为了简化符号，我们省略了上标（*i*），尽管它仍然隐含存在。
- en: Now, when the GT is class 1 (that is, *ȳ* = 1), the entity (1−*y*) measures
    the departure of the prediction from GT. We can multiply the loss by this to weigh
    down the losses from good predictions and weigh up the losses from bad predictions.
    In practice, we multiply by (1−*y*)^γ for some value of *γ* (such as *γ* = 2).
    Similarly, when the GT is class 0 (that is, *ȳ* = 0), the entity *y* measures
    the departure of the prediction from the GT. In this case, we multiply the loss
    by *y^γ*. The overall loss then becomes
  id: totrans-142
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，当 GT 是类别 1（即 *ȳ* = 1）时，实体 (1−*y*) 衡量预测与 GT 的偏差。我们可以将损失乘以这个值来加重良好预测的损失，减轻不良预测的损失。在实践中，我们乘以
    (1−*y*)^γ，其中 *γ* 是某个值（例如 *γ* = 2）。同样，当 GT 是类别 0（即 *ȳ* = 0）时，实体 *y* 衡量预测与 GT 的偏差。在这种情况下，我们乘以
    *y^γ*。因此，整体损失变为
- en: '![](../../OEBPS/Images/eq_09-06-e.png)'
  id: totrans-143
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-06-e.png)'
- en: We can have a somewhat simplified expression
  id: totrans-144
  prefs: []
  type: TYPE_NORMAL
  zh: 我们可以有一个相对简化的表达式
- en: '![](../../OEBPS/Images/eq_09-07.png)'
  id: totrans-145
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-07.png)'
- en: Equation 9.7
  id: totrans-146
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 9.7
- en: where
  id: totrans-147
  prefs: []
  type: TYPE_NORMAL
  zh: 其中
- en: '![](../../OEBPS/Images/eq_09-07-a.png)'
  id: totrans-148
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-07-a.png)'
- en: Equation [9.7](#eq-focal-loss) is the popular expression of focal loss. Its
    graph at various values of *γ* is shown in figure [9.4](#fig-focal-loss). Note
    how the loss becomes more and more subdued as the probability of the GT increases
    toward the right until it flattens out at the bottom.
  id: totrans-149
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 [9.7](#eq-focal-loss) 是 focal loss 的流行表达式。它在 *γ* 不同值下的图形显示在图 [9.4](#fig-focal-loss)
    中。注意，随着 GT 的概率向右增加，损失变得越来越微弱，直到在底部变平。
- en: NOTE Fully functional code for focal loss, executable via Jupyter Notebook,
    can be found at [http://mng.bz/g1a8](http://mng.bz/g1a8).
  id: totrans-150
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：完整的 focal loss 代码，可通过 Jupyter Notebook 执行，可以在 [http://mng.bz/g1a8](http://mng.bz/g1a8)
    找到。
- en: Listing 9.6 PyTorch code for focal loss
  id: totrans-151
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 9.6 PyTorch 代码实现 focal loss
- en: '[PRE5]'
  id: totrans-152
  prefs: []
  type: TYPE_PRE
  zh: '[PRE5]'
- en: ① *y[t]* = *y* if *y[gt]* is 1
  id: totrans-153
  prefs: []
  type: TYPE_NORMAL
  zh: ① *y[t]* = *y* 如果 *y[gt]* 是 1
- en: '*y[t]* = 1 − *y* if *y[gt]* is 0'
  id: totrans-154
  prefs: []
  type: TYPE_NORMAL
  zh: '*y[t]* = 1 − *y* 如果 *y[gt]* 是 0'
- en: 9.1.8 Hinge loss
  id: totrans-155
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.1.8 Hinge loss
- en: 'The softmax CE loss becomes zero only under the ideal condition: the correct
    class has a finite score, and other classes have a score of negative infinity.
    Hence, that loss will continue to push the network toward improvement until the
    ideal is achieved (which never happens in practice). Sometimes we prefer to stop
    changing the network when the correct class has the maximum score, and we do not
    care about increasing the distance between correct and incorrect scores any further.
    This is where hinge loss comes in.'
  id: totrans-156
  prefs: []
  type: TYPE_NORMAL
  zh: 只有在理想条件下，softmax CE 损失才会变为零：正确的类别有一个有限的分数，而其他类别的分数为负无穷。因此，该损失将继续推动网络向改进方向前进，直到达到理想状态（在实践中永远不会发生）。有时我们更喜欢在正确类别的分数达到最大时停止改变网络，我们不再关心增加正确和错误分数之间的距离。这就是
    hinge 损失出现的地方。
- en: '![](../../OEBPS/Images/CH09_F04_Chaudhury.jpg)'
  id: totrans-157
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH09_F04_Chaudhury.jpg)'
- en: Figure 9.4 Focal loss graph (various *γ* values)
  id: totrans-158
  prefs: []
  type: TYPE_NORMAL
  zh: 图 9.4 Focal 损失图（各种 *γ* 值）
- en: A hinged door opens in one direction but not in the other direction. Similarly,
    a hinge loss function increases if a certain goodness criterion is *not* satisfied
    but becomes zero (and does not reduce any further) if the criterion is satisfied.
    This is akin to saying, “If you are not my friend, the distance between us can
    vary from small to large (unboundedly), but I don’t distinguish between friends.
    All my friends are at a distance of zero from me.”
  id: totrans-159
  prefs: []
  type: TYPE_NORMAL
  zh: 一个铰链门只能向一个方向打开，但不能向另一个方向打开。同样，如果某个良好标准不满足，则 hinge 损失函数会增加，如果标准满足，则损失函数变为零（并且不再进一步减少）。这就像说，“如果你不是我的朋友，我们之间的距离可以从小到大变化（无界），但我不会区分朋友。所有我的朋友都与我保持零距离。”
- en: 'Multiclass support vector machine loss: Hinge loss for classification'
  id: totrans-160
  prefs: []
  type: TYPE_NORMAL
  zh: 多类支持向量机损失：分类的 hinge 损失
- en: 'Consider again our old friend, the classifier that predicts whether an image
    contains a cat (class 0), dog class 1), airplane (class 2), or automobile (class
    3). Our classifier emits an output vector ![](../../OEBPS/Images/AR_y.png) corresponding
    to an input image. Here, the outputs are scores: *y[j]* is the score corresponding
    to the *j*th class. (In this subsection, we have dropped the superscripts indicating
    the training data index to simplify notations.)'
  id: totrans-161
  prefs: []
  type: TYPE_NORMAL
  zh: 再次考虑我们的老朋友，这个分类器预测图像是否包含猫（类别 0）、狗（类别 1）、飞机（类别 2）或汽车（类别 3）。我们的分类器输出一个与输入图像对应的输出向量
    ![](../../OEBPS/Images/AR_y.png)。在这里，输出是分数：*y[j]* 是对应于第 *j* 个类别的分数。（在本小节中，我们省略了表示训练数据索引的上标，以简化符号。）
- en: Given a (training data instance, GT label) pair (![](../../OEBPS/Images/AR_x.png),
    *c*) (that is, the GT class corresponding to the input ![](../../OEBPS/Images/AR_x.png)
    is *c*), the multiclass support vector machine (SVM) loss is
  id: totrans-162
  prefs: []
  type: TYPE_NORMAL
  zh: 给定一个（训练数据实例，GT 标签）对 (![](../../OEBPS/Images/AR_x.png), *c*)（即，GT 类别对应于输入 ![](../../OEBPS/Images/AR_x.png)
    是 *c*），多类支持向量机（SVM）损失是
- en: '![](../../OEBPS/Images/eq_09-08.png)'
  id: totrans-163
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-08.png)'
- en: Equation 9.8
  id: totrans-164
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 9.8
- en: where *m* is a margin usually *m* = 1).
  id: totrans-165
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *m* 是一个通常 *m* = 1 的边界）。
- en: 'To understand this, consider the equation without the margin first:'
  id: totrans-166
  prefs: []
  type: TYPE_NORMAL
  zh: 要理解这一点，首先考虑没有边界的方程：
- en: '![](../../OEBPS/Images/eq_09-08-a.png)'
  id: totrans-167
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-08-a.png)'
- en: 'In equation [9.8](#eq-muticlass-svm-loss), we are summing over all the classes
    except the one that matches the GT. In other words, we are summing over only the
    incorrect classes. For these, we want the score *y[j]* to be smaller than the
    score *y[c]* for the correct class. There are two possibilities:'
  id: totrans-168
  prefs: []
  type: TYPE_NORMAL
  zh: 在方程 [9.8](#eq-muticlass-svm-loss) 中，我们对除了匹配 GT 的类别之外的所有类别进行求和。换句话说，我们只对错误类别进行求和。对于这些类别，我们希望分数
    *y[j]* 小于正确类别的分数 *y[c]*。有两种可能性：
- en: '*Good output*—Incorrect class score less than correct class score:'
  id: totrans-169
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*良好输出*—错误类别的分数小于正确类别的分数：'
- en: '*y[j]* − *y[c]* < 0 ⟹ *max*(0, *y[j]* − *y[c]*) = 0'
  id: totrans-170
  prefs:
  - PREF_IND
  type: TYPE_NORMAL
  zh: '*y[j]* − *y[c]* < 0 ⟹ *max*(0, *y[j]* − *y[c]*) = 0'
- en: 'The contribution to the loss is zero (we do not distinguish between correct
    scores: all friends are at zero distance).'
  id: totrans-171
  prefs:
  - PREF_IND
  type: TYPE_NORMAL
  zh: 损失的贡献是零（我们不区分正确分数：所有朋友都与我保持零距离）。
- en: '*Bad output*—Incorrect class score more than correct class score:'
  id: totrans-172
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*不良输出*—错误类别的分数大于正确类别的分数：'
- en: '*y[j]* > *y[c]* ⟹ *max*(0, *y[j]* – *y[c]*) = *y[j]* − *y[c]*'
  id: totrans-173
  prefs:
  - PREF_IND
  type: TYPE_NORMAL
  zh: '*y[j]* > *y[c]* ⟹ *max*(0, *y[j]* – *y[c]*) = *y[j]* − *y[c]*'
- en: The contribution to the loss is positive (non-friends are at a positive distance
    that varies with the degree of non-friendness).
  id: totrans-174
  prefs:
  - PREF_IND
  type: TYPE_NORMAL
  zh: 损失的贡献是正的（非朋友之间的距离是正的，并且随着非朋友程度的增加而变化）。
- en: In practical settings, the margin is set to a positive number usually 1) to
    penalize predictions where the score of the correct class is marginally greater
    than that of the incorrect classes. This forces the classifier to learn to predict
    the correct class with high confidence. Figure [9.8](#fig-hinge-loss-good-bad-visual)
    shows how hinge loss differs for good and bad output predictions.
  id: totrans-175
  prefs: []
  type: TYPE_NORMAL
  zh: 在实际设置中，边缘设置为正数，通常为1，以惩罚正确类别的分数仅略高于错误类别的预测。这迫使分类器学会以高置信度预测正确类别。图[9.8](#fig-hinge-loss-good-bad-visual)显示了良好和不良输出预测的hinge损失如何不同。
- en: '![](../../OEBPS/Images/CH09_F05_Chaudhury.png)'
  id: totrans-176
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/CH09_F05_Chaudhury.png)'
- en: Figure 9.5 Hinge loss for good and bad output predictions
  id: totrans-177
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.5 良好和不良输出预测的Hinge损失
- en: One mental model to have about the multiclass SVM loss is that it is lazy. It
    stops changing as soon as the correct class score exceeds the incorrect scores
    by the margin *m*. The loss does not change if the correct class score goes still
    higher, which means it does not push the machine to improve beyond that point.
    This behavior is different from the softmax CE loss, which tries to push the machine
    to achieve an infinite score for the correct class.
  id: totrans-178
  prefs: []
  type: TYPE_NORMAL
  zh: 关于多类SVM损失的一个心理模型是它很懒惰。一旦正确的类别分数超过错误分数的边缘 *m*，它就停止变化。如果正确的类别分数继续上升，损失不会改变，这意味着它不会推动机器在这个点之后继续改进。这种行为与softmax
    CE损失不同，softmax CE损失试图推动机器为正确的类别达到无限分数。
- en: 9.2 Optimization
  id: totrans-179
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 9.2 优化
- en: Neural network models define a loss function that estimates the badness of the
    network’s output. During supervised training, the output on a particular training
    instance input is compared to a known output GT) for that particular training
    instance. The difference between the GT and the network-generated output is called
    loss. We can sum up the losses from individual training data instances and compute
    the total loss over all the training data.
  id: totrans-180
  prefs: []
  type: TYPE_NORMAL
  zh: 神经网络模型定义了一个损失函数，该函数估计网络输出的不良程度。在监督训练期间，特定训练实例输入的输出与该特定训练实例的已知输出（GT）进行比较。GT和网络生成的输出之间的差异称为损失。我们可以将单个训练数据实例的损失相加，并计算所有训练数据上的总损失。
- en: These losses are, of course, functions of the network parameters, ![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png). We can imagine a space whose dimensionality
    is *dim*(![](../../OEBPS/Images/AR_w.png)) + *dim*(![](../../OEBPS/Images/AR_b.png)).
    At each point in this space, we have a value for the total training loss. Thus,
    we can imagine a loss surface—a surface whose height represents the loss value—defined
    over the high-dimensional domain of network parameters (weights and biases).
  id: totrans-181
  prefs: []
  type: TYPE_NORMAL
  zh: 这些损失当然是网络参数的函数，![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png)。我们可以想象一个维度为
    *dim*(![](../../OEBPS/Images/AR_w.png)) + *dim*(![](../../OEBPS/Images/AR_b.png))
    的空间。在这个空间的每一个点上，我们都有一个总训练损失的值。因此，我们可以想象一个损失表面——一个高度代表损失值的表面——它定义在网络参数（权重和偏差）的高维域上。
- en: 'Optimization is nothing but finding the lowest point on this surface. During
    training, we start with random values of network parameters: this is akin to starting
    at a random point on the surface. Then we constantly move locally downhill on
    the loss surface in the direction of the negative gradient. We hope this eventually
    takes us to the minimum or a sufficiently low point. Continuing our analogy of
    the loss surface as a ravine, the minimum is at sea level. This minimum provides
    us with the network parameter values (weights and biases) that will yield the
    least loss on the training data. If the training data set adequately represents
    the problem, the trained model will perform well on unseen data.'
  id: totrans-182
  prefs: []
  type: TYPE_NORMAL
  zh: 优化不过是找到这个表面上的最低点。在训练过程中，我们从网络参数的随机值开始：这就像在表面上随机选择一个点开始。然后我们不断地在损失表面上沿着负梯度的方向局部下降。我们希望这最终能带我们到最小值或足够低的位置。继续我们关于损失表面像峡谷的类比，最小值在海平面。这个最小值为我们提供了网络参数值（权重和偏差），这些值将在训练数据上产生最小的损失。如果训练数据集充分代表了问题，训练好的模型将在未见过的数据上表现良好。
- en: This process of traveling toward the minimum, the iterative updating of weights
    and biases to have minimal loss over the training dataset, is called *optimization*.
    The basic math was introduced in section [8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc)
    (equation [8.12](../Text/08.xhtml#eq-wtsbiases-update-vector)). Here we study
    many practical nuances and variants.
  id: totrans-183
  prefs: []
  type: TYPE_NORMAL
  zh: 这个朝着最小值前进的过程，即对训练数据集进行权重和偏差的迭代更新以实现最小损失，被称为*优化*。基本数学原理在第八章[8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc)（方程[8.12](../Text/08.xhtml#eq-wtsbiases-update-vector)）中介绍。在这里，我们研究了许多实际细节和变体。
- en: 'At every iteration, we update the weights and biases, so if *t* denotes the
    iteration number, ![](../../OEBPS/Images/AR_w.png)*[t]* denotes the weight values
    at iteration *t*, *δ*![](../../OEBPS/Images/AR_w.png)*[t]* denotes the weight
    updates at iteration *t*, and so on:'
  id: totrans-184
  prefs: []
  type: TYPE_NORMAL
  zh: 在每次迭代中，我们更新权重和偏差，所以如果*t*表示迭代次数，![图片描述](../../OEBPS/Images/AR_w.png)*[t]*表示*t*次迭代的权重值，*δ*![图片描述](../../OEBPS/Images/AR_w.png)*[t]*表示*t*次迭代的权重更新，等等：
- en: '![](../../OEBPS/Images/eq_09-09.png)'
  id: totrans-185
  prefs: []
  type: TYPE_IMG
  zh: '![图片描述](../../OEBPS/Images/eq_09-09.png)'
- en: Equation 9.9
  id: totrans-186
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 9.9
- en: 'The basic update is along the direction of the negative gradient (see equation
    [8.12](../Text/08.xhtml#eq-wtsbiases-update-vector)):'
  id: totrans-187
  prefs: []
  type: TYPE_NORMAL
  zh: 基本更新是沿着负梯度的方向（参见方程[8.12](../Text/08.xhtml#eq-wtsbiases-update-vector)）：
- en: '![](../../OEBPS/Images/eq_09-10.png)'
  id: totrans-188
  prefs: []
  type: TYPE_IMG
  zh: '![图片描述](../../OEBPS/Images/eq_09-10.png)'
- en: Equation 9.10
  id: totrans-189
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 9.10
- en: 'Here, 𝕃(![](../../OEBPS/Images/AR_w.png)*[t]*, ![](../../OEBPS/Images/AR_b.png)*[t]*)
    denotes the loss at iteration *t*. Ideally, we should evaluate the loss on every
    training data instance and average them. But that would imply that we must process
    every training data instance for every iteration, which is prohibitively expensive.
    Instead, we use sampling see section [9.2.2](../Text/09.xhtml#sec-SGD)):'
  id: totrans-190
  prefs: []
  type: TYPE_NORMAL
  zh: 这里，𝕃(![图片描述](../../OEBPS/Images/AR_w.png)*[t]*, ![图片描述](../../OEBPS/Images/AR_b.png)*[t]*)表示*t*次迭代的损失。理想情况下，我们应该评估每个训练数据实例的损失并取平均值。但这将意味着我们必须为每次迭代处理每个训练数据实例，这是非常昂贵的。相反，我们使用采样（参见[9.2.2](../Text/09.xhtml#sec-SGD)）：
- en: 'The constant *η* is called the *learning rate* (LR). A larger LR results in
    bigger steps (bigger adjustments to weights and biases per update) and vice versa.
    We use larger values of LR in the beginning: when the network is completely untrained,
    we want to take large steps toward the minimum. When we are close to the minimum,
    on the other hand, we want to take smaller steps, lest we overshoot it. The LR
    is typically a small number, like *η* = 0.01.'
  id: totrans-191
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 常数*η*被称为*学习率*（LR）。较大的LR会导致更大的步长（每次更新对权重和偏差的调整更大）反之亦然。我们最初使用较大的LR值：当网络完全未训练时，我们希望向最小值迈出大步。另一方面，当我们接近最小值时，我们希望迈出更小的步子，以免超出它。LR通常是一个很小的数字，比如*η*
    = 0.01。
- en: In stochastic gradient descent (SGD; a popular approach), the LR *η* is typically
    held constant during an epoch (an *epoch* is a single pass over all the training
    data). Then the LR is decreased after one or more epochs. This process is called
    *learning rate decay*. So, the LR is not exactly a constant. We could have written
    it *η[t]* to indicate the temporal nature, but we chose to keep it simple because
    (in SGD, at least) it changes infrequently.
  id: totrans-192
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在随机梯度下降（SGD；一种流行的方法）中，LR *η* 通常在一个epoch（一个epoch是遍历所有训练数据的一次）中保持不变。然后在一个或多个epoch之后降低LR。这个过程被称为*学习率衰减*。因此，LR并不完全是一个常数。我们可以将其写作*η[t]*来表示其时间性质，但我们选择保持简单，因为（至少在SGD中）它变化不频繁。
- en: We have to reevaluate the loss and its gradients in each iteration since their
    values will change in every iteration, because the weights and biases of the underlying
    neural network are changing.
  id: totrans-193
  prefs: []
  type: TYPE_NORMAL
  zh: 由于底层神经网络的权重和偏差在每次迭代中都会变化，我们必须在每次迭代中重新评估损失及其梯度。
- en: How many iterations are necessary? Typically, this is a large number. We iterate
    *multiple times over the entire training dataset*. A typical training session
    has multiple epochs. In this context, note that for proper convergence, it is
    *extremely important* to randomly shuffle the order of occurrence of the training
    data after every epoch. In the following sections, we look at some practical nuances
    of the process.
  id: totrans-194
  prefs: []
  type: TYPE_NORMAL
  zh: 需要进行多少次迭代？通常，这是一个很大的数字。我们多次在整个训练数据集上迭代。在这次训练中，请注意，为了确保收敛，在每个epoch之后随机打乱训练数据的顺序是*极其重要*的。在接下来的章节中，我们将探讨这个过程的一些实际细节。
- en: 9.2.1 Geometrical view of optimization
  id: totrans-195
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.1 优化的几何视图
- en: This topic is described in detail in section [8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc).
    You are encouraged to revisit that discussion if necessary.
  id: totrans-196
  prefs: []
  type: TYPE_NORMAL
  zh: 这个主题在[8.4.2](../Text/08.xhtml#sec-loss-surface-graddesc)节中有详细描述。如果需要，你被鼓励重新回顾那个讨论。
- en: Overall, neural network optimization is an iterative process. Ideally, in each
    iteration, we compute the gradient of the loss with respect to the current parameters
    (weights and biases) and obtain improved values for them by moving in the direction
    of the negative gradient.
  id: totrans-197
  prefs: []
  type: TYPE_NORMAL
  zh: 总体来说，神经网络优化是一个迭代过程。理想情况下，在每次迭代中，我们计算当前参数（权重和偏置）相对于损失函数的梯度，并通过沿着负梯度方向移动来获得它们的改进值。
- en: 9.2.2 Stochastic gradient descent and minibatches
  id: totrans-198
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.2 随机梯度下降和小批量
- en: 'How do we compute the gradient of the loss function? The loss is different
    for every training data instance. The sensible thing to do is to average them
    out. But as we mentioned earlier, that leads to a practical problem: we would
    have to process the entire training dataset on every iteration. If the size of
    the training dataset is *n*, an epoch is an *O*(*n*²) operation every iteration,
    we have to process all of the *n* training data instances to compute the gradient,
    and an epoch has *n* iterations). Since *n* is a large number, often in the millions,
    *O*(*n*²) is prohibitively expensive.'
  id: totrans-199
  prefs: []
  type: TYPE_NORMAL
  zh: 我们如何计算损失函数的梯度？对于每个训练数据实例，损失是不同的。合乎逻辑的做法是平均它们。但如我们之前提到的，这会导致一个实际的问题：我们每次迭代都必须处理整个训练数据集。如果训练数据集的大小是*n*，那么一个epoch是每次迭代的*O(n²)*操作，我们必须处理所有的*n*个训练数据实例来计算梯度，而一个epoch有*n*次迭代）。由于*n*通常是一个很大的数字，经常是数百万，*O(n²)*是过于昂贵的。
- en: In SGD, we do not average over the entire training data set to produce the gradient.
    Instead, we average over a random sampled subset of the training data. This randomly
    sampled subset of training data is called a *minibatch*. The gradient is computed
    by averaging the loss over the minibatch (as opposed to the entire training dataset).
    This gradient is used to update the weight and bias parameters.
  id: totrans-200
  prefs: []
  type: TYPE_NORMAL
  zh: 在SGD中，我们不是在整个训练数据集上平均来产生梯度。相反，我们在训练数据的一个随机样本子集上平均。这个随机样本的子集被称为*小批量*。梯度是通过平均小批量中的损失来计算的（而不是整个训练数据集）。这个梯度用于更新权重和偏置参数。
- en: 9.2.3 PyTorch code for SGD
  id: totrans-201
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.3 SGD的PyTorch代码
- en: Now, let’s implement SGD in PyTorch.
  id: totrans-202
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，让我们在PyTorch中实现SGD。
- en: NOTE Fully functional code for SGD, executable via Jupyter Notebook, can be
    found at [http://mng.bz/ePyG](http://mng.bz/ePyG).
  id: totrans-203
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：SGD的完整功能代码，可通过Jupyter Notebook执行，可以在[http://mng.bz/ePyG](http://mng.bz/ePyG)找到。
- en: Let’s consider the example discussed in section [6.9](../Text/06.xhtml#sec-GMM).
    Our goal is to build a model that can predict whether a Statsville resident is
    a man, woman, or child using height and weight as input data. For this purpose,
    let’s assume we have a large dataset *X* containing the heights and weights of
    various Statsville residents. *X* is of shape (*num*_*samples*,2), where each
    row represents the (*height*, *weight*) pair of a single resident. The corresponding
    labels are stored in y[gt], which contains *num*_*samples* elements. Each row
    of y[gt] can be 0, 1, or 2, depending on whether the resident is a man, woman,
    or child. Figure [9.6](#fig-sgd-example-gt-dist) shows an example distribution
    of *X*.
  id: totrans-204
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们考虑[6.9](../Text/06.xhtml#sec-GMM)节中讨论的例子。我们的目标是构建一个模型，该模型可以使用身高和体重作为输入数据来预测Statsville居民是男性、女性还是儿童。为此，让我们假设我们有一个包含各种Statsville居民身高和体重的庞大数据集*X*。*X*的形状是(*num_samples*,2)，其中每一行代表单个居民的(*height*,
    *weight*)对。相应的标签存储在y[gt]中，它包含*num_samples*个元素。y[gt]的每一行可以是0、1或2，具体取决于居民是男性、女性还是儿童。图[9.6](#fig-sgd-example-gt-dist)显示了*X*的一个示例分布。
- en: '![](../../OEBPS/Images/CH09_F06_Chaudhury.png)'
  id: totrans-205
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F06_Chaudhury.png)'
- en: Figure 9.6 Height and weight of various Statsville residents. Class 0 (man)
    is represented by the right-most cluster, class 1 (woman) by the middle cluster,
    and class 2 (child) by the left-most cluster.
  id: totrans-206
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.6 Statsville居民的身高和体重。类别0（男性）由最右侧的簇表示，类别1（女性）由中间的簇表示，类别2（儿童）由最左侧的簇表示。
- en: Before training a model, we must first convert the data into a training-friendly
    format. We subclass `torch.utils.data.Dataset` to do so and implement the `__len__`
    and `__getitem__` methods. The *i*th training data instance can be accessed by
    calling `data_set[i]`. Remember that in SGD, we feed in minibatches that contain
    `batch_size` elements in every iteration. This can be achieved by calling the
    `__getitem__` method `batch_size` times. However, instead of doing this ourselves,
    we use PyTorch’s `DataLoader`, which gives us a convenient wrapper. Using `DataLoader`
    is recommended in production settings because it provides a simple API through
    which we can (1) create minibatches, 2) speed up data-loading times via multiprocessing,
    and (3) randomly shuffle data in every epoch to prevent overfitting. The following
    code creates a custom PyTorch data set.
  id: totrans-207
  prefs: []
  type: TYPE_NORMAL
  zh: 在训练模型之前，我们必须首先将数据转换为适合训练的格式。我们通过继承`torch.utils.data.Dataset`来实现这一点，并实现`__len__`和`__getitem__`方法。可以通过调用`data_set[i]`来访问第*i*个训练数据实例。记住，在SGD中，我们每次迭代都输入包含`batch_size`个元素的minibatch。这可以通过调用`__getitem__`方法`batch_size`次来实现。然而，我们不是自己这样做，而是使用PyTorch的`DataLoader`，它提供了一个方便的包装器。在生产环境中推荐使用`DataLoader`，因为它提供了一个简单的API，通过它可以（1）创建minibatch，2）通过多进程加速数据加载时间，3）在每轮中随机打乱数据以防止过拟合。以下代码创建了一个自定义的PyTorch数据集。
- en: Listing 9.7 PyTorch code to create a custom dataset
  id: totrans-208
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.7 PyTorch代码用于创建自定义数据集
- en: '[PRE6]'
  id: totrans-209
  prefs: []
  type: TYPE_PRE
  zh: '[PRE6]'
- en: ① Subclasses torch.utils.data.Dataset
  id: totrans-210
  prefs: []
  type: TYPE_NORMAL
  zh: ① 继承自torch.utils.data.Dataset
- en: ② Returns the size of the data set
  id: totrans-211
  prefs: []
  type: TYPE_NORMAL
  zh: ② 返回数据集的大小
- en: ③ Returns the *i*th training data element
  id: totrans-212
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 返回第*i*个训练数据元素
- en: ④ Instantiates the data set
  id: totrans-213
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 实例化数据集
- en: ⑤ Instantiates the data loader with a batch size of 10 and shuffle on
  id: totrans-214
  prefs: []
  type: TYPE_NORMAL
  zh: 使用批大小为10并开启随机打乱的方式实例化数据加载器
- en: 'Our next step is to create a classifier model that can take the height and
    weight data (*X*) as input and predict the output class. Here, we create a simple
    neural network model that consists of two linear layers followed by a softmax
    layer. The output of the softmax layer has three values representing the probability
    of each of the three classes (man, woman, and child), respectively. Note that
    in the forward pass, we don’t call the softmax layer because our loss function,
    PyTorch’s CE loss, expects raw, un-normalized scores as input. Hence we pass the
    output of the second linear layer to the loss function. However, during prediction,
    we pass the scores to the softmax layer to get a probability vector and then take
    an argmax to get the predicted class. Notice that we have a function to initialize
    the weights of the linear layers: this is important because the starting value
    of the weights can often affect convergence. If the model starts too far from
    the minimum, it may never converge.'
  id: totrans-215
  prefs: []
  type: TYPE_NORMAL
  zh: 我们下一步是创建一个分类器模型，该模型可以接收身高和体重数据(*X*)作为输入并预测输出类别。在这里，我们创建了一个简单的神经网络模型，该模型由两个线性层和一个softmax层组成。softmax层的输出有三个值，分别代表三个类别（男性、女性和儿童）的概率。请注意，在正向传播过程中，我们没有调用softmax层，因为我们的损失函数，PyTorch的CE损失，期望输入的是原始、未归一化的分数。因此，我们将第二个线性层的输出传递给损失函数。然而，在预测过程中，我们将分数传递给softmax层以获得一个概率向量，然后通过argmax获取预测类别。请注意，我们有一个函数用于初始化线性层的权重：这很重要，因为权重的起始值通常会影响到收敛。如果模型起始点离最小值太远，它可能永远无法收敛。
- en: Listing 9.8 PyTorch code to create a custom neural network model
  id: totrans-216
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.8 PyTorch代码用于创建自定义神经网络模型
- en: '[PRE7]'
  id: totrans-217
  prefs: []
  type: TYPE_PRE
  zh: '[PRE7]'
- en: ① Subclasses torch.nn.Module
  id: totrans-218
  prefs: []
  type: TYPE_NORMAL
  zh: ① 继承自torch.nn.Module
- en: ② Instantiates the linear layers and the softmax layer
  id: totrans-219
  prefs: []
  type: TYPE_NORMAL
  zh: ② 实例化线性层和softmax层
- en: ③ Feeds forward the input through the two linear layers
  id: totrans-220
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 将输入通过两个线性层进行前向传播
- en: ④ Predicts the output class index
  id: totrans-221
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 预测输出类索引
- en: ⑤ Initializes the weights to help the model converge better
  id: totrans-222
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ 初始化权重以帮助模型更好地收敛
- en: Now that we have our data set and model, let’s define our loss function and
    instantiate our SGD optimizer.
  id: totrans-223
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们已经有了数据集和模型，接下来定义我们的损失函数并实例化我们的SGD优化器
- en: Listing 9.9 PyTorch code for a loss function and SGD optimizer
  id: totrans-224
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.9 PyTorch代码用于损失函数和SGD优化器
- en: '[PRE8]'
  id: totrans-225
  prefs: []
  type: TYPE_PRE
  zh: '[PRE8]'
- en: ① Instantiates the SGD optimizer with learning rate = 0.02
  id: totrans-226
  prefs: []
  type: TYPE_NORMAL
  zh: ① 使用学习率=0.02实例化SGD优化器
- en: Now we define the training loop, which is essentially one pass over the entire
    dataset. We iterate over the dataset in batches of size `batch_size`, run the
    forward pass, compute the gradients, and update the weights in the direction of
    the negative gradient. Note that we call `optimizer.zero_grad()` in every iteration
    to prevent the accumulation of gradients from the previous steps.
  id: totrans-227
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们定义训练循环，这本质上是对整个数据集的一次遍历。我们以`batch_size`的大小对数据集进行批处理迭代，运行前向传播，计算梯度，并沿着负梯度方向更新权重。请注意，我们在每次迭代中调用`optimizer.zero_grad()`以防止之前步骤的梯度累积。
- en: Listing 9.10 PyTorch code for one training loop
  id: totrans-228
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.10 一个训练循环的PyTorch代码
- en: '[PRE9]'
  id: totrans-229
  prefs: []
  type: TYPE_PRE
  zh: '[PRE9]'
- en: ① Iterates through the data set in batches
  id: totrans-230
  prefs: []
  type: TYPE_NORMAL
  zh: ① 以批处理方式遍历数据集
- en: ② Feeds forward the model to compute scores
  id: totrans-231
  prefs: []
  type: TYPE_NORMAL
  zh: ② 将模型前向传播以计算分数
- en: ③ Computes the cross-entropy loss
  id: totrans-232
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 计算交叉熵损失
- en: ④ Clears the gradients accumulated from the previous step
  id: totrans-233
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 清除上一步累积的梯度
- en: ⑤ Runs backpropagation and computes the gradients
  id: totrans-234
  prefs: []
  type: TYPE_NORMAL
  zh: ⑤ 运行反向传播并计算梯度
- en: ⑥ Updates the weights
  id: totrans-235
  prefs: []
  type: TYPE_NORMAL
  zh: ⑥ 更新权重
- en: With this, we are ready to train our model. The following code shows how to
    do so. Figure [9.7](#fig-sgd-examples-model-pred) shows the output predictions
    and loss at the end of every epoch.
  id: totrans-236
  prefs: []
  type: TYPE_NORMAL
  zh: 这样，我们就准备好训练我们的模型了。以下代码展示了如何进行训练。图[9.7](#fig-sgd-examples-model-pred)显示了每个epoch结束时的输出预测和损失。
- en: '![](../../OEBPS/Images/CH09_F07_Chaudhury.png)'
  id: totrans-237
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F07_Chaudhury.png)'
- en: Figure 9.7 Model predictions at the end of every epoch. Notice how the loss
    is reduced with every epoch. In the beginning, all training data points are wrongly
    classified as class 1\. After the end of epoch 1, most of the training data is
    classified correctly, and the distribution of the classifier’s output has become
    close to the ground truth. The loss continues to decrease until epoch 4 although
    the improvements are harder to see visually).
  id: totrans-238
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.7 每个epoch结束时模型的预测。注意损失是如何随着每个epoch减少的。一开始，所有训练数据点都被错误地分类为类别1。在epoch 1结束后，大部分训练数据被正确分类，分类器输出的分布已经接近真实值。损失持续下降直到epoch
    4，尽管改进的视觉效果更难察觉）。
- en: Listing 9.11 PyTorch code to run the training loop `num_epochs` times
  id: totrans-239
  prefs: []
  type: TYPE_NORMAL
  zh: 列表9.11 运行训练循环`num_epochs`次的PyTorch代码
- en: '[PRE10]'
  id: totrans-240
  prefs: []
  type: TYPE_PRE
  zh: '[PRE10]'
- en: 9.2.4 Momentum
  id: totrans-241
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.4 动量
- en: For real-life loss surfaces in high dimensions, the ravine analogy is quite
    appropriate. A loss surface is hardly like a porcelain cup with smooth walls;
    it is much more like the walls of the Grand Canyon (see figure [9.8](#fig-grand-canyon-momentum)).
    Furthermore, the gradient estimate (usually done over a minibatch) is noisy. Consequently,
    gradient estimates are never aligned in one direction—they tend to go hither and
    thither. Nonetheless, most of them tend to have a good downhill component. The
    other (non-downhill component) is somewhat random (see figure [9.8](#fig-grand-canyon-momentum)).
    So if we average them, the downhill components reinforce each other and are strengthened,
    while the non-downhill components cancel each other and are weakened.
  id: totrans-242
  prefs: []
  type: TYPE_NORMAL
  zh: 对于高维度的现实损失表面，峡谷类比非常恰当。损失表面几乎不像一个有光滑壁的瓷杯；它更像是大峡谷的墙壁（见图[9.8](#fig-grand-canyon-momentum)）。此外，梯度估计（通常在minibatch上执行）是噪声的。因此，梯度估计永远不会在一个方向上对齐——它们往往四处游走。尽管如此，大多数梯度都有一个很好的下降分量。其他（非下降分量）是有些随机的（见图[9.8](#fig-grand-canyon-momentum)）。所以如果我们平均它们，下降分量会相互加强并得到增强，而非下降分量则会相互抵消并减弱。
- en: '![](../../OEBPS/Images/CH09_F08_Chaudhury.png)'
  id: totrans-243
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F08_Chaudhury.png)'
- en: Figure 9.8 Momentum. Noisy stochastic gradient estimates at different points
    on the loss surface (thick solid arrows) are not aligned in direction, but they
    all have a significant downhill component (thin solid arrows). The non-downhill
    components thin dashed arrows) point in random directions. Hence, averaging tends
    to strengthen the downhill component and cancel out the non-downhill components.
  id: totrans-244
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.8 动量。在损失表面不同点上的噪声随机梯度估计（粗实箭头）在方向上不一致，但它们都有一个显著的下降分量（细实箭头）。非下降分量（细虚线箭头）指向随机方向。因此，平均化往往加强下降分量并抵消非下降分量。
- en: Furthermore, if there is a small flat region with downhill areas preceding and
    following it, the vanilla gradient-based approach will get stuck in the small
    flat region (since the gradient there is zero). But averaging with the past allows
    us to have nonzero updates that take the optimization out of the local small flat
    region so that it can continue downhill.
  id: totrans-245
  prefs: []
  type: TYPE_NORMAL
  zh: 此外，如果存在一个小平坦区域，其前后都有下山区域，那么基于梯度的传统方法会陷入这个小平坦区域（因为那里的梯度为零）。但是，与过去的平均可以让我们有非零的更新，从而将优化从局部小平坦区域中带出，以便它可以继续下山。
- en: A good mental picture in this context is that of a ball rolling downhill. The
    surface of the hill is rough, and the ball is not going directly downward. Rather,
    it takes a zigzag path. But as it travels, it gathers downward momentum, and its
    downward velocity becomes greater.
  id: totrans-246
  prefs: []
  type: TYPE_NORMAL
  zh: 在这个背景下，一个很好的心理图像是一个球在山丘上滚下。山丘表面是粗糙的，球并不是直接向下滚动。相反，它采取了一个之字形路径。但随着它的移动，它聚集了向下的动量，其向下的速度变得更大。
- en: 'Following this theory, we take the weighted average of the gradient computed
    at this iteration with the update used in the previous iteration:'
  id: totrans-247
  prefs: []
  type: TYPE_NORMAL
  zh: 按照这个理论，我们取当前迭代计算的梯度与之前迭代中使用的更新的加权平均值：
- en: '![](../../OEBPS/Images/eq_09-11.png)'
  id: totrans-248
  prefs: []
  type: TYPE_IMG
  zh: '![方程 9.11 图](../../OEBPS/Images/eq_09-11.png)'
- en: Equation 9.11
  id: totrans-249
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.11
- en: where *γ*, *η* are positive constants with values less than 1. The weights and
    bias parameters are updated in the usual fashion using equation [9.9](#eq-optim-updates).
  id: totrans-250
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *γ*，*η* 是小于1的正常数。权重和偏差参数使用方程 [9.9](#eq-optim-updates) 以通常的方式更新。
- en: Unrolling the recursion of the momentum equation
  id: totrans-251
  prefs: []
  type: TYPE_NORMAL
  zh: 展开动量方程的递归
- en: Unraveling the recursive equation [9.11](#eq-momentum), we see
  id: totrans-252
  prefs: []
  type: TYPE_NORMAL
  zh: 展开递归方程 [9.11](#eq-momentum)，我们看到
- en: '![](../../OEBPS/Images/eq_09-11-a.png)'
  id: totrans-253
  prefs: []
  type: TYPE_IMG
  zh: '![方程 9.12 图](../../OEBPS/Images/eq_09-11-a.png)'
- en: Assuming *δ*![](../../OEBPS/Images/AR_w.png)[−1] = 0, we get
  id: totrans-254
  prefs: []
  type: TYPE_NORMAL
  zh: 假设 *δ*![逆矩阵](../../OEBPS/Images/AR_w.png)[−1] = 0，我们得到
- en: '![](../../OEBPS/Images/eq_09-12.png)'
  id: totrans-255
  prefs: []
  type: TYPE_IMG
  zh: '![泰勒展开图](../../OEBPS/Images/eq_09-12.png)'
- en: Equation 9.12
  id: totrans-256
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.12
- en: Thus,
  id: totrans-257
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，
- en: We are taking a weighted sum of the gradients from past iterations. This is
    not quite a weighted average, though, as explained later.
  id: totrans-258
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 我们正在对过去迭代的梯度进行加权求和。然而，这并不完全是一个加权平均，如稍后所述。
- en: Older gradients are weighted by higher powers of *γ*. Since *γ* < 1, weights
    decrease with age long-past iterations have less influence).
  id: totrans-259
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 较旧的梯度由 *γ* 的更高次幂加权。由于 *γ* < 1，权重会随着迭代次数的增加而减少（长期迭代的影响较小）。
- en: The sum of the weights of the gradients, going backward from now to the beginning
    of time (the 0th iteration), is
  id: totrans-260
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 从现在开始向时间开始（第0次迭代）的梯度权重之和是
- en: '![](../../OEBPS/Images/eq_09-12-a.png)'
  id: totrans-261
  prefs: []
  type: TYPE_IMG
  zh: '![方程 9.12 图](../../OEBPS/Images/eq_09-12-a.png)'
- en: Now, using Taylor expansion,
  id: totrans-262
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，使用泰勒展开，
- en: '![](../../OEBPS/Images/eq_09-12-b.png)'
  id: totrans-263
  prefs: []
  type: TYPE_IMG
  zh: '![方程 9.12 图](../../OEBPS/Images/eq_09-12-b.png)'
- en: Thus the sum of the weights of the past gradients in momentum-based gradient
    descent is *η*/(1–*γ*) ≠ 1. In other words, this is not quite a weighted average
    (where the weights should sum up to 1). This is a somewhat undesirable property
    and is rectified in the Adam algorithm discussed later.
  id: totrans-264
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，基于动量的梯度下降中过去梯度的权重之和为 *η*/(1–*γ*) ≠ 1。换句话说，这并不完全是一个加权平均（其中权重应加起来等于1）。这是一个有些不理想的特点，在稍后讨论的
    Adam 算法中得到纠正。
- en: A similar analysis can be done for the biases.
  id: totrans-265
  prefs: []
  type: TYPE_NORMAL
  zh: 可以对偏差进行类似的分析。
- en: '9.2.5 Geometric view: Constant loss contours, gradient descent, and momentum'
  id: totrans-266
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.5 几何视图：常量损失等高线、梯度下降和动量
- en: Consider a network with a tiny two-element weight vector ![](../../OEBPS/Images/eq_09-12-c2.png)
    and no bias. Further, suppose that the loss function is 𝕃 = ||![](../../OEBPS/Images/AR_w.png)||²
    = *w*[0]² + *w*[1]². The constant loss contours are concentric circles with the
    origin as the center. The radius of the circle indicates the loss magnitude.
  id: totrans-267
  prefs: []
  type: TYPE_NORMAL
  zh: 考虑一个具有微小的两个元素权重向量 ![权重向量](../../OEBPS/Images/eq_09-12-c2.png) 和没有偏差的网络。此外，假设损失函数是
    𝕃 = ||![权重矩阵](../../OEBPS/Images/AR_w.png)||² = *w*[0]² + *w*[1]²。常量损失等高线是以原点为中心的同心圆。圆的半径表示损失的大小。
- en: 'If we move along the circle’s circumference, the loss does not change. The
    loss changes maximally along the orthogonal direction to that: the radius of the
    circle. This intuitive observation is confirmed by evaluating the gradient'
  id: totrans-268
  prefs: []
  type: TYPE_NORMAL
  zh: 如果我们沿着圆的周长移动，损失不会改变。损失在垂直于该方向的正交方向上变化最大：圆的半径。这个直观的观察通过评估梯度得到证实。
- en: '![](../../OEBPS/Images/eq_09-12-d.png)'
  id: totrans-269
  prefs: []
  type: TYPE_IMG
  zh: '![方程 9.12 图](../../OEBPS/Images/eq_09-12-d.png)'
- en: Thus the gradient is along the radius, and the negative gradient direction is
    radially inward. So the loss decreases most rapidly if we move radially inward.
    If we move orthogonal to the radius (that is, along the circumference), the loss
    remains unchanged; of course, we are moving along the constant loss contour.
  id: totrans-270
  prefs: []
  type: TYPE_NORMAL
  zh: 因此梯度沿着半径方向，负梯度方向是径向向内的。所以如果我们径向向内移动，损失会迅速减少。如果我们垂直于半径移动（即沿着圆周），损失保持不变；当然，我们是在沿着恒定损失等高线移动。
- en: Optimization then takes us from outer, larger-radius circles to inner, smaller-radius
    circles. The minimum is at the origin; ideally, optimization should stop once
    we reach the origin.
  id: totrans-271
  prefs: []
  type: TYPE_NORMAL
  zh: 优化过程使我们从外部的较大半径圆逐渐过渡到内部的较小半径圆。最小值位于原点；理想情况下，一旦达到原点，优化就应该停止。
- en: Figure [9.9](#fig-graddesc-momentum-constant-loss-contours) shows optimization
    for a simple loss function 𝕃 = ||![](../../OEBPS/Images/AR_w.png)||² = *w*[0]²
    + *w*[1]². We start at an arbitrary pair of weight values and repeatedly update
    them via equation [9.9](#eq-optim-updates). For figure [9.9a](#fig-graddesc-momentum-constant-loss-contours),
    we use update without momentum (equation [9.10](#eq-optim-vanilla)). For figure
    [9.9b](#fig-graddesc-momentum-constant-loss-contours), we use update with momentum
    (equation [9.11](#eq-momentum)).
  id: totrans-272
  prefs: []
  type: TYPE_NORMAL
  zh: 图 [9.9](#fig-graddesc-momentum-constant-loss-contours) 展示了对于简单损失函数 𝕃 = ||![图片](../../OEBPS/Images/AR_w.png)||²
    = *w*[0]² + *w*[1]² 的优化过程。我们从任意一对权重值开始，并通过方程 [9.9](#eq-optim-updates) 重复更新它们。对于图
    [9.9a](#fig-graddesc-momentum-constant-loss-contours)，我们使用不带动量的更新（方程 [9.10](#eq-optim-vanilla)）。对于图
    [9.9b](#fig-graddesc-momentum-constant-loss-contours)，我们使用带动量的更新（方程 [9.11](#eq-momentum)）。
- en: '![](../../OEBPS/Images/CH09_F09a_Chaudhury.png)'
  id: totrans-273
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F09a_Chaudhury.png)'
- en: (a) A trajectory through constant loss contours for gradient descent *without*
    momentum
  id: totrans-274
  prefs: []
  type: TYPE_NORMAL
  zh: (a) 梯度下降不带动量时的恒定损失等高线轨迹
- en: '![](../../OEBPS/Images/CH09_F09b_Chaudhury.png)'
  id: totrans-275
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F09b_Chaudhury.png)'
- en: (b) A trajectory through constant loss contours for gradient descent *with*
    momentum
  id: totrans-276
  prefs: []
  type: TYPE_NORMAL
  zh: (b) 梯度下降带动量时的恒定损失等高线轨迹
- en: Figure 9.9 Constant loss contours and optimization trajectory for the loss function
    𝕃 = ||![](../../OEBPS/Images/AR_w.png)||² = *w*[0]² + *w*[1]². The loss surface
    is a cone with its apex on the origin and its base a circle in a plane parallel
    to the *w*[0], *w*[1] plane. Optimization takes us down the cone toward smaller
    and smaller cross-sections as we approach the minimum at the origin. Updates are
    shown with arrows. Note how the momentum version arrives at a smaller circle in
    fewer steps.
  id: totrans-277
  prefs: []
  type: TYPE_NORMAL
  zh: 图 9.9 损失函数 𝕃 = ||![图片](../../OEBPS/Images/AR_w.png)||² = *w*[0]² + *w*[1]² 的恒定损失等高线和优化轨迹。损失表面是一个以原点为顶点，其底面在
    *w*[0]，*w*[1] 平面平行平面上的圆。随着我们接近原点的最小值，优化过程沿着圆锥向越来越小的横截面下降。更新用箭头表示。注意动量版本如何以更少的步骤到达较小的圆。
- en: 'When the constant loss contours are concentric circles with the origin as the
    center, the loss surface is a cone with its apex on the origin. The cross sections
    are circles on planes parallel to the *w*[0], *w*[1] plane. Optimization takes
    us down the inner walls of the cone through smaller and smaller circular cross-sections
    as we approach the minimum. The global minimum is at the origin and corresponds
    to zero loss. The zero-loss contour is a circle with a zero radius, which is effectively
    a single point: the origin.'
  id: totrans-278
  prefs: []
  type: TYPE_NORMAL
  zh: 当恒定损失等高线是中心为原点的同心圆时，损失表面是一个以原点为顶点的圆锥。横截面是与 *w*[0]，*w*[1] 平面平行的圆。随着我们接近最小值，优化过程通过越来越小的圆形横截面沿着圆锥的内壁下降。全局最小值位于原点，对应于零损失。零损失等高线是一个半径为零的圆，实际上是一个单点：原点。
- en: 'In both cases, progress slows down (step sizes become smaller) as we approach
    the minimum. This is because the magnitude of the gradient becomes smaller and
    smaller as we get closer to the minimum (imagine a bowl: it becomes flatter as
    we get closer to the bottom). However, this effect is countered to a certain extent
    if we have momentum. So, we can see that we need fewer steps to reach a circle
    with a smaller radius when we have momentum.'
  id: totrans-279
  prefs: []
  type: TYPE_NORMAL
  zh: 在这两种情况下，随着我们接近最小值，进度会减慢（步长变得较小）。这是因为随着我们接近最小值，梯度的幅度会越来越小（想象一个碗：当我们接近底部时，它变得越平）。然而，如果我们有动量，这种效应在一定程度上会被抵消。因此，我们可以看到，当我们有动量时，到达较小半径的圆需要更少的步骤。
- en: 9.2.6 Nesterov accelerated gradients
  id: totrans-280
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.6 Nesterov 加速梯度
- en: One problem with momentum-based gradient descent is that it may overshoot the
    minimum. This can be seen in figure [9.10a](#fig-nesterov-momentum-constant-loss-contours)
    where the loss decreases through a series of updates and then, when we are close
    to the minimum, an update (shown with a dotted arrow) overshoots the minimum and
    increases the loss (shown with a dotted circle).
  id: totrans-281
  prefs: []
  type: TYPE_NORMAL
  zh: 基于动量的梯度下降的一个问题是它可能会越过最小值。这可以在图[9.10a](#fig-nesterov-momentum-constant-loss-contours)中看到，损失通过一系列更新而减少，然后，当我们接近最小值时，一个更新（用虚线箭头表示）越过最小值并增加了损失（用虚线圆表示）。
- en: '![](../../OEBPS/Images/CH09_F10a_Chaudhury.png)'
  id: totrans-282
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F10a_Chaudhury.png)'
- en: (a) Momentum gradient descent overshooting the minimum. Loss decreases for a
    while and then increases dotted circle and arrow)
  id: totrans-283
  prefs: []
  type: TYPE_NORMAL
  zh: (a) 动量梯度下降越过最小值。损失一度减少，然后增加（用虚线圆和箭头表示）
- en: '![](../../OEBPS/Images/CH09_F10b_Chaudhury.png)'
  id: totrans-284
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F10b_Chaudhury.png)'
- en: (b) Nesterov reduces the step size when we are about to overshoot the minimum.
  id: totrans-285
  prefs: []
  type: TYPE_NORMAL
  zh: (b) Nesterov在我们即将越过最小值时减小步长。
- en: Figure 9.10 Figure (a) shows momentum-based gradient descent overshooting the
    minimum. Over-shooting the update is shown with a dotted arrow. A circle with
    a radius larger than the last step is shown with a dotted outline. Nesterov does
    better by taking smaller steps when overshooting is imminent.
  id: totrans-286
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.10 图(a)显示了基于动量的梯度下降越过最小值。过冲更新用虚线箭头表示。一个半径大于上一步的圆用虚线轮廓表示。Nesterov在即将过冲时采取较小的步长，效果更好。
- en: This is the phenomenon that Nesterov’s accelerated gradient-based optimization
    tries to tackle.
  id: totrans-287
  prefs: []
  type: TYPE_NORMAL
  zh: 这是Nesterov加速梯度优化试图解决的问题。
- en: 'In Nesterov’s technique, we do the following:'
  id: totrans-288
  prefs: []
  type: TYPE_NORMAL
  zh: 在Nesterov的技术中，我们做以下操作：
- en: Estimate where this update will take us (that is, the destination point) by
    adding the previous step’s update to the current point.
  id: totrans-289
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 通过将上一步的更新加到当前点上，估计这次更新将带我们到何处（即目标点）。
- en: Compute the gradient at the estimated destination point. This is where the approach
    differs from the vanilla momentum-based approach, which takes the gradient at
    the current point.
  id: totrans-290
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 计算估计目标点的梯度。这与传统的基于动量的方法不同，后者取当前点的梯度。
- en: Take a weighted average of the gradient (at the estimated destination point)
    with the previous step’s update. That is the update for the current step.
  id: totrans-291
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 对梯度（在估计的目标点）和上一步的更新取加权平均值。这就是当前步的更新。
- en: Mathematically speaking,
  id: totrans-292
  prefs: []
  type: TYPE_NORMAL
  zh: 从数学上讲，
- en: '![](../../OEBPS/Images/eq_09-13.png)'
  id: totrans-293
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-13.png)'
- en: Equation 9.13
  id: totrans-294
  prefs: []
  type: TYPE_NORMAL
  zh: 方程9.13
- en: where *γ*, *η* are constants with values less than 1. The weights and biases
    are updated in the usual fashion using equation [9.9](#eq-optim-updates).
  id: totrans-295
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *γ*, *η* 是小于1的常数。权重和偏差使用方程[9.9](#eq-optim-updates)的常规方式更新。
- en: 'Why does this help? Well, consider the following:'
  id: totrans-296
  prefs: []
  type: TYPE_NORMAL
  zh: 这为什么有帮助呢？好吧，考虑以下内容：
- en: When we are somewhat far away from the minimum (no possibility of overshooting),
    the gradient at the estimated destination is more or less the same as the gradient
    at the current point, so we progress toward the minimum in a fashion similar to
    momentum-based gradient descent.
  id: totrans-297
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当我们离最小值有一定距离（没有过冲的可能性）时，估计目标点的梯度与当前点的梯度大致相同，因此我们以类似于基于动量的梯度下降的方式向最小值前进。
- en: But when we are close to the minimum and the current update may potentially
    take us past it (see the dotted arrow in figure [9.10a](#fig-nesterov-momentum-constant-loss-contours)),
    the gradient at the estimated destination will lie on the other side of the minimum.
    As before, imagine this loss surface as a cone with its apex at the origin and
    base above the apex. We have traveled down one of the cone’s side walls and just
    started climbing back up. Thus the gradient at the estimated destination is in
    the opposite direction from the previous step. When we take their weighted average,
    they will cancel each other in some dimensions, resulting in a smaller magnitude.
    The resulting smaller step will mitigate the overshooting phenomenon.
  id: totrans-298
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 但当我们接近最小值，并且当前的更新可能会使我们越过它（见图[9.10a](#fig-nesterov-momentum-constant-loss-contours)中的虚线箭头）时，估计目标点的梯度将位于最小值的另一侧。就像之前一样，想象这个损失表面是一个以原点为顶点、以顶点之上的基座为底部的圆锥。我们已经沿着圆锥的一侧壁向下移动，并刚刚开始向上爬。因此，估计目标点的梯度与上一步的方向相反。当我们取它们的加权平均值时，它们在某些维度上会相互抵消，导致幅度减小。结果，较小的步长将减轻过冲现象。
- en: NOTE Fully functional code for momentum and Nesterov accelerated gradients,
    executable via Jupyter notebook, can be found at [http://mng.bz/p9KR](http://mng.bz/p9KR).
  id: totrans-299
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：动量和Nesterov加速梯度的完整功能代码，可通过Jupyter笔记本执行，可以在[http://mng.bz/p9KR](http://mng.bz/p9KR)找到。
- en: 9.2.7 AdaGrad
  id: totrans-300
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.7 AdaGrad
- en: 'The momentum-based optimization approach (equation [9.11](#eq-momentum)) and
    the Nesterov approach (equation [9.13](#eq-nag)) both suffer from a serious drawback:
    they treat all dimensions of the parameter vectors ![](../../OEBPS/Images/AR_w.png),
    ![](../../OEBPS/Images/AR_b.png) equally. But the loss surface is *not* symmetrical
    in all dimensions. The slope can be high along some dimensions and low along others.
    We cannot control everything with a single LR for all the dimensions. If we set
    the LR high, the high-slope dimensions will exhibit too much variance. If we set
    it low, the low-slope dimensions will barely progress toward the minimum.'
  id: totrans-301
  prefs: []
  type: TYPE_NORMAL
  zh: 基于动量的优化方法（方程 [9.11](#eq-momentum)）和Nesterov方法（方程 [9.13](#eq-nag)）都存在一个严重的缺点：它们将参数向量
    ![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png) 的所有维度同等对待。但损失表面在所有维度上并不对称。某些维度的斜率可能很高，而其他维度的斜率可能很低。我们不能用单个学习率（LR）控制所有维度。如果我们设置LR较高，高斜率维度将表现出过多的方差。如果我们设置它较低，低斜率维度几乎无法向最小值方向进步。
- en: What we need is *per-parameter LR*. Then each LR will adapt to the slope of
    its particular dimension. This is what AdaGrad tries to achieve. Dimensions with
    historically larger gradients have smaller LRs, while dimensions with historically
    smaller gradients have larger LRs.
  id: totrans-302
  prefs: []
  type: TYPE_NORMAL
  zh: 我们需要的是**按参数的学习率**。然后每个学习率将适应其特定维度的斜率。这正是AdaGrad试图实现的目标。具有历史较大梯度的维度具有较小的学习率，而具有历史较小梯度的维度具有较大的学习率。
- en: 'How do we keep track of the historic magnitudes of gradients? To do this, AdaGrad
    maintains a state vector in which it accumulates the sum of the squared partial
    derivatives for each dimension of the gradient vector seen so far during training:'
  id: totrans-303
  prefs: []
  type: TYPE_NORMAL
  zh: 我们如何跟踪梯度历史大小？为了做到这一点，AdaGrad维护一个状态向量，在其中累积训练过程中看到的梯度向量的每个维度的平方偏导数的总和：
- en: '![](../../OEBPS/Images/eq_09-13-a.png)'
  id: totrans-304
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-13-a.png)'
- en: where ∘ denotes the Hadamard operator (elementwise multiplication of the two
    vectors). We can express the previous equation a bit more succinctly as
  id: totrans-305
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 ∘ 表示Hadamard算子（两个向量的逐元素乘积）。我们可以将前面的方程表达得更简洁一些：
- en: '![](../../OEBPS/Images/eq_09-13-b.png)'
  id: totrans-306
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-13-b.png)'
- en: Unrolling the recursion, we get
  id: totrans-307
  prefs: []
  type: TYPE_NORMAL
  zh: 展开递归，我们得到
- en: '![](../../OEBPS/Images/eq_09-13-c.png)'
  id: totrans-308
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-13-c.png)'
- en: 'assuming ![](../../OEBPS/Images/AR_s.png)[−1] = 0, *![](../../OEBPS/Images/AR_s.png)[t]*
    is a vector that holds the cumulative sum over all training iterations of the
    squared slope for each dimension. For the dimensions with historically high slopes,
    the corresponding element of *![](../../OEBPS/Images/AR_s.png)[t]* is large, and
    vice versa. The overall update vector looks like this:'
  id: totrans-309
  prefs: []
  type: TYPE_NORMAL
  zh: 假设 ![](../../OEBPS/Images/AR_s.png)[−1] = 0，*![](../../OEBPS/Images/AR_s.png)[t]*
    是一个向量，它保存了所有训练迭代中每个维度的平方斜率的累积总和。对于具有历史高斜率的维度，*![](../../OEBPS/Images/AR_s.png)[t]*
    中相应的元素较大，反之亦然。整体更新向量看起来是这样的：
- en: '![](../../OEBPS/Images/eq_09-14.png)'
  id: totrans-310
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-14.png)'
- en: Equation 9.14
  id: totrans-311
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.14
- en: Here *ϵ* is a very small constant added to prevent division by zero. Then we
    use equation [9.9](#eq-optim-updates) to update the weights as usual. A parallel
    set of equations exist for the bias.
  id: totrans-312
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *ϵ* 是一个非常小的常数，用于防止除以零。然后我们使用方程 [9.9](#eq-optim-updates) 通常是更新权重。对于偏差也存在一组平行的方程。
- en: Using AdaGrad, in the loss gradient, the dimensions that have seen big slopes
    in earlier iterations are given less importance during the update. We emphasize
    the dimensions that have not seen much progress in the past. This is a bit like
    saying, “I will pay less attention to a person who speaks all the time, and I
    will pay more attention to someone who speaks infrequently.”
  id: totrans-313
  prefs: []
  type: TYPE_NORMAL
  zh: 使用AdaGrad，在损失梯度中，在早期迭代中看到大斜率的维度在更新期间被赋予较少的重要性。我们强调那些在过去没有取得太多进展的维度。这有点像说，“我将减少对总是说话的人的关注，而将更多关注那些不常说话的人。”
- en: 9.2.8 Root-mean-squared propagation
  id: totrans-314
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.8 根均方传播
- en: A significant drawback of the AdaGrad algorithm is that the magnitude of the
    vector *![](../../OEBPS/Images/AR_s.png)[t]* keeps increasing as iteration progresses.
    This causes the LR for all dimensions to become smaller and smaller. Eventually,
    when the number of iterations is high, the LR becomes close to zero, and the updates
    do hardly anything; progress toward the minimum slows to a virtual halt. Thus
    AdaGrad is an impractical algorithm to use in real life, although the idea of
    per-component LR is good.
  id: totrans-315
  prefs: []
  type: TYPE_NORMAL
  zh: AdaGrad算法的一个显著缺点是向量 *![图片](../../OEBPS/Images/AR_s.png)[t]* 的幅度随着迭代的进行而不断增加。这导致所有维度的LR变得越来越小。最终，当迭代次数很高时，LR接近零，更新几乎不起作用；向最小值的进展几乎完全停止。因此，AdaGrad在现实生活中是一个不实用的算法，尽管按组件LR的想法是好的。
- en: Root mean squared propagation (RMSProp) addresses this drawback without sacrificing
    the dimension-adaptive nature of AdaGrad. Here again there is a state vector,
    but its equation is
  id: totrans-316
  prefs: []
  type: TYPE_NORMAL
  zh: 根均方传播（RMSProp）解决了这个缺点，而没有牺牲AdaGrad的维度自适应性质。在这里，同样有一个状态向量，但它的方程是
- en: '![](../../OEBPS/Images/eq_09-14-a.png)'
  id: totrans-317
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-14-a.png)'
- en: 'Compare this with the state vector equation in AdaGrad:'
  id: totrans-318
  prefs: []
  type: TYPE_NORMAL
  zh: 将其与AdaGrad的状态向量方程进行比较：
- en: '![](../../OEBPS/Images/eq_09-14-b.png)'
  id: totrans-319
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-14-b.png)'
- en: 'They are almost the same, but the terms are weighted by (1−*γ*) and *γ*, where
    0 < *γ* < 1 is a constant. This particular pair of weights in a recursive equation
    has a very interesting effect. To see it, we have to unroll the recursion:'
  id: totrans-320
  prefs: []
  type: TYPE_NORMAL
  zh: 它们几乎相同，但项被（1−*γ*）和*γ*加权，其中0 < *γ* < 1是一个常数。这个递归方程中的特定权重对具有一个非常有趣的效果。为了看到它，我们必须展开递归：
- en: '![](../../OEBPS/Images/eq_09-14-c.png)'
  id: totrans-321
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-14-c.png)'
- en: Thus *![](../../OEBPS/Images/AR_s.png)[t]* is a weighted sum of the past term-wise
    squared gradient magnitude vectors. Going back from now to the beginning of time
    (the 0th iteration), the weights are (1−*γ*), (1−*γ*) *γ*, (1−*γ*) *γ*², ⋯, (1−*γ*)
    *γ^t*. If we add these weights, we get
  id: totrans-322
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，*![图片](../../OEBPS/Images/AR_s.png)[t]* 是过去项梯度平方幅度向量的加权总和。从现在开始回溯到时间的起点（第0次迭代），权重为（1−*γ*），（1−*γ*）*γ*，（1−*γ*）*γ*²，⋯，（1−*γ*）*γ^t*。如果我们加上这些权重，我们得到
- en: '![](../../OEBPS/Images/eq_09-14-d.png)'
  id: totrans-323
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-14-d.png)'
- en: As the number of iterations becomes high (*t* → ∞), this becomes
  id: totrans-324
  prefs: []
  type: TYPE_NORMAL
  zh: 当迭代次数变得很高（*t* → ∞）时，这变成
- en: '![](../../OEBPS/Images/eq_09-14-e.png)'
  id: totrans-325
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-14-e.png)'
- en: where Taylor expansion has been used to evaluate the term in parentheses.
  id: totrans-326
  prefs: []
  type: TYPE_NORMAL
  zh: 其中使用了泰勒展开来评估括号中的项。
- en: For a large number of iterations, the sum of the weights approaches 1. This
    implies that after many iterations, the RMSProp state vector effectively takes
    the *weighted average of the past term-wise squared gradient magnitude vectors*.
    With more iterations, the weights are redistributed and older terms become de-emphasized,
    but the overall magnitude does not increase. This eliminates the vanishing LR
    problem from AdaGrad. RMSProp continues de-emphasizing the dimensions with high
    cumulative partial derivatives with lower LRs, but it does so without making the
    LR vanishingly small.
  id: totrans-327
  prefs: []
  type: TYPE_NORMAL
  zh: 对于大量的迭代，权重的总和趋近于1。这意味着在多次迭代之后，RMSProp状态向量实际上取了过去项梯度平方幅度向量的*加权平均*。随着更多迭代的进行，权重被重新分配，较旧的项被弱化，但整体幅度不会增加。这消除了AdaGrad中的消失LR问题。RMSProp继续降低具有高累积偏导数的维度的LR，但这样做不会使LR变得无限小。
- en: The overall RMSProp update equations are
  id: totrans-328
  prefs: []
  type: TYPE_NORMAL
  zh: RMSProp的整体更新方程是
- en: '![](../../OEBPS/Images/eq_09-15.png)'
  id: totrans-329
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-15.png)'
- en: Equation 9.15
  id: totrans-330
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式9.15
- en: There is a parallel set of equations for the bias. The weights and bias parameters
    are updated in the usual fashion using equation [9.9](#eq-optim-updates).
  id: totrans-331
  prefs: []
  type: TYPE_NORMAL
  zh: 对于偏差也有一个平行的方程组。权重和偏差参数使用方程[9.9](#eq-optim-updates)以通常的方式更新。
- en: 9.2.9 Adam optimizer
  id: totrans-332
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.2.9 Adam优化器
- en: Momentum-based gradient descent amplifies the downhill component more and more
    as iterations progress. On the other hand, RMSProp reduces the LR for dimensions
    that are seeing large gradients and vice versa to balance the progress rate along
    all dimensions.
  id: totrans-333
  prefs: []
  type: TYPE_NORMAL
  zh: 基于动量的梯度下降法随着迭代的进行，越来越增强下坡分量。另一方面，RMSProp会减少看到大梯度的维度的学习率（LR），反之亦然，以平衡所有维度的进度率。
- en: Both of these are desirable properties. We want an optimization algorithm that
    combines them, and that algorithm is Adam. It is increasingly becoming the optimizer
    of choice for most deep learning researchers.
  id: totrans-334
  prefs: []
  type: TYPE_NORMAL
  zh: 这两种都是理想性质。我们希望有一个优化算法能够结合它们，而这个算法就是Adam。它正越来越多地成为大多数深度学习研究者的首选优化器。
- en: 'The Adam optimization algorithm maintains two state vectors:'
  id: totrans-335
  prefs: []
  type: TYPE_NORMAL
  zh: Adam优化算法维护两个状态向量：
- en: '![](../../OEBPS/Images/eq_09-16.png)'
  id: totrans-336
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-16.png)'
- en: Equation 9.16
  id: totrans-337
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.16
- en: '![](../../OEBPS/Images/eq_09-17.png)'
  id: totrans-338
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-17.png)'
- en: Equation 9.17
  id: totrans-339
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.17
- en: 'where 0 < *β*[1] < 1 and 0 < *β*[2] < 1 are constants. Note the following:'
  id: totrans-340
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 0 < *β*[1] < 1 和 0 < *β*[2] < 1 是常数。注意以下内容：
- en: Equation [9.16](#eq-adam-vt) is essentially the momentum equation of equation
    [9.11](#eq-momentum) with one significant difference. We have changed the term
    weights to *β*[1] and (1−*β*[1]). As we’ve seen, with *t* → ∞, this results in
    the state vector being a weighted average of all the past gradients. This is an
    improvement over the original momentum scheme.
  id: totrans-341
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 方程 [9.16](#eq-adam-vt) 实质上是方程 [9.11](#eq-momentum) 的动量方程，有一个显著的区别。我们将权重项改为 *β*[1]
    和 (1−*β*[1])。正如我们所见，随着 *t* → ∞，这导致状态向量成为所有过去梯度的加权平均值。这比原始动量方案有所改进。
- en: The second state vector is basically the one from the RMSProp equation [9.15](#eq-rmsprop).
  id: totrans-342
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 第二个状态向量基本上是RMSProp方程 [9.15](#eq-rmsprop) 中的那个。
- en: 'Using these two state vectors, Adam creates the update vector as follows:'
  id: totrans-343
  prefs: []
  type: TYPE_NORMAL
  zh: 使用这两个状态向量，Adam创建更新向量如下：
- en: '![](../../OEBPS/Images/eq_09-18.png)'
  id: totrans-344
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-18.png)'
- en: Equation 9.18
  id: totrans-345
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.18
- en: The ![](../../OEBPS/Images/AR_v.png)*[t]* in the numerator pulls in the benefits
    of the momentum approach (with the enhancement of averaging). Otherwise, the equation
    is almost identical to the RMSProp; those benefits are also pulled in.
  id: totrans-346
  prefs: []
  type: TYPE_NORMAL
  zh: 分子中的 ![](../../OEBPS/Images/AR_v.png)*[t]* 吸收了动量方法（平均增强）的好处。否则，方程几乎与RMSProp相同；这些好处也被吸收了。
- en: Finally, the weights and bias parameters are updated in the usual fashion using
    equation [9.9](#eq-optim-updates).
  id: totrans-347
  prefs: []
  type: TYPE_NORMAL
  zh: 最后，使用方程 [9.9](#eq-optim-updates) 以常规方式更新权重和偏差参数。
- en: Bias correction
  id: totrans-348
  prefs: []
  type: TYPE_NORMAL
  zh: 偏差校正
- en: 'The sum of weights of past values in the state vectors ![](../../OEBPS/Images/AR_v.png)*[t]*,
    *![](../../OEBPS/Images/AR_s.png)[t]* will approach ∞ only at large values of
    *t*. To improve the approximation at smaller values of *t*, Adam introduces a
    bias correction:'
  id: totrans-349
  prefs: []
  type: TYPE_NORMAL
  zh: 状态向量中过去值的权重之和 ![](../../OEBPS/Images/AR_v.png)*[t]*, *![](../../OEBPS/Images/AR_s.png)[t]*
    只有在 *t* 的较大值时才会趋近于 ∞。为了改善 *t* 较小值时的近似，Adam引入了偏差校正：
- en: '![](../../OEBPS/Images/eq_09-16.png)'
  id: totrans-350
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-16.png)'
- en: Equation 9.19
  id: totrans-351
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.19
- en: '![](../../OEBPS/Images/eq_09-20.png)'
  id: totrans-352
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-20.png)'
- en: Equation 9.20
  id: totrans-353
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.20
- en: Instead of ![](../../OEBPS/Images/AR_v.png)*[t]* and *![](../../OEBPS/Images/AR_s.png)[t]*,
    we use the bias-corrected entities *v̂[t]* and *ŝ[t]* in equation [9.18](#eq-adam-update).
  id: totrans-354
  prefs: []
  type: TYPE_NORMAL
  zh: 我们使用偏差校正的实体 *v̂[t]* 和 *ŝ[t]* 而不是 *![](../../OEBPS/Images/AR_v.png)*[t]* 和 *![](../../OEBPS/Images/AR_s.png)*[t]*，在方程
    [9.18](#eq-adam-update) 中。
- en: Listing 9.12 PyTorch code for various optimizers
  id: totrans-355
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 9.12 为各种优化器提供的PyTorch代码
- en: '[PRE11]'
  id: totrans-356
  prefs: []
  type: TYPE_PRE
  zh: '[PRE11]'
- en: ① Sets the learning rate to 0.01
  id: totrans-357
  prefs: []
  type: TYPE_NORMAL
  zh: ① 将学习率设置为0.01
- en: ② Sets the momentum to 0.9
  id: totrans-358
  prefs: []
  type: TYPE_NORMAL
  zh: ② 将动量设置为0.9
- en: ③ Sets the Nesterov flag to True
  id: totrans-359
  prefs: []
  type: TYPE_NORMAL
  zh: ③ 将Nesterov标志设置为True
- en: ④ Sets the smoothing constant to 0.99 (*γ* in [9.15](#eq-rmsprop))
  id: totrans-360
  prefs: []
  type: TYPE_NORMAL
  zh: ④ 将平滑常数设置为0.99 (*γ* 在 [9.15](#eq-rmsprop))
- en: 9.3 Regularization
  id: totrans-361
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 9.3 正则化
- en: 'Suppose we are teaching a baby to recognize cars. We show them red cars, blue
    cars, black cars, large cars, small cars, medium cars, cars with round tops, cars
    with rectangular tops, and so on. Soon the baby’s brain realizes that there are
    too many varieties to remember them all by rote. So the brain starts forming *abstractions*:
    mysterious common features that occur together are stored in the baby’s brain
    with the label *car*. The brain has learned to classify an abstract entity called
    a car. Even though it fails to *remember* every car it has seen, it can *recognize*
    cars. We can say it has developed *experience*. And so it is with neural networks.
    We do not want our network to *remember* every training instance. Rather, we want
    the network to form *abstractions* that will enable it to *recognize* an object
    during inferencing even though the exact likeness of the object instance encountered
    during inferencing was never seen during training.'
  id: totrans-362
  prefs: []
  type: TYPE_NORMAL
  zh: 假设我们正在教一个婴儿识别汽车。我们向他们展示红色汽车、蓝色汽车、黑色汽车、大汽车、小汽车、中等汽车、圆顶汽车、矩形顶汽车等等。很快，婴儿的大脑意识到有太多种类难以全部记住。所以大脑开始形成
    *抽象*：神秘的共同特征被存储在婴儿的大脑中，并带有 *汽车* 这个标签。大脑已经学会了分类一个名为汽车的抽象实体。尽管它无法 *记住* 它所看到的每一辆汽车，但它可以
    *识别* 汽车。我们可以说它已经发展了 *经验*。神经网络也是如此。我们不想我们的网络记住每一个训练实例。相反，我们希望网络形成 *抽象*，这样它就能在推理过程中识别对象，即使训练过程中从未见过推理过程中遇到的对象实例的精确相似性。
- en: Overfitting and underfitting
  id: totrans-363
  prefs: []
  type: TYPE_NORMAL
  zh: 过拟合和欠拟合
- en: If the network has too much expressive power (too many perceptrons or, equivalently,
    too many weights) relative to the number of training instances, the network can
    and often will rote-remember the training instances. This phenomenon is called
    *overfitting*. Overfitting causes the network to perform very well over the training
    data but badly during testing or real-life inferencing. Stated another way, the
    network has adjusted itself to every nook, bend, and kink of the training data
    and thereby performs great on the training data—to the detriment of test data
    performance. This is illustrated in figure [9.11](#fig-overfitting). *Regularization*
    refers to a bag of tricks that, in general, try to prevent overfitting. This is
    the topic of this section.
  id: totrans-364
  prefs: []
  type: TYPE_NORMAL
  zh: 如果网络相对于训练实例的数量具有过多的表达能力（太多感知器或，等价地，太多权重），则网络可以并且通常会死记硬背训练实例。这种现象称为*过拟合*。过拟合导致网络在训练数据上表现非常好，但在测试或实际推理中表现很差。换句话说，网络已经调整自己以适应训练数据的每一个角落、弯道和曲折，因此在训练数据上表现良好——损害了测试数据的表现。这如图[9.11](#fig-overfitting)所示。*正则化*指的是一系列技巧，通常试图防止过拟合。这是本节的主题。
- en: There is another phenomenon called *underfitting*, where the network simply
    does not have enough expressive power to model the training data. The symptom
    of underfitting is that the network performs badly on both training and testing
    data. If we see this, we should try a more complex network with more perceptrons.
  id: totrans-365
  prefs: []
  type: TYPE_NORMAL
  zh: 另有一种现象称为*欠拟合*，其中网络简单地没有足够的表达能力来模拟训练数据。欠拟合的症状是网络在训练和测试数据上表现都差。如果我们看到这种情况，我们应该尝试一个更复杂的网络，具有更多的感知器。
- en: '![](../../OEBPS/Images/CH09_F11_Chaudhury.png)'
  id: totrans-366
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F11_Chaudhury.png)'
- en: 'Figure 9.11 Overfitting: data points for a binary classifier. Points belonging
    to different classes are visually demarcated as squares and circles. Filled squares/circles
    indicate training data, and unfilled squares/circles indicate test data. There
    are some anomalous training data instances (filled circles in the square zone).
    The estimated decision boundary (solid line) has become wriggly to accommodate
    them, which is causing many test points (unfilled squares/circles) to be misclassified.
    The wiggly solid curve is an example of an overfitted decision boundary. If we
    had chosen a “simpler” decision boundary (dashed straight line), the two anomalous
    training points would have been misclassified, but the machine would have performed
    much better in tests.'
  id: totrans-367
  prefs: []
  type: TYPE_NORMAL
  zh: 图9.11 过拟合：二元分类器的数据点。属于不同类别的点被视觉上划分为正方形和圆形。实心正方形/圆形表示训练数据，空心正方形/圆形表示测试数据。有一些异常的训练数据实例（正方形区域中的实心圆圈）。估计的决策边界（实线）变得扭曲以适应它们，这导致许多测试点（空心正方形/圆形）被错误分类。扭曲的实线曲线是一个过拟合决策边界的例子。如果我们选择一个“更简单”的决策边界（虚线直线），两个异常训练点将被错误分类，但机器在测试中的表现会更好。
- en: '9.3.1 Minimum descriptor length: An Occam’s razor view of optimization'
  id: totrans-368
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.3.1 最小描述符长度：奥卡姆剃刀原理看优化
- en: Is the set of weights and biases that minimizes a particular loss function unique?
    Let’s examine a single perceptron (equation [7.3](../Text/07.xhtml#eq-perceptron))
    with the output *ϕ*(![](../../OEBPS/Images/AR_w.png)*^T*![](../../OEBPS/Images/AR_x.png)
    + *b*). Let’s say *ϕ* is the Heaviside step function (see section [7.3.1](../Text/07.xhtml#step-func)).
    Let ![](../../OEBPS/Images/AR_w.png)[*], *b*[*] be the weights and biases minimizing
    the loss function. It is easy to see that the perceptron output remains the same
    if we scale the weights, like *α*![](../../OEBPS/Images/AR_w.png)[*] for all positive
    real values of *α*. Thus the weight vector 7![](../../OEBPS/Images/AR_w.png)[*]
    will also minimize the loss function.
  id: totrans-369
  prefs: []
  type: TYPE_NORMAL
  zh: 那些最小化特定损失函数的权重和偏置集是唯一的吗？让我们考察一个单个感知器（方程[7.3](../Text/07.xhtml#eq-perceptron)）的输出
    *ϕ*(![](../../OEBPS/Images/AR_w.png)*^T*![](../../OEBPS/Images/AR_x.png) + *b*）。假设
    *ϕ* 是Heaviside阶跃函数（见[7.3.1](../Text/07.xhtml#step-func)节）。假设 ![](../../OEBPS/Images/AR_w.png)[*],
    *b*[*] 是最小化损失函数的权重和偏置。很容易看出，如果我们对权重进行缩放，例如 *α*![](../../OEBPS/Images/AR_w.png)[*]
    对于所有正实数 *α*，感知器的输出将保持不变。因此，权重向量 7![](../../OEBPS/Images/AR_w.png)[*] 也将最小化损失函数。
- en: 'This is true in general for arbitrary neural networks (composed of many perceptrons):
    the set of weights and biases minimizing a loss function is non-unique. How does
    the neural network choose one? Which of them is correct? We can use the principle
    of Occam’s razor to answer that.'
  id: totrans-370
  prefs: []
  type: TYPE_NORMAL
  zh: 这对于任意神经网络（由许多感知器组成）通常都是正确的：最小化损失函数的权重和偏置集不是唯一的。神经网络如何选择一个？哪一个才是正确的？我们可以使用奥卡姆剃刀原理来回答这个问题。
- en: 'Occam’s razor is a philosophical principle. Its literal translation from Latin
    states, “Entities should not be multiplied beyond necessity.” This is roughly
    taken to mean *among adequate explanations, the simplest one is the best*. In
    machine learning, this principle is typically interpreted as follows:'
  id: totrans-371
  prefs: []
  type: TYPE_NORMAL
  zh: 奥卡姆剃刀是一个哲学原则。其拉丁文的直译为，“实体不应超出必要性而增加。”这大致意味着 *在充分的解释中，最简单的一个是最好的*。在机器学习中，这个原则通常被解释如下：
- en: Among the set of candidate neural network parameter values (weights and biases)
    that minimize the loss, the “simplest” one should be chosen.
  id: totrans-372
  prefs: []
  type: TYPE_NORMAL
  zh: 在使损失最小化的候选神经网络参数值（权重和偏差）集合中，“最简单”的一个应该被选择。
- en: The general idea is as follows. Suppose we are trying to minimize 𝕃(*θ*) (here
    we have used *θ* to denote ![](../../OEBPS/Images/AR_w.png) and ![](../../OEBPS/Images/AR_b.png)
    together). We also want the solution to be as simple as possible. To achieve that,
    we add a penalty for departing from “simplicity” to the original loss term. Thus
    we minimize
  id: totrans-373
  prefs: []
  type: TYPE_NORMAL
  zh: 一般思路如下。假设我们正在尝试最小化 𝕃(*θ*)（在这里我们用 *θ* 来表示 ![](../../OEBPS/Images/AR_w.png) 和
    ![](../../OEBPS/Images/AR_b.png) 的组合）。我们还希望解决方案尽可能简单。为了实现这一点，我们在原始损失项中添加了对“简单性”偏离的惩罚。因此，我们最小化
- en: 𝕃(*θ*) + *λR*(*θ*)
  id: totrans-374
  prefs: []
  type: TYPE_NORMAL
  zh: 𝕃(*θ*) + *λR*(*θ*)
- en: Here,
  id: totrans-375
  prefs: []
  type: TYPE_NORMAL
  zh: 这里，
- en: The expression *R*(*θ*) indicates a measure for un-simplicity. It is sometimes
    called the *regularization penalty*. Adding a regularization penalty to the loss
    incentivizes the network to try to minimize un-simplicity *R*(*θ*) (or, equivalently,
    maximize simplicity) while trying to minimize the original loss term 𝕃(*θ*).
  id: totrans-376
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 表达式 *R*(*θ*) 表示不简单性的度量。有时被称为 *正则化惩罚*。将正则化惩罚添加到损失中，激励网络在尝试最小化原始损失项 𝕃(*θ*) 的同时，尝试最小化不简单性
    *R*(*θ*)（或者，等价地，最大化简单性）。
- en: '*λ* is a hyperparameter. Its value should be carefully chosen via trial and
    error. If *λ* is too low, this is akin to no regularization, and the network becomes
    prone to overfitting. If *λ* is too high, the regularization penalty will dominate,
    and the network will not adequately minimize the actual loss term.'
  id: totrans-377
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '*λ* 是一个超参数。其值应通过试错仔细选择。如果 *λ* 太低，这类似于没有正则化，网络容易过拟合。如果 *λ* 太高，正则化惩罚将占主导地位，网络将不足以最小化实际的损失项。'
- en: There are two popular ways of estimating *R*(*θ*), outlined in sections [9.3.2](#sec-l2-reg)
    and [9.3.3](#sec-l1-reg), respectively. Both try to minimize some norm (length)
    of the parameter vector (which is basically a network descriptor). This is why
    regularization can be viewed as minimizing the descriptor length.
  id: totrans-378
  prefs: []
  type: TYPE_NORMAL
  zh: 有两种流行的估计 *R*(*θ*) 的方法，分别在 [9.3.2](#sec-l2-reg) 和 [9.3.3](#sec-l1-reg) 节中概述。两者都试图最小化参数向量（基本上是网络描述符）的某些范数（长度）。这就是为什么正则化可以被视为最小化描述符长度。
- en: 9.3.2 L2 regularization
  id: totrans-379
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.3.2 L2 正则化
- en: In L2 regularization, we posit that shorter-length vectors are simpler. In other
    words, simplicity is inversely proportional to the square of the L2 norm (aka
    Euclidean norm). Thus,
  id: totrans-380
  prefs: []
  type: TYPE_NORMAL
  zh: 在 L2 正则化中，我们假设短向量更简单。换句话说，简单性与 L2 范数（也称为欧几里得范数）的平方成反比。因此，
- en: '*R*(*θ*) = (||![](../../OEBPS/Images/AR_w.png)||²+||![](../../OEBPS/Images/AR_b.png)||²)'
  id: totrans-381
  prefs: []
  type: TYPE_NORMAL
  zh: '*R*(*θ*) = (||![](../../OEBPS/Images/AR_w.png)||²+||![](../../OEBPS/Images/AR_b.png)||²)'
- en: Overall, we minimize
  id: totrans-382
  prefs: []
  type: TYPE_NORMAL
  zh: 总体来说，我们最小化
- en: '![](../../OEBPS/Images/eq_09-21.png)'
  id: totrans-383
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-21.png)'
- en: Equation 9.21
  id: totrans-384
  prefs: []
  type: TYPE_NORMAL
  zh: 方程 9.21
- en: Compare this with equation [9.2](#eq-loss).
  id: totrans-385
  prefs: []
  type: TYPE_NORMAL
  zh: 将其与方程 [9.2](#eq-loss) 进行比较。
- en: L2 regularization is by far the most popular form of regularization. From this
    point on, we often use 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    to mean the L2-regularized version (that is, equation [9.21](#eq-l2-reg)). The
    hyperparameter *λ* is often called *weight decay* in PyTorch. Weight decay is
    usually set to a small number so that the second term of equation [9.21](#eq-l2-reg)
    the norm of the weight vector) does not drown the actual loss term. The following
    code shows how to instantiate an optimizer with regularization enabled.
  id: totrans-386
  prefs: []
  type: TYPE_NORMAL
  zh: L2 正则化到目前为止是最受欢迎的正则化形式。从现在开始，我们通常用 𝕃(![](../../OEBPS/Images/AR_w.png), ![](../../OEBPS/Images/AR_b.png))
    来表示 L2 正则化的版本（即方程 [9.21](#eq-l2-reg)）。超参数 *λ* 在 PyTorch 中通常被称为 *权重衰减*。权重衰减通常设置为一个很小的数字，这样方程
    [9.21](#eq-l2-reg) 的第二项（权重向量的范数）就不会淹没实际的损失项。以下代码展示了如何实例化一个启用了正则化的优化器。
- en: Listing 9.13 PyTorch code to enable L2 regularization
  id: totrans-387
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 9.13 启用 L2 正则化的 PyTorch 代码
- en: '[PRE12]'
  id: totrans-388
  prefs: []
  type: TYPE_PRE
  zh: '[PRE12]'
- en: ① Sets the weight decay to 0.01
  id: totrans-389
  prefs: []
  type: TYPE_NORMAL
  zh: ① 将权重衰减设置为 0.01
- en: '![](../../OEBPS/Images/CH09_F12a_Chaudhury.png)'
  id: totrans-390
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F12a_Chaudhury.png)'
- en: (a) L1 regularization
  id: totrans-391
  prefs: []
  type: TYPE_NORMAL
  zh: (a) L1 正则化
- en: '![](../../OEBPS/Images/CH09_F12b_Chaudhury.png)'
  id: totrans-392
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F12b_Chaudhury.png)'
- en: (b) L2 regularization
  id: totrans-393
  prefs: []
  type: TYPE_NORMAL
  zh: (b) L2 正则化
- en: Figure 9.12 L1 and L2 regularization
  id: totrans-394
  prefs: []
  type: TYPE_NORMAL
  zh: 图 9.12 L1 和 L2 正则化
- en: 9.3.3 L1 regularization
  id: totrans-395
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.3.3 L1 正则化
- en: 'L1 regularization is similar in principle to L2 regularization, but it defines
    simplicity as the sum of the absolute values of the weights and biases:'
  id: totrans-396
  prefs: []
  type: TYPE_NORMAL
  zh: L1 正则化在原则上与 L2 正则化相似，但它将简单性定义为权重和偏置的绝对值之和：
- en: '*R*(*θ*) = (|![](../../OEBPS/Images/AR_w.png)|+|![](../../OEBPS/Images/AR_b.png)|)'
  id: totrans-397
  prefs: []
  type: TYPE_NORMAL
  zh: '*R*(*θ*) = (|![图片](../../OEBPS/Images/AR_w.png)|+|![图片](../../OEBPS/Images/AR_b.png)|)'
- en: Thus, here we minimize
  id: totrans-398
  prefs: []
  type: TYPE_NORMAL
  zh: 因此，这里我们最小化
- en: '![](../../OEBPS/Images/eq_09-22.png)'
  id: totrans-399
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-22.png)'
- en: Equation 9.22
  id: totrans-400
  prefs: []
  type: TYPE_NORMAL
  zh: 方程式 9.22
- en: '9.3.4 Sparsity: L1 vs. L2 regularization'
  id: totrans-401
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.3.4 稀疏性：L1 与 L2 正则化
- en: L1 regularization tends to create sparse models where many of the weights are
    0\. In comparison, L2 regularization tends to create models with low (but nonzero)
    weights.
  id: totrans-402
  prefs: []
  type: TYPE_NORMAL
  zh: L1 正则化倾向于创建稀疏模型，其中许多权重为 0。相比之下，L2 正则化倾向于创建具有低（但非零）权重的模型。
- en: To understand this, consider figure [9.12](#fig-l1-l2-reg), which plots the
    loss function and its derivative for both L1 and L2 regularization. Let *w* be
    a single element of the weight vector ![](../../OEBPS/Images/AR_w.png). In L1
    regularization,
  id: totrans-403
  prefs: []
  type: TYPE_NORMAL
  zh: 要理解这一点，请考虑图 [9.12](#fig-l1-l2-reg)，该图绘制了 L1 和 L2 正则化下的损失函数及其导数。设 *w* 为权重向量 ![图片](../../OEBPS/Images/AR_w.png)
    的单个元素。在 L1 正则化中，
- en: '![](../../OEBPS/Images/eq_09-22-a.png)'
  id: totrans-404
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-22-a.png)'
- en: Since the gradient is constant for all values of *w*, L1 regularization pushes
    the weight toward 0 with the same step size at all values of *w*. In particular,
    the step toward 0 does not reduce in magnitude when close to 0\. In L2 regularization,
  id: totrans-405
  prefs: []
  type: TYPE_NORMAL
  zh: 由于梯度对所有 *w* 的值都是常数，L1 正则化在所有 *w* 的值上以相同的步长将权重推向 0。特别是，当接近 0 时，向 0 的步长不会减小其幅度。在
    L2 正则化中，
- en: '![](../../OEBPS/Images/eq_09-22-b.png)'
  id: totrans-406
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-22-b.png)'
- en: Here, the gradient keeps decreasing in magnitude as *w* approaches 0\. Hence,
    *w* comes closer to 0 but may never reach 0 since the updates take smaller and
    smaller steps as *w* approaches 0\. Therefore, L2 regularization produces more
    dense weight vectors than L1 regularization, which produces sparse weight vectors
    with many 0s.
  id: totrans-407
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，随着 *w* 接近 0，梯度的大小持续减小。因此，*w* 越来越接近 0，但由于更新步骤随着 *w* 接近 0 而变得越来越小，它可能永远不会达到
    0。因此，L2 正则化产生的权重向量比 L1 正则化更密集，而 L1 正则化产生的权重向量稀疏，包含许多 0。
- en: 9.3.5 Bayes’ theorem and the stochastic view of optimization
  id: totrans-408
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.3.5 贝叶斯定理和优化的随机视图
- en: In sections [6.6.2](../Text/06.xhtml#sec-max_likelihood_estimation) and [6.6.3](../Text/06.xhtml#sec-MAP_estimation),
    we discussed maximum likelihood estimation (MLE) and maximum a posteriori (MAP)
    in the context of unsupervised learning (you are encouraged to revisit those sections
    if necessary). Here, we study them in the context of supervised learning.
  id: totrans-409
  prefs: []
  type: TYPE_NORMAL
  zh: 在章节 [6.6.2](../Text/06.xhtml#sec-max_likelihood_estimation) 和 [6.6.3](../Text/06.xhtml#sec-MAP_estimation)
    中，我们讨论了无监督学习中的最大似然估计（MLE）和最大后验（MAP）（如果需要，请鼓励您回顾这些章节）。在这里，我们将在监督学习的背景下研究它们。
- en: In the supervised optimization process, we have samples of the input and known
    output pairs (in the form of training data) ⟨![](../../OEBPS/Images/AR_x.png)^((*i*)),
    *ȳ*^((*i*))⟩. Of course, we can do the forward pass and generate the network output
    at each training data point
  id: totrans-410
  prefs: []
  type: TYPE_NORMAL
  zh: 在监督优化过程中，我们有输入和已知输出对的样本（以训练数据的形式）⟨![图片](../../OEBPS/Images/AR_x.png)^((*i*)),
    *ȳ*^((*i*))⟩。当然，我们可以在每个训练数据点上执行前向传递并生成网络输出
- en: '![](../../OEBPS/Images/eq_09-22-c.png)'
  id: totrans-411
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-22-c.png)'
- en: where *θ* is the network’s parameter set (representing weights and biases together).
  id: totrans-412
  prefs: []
  type: TYPE_NORMAL
  zh: 其中 *θ* 是网络的参数集（代表权重和偏置的总和）。
- en: Suppose we view the sample training data generation as a stochastic process.
    We can model the probability of a training instance *T*^((*i*)) = ⟨*ȳ*^((*i*)),
    ![](../../OEBPS/Images/AR_x.png)^((*i*))⟩ given the current network parameters
    *θ* as
  id: totrans-413
  prefs: []
  type: TYPE_NORMAL
  zh: 假设我们将样本训练数据生成视为一个随机过程。我们可以根据当前的网络参数 *θ* 建模训练实例 *T*^((*i*)) = ⟨*ȳ*^((*i*)), ![图片](../../OEBPS/Images/AR_x.png)^((*i*))⟩
    的概率。
- en: '![](../../OEBPS/Images/eq_09-22-d.png)'
  id: totrans-414
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/eq_09-22-d.png)'
- en: This makes intuitive sense. At an optimal value of *θ*, the network output *f*(![](../../OEBPS/Images/AR_x.png)^((*i*)),
    *θ*) would match the GT *ȳ*^((*i*)). We want our model distribution to have the
    highest probability density at the location where the network output matches the
    GT. The probability density should fall off with distance from that location.
  id: totrans-415
  prefs: []
  type: TYPE_NORMAL
  zh: 这在直观上是合理的。在 *θ* 的最优值处，网络输出 *f*(![图片](../../OEBPS/Images/AR_x.png)^((*i*)), *θ*)
    将与 GT *ȳ*^((*i*)) 匹配。我们希望我们的模型分布在网络输出与 GT 匹配的位置具有最高的概率密度。概率密度应该随着与该位置的距离增加而减小。
- en: A little thought reveals that this Gaussian-like formulation, which leads to
    a regression-loss-like numerator, is not the only one possible. We can use any
    loss function in the numerator since all of them have the property of being minimum
    when the network output matches the GT and gradually increase as the mismatch
    grows. In general,
  id: totrans-416
  prefs: []
  type: TYPE_NORMAL
  zh: 仔细思考一下，我们可以发现这个类似于高斯公式的形式，它导致了一个类似于回归损失的分子，并不是唯一可能的。我们可以在分子中使用任何损失函数，因为它们都具有当网络输出与真实值匹配时达到最小值，随着不匹配的增加而逐渐增大的特性。一般来说，
- en: '![](../../OEBPS/Images/eq_09-22-e.png)'
  id: totrans-417
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-e](../../OEBPS/Images/eq_09-22-e.png)'
- en: Assuming the training instances are mutually independent, the probability of
    the entire training dataset occurring jointly is the product of individual instance
    probabilities. If we denote the entire training dataset as *T*,
  id: totrans-418
  prefs: []
  type: TYPE_NORMAL
  zh: 假设训练实例是相互独立的，整个训练数据集同时出现的概率是各个实例概率的乘积。如果我们用 *T* 表示整个训练数据集，
- en: '![](../../OEBPS/Images/eq_09-22-f.png)'
  id: totrans-419
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-f](../../OEBPS/Images/eq_09-22-f.png)'
- en: Then
  id: totrans-420
  prefs: []
  type: TYPE_NORMAL
  zh: 然后
- en: '![](../../OEBPS/Images/eq_09-22-g.png)'
  id: totrans-421
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-g](../../OEBPS/Images/eq_09-22-g.png)'
- en: At this point, we can take one of two possible approaches described in the next
    two subsections.
  id: totrans-422
  prefs: []
  type: TYPE_NORMAL
  zh: 在这一点上，我们可以采取下两个小节中描述的两种可能的方法之一。
- en: MLE-based optimization
  id: totrans-423
  prefs: []
  type: TYPE_NORMAL
  zh: 基于MLE的优化
- en: In this approach, we choose the optimal value for the parameter set *θ* by maximizing
    the likelihood
  id: totrans-424
  prefs: []
  type: TYPE_NORMAL
  zh: 在这种方法中，我们通过最大化似然函数来选择参数集 *θ* 的最优值
- en: '![](../../OEBPS/Images/eq_09-22-h.png)'
  id: totrans-425
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-h](../../OEBPS/Images/eq_09-22-h.png)'
- en: This is equivalent to saying we will choose the optimal parameters *θ* such
    that the probability of occurrence of the training data is maximized. Thus the
    optimal parameter set *θ*[*] is yielded by
  id: totrans-426
  prefs: []
  type: TYPE_NORMAL
  zh: 这相当于说我们将选择最优参数 *θ*，使得训练数据的出现概率最大化。因此，最优参数集 *θ*[*] 由以下公式得出
- en: '![](../../OEBPS/Images/eq_09-22-i.png)'
  id: totrans-427
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-i](../../OEBPS/Images/eq_09-22-i.png)'
- en: Obviously, the optimal *θ* that maximizes the likelihood is the one that minimizes
    𝕃(*θ*). So, the maximum likelihood formulation is nothing but minimizing the total
    mismatch between predicted and GT output over the entire training dataset.
  id: totrans-428
  prefs: []
  type: TYPE_NORMAL
  zh: 显然，最大化似然的最优 *θ* 是最小化 𝕃(*θ*) 的那个。所以，最大似然公式实际上就是最小化整个训练数据集上预测值和真实值之间的总不匹配。
- en: MAP optimization
  id: totrans-429
  prefs: []
  type: TYPE_NORMAL
  zh: MAP 优化
- en: By Bayes’ theorem (equation [6.1](../Text/06.xhtml#eq-bayes-theorem-2var)),
  id: totrans-430
  prefs: []
  type: TYPE_NORMAL
  zh: 根据贝叶斯定理（方程 [6.1](../Text/06.xhtml#eq-bayes-theorem-2var)），
- en: '![](../../OEBPS/Images/eq_09-22-j.png)'
  id: totrans-431
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-j](../../OEBPS/Images/eq_09-22-j.png)'
- en: To estimate the optimal *θ*, we can also maximize the posterior probability
    on the left side of this equation. This is equivalent to saying we will choose
    the optimal parameters *θ* such that *θ* has the maximal conditional probability
    given the training dataset. Thus the optimal value for the parameter set *θ* is
    yielded by
  id: totrans-432
  prefs: []
  type: TYPE_NORMAL
  zh: 为了估计最优 *θ*，我们也可以最大化该方程左侧的后验概率。这相当于说我们将选择最优参数 *θ*，使得在训练数据集给定的情况下 *θ* 具有最大的条件概率。因此，最优参数集
    *θ* 的值由以下公式得出
- en: '![](../../OEBPS/Images/eq_09-22-k.png)'
  id: totrans-433
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-k](../../OEBPS/Images/eq_09-22-k.png)'
- en: where the last equality is derived via Bayes’ theorem.
  id: totrans-434
  prefs: []
  type: TYPE_NORMAL
  zh: 其中最后一个等式是通过贝叶斯定理推导出来的。
- en: Observing the previous equation, we see that the denominator *p*(*T*) in the
    rightmost term does not involve *θ* and hence can be dropped from the optimization.
    So,
  id: totrans-435
  prefs: []
  type: TYPE_NORMAL
  zh: 观察前面的方程，我们可以看到最右侧项的分母 *p*(*T*) 不涉及 *θ*，因此可以从优化中省略。所以，
- en: '![](../../OEBPS/Images/eq_09-22-l.png)'
  id: totrans-436
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-l](../../OEBPS/Images/eq_09-22-l.png)'
- en: How do we model the *a priori* probability *p*(*θ*)? We can use Occam’s razor
    and say that we assign a higher *a priori* probability to smaller parameter values.
    Thus, we can say
  id: totrans-437
  prefs: []
  type: TYPE_NORMAL
  zh: 我们如何建模 *a priori* 概率 *p*(*θ*)？我们可以使用奥卡姆剃刀原则，并说我们赋予较小的参数值更高的 *a priori* 概率。因此，我们可以表示为
- en: '![](../../OEBPS/Images/eq_09-22-m.png)'
  id: totrans-438
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-m](../../OEBPS/Images/eq_09-22-m.png)'
- en: Then the overall posterior probability maximization becomes
  id: totrans-439
  prefs: []
  type: TYPE_NORMAL
  zh: 然后，整体后验概率最大化变为
- en: '![](../../OEBPS/Images/eq_09-22-n.png)'
  id: totrans-440
  prefs: []
  type: TYPE_IMG
  zh: '![公式 09-22-n](../../OEBPS/Images/eq_09-22-n.png)'
- en: NOTE Maximizing this posterior probability is equivalent to minimizing the regularized
    loss. Maximizing the likelihood is equivalent to minimizing the un-regularized
    loss.
  id: totrans-441
  prefs: []
  type: TYPE_NORMAL
  zh: 注意：最大化这个后验概率等价于最小化正则化损失。最大化似然等价于最小化未正则化损失。
- en: 9.3.6 Dropout
  id: totrans-442
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 9.3.6 Dropout
- en: In the introduction for section [9.3](../Text/09.xhtml#sec-regularization),
    we saw that too much expressive power (too many perceptron nodes) sometimes prevents
    the machine from developing general *abstractions* (aka *experience*). Instead,
    the machine may remember the training data instances (see figure [9.11](#fig-overfitting)).
    This phenomenon is called overfitting. We have already seen that one way to mitigate
    this problem is to add a regularization penalty—such as adding the L2 norm of
    the weights—to the loss to discourage the network from learning large weight values.
  id: totrans-443
  prefs: []
  type: TYPE_NORMAL
  zh: 在 [9.3](../Text/09.xhtml#sec-regularization) 节的介绍中，我们了解到，过多的表达能力（过多的感知器节点）有时会阻止机器发展出一般的
    *抽象*（也称为 *经验*）。相反，机器可能会记住训练数据实例（见图 [9.11](#fig-overfitting)）。这种现象称为过拟合。我们已经看到，缓解这种问题的一种方法是在损失中添加正则化惩罚——例如添加权重的
    L2 范数——以阻止网络学习大的权重值。
- en: Dropout is another method of regularization. Here, somewhat crazily, we turn
    off random perceptrons in the network (set their value to 0) during training.
    To be precise, we attach a probability *p[i]^l* with the *i*th node (perceptron)
    in layer *l*. In any training iteration, the node (perceptron) is off with a probability
    of (1−*p*). Typically, dropout is only enabled during training and is turned off
    during inferencing.
  id: totrans-444
  prefs: []
  type: TYPE_NORMAL
  zh: Dropout 是另一种正则化方法。在这里，有些疯狂的是，我们在训练期间关闭网络中的随机感知器（将它们的值设置为 0）。更准确地说，我们给第 *l* 层的第
    *i* 个节点（感知器）附加一个概率 *p[i]^l*。在任何训练迭代中，节点（感知器）关闭的概率为 (1−*p*）。通常，dropout 只在训练期间启用，在推理期间关闭。
- en: What good does it do? Well,
  id: totrans-445
  prefs: []
  type: TYPE_NORMAL
  zh: 这有什么好处呢？好吧，
- en: Dropout prevents the network from relying too much on a small number of nodes.
    Instead, the network is forced to use all the nodes.
  id: totrans-446
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Dropout 防止网络过度依赖少数节点。相反，网络被迫使用所有节点。
- en: Equivalently, dropout encourages the training process to spread the weights
    to multiple nodes instead of putting much weight on a few nodes. This makes the
    effect somewhat similar to L2 regularization.
  id: totrans-447
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 等价地，dropout 鼓励训练过程将权重分散到多个节点，而不是过多地依赖于少数节点。这使得效果与 L2 正则化有些相似。
- en: 'Dropout mitigates *co-adaptation*: a behavior whereby a group of nodes in the
    network behave in a highly correlated fashion, emitting similar outputs most of
    the time. This means the network could retain only one of them with no significant
    loss of accuracy.'
  id: totrans-448
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Dropout 缓解了 *共适应*：网络中一组节点以高度相关的方式行为，大多数时候发出相似的输出。这意味着网络可能只保留其中一个，而不会造成显著的精度损失。
- en: Dropout simulates an ensemble of subnetworks
  id: totrans-449
  prefs: []
  type: TYPE_NORMAL
  zh: Dropout 模拟了一组子网络
- en: 'Consider a small three-node intermediate layer of some neural network with
    dropout. The *k*th input to this layer can turn on with probability *p[k]*. This
    means the probability of that input node turning off is (1−*p[k]*). We can express
    this with a *binary* stochastic variable *δ[k]* for *k* = 0 or *k* = 1 or *k*
    = 2. This variable *δ[k]* takes one of two possible values: 0 or 1. The probability
    of it taking the value 1 is *p[k]*. In other words, *p*(*δ[k]* = 1) = *p[k]*,
    and *p*(*δ[k]* = 0) = 1 − *p[k]*. The output of this small three-node layer with
    dropout can be expressed as'
  id: totrans-450
  prefs: []
  type: TYPE_NORMAL
  zh: 考虑一个具有 dropout 的某个神经网络的三个节点的小型中间层。该层的第 *k* 个输入以概率 *p[k]* 被激活。这意味着该输入节点关闭的概率是
    (1−*p[k]*)。我们可以用 *k* = 0 或 *k* = 1 或 *k* = 2 的 *二进制* 随机变量 *δ[k]* 来表示这一点。这个变量 *δ[k]*
    可以取两个可能值之一：0 或 1。它取值为 1 的概率是 *p[k]*。换句话说，*p*(*δ[k]* = 1) = *p[k]*，而 *p*(*δ[k]*
    = 0) = 1 − *p[k]*。具有 dropout 的这个小三个节点层的输出可以表示为
- en: '![](../../OEBPS/Images/eq_09-22-o.png)'
  id: totrans-451
  prefs: []
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-22-o.png)'
- en: 'We have three variables *δ*[0], *δ*[1], and *δ*[2], each of which can take
    two values. Altogether we have 2³ = 8 possible combinations. Each combination
    leads to a subnode shown in figure [9.13](#fig-dropout_3layer). Each of these
    combinations has a probability of occurrence *P[i]*, also shown in the figure.
    These observations lead to a very important insight:'
  id: totrans-452
  prefs: []
  type: TYPE_NORMAL
  zh: 我们有三个变量 *δ*[0]，*δ*[1] 和 *δ*[2]，每个变量可以取两个值。总共我们有 2³ = 8 种可能的组合。每种组合对应图 [9.13](#fig-dropout_3layer)
    中显示的一个子节点。这些组合中的每一个都有发生概率 *P[i]*，也在图中显示。这些观察结果导致了一个非常重要的洞察：
- en: The expected value of the output—that is, 𝔼(*a[l]*)—is the same as the expected
    value of the output if we deployed the subnetworks in figure [9.13](#fig-dropout_3layer)
    randomly, with probabilities *P[i]*.
  id: totrans-453
  prefs: []
  type: TYPE_NORMAL
  zh: 输出的期望值——即 𝔼(*a[l]*)——与如果我们随机部署图 [9.13](#fig-dropout_3layer) 中的子网络时的输出期望值相同，概率为
    *P[i]*。
- en: Why does this matter? Well, given a problem, none of us know the right number
    of perceptrons for the network to deploy. One thing to do under these circumstances
    is to deploy networks of various strengths randomly and take an average of their
    outputs. We have just established that dropout (turning inputs off randomly) achieves
    the same thing. But deploying a network where inputs get turned on or off randomly
    is much simpler than deploying a large number of subnetworks. All we have to do
    is deploy a *dropout* layer.
  id: totrans-454
  prefs: []
  type: TYPE_NORMAL
  zh: 这为什么很重要呢？好吧，给定一个问题，我们中没有人知道网络部署的正确感知器数量。在这种情况下可以做的事情之一是随机部署各种强度的网络，并取其输出的平均值。我们刚刚已经证明，丢弃（随机关闭输入）可以达到同样的效果。但是部署一个随机打开或关闭输入的网络比部署大量子网络要简单得多。我们只需要部署一个
    *丢弃* 层。
- en: PyTorch code for dropout
  id: totrans-455
  prefs: []
  type: TYPE_NORMAL
  zh: Dropout 的 PyTorch 代码
- en: In section [9.2.3](#sec-pytorch-sgd), we created a simple two-layer neural network
    classifier to predict whether a Statsville resident is a man, woman, or child
    based on height and weight data. In this section, we show the same model with
    dropout layers added. Note that dropout should be enabled only during training,
    not during inferencing. To do this in PyTorch, you can call `model.eval()` before
    running inferencing. This way, your training and inferencing code remains the
    same, but PyTorch knows under the hood when to include the dropout layers and
    when not to.
  id: totrans-456
  prefs: []
  type: TYPE_NORMAL
  zh: 在 [9.2.3](#sec-pytorch-sgd) 节中，我们创建了一个简单的两层神经网络分类器，根据身高和体重数据预测 Statsville 居民是男性、女性还是儿童。在本节中，我们展示了添加了丢弃层的相同模型。请注意，丢弃层仅在训练期间启用，而不是在推理期间。要在
    PyTorch 中实现这一点，您可以在运行推理之前调用 `model.eval()`。这样，您的训练和推理代码保持不变，但 PyTorch 在幕后知道何时包含丢弃层，何时不包含。
- en: Listing 9.14 Dropout
  id: totrans-457
  prefs: []
  type: TYPE_NORMAL
  zh: 列表 9.14 Dropout
- en: '[PRE13]'
  id: totrans-458
  prefs: []
  type: TYPE_PRE
  zh: '[PRE13]'
- en: ① Instantiates a dropout layer with a probability of dropout = 0.2
  id: totrans-459
  prefs: []
  type: TYPE_NORMAL
  zh: ① 实例化一个丢弃层，丢弃概率为 0.2
- en: '![](../../OEBPS/Images/CH09_F13a_Chaudhury.png)'
  id: totrans-460
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13a_Chaudhury.png)'
- en: '(a) Subnetwork 0: probability *P*[0] = (1−*p*[0]) (1−*p*[1]) (1−*p*[2])'
  id: totrans-461
  prefs: []
  type: TYPE_NORMAL
  zh: (a) 子网络 0：概率 *P*[0] = (1−*p*[0]) (1−*p*[1]) (1−*p*[2])
- en: '![](../../OEBPS/Images/CH09_F13b_Chaudhury.png)'
  id: totrans-462
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13b_Chaudhury.png)'
- en: '(b) Subnetwork 1: probability *P*[1] = (1−*p*[0]) (1−*p*[1]) *p*[2]'
  id: totrans-463
  prefs: []
  type: TYPE_NORMAL
  zh: (b) 子网络 1：概率 *P*[1] = (1−*p*[0]) (1−*p*[1]) *p*[2]
- en: '![](../../OEBPS/Images/CH09_F13c_Chaudhury.png)'
  id: totrans-464
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13c_Chaudhury.png)'
- en: '(c) Subnetwork 2: probability *P*[2] = (1−*p*[0] )*p*[1] (1−*p*[2])'
  id: totrans-465
  prefs: []
  type: TYPE_NORMAL
  zh: (c) 子网络 2：概率 *P*[2] = (1−*p*[0] )*p*[1] (1−*p*[2])
- en: '![](../../OEBPS/Images/CH09_F13d_Chaudhury.png)'
  id: totrans-466
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13d_Chaudhury.png)'
- en: '(d) Subnetwork 3: probability *P*[3] = (1−*p*[0] )*p*[1] *p*[2]'
  id: totrans-467
  prefs: []
  type: TYPE_NORMAL
  zh: (d) 子网络 3：概率 *P*[3] = (1−*p*[0] )*p*[1] *p*[2]
- en: '![](../../OEBPS/Images/CH09_F13e_Chaudhury.png)'
  id: totrans-468
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13e_Chaudhury.png)'
- en: '(e) Subnetwork 4: probability *P*[4] = *p*[0] (1−*p*[1]) (1−*p*[2])'
  id: totrans-469
  prefs: []
  type: TYPE_NORMAL
  zh: (e) 子网络 4：概率 *P*[4] = *p*[0] (1−*p*[1]) (1−*p*[2])
- en: '![](../../OEBPS/Images/CH09_F13f_Chaudhury.png)'
  id: totrans-470
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13f_Chaudhury.png)'
- en: '(f) Subnetwork 5: probability *P*[5] = *p*[0] (1−*p*[1]) *p*[2]'
  id: totrans-471
  prefs: []
  type: TYPE_NORMAL
  zh: (f) 子网络 5：概率 *P*[5] = *p*[0] (1−*p*[1]) *p*[2]
- en: '![](../../OEBPS/Images/CH09_F13g_Chaudhury.png)'
  id: totrans-472
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13g_Chaudhury.png)'
- en: '(g) Subnetwork 6: probability *P*[6] = *p*[0] *p*[1] (1−*p*[2])'
  id: totrans-473
  prefs: []
  type: TYPE_NORMAL
  zh: (g) 子网络 6：概率 *P*[6] = *p*[0] *p*[1] (1−*p*[2])
- en: '![](../../OEBPS/Images/CH09_F13h_Chaudhury.png)'
  id: totrans-474
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../../OEBPS/Images/CH09_F13h_Chaudhury.png)'
- en: '(h) Subnetwork 7: probability *P*[7] = *p*[0] *p*[1] *p*[2]'
  id: totrans-475
  prefs: []
  type: TYPE_NORMAL
  zh: (h) 子网络 7：概率 *P*[7] = *p*[0] *p*[1] *p*[2]
- en: 'Figure 9.13 Dropout simulates subnetworks: illustrated with a three-node intermediate
    layer of a neural network. The probability of input node *a[k]*^((*l* − 1)) being
    *on* is *p*(*δ[k]* = 1) = *p[k]*.'
  id: totrans-476
  prefs: []
  type: TYPE_NORMAL
  zh: 图 9.13 Dropout 模拟子网络：以神经网络的三节点中间层为例。输入节点 *a[k]*^((*l* − 1)) 被激活的概率为 *p*(*δ[k]*
    = 1) = *p[k]*。
- en: Summary
  id: totrans-477
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 摘要
- en: 'Training is the process by which a neural network identifies the optimal values
    for its parameters (weights and biases of the individual perceptrons). Training
    progresses iteratively: in each iteration, we estimate the loss and run an optimization
    step that updates the parameter values so as to decrease the loss. After doing
    this many times, we hope to arrive at optimal parameter values.'
  id: totrans-478
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 训练是神经网络识别其参数（各个感知器的权重和偏置）最优值的过程。训练是迭代进行的：在每次迭代中，我们估计损失并运行一个优化步骤，以更新参数值以减少损失。经过多次这样做后，我们希望达到最优参数值。
- en: In a supervised neural network, loss quantifies the mismatch between the desired
    output and the network output over sampled training data instances. The desired
    output (ground truth) is often estimated via manual effort. Training the neural
    network essentially identifies the weights and biases of the neural network that
    minimize the loss.
  id: totrans-479
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在监督神经网络中，损失量化了在采样训练数据实例上所需输出和网络输出之间的不匹配。所需输出（真实值）通常通过人工努力来估计。训练神经网络本质上识别了使损失最小化的神经网络权重和偏差。
- en: The discrepancy between the ground truth and network output can be expressed
    in many different ways. Each corresponds to a different loss function. Denoting
    the ground truth probabilities of all the possible classes given the *i*th training
    input ![](../../OEBPS/Images/AR_x.png)^((*i*)) as a vector *ȳ*^((*i*)) and the
    network output on the same as *y*^((*i*)),
  id: totrans-480
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 真实值和网络输出之间的差异可以用许多不同的方式表示。每个都对应于不同的损失函数。将第 *i* 个训练输入给出的所有可能类别的真实概率表示为向量 *ȳ*^((*i*))，并将网络输出表示为
    *y*^((*i*))，
- en: Regression loss takes the L2 norm of the vector difference between the network
    output and ground-truth vectors and is mathematically expressed as ![](../../OEBPS/Images/eq_09-22-p1.png).
  id: totrans-481
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: 回归损失取网络输出和真实向量之间向量差异的 L2 范数，并在数学上表示为 ![](../../OEBPS/Images/eq_09-22-p1.png)。
- en: If the neural network is a classifier, it typically outputs a vector of class
    probabilities. This means
  id: totrans-482
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: 如果神经网络是一个分类器，它通常输出一个类概率向量。这意味着
- en: '![](../../OEBPS/Images/eq_09-22-p2.png)'
  id: totrans-483
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-22-p2.png)'
- en: 'where *p[j]*(![](../../OEBPS/Images/AR_x.png)^((*i*))) is the network estimated
    probability of the input belonging to class *j*, and *N* is the number of possible
    classes. In general, given an input, a neural network computes per-class scores:
    the class with the highest score is the predicted class. The scores are unbounded
    numbers and can be arbitrarily large or small (even negative). The operation converts
    them into probabilities. If'
  id: totrans-484
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_NORMAL
  zh: 其中 *p[j]*(![](../../OEBPS/Images/AR_x.png)^((*i*))) 是网络估计的输入属于类别 *j* 的概率，*N*
    是可能类别的数量。一般来说，给定一个输入，神经网络计算每个类别的得分：得分最高的类别是预测的类别。得分是无界的数字，可以是任意大或小（甚至是负数）。该操作将它们转换为概率。如果
- en: '![](../../OEBPS/Images/eq_09-22-p3.png)'
  id: totrans-485
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_IMG
  zh: '![](../../OEBPS/Images/eq_09-22-p3.png)'
- en: denotes the score vector, the corresponding softmax vector is ![](../../OEBPS/Images/eq_09-22-p4.png)
  id: totrans-486
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_NORMAL
  zh: 表示得分向量，相应的 softmax 向量是 ![](../../OEBPS/Images/eq_09-22-p4.png)
- en: where ![](../../OEBPS/Images/eq_09-22-p5.png). The softmax output vector consists
    of probabilities, meaning they are numbers between 0 and 1 and they sum to 1.
  id: totrans-487
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_NORMAL
  zh: 其中 ![](../../OEBPS/Images/eq_09-22-p5.png)。softmax 输出向量由概率组成，这意味着它们是介于 0 和 1
    之间的数字，并且它们的总和为 1。
- en: Given the probability of each class, classifiers can employ the cross-entropy
    loss, which can be expressed as
  id: totrans-488
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: 给定每个类的概率，分类器可以使用交叉熵损失，它可以表示为
- en: '![](../../OEBPS/Images/eq_09-22-p6.png).'
  id: totrans-489
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_NORMAL
  zh: '![](../../OEBPS/Images/eq_09-22-p6.png)。'
- en: 'Note that since the ground-truth vector is one-hot, only a single term in this
    expression survives: the one corresponding to the desired class, denoted *j*^*.
    The loss becomes the logarithm of the corresponding network output, −*log*(*y*[*j*^*]^((*i*))),
    and is zero if *y*[*j*^*]^((*i*)) = 1. This agrees with our expectation: if the
    network is predicting the GT class with probability 1, there is no loss.'
  id: totrans-490
  prefs:
  - PREF_IND
  - PREF_IND
  type: TYPE_NORMAL
  zh: 注意，由于真实向量是 one-hot 编码的，这个表达式中的只有一个项会幸存：对应于所需类别的那个项，用 *j*^* 表示。损失成为相应网络输出的对数，即
    −*log*(*y*[*j*^*]^((*i*))), 如果 *y*[*j*^*]^((*i*)) = 1，则损失为零。这与我们的预期一致：如果网络以概率
    1 预测 GT 类，则没有损失。
- en: Since during training, softmax is almost always followed by cross-entropy loss,
    PyTorch has a combined operator called softmax cross-entropy loss. It is over
    doing these operations individually because it has better numerical properties.
  id: totrans-491
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: 由于在训练过程中，softmax 几乎总是紧跟着交叉熵损失，PyTorch 有一个名为 softmax 交叉熵损失的组合运算符。单独执行这些操作是多余的，因为它具有更好的数值特性。
- en: Focal loss tries to tackle the data-imbalance problem by putting more weight
    on the “hard” examples with higher loss.
  id: totrans-492
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: Focal loss 通过对具有更高损失的“困难”示例赋予更多权重来尝试解决数据不平衡问题。
- en: Hinge loss is another popular loss that becomes zero when the correct class
    is predicted with the maximum score. Once that criterion is achieved, it no longer
    tries to improve the relative values of scores.
  id: totrans-493
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: Hinge loss 是另一种流行的损失，当以最大分数预测正确类别时变为零。一旦达到这个标准，它就不再尝试改进分数的相对值。
- en: Total loss can be obtained by adding the losses from all the individual training
    data instances. However, this requires us to process all the training data points
    in every iteration, which is prohibitively expensive. Hence, we sample a subset
    of training data points to create a minibatch, estimate losses for each input
    data instance in the minibatch, and add them to obtain an estimate for the total
    loss. This process is known as stochastic gradient descent (SGD).
  id: totrans-494
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 总损失可以通过将所有单个训练数据实例的损失相加来获得。然而，这要求我们在每次迭代中处理所有训练数据点，这是非常昂贵的。因此，我们采样训练数据点的子集来创建一个小批量，估计小批量中每个输入数据实例的损失，并将它们相加以获得总损失的估计。这个过程被称为随机梯度下降（SGD）。
- en: Optimization is the process of updating the neural network parameters (weights
    and biases of perceptrons) so as to reduce the loss. We can plot the loss against
    weights and bias values and obtain a hypersurface defined over the domain of weights
    and bias values. Our ultimate goal is to reach the bottom (minimum point) of this
    loss hypersurface. Optimization is geometrically equivalent to moving down the
    hypersurface to reduce the loss. The steepest descent (toward the nearest minimum)
    happens along a negative gradient.
  id: totrans-495
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 优化是更新神经网络参数（感知器的权重和偏置）的过程，以减少损失。我们可以将损失与权重和偏置值绘制成图，并得到一个在权重和偏置值域上定义的超曲面。我们的最终目标是达到这个损失超曲面的底部（最小点）。优化在几何上等同于沿着超曲面向下移动以减少损失。最陡下降（朝向最近的极小值）沿着负梯度发生。
- en: 'We can combine several other criteria with the gradient to improve the convergence
    to the minimum loss value. Each of them results in a different optimization technique:'
  id: totrans-496
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 我们可以将多个其他标准与梯度结合，以改善收敛到最小损失值。每个标准都会导致不同的优化技术：
- en: Due to noisy estimations, the local gradient may not always point toward the
    minimum, but it will have a strong component toward that minimum along with other
    noisy components. Instead of blindly following the current gradient, we can follow
    the direction corresponding to a weighted average of the current gradient estimate
    and the gradient estimate from the previous iteration. This is a recursive process,
    which effectively means the direction followed at any iteration is a weighted
    average of the current and all gradient estimates from the beginning of training.
    Recent gradients are weighted higher, and older gradients are weighed lower. All
    these gradients have strong components toward the minimum that reinforce each
    other, while the noisy components point in random directions and tend to cancel
    each other. Thus the overall weighted sum is a more reliable move toward the minimum.
    This is called momentum-based gradient descent.
  id: totrans-497
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: 由于估计值存在噪声，局部梯度可能并不总是指向最小值，但它将包含一个指向该最小值的主成分，同时还有其他噪声成分。我们不是盲目地跟随当前梯度，而是可以跟随当前梯度估计值和前一次迭代梯度估计值的加权平均方向。这是一个递归过程，有效意味着任何迭代中跟随的方向是当前和从训练开始的所有梯度估计值的加权平均。最近的梯度被赋予更高的权重，而较旧的梯度被赋予较低的权重。所有这些梯度都包含指向最小值的主成分，相互强化，而噪声成分则指向随机方向，并倾向于相互抵消。因此，整体加权总和是一个更可靠的朝向最小值的移动。这被称为基于动量的梯度下降。
- en: A drawback of momentum is that it can result in overshooting the minimum. Nesterov
    accelerated gradients correct this drawback by calculating gradients via one-step
    lookahead. If the current update takes us to the other side of the minimum, the
    gradient will have an opposite direction there. We take a weighted average of
    the update suggested by momentum-based gradient descent and the gradient at the
    estimated destination point. If we are far from the minimum, it will be roughly
    equivalent to momentum-based gradient descent. But if we are about to overshoot
    the minimum, there will be cancelation, and the update will be smaller than that
    of the pure momentum case. Thus we reduce the chances of overshooting the minimum.
  id: totrans-498
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: 动量的一个缺点是它可能导致超过最小值。Nesterov加速梯度通过一步前瞻计算梯度来纠正这一缺点。如果当前更新使我们到达最小值的另一侧，那么梯度将具有相反的方向。我们取基于动量的梯度下降建议的更新和估计目标点处的梯度的加权平均。如果我们离最小值很远，这大致等同于基于动量的梯度下降。但如果我们即将超过最小值，将会发生抵消，更新将小于纯动量情况下的更新。因此，我们减少了超过最小值的机会。
- en: AdaGrad is an optimization technique that imparts additional weight to infrequently
    changing axes of the loss hypersurface. The Adam optimizer combines many advantages
    of other optimizers and is often the modern optimizer of choice.
  id: totrans-499
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: AdaGrad是一种优化技术，它给损失超曲面中不经常变化的轴赋予额外的权重。Adam优化器结合了许多其他优化器的优点，通常是现代优化器的首选。
- en: The principle of Occam’s razor essentially says that among adequate explanations,
    the simplest one should be preferred. In machine learning, this leads to regularization.
    There are typically many solutions to the loss-minimization problem. We want to
    choose the simplest one. Accordingly, we add a penalty for departing from simplicity
    (regularization loss) to whatever other losses we have. This incentivizes the
    system to go for the simplest solution. Regularization loss is often the total
    length of the parameter vector; thus, regularization pushes us toward solutions
    with smaller absolute values for parameters.
  id: totrans-500
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 奥卡姆剃刀原理本质上说的是，在足够的解释中，最简单的一个应该被优先考虑。在机器学习中，这导致了正则化。通常有很多解决损失最小化问题的方法。我们希望选择最简单的一个。因此，我们在其他任何损失上添加一个偏离简单性的惩罚（正则化损失）。这激励系统寻求最简单的解决方案。正则化损失通常是参数向量的总长度；因此，正则化推动我们朝着参数绝对值更小的解决方案发展。
- en: Minimizing the loss function without the regularization term is equivalent to
    the Bayesian maximum likelihood estimation (MLE) technique. On the other hand,
    minimizing loss with the regularized term is equivalent to maximum a posteriori
    (MAP) estimation.
  id: totrans-501
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在没有正则化项的情况下最小化损失函数等价于贝叶斯最大似然估计（MLE）技术。另一方面，带有正则化项的最小化损失等价于最大后验（MAP）估计。
- en: Overfitting is a phenomenon where the neural network has learned all the nuances
    of the training data. Since anomalous nuances in training data points are often
    caused by noise, this leads to worse performance during inferencing. Overfitting
    is symptomized by great accuracy (low loss) on training data but bad accuracy
    on test data. It often happens because the network has more expressive power than
    necessary for the problem and is trying to memorize all nooks and bends of the
    training data. Regularization mitigates the problem. Another trick is dropout,
    where we deliberately turn off a random subset of neurons during training iterations.
    This forces all the neurons to learn to do the job with a reduced set of neurons.
  id: totrans-502
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 过度拟合是一种现象，其中神经网络已经学会了训练数据中的所有细微差别。由于训练数据中的异常细微差别通常是由噪声引起的，这导致在推理过程中的性能变差。过度拟合表现为在训练数据上具有很高的准确率（低损失），但在测试数据上准确率较差。这通常是因为网络的表达能力超过了解决问题所需的程度，并试图记住训练数据中的所有角落和弯道。正则化可以缓解这个问题。另一个技巧是dropout，我们在训练迭代中故意关闭一个随机子集的神经元。这迫使所有神经元学会用更少的神经元来完成工作。
